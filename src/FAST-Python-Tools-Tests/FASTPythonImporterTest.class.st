"
A FASTPythonImporterTest is a test class for testing the behavior of FASTPythonImporter
"
Class {
	#name : 'FASTPythonImporterTest',
	#superclass : 'TestCase',
	#instVars : [
		'importer',
		'fast',
		'stack'
	],
	#category : 'FAST-Python-Tools-Tests',
	#package : 'FAST-Python-Tools-Tests'
}

{ #category : 'test generation' }
FASTPythonImporterTest class >> generateTestNamed: aString fromCode: code [

	^ self generateTestNamed: aString fromCode: code protocol: 'tests'
]

{ #category : 'test generation' }
FASTPythonImporterTest class >> generateTestNamed: aString fromCode: code protocol: protocol [

	(FASTPyImporterTestGenerator testCase: self) generateTestNamed: aString fromCode: code protocol: protocol
]

{ #category : 'class initialization' }
FASTPythonImporterTest class >> initialize [
	"We ensure the installation of the library before running the tests because it could cause a time limit to fails for the test installing the libraries."

	TSLibrariesPython new ensurePythonLibraryExists
]

{ #category : 'test generation' }
FASTPythonImporterTest class >> propertiesToExclude [

	^ #( startPos endPos )
]

{ #category : 'test generation' }
FASTPythonImporterTest class >> regenerateAllTests [

	<script>
	self methods
		select: [ :method | method hasPragmaNamed: #generated ]
		thenDo: [ :method | self regenerateTest: method selector ]
]

{ #category : 'test generation' }
FASTPythonImporterTest class >> regenerateTest: aSymbol [

	(FASTPyImporterTestGenerator testCase: self) regenerateTest: self >> aSymbol
]

{ #category : 'test generation' }
FASTPythonImporterTest class >> traitsToIgnore [
	"Return a collection of traits to ignore in generated tests."

	^ {
		  FASTTEntity. "Too global."
		  FASTTScopableEntity. "Not used for now?"
		  FASTTBehaviouralEntity. "To Review"
		  FASTTNamedBehaviouralEntity. "To Review"
		  FASTTNamedEntity. "Only brings a property that is already tested."
		  FASTTStatement. "I need to enable this one later but for now I'm not sure how to manage expression statements"
		  TEntityMetaLevelDependency. "Too general"
		  FASTTExpression. "I need to enable this one later but I'm still unsure of how to manage expression statements"
		  FASTPyTDecoratorExpression. "Adds too much noise in tests"
		  FASTTWithComments. "Everything has that"
		  FASTPyTSplatExpression. "Too many elements with it."
		  FASTPyTAsPatternTarget.
		  FASTPyTAsPatternSource.
		  FASTPyTWithStatementItem.
		  FASTPyTRaised.
		  FASTPyTReturnReferenceable.
		  FASTPyTAssignable.
		  FASTPyTSplatParameter.
		  FASTPyTSplatParameterTarget.
		  FASTPyTPattern.
		  FASTPyTClassPatternElement.
		  FASTPyTDeletable.
		  FASTPyTTypeContent.
		  FASTPyTWithTypeParameters }
]

{ #category : 'running' }
FASTPythonImporterTest >> importer [

	^ importer ifNil: [ importer := self importerClass new ]
]

{ #category : 'running' }
FASTPythonImporterTest >> importerClass [

	^ self class environment at: (self class name withoutSuffix: 'Test') asSymbol
]

{ #category : 'running' }
FASTPythonImporterTest >> parse: aString [

	^ fast := self importer
		          errorReportBlock: [ :i | i errors ifNotEmpty: [ self fail: 'Errors happened during parsing. Errors: ' , i errors printString ] ];
		          parse: aString withPlatformLineEndings
]

{ #category : 'private' }
FASTPythonImporterTest >> performTest [
	"If an error happens, dump the AST in the message text so that we can have an idea of what is the problem in the logs."

	"[ super performTest ]
		on: Error
		do: [ :failure |
				fast ifNotNil: [ failure messageText: failure messageText , ' FASTDump: ' , (RBParser parseExpression: fast rootEntities anyOne dump) formattedCode ].
				failure pass ]"

	super performTest
]

{ #category : 'running' }
FASTPythonImporterTest >> setUp [

	super setUp.

	stack := Stack new
]

{ #category : 'tests - with statement' }
FASTPythonImporterTest >> testAsPatternWithAttribute [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testAsPatternWithAttribute 	"

	self parse: 'with get() as container.resource: pass'.

	self assert: (fast allWithType: FASTPyAsPattern) size equals: 1.
	self assert: (fast allWithType: FASTPyAttributeAccess) size equals: 1.
	self assert: (fast allWithType: FASTPyCall) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 3.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPassStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyWithStatement) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'with get() as container.resource: pass'.
	self assert: (self topEntity isOfType: FASTPyWithStatement).
	self assert: (self topEntity isOfType: FASTTStatementBlock).
	
	"Testing value of multivalued relation items"
	self assert: self topEntity items size equals: 1.
	
		stack push: (stack top items at: 1).
		self assert: self topEntity sourceCode equals: 'get() as container.resource'.
		self assert: (self topEntity isOfType: FASTPyAsPattern).
		
		"Testing value of monovalue relation source"
		self assert: self topEntity source isNotNil.

			stack push: self topEntity source.
			self assert: self topEntity sourceCode equals: 'get()'.
			self assert: (self topEntity isOfType: FASTPyCall).
			self assert: (self topEntity isOfType: FASTTInvocation).
			self assert: (self topEntity isOfType: FASTTWithArguments).
			
			"Testing value of monovalue relation callee"
			self assert: self topEntity callee isNotNil.

				stack push: self topEntity callee.
				self assert: self topEntity sourceCode equals: 'get'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			stack pop.
		
		"Testing value of monovalue relation target"
		self assert: self topEntity target isNotNil.

			stack push: self topEntity target.
			self assert: self topEntity sourceCode equals: 'container.resource'.
			self assert: (self topEntity isOfType: FASTPyAttributeAccess).
			
			"Testing value of monovalue relation attribute"
			self assert: self topEntity attribute isNotNil.

				stack push: self topEntity attribute.
				self assert: self topEntity sourceCode equals: 'resource'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			
			"Testing value of monovalue relation object"
			self assert: self topEntity object isNotNil.

				stack push: self topEntity object.
				self assert: self topEntity sourceCode equals: 'container'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			stack pop.
		stack pop.
	
	"Testing value of multivalued relation statements"
	self assert: self topEntity statements size equals: 1.
	
		stack push: (stack top statements at: 1).
		self assert: self topEntity sourceCode equals: 'pass'.
		self assert: (self topEntity isOfType: FASTPyPassStatement)
]

{ #category : 'tests - with statement' }
FASTPythonImporterTest >> testAsPatternWithList [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testAsPatternWithList 	"

	self parse: 'with open() as [reader, writer]: pass'.

	self assert: (fast allWithType: FASTPyAsPattern) size equals: 1.
	self assert: (fast allWithType: FASTPyCall) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 3.
	self assert: (fast allWithType: FASTPyList) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPassStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyWithStatement) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'with open() as [reader, writer]: pass'.
	self assert: (self topEntity isOfType: FASTPyWithStatement).
	self assert: (self topEntity isOfType: FASTTStatementBlock).
	
	"Testing value of multivalued relation items"
	self assert: self topEntity items size equals: 1.
	
		stack push: (stack top items at: 1).
		self assert: self topEntity sourceCode equals: 'open() as [reader, writer]'.
		self assert: (self topEntity isOfType: FASTPyAsPattern).
		
		"Testing value of monovalue relation source"
		self assert: self topEntity source isNotNil.

			stack push: self topEntity source.
			self assert: self topEntity sourceCode equals: 'open()'.
			self assert: (self topEntity isOfType: FASTPyCall).
			self assert: (self topEntity isOfType: FASTTInvocation).
			self assert: (self topEntity isOfType: FASTTWithArguments).
			
			"Testing value of monovalue relation callee"
			self assert: self topEntity callee isNotNil.

				stack push: self topEntity callee.
				self assert: self topEntity sourceCode equals: 'open'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			stack pop.
		
		"Testing value of monovalue relation target"
		self assert: self topEntity target isNotNil.

			stack push: self topEntity target.
			self assert: self topEntity sourceCode equals: '[reader, writer]'.
			self assert: (self topEntity isOfType: FASTPyList).
			
			"Testing value of multivalued relation initializers"
			self assert: self topEntity initializers size equals: 2.
			
				stack push: (stack top initializers at: 1).
				self assert: self topEntity sourceCode equals: 'reader'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			
				stack push: (stack top initializers at: 2).
				self assert: self topEntity sourceCode equals: 'writer'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			stack pop.
		stack pop.
	
	"Testing value of multivalued relation statements"
	self assert: self topEntity statements size equals: 1.
	
		stack push: (stack top statements at: 1).
		self assert: self topEntity sourceCode equals: 'pass'.
		self assert: (self topEntity isOfType: FASTPyPassStatement)
]

{ #category : 'tests - with statement' }
FASTPythonImporterTest >> testAsPatternWithSourceAttributeAccess [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testAsPatternWithSourceAttributeAccess 	"

	self parse: 'with x.y as f: pass'.

	self assert: (fast allWithType: FASTPyAsPattern) size equals: 1.
	self assert: (fast allWithType: FASTPyAttributeAccess) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 3.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPassStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyWithStatement) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'with x.y as f: pass'.
	self assert: (self topEntity isOfType: FASTPyWithStatement).
	self assert: (self topEntity isOfType: FASTTStatementBlock).
	
	"Testing value of multivalued relation items"
	self assert: self topEntity items size equals: 1.
	
		stack push: (stack top items at: 1).
		self assert: self topEntity sourceCode equals: 'x.y as f'.
		self assert: (self topEntity isOfType: FASTPyAsPattern).
		
		"Testing value of monovalue relation source"
		self assert: self topEntity source isNotNil.

			stack push: self topEntity source.
			self assert: self topEntity sourceCode equals: 'x.y'.
			self assert: (self topEntity isOfType: FASTPyAttributeAccess).
			
			"Testing value of monovalue relation attribute"
			self assert: self topEntity attribute isNotNil.

				stack push: self topEntity attribute.
				self assert: self topEntity sourceCode equals: 'y'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			
			"Testing value of monovalue relation object"
			self assert: self topEntity object isNotNil.

				stack push: self topEntity object.
				self assert: self topEntity sourceCode equals: 'x'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			stack pop.
		
		"Testing value of monovalue relation target"
		self assert: self topEntity target isNotNil.

			stack push: self topEntity target.
			self assert: self topEntity sourceCode equals: 'f'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		stack pop.
	
	"Testing value of multivalued relation statements"
	self assert: self topEntity statements size equals: 1.
	
		stack push: (stack top statements at: 1).
		self assert: self topEntity sourceCode equals: 'pass'.
		self assert: (self topEntity isOfType: FASTPyPassStatement)
]

{ #category : 'tests - with statement' }
FASTPythonImporterTest >> testAsPatternWithSourceCall [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testAsPatternWithSourceCall 	"

	self parse: 'with x() as f: pass'.

	self assert: (fast allWithType: FASTPyAsPattern) size equals: 1.
	self assert: (fast allWithType: FASTPyCall) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 2.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPassStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyWithStatement) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'with x() as f: pass'.
	self assert: (self topEntity isOfType: FASTPyWithStatement).
	self assert: (self topEntity isOfType: FASTTStatementBlock).
	
	"Testing value of multivalued relation items"
	self assert: self topEntity items size equals: 1.
	
		stack push: (stack top items at: 1).
		self assert: self topEntity sourceCode equals: 'x() as f'.
		self assert: (self topEntity isOfType: FASTPyAsPattern).
		
		"Testing value of monovalue relation source"
		self assert: self topEntity source isNotNil.

			stack push: self topEntity source.
			self assert: self topEntity sourceCode equals: 'x()'.
			self assert: (self topEntity isOfType: FASTPyCall).
			self assert: (self topEntity isOfType: FASTTInvocation).
			self assert: (self topEntity isOfType: FASTTWithArguments).
			
			"Testing value of monovalue relation callee"
			self assert: self topEntity callee isNotNil.

				stack push: self topEntity callee.
				self assert: self topEntity sourceCode equals: 'x'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			stack pop.
		
		"Testing value of monovalue relation target"
		self assert: self topEntity target isNotNil.

			stack push: self topEntity target.
			self assert: self topEntity sourceCode equals: 'f'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		stack pop.
	
	"Testing value of multivalued relation statements"
	self assert: self topEntity statements size equals: 1.
	
		stack push: (stack top statements at: 1).
		self assert: self topEntity sourceCode equals: 'pass'.
		self assert: (self topEntity isOfType: FASTPyPassStatement)
]

{ #category : 'tests - with statement' }
FASTPythonImporterTest >> testAsPatternWithSourceIdentifier [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testAsPatternWithSourceIdentifier 	"

	self parse: 'with x as f: pass'.

	self assert: (fast allWithType: FASTPyAsPattern) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 2.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPassStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyWithStatement) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'with x as f: pass'.
	self assert: (self topEntity isOfType: FASTPyWithStatement).
	self assert: (self topEntity isOfType: FASTTStatementBlock).
	
	"Testing value of multivalued relation items"
	self assert: self topEntity items size equals: 1.
	
		stack push: (stack top items at: 1).
		self assert: self topEntity sourceCode equals: 'x as f'.
		self assert: (self topEntity isOfType: FASTPyAsPattern).
		
		"Testing value of monovalue relation source"
		self assert: self topEntity source isNotNil.

			stack push: self topEntity source.
			self assert: self topEntity sourceCode equals: 'x'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		
		"Testing value of monovalue relation target"
		self assert: self topEntity target isNotNil.

			stack push: self topEntity target.
			self assert: self topEntity sourceCode equals: 'f'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		stack pop.
	
	"Testing value of multivalued relation statements"
	self assert: self topEntity statements size equals: 1.
	
		stack push: (stack top statements at: 1).
		self assert: self topEntity sourceCode equals: 'pass'.
		self assert: (self topEntity isOfType: FASTPyPassStatement)
]

{ #category : 'tests - with statement' }
FASTPythonImporterTest >> testAsPatternWithSourceSubscript [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testAsPatternWithSourceSubscript 	"

	self parse: 'with x[y] as f: pass'.

	self assert: (fast allWithType: FASTPyAsPattern) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 3.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPassStatement) size equals: 1.
	self assert: (fast allWithType: FASTPySubscript) size equals: 1.
	self assert: (fast allWithType: FASTPyWithStatement) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'with x[y] as f: pass'.
	self assert: (self topEntity isOfType: FASTPyWithStatement).
	self assert: (self topEntity isOfType: FASTTStatementBlock).
	
	"Testing value of multivalued relation items"
	self assert: self topEntity items size equals: 1.
	
		stack push: (stack top items at: 1).
		self assert: self topEntity sourceCode equals: 'x[y] as f'.
		self assert: (self topEntity isOfType: FASTPyAsPattern).
		
		"Testing value of monovalue relation source"
		self assert: self topEntity source isNotNil.

			stack push: self topEntity source.
			self assert: self topEntity sourceCode equals: 'x[y]'.
			self assert: (self topEntity isOfType: FASTPySubscript).
			
			"Testing value of multivalued relation indices"
			self assert: self topEntity indices size equals: 1.
			
				stack push: (stack top indices at: 1).
				self assert: self topEntity sourceCode equals: 'y'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			
			"Testing value of monovalue relation value"
			self assert: self topEntity value isNotNil.

				stack push: self topEntity value.
				self assert: self topEntity sourceCode equals: 'x'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			stack pop.
		
		"Testing value of monovalue relation target"
		self assert: self topEntity target isNotNil.

			stack push: self topEntity target.
			self assert: self topEntity sourceCode equals: 'f'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		stack pop.
	
	"Testing value of multivalued relation statements"
	self assert: self topEntity statements size equals: 1.
	
		stack push: (stack top statements at: 1).
		self assert: self topEntity sourceCode equals: 'pass'.
		self assert: (self topEntity isOfType: FASTPyPassStatement)
]

{ #category : 'tests - with statement' }
FASTPythonImporterTest >> testAsPatternWithSubscript [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testAsPatternWithSubscript 	"

	self parse: 'with get() as targets[1][0]: pass'.

	self assert: (fast allWithType: FASTPyAsPattern) size equals: 1.
	self assert: (fast allWithType: FASTPyCall) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 2.
	self assert: (fast allWithType: FASTPyInteger) size equals: 2.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPassStatement) size equals: 1.
	self assert: (fast allWithType: FASTPySubscript) size equals: 2.
	self assert: (fast allWithType: FASTPyWithStatement) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'with get() as targets[1][0]: pass'.
	self assert: (self topEntity isOfType: FASTPyWithStatement).
	self assert: (self topEntity isOfType: FASTTStatementBlock).
	
	"Testing value of multivalued relation items"
	self assert: self topEntity items size equals: 1.
	
		stack push: (stack top items at: 1).
		self assert: self topEntity sourceCode equals: 'get() as targets[1][0]'.
		self assert: (self topEntity isOfType: FASTPyAsPattern).
		
		"Testing value of monovalue relation source"
		self assert: self topEntity source isNotNil.

			stack push: self topEntity source.
			self assert: self topEntity sourceCode equals: 'get()'.
			self assert: (self topEntity isOfType: FASTPyCall).
			self assert: (self topEntity isOfType: FASTTInvocation).
			self assert: (self topEntity isOfType: FASTTWithArguments).
			
			"Testing value of monovalue relation callee"
			self assert: self topEntity callee isNotNil.

				stack push: self topEntity callee.
				self assert: self topEntity sourceCode equals: 'get'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			stack pop.
		
		"Testing value of monovalue relation target"
		self assert: self topEntity target isNotNil.

			stack push: self topEntity target.
			self assert: self topEntity sourceCode equals: 'targets[1][0]'.
			self assert: (self topEntity isOfType: FASTPySubscript).
			
			"Testing value of multivalued relation indices"
			self assert: self topEntity indices size equals: 1.
			
				stack push: (stack top indices at: 1).
				self assert: self topEntity sourceCode equals: '0'.
				self assert: (self topEntity isOfType: FASTPyInteger).
				self assert: (self topEntity isOfType: FASTTLiteral).
				self assert: (self topEntity isOfType: FASTTNumericalLiteral).
				stack pop.
			
			"Testing value of monovalue relation value"
			self assert: self topEntity value isNotNil.

				stack push: self topEntity value.
				self assert: self topEntity sourceCode equals: 'targets[1]'.
				self assert: (self topEntity isOfType: FASTPySubscript).
				
				"Testing value of multivalued relation indices"
				self assert: self topEntity indices size equals: 1.
				
					stack push: (stack top indices at: 1).
					self assert: self topEntity sourceCode equals: '1'.
					self assert: (self topEntity isOfType: FASTPyInteger).
					self assert: (self topEntity isOfType: FASTTLiteral).
					self assert: (self topEntity isOfType: FASTTNumericalLiteral).
					stack pop.
				
				"Testing value of monovalue relation value"
				self assert: self topEntity value isNotNil.

					stack push: self topEntity value.
					self assert: self topEntity sourceCode equals: 'targets'.
					self assert: (self topEntity isOfType: FASTPyIdentifier).
					stack pop.
				stack pop.
			stack pop.
		stack pop.
	
	"Testing value of multivalued relation statements"
	self assert: self topEntity statements size equals: 1.
	
		stack push: (stack top statements at: 1).
		self assert: self topEntity sourceCode equals: 'pass'.
		self assert: (self topEntity isOfType: FASTPyPassStatement)
]

{ #category : 'tests - with statement' }
FASTPythonImporterTest >> testAsPatternWithTuple [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testAsPatternWithTuple 	"

	self parse: 'with open() as (f, g): pass'.

	self assert: (fast allWithType: FASTPyAsPattern) size equals: 1.
	self assert: (fast allWithType: FASTPyCall) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 3.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPassStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyTuple) size equals: 1.
	self assert: (fast allWithType: FASTPyWithStatement) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'with open() as (f, g): pass'.
	self assert: (self topEntity isOfType: FASTPyWithStatement).
	self assert: (self topEntity isOfType: FASTTStatementBlock).
	
	"Testing value of multivalued relation items"
	self assert: self topEntity items size equals: 1.
	
		stack push: (stack top items at: 1).
		self assert: self topEntity sourceCode equals: 'open() as (f, g)'.
		self assert: (self topEntity isOfType: FASTPyAsPattern).
		
		"Testing value of monovalue relation source"
		self assert: self topEntity source isNotNil.

			stack push: self topEntity source.
			self assert: self topEntity sourceCode equals: 'open()'.
			self assert: (self topEntity isOfType: FASTPyCall).
			self assert: (self topEntity isOfType: FASTTInvocation).
			self assert: (self topEntity isOfType: FASTTWithArguments).
			
			"Testing value of monovalue relation callee"
			self assert: self topEntity callee isNotNil.

				stack push: self topEntity callee.
				self assert: self topEntity sourceCode equals: 'open'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			stack pop.
		
		"Testing value of monovalue relation target"
		self assert: self topEntity target isNotNil.

			stack push: self topEntity target.
			self assert: self topEntity sourceCode equals: '(f, g)'.
			self assert: (self topEntity isOfType: FASTPyTuple).
			
			"Testing value of multivalued relation initializers"
			self assert: self topEntity initializers size equals: 2.
			
				stack push: (stack top initializers at: 1).
				self assert: self topEntity sourceCode equals: 'f'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			
				stack push: (stack top initializers at: 2).
				self assert: self topEntity sourceCode equals: 'g'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			stack pop.
		stack pop.
	
	"Testing value of multivalued relation statements"
	self assert: self topEntity statements size equals: 1.
	
		stack push: (stack top statements at: 1).
		self assert: self topEntity sourceCode equals: 'pass'.
		self assert: (self topEntity isOfType: FASTPyPassStatement)
]

{ #category : 'tests - statements' }
FASTPythonImporterTest >> testAssertStatement [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testAssertStatement 	"

	self parse: 'def aFunction(a, b):
    assert b != 0'.

	self assert: (fast allWithType: FASTPyAssertStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyBlock) size equals: 1.
	self assert: (fast allWithType: FASTPyComparisonOperator) size equals: 1.
	self assert: (fast allWithType: FASTPyFunctionDefinition) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyInteger) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyParameter) size equals: 2.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'def aFunction(a, b):
    assert b != 0' withPlatformLineEndings.
	self assert: (self topEntity isOfType: FASTPyFunctionDefinition).
	self assert: (self topEntity isOfType: FASTPyTDefinition).
	self assert: (self topEntity isOfType: FASTTWithParameters).
	self assert: (self topEntity isOfType: FASTTWithStatements).
	self assert: self topEntity name equals: 'aFunction'.
	
	"Testing value of multivalued relation parameters"
	self assert: self topEntity parameters size equals: 2.
	
		stack push: (stack top parameters at: 1).
		self assert: self topEntity sourceCode equals: 'a'.
		self assert: (self topEntity isOfType: FASTPyParameter).
		self assert: (self topEntity isOfType: FASTTVariableEntity).
		self assert: self topEntity name equals: 'a'.
		stack pop.
	
		stack push: (stack top parameters at: 2).
		self assert: self topEntity sourceCode equals: 'b'.
		self assert: (self topEntity isOfType: FASTPyParameter).
		self assert: (self topEntity isOfType: FASTTVariableEntity).
		self assert: self topEntity name equals: 'b'.
		stack pop.
	
	"Testing value of monovalue relation statementBlock"
	self assert: self topEntity statementBlock isNotNil.

		stack push: self topEntity statementBlock.
		self assert: self topEntity sourceCode equals: 'assert b != 0'.
		self assert: (self topEntity isOfType: FASTPyBlock).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'assert b != 0'.
			self assert: (self topEntity isOfType: FASTPyAssertStatement).
			
			"Testing value of multivalued relation expressions"
			self assert: self topEntity expressions size equals: 1.
			
				stack push: (stack top expressions at: 1).
				self assert: self topEntity sourceCode equals: 'b != 0'.
				self assert: (self topEntity isOfType: FASTPyComparisonOperator).
				
				"Testing value of multivalued relation operands"
				self assert: self topEntity operands size equals: 2.
				
					stack push: (stack top operands at: 1).
					self assert: self topEntity sourceCode equals: 'b'.
					self assert: (self topEntity isOfType: FASTPyIdentifier).
					stack pop.
				
					stack push: (stack top operands at: 2).
					self assert: self topEntity sourceCode equals: '0'.
					self assert: (self topEntity isOfType: FASTPyInteger).
					self assert: (self topEntity isOfType: FASTTLiteral).
					self assert: (self topEntity isOfType: FASTTNumericalLiteral).
					stack pop.
				self assert: self topEntity operators equals: #('!=')
]

{ #category : 'tests - statements' }
FASTPythonImporterTest >> testAssertStatementWithConcatenatedString [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testAssertStatementWithConcatenatedString 	"

	self parse: 'assert 0, "Could not find method in self.functions and no "\
                          "instance installed"'.

	self assert: (fast allWithType: FASTPyAssertStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyConcatenatedString) size equals: 1.
	self assert: (fast allWithType: FASTPyInteger) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyString) size equals: 2.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'assert 0, "Could not find method in self.functions and no "\
                          "instance installed"' withPlatformLineEndings.
	self assert: (self topEntity isOfType: FASTPyAssertStatement).
	
	"Testing value of multivalued relation expressions"
	self assert: self topEntity expressions size equals: 2.
	
		stack push: (stack top expressions at: 1).
		self assert: self topEntity sourceCode equals: '0'.
		self assert: (self topEntity isOfType: FASTPyInteger).
		self assert: (self topEntity isOfType: FASTTLiteral).
		self assert: (self topEntity isOfType: FASTTNumericalLiteral).
		stack pop.
	
		stack push: (stack top expressions at: 2).
		self assert: self topEntity sourceCode equals: '"Could not find method in self.functions and no "\
                          "instance installed"' withPlatformLineEndings.
		self assert: (self topEntity isOfType: FASTPyConcatenatedString).
		
		"Testing value of multivalued relation strings"
		self assert: self topEntity strings size equals: 2.
		
			stack push: (stack top strings at: 1).
			self assert: self topEntity sourceCode equals: '"Could not find method in self.functions and no "'.
			self assert: (self topEntity isOfType: FASTPyString).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTStringLiteral).
			stack pop.
		
			stack push: (stack top strings at: 2).
			self assert: self topEntity sourceCode equals: '"instance installed"'.
			self assert: (self topEntity isOfType: FASTPyString).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTStringLiteral)
]

{ #category : 'tests - assignments' }
FASTPythonImporterTest >> testAssignment [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testAssignment 	"

	self parse: 'x = 1'.

	self assert: (fast allWithType: FASTPyAssignment) size equals: 1.
	self assert: (fast allWithType: FASTPyInteger) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyVariable) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'x = 1'.
	self assert: (self topEntity isOfType: FASTPyAssignment).
	
	"Testing value of monovalue relation left"
	self assert: self topEntity left isNotNil.

		stack push: self topEntity left.
		self assert: self topEntity sourceCode equals: 'x'.
		self assert: (self topEntity isOfType: FASTPyVariable).
		self assert: (self topEntity isOfType: FASTTVariableEntity).
		stack pop.
	
	"Testing value of monovalue relation right"
	self assert: self topEntity right isNotNil.

		stack push: self topEntity right.
		self assert: self topEntity sourceCode equals: '1'.
		self assert: (self topEntity isOfType: FASTPyInteger).
		self assert: (self topEntity isOfType: FASTTLiteral).
		self assert: (self topEntity isOfType: FASTTNumericalLiteral)
]

{ #category : 'tests - assignments' }
FASTPythonImporterTest >> testAssignmentInAttribute [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testAssignmentInAttribute 	"

	self parse: 'x.y = 1'.

	self assert: (fast allWithType: FASTPyAssignment) size equals: 1.
	self assert: (fast allWithType: FASTPyAttributeAccess) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 2.
	self assert: (fast allWithType: FASTPyInteger) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'x.y = 1'.
	self assert: (self topEntity isOfType: FASTPyAssignment).
	
	"Testing value of monovalue relation left"
	self assert: self topEntity left isNotNil.

		stack push: self topEntity left.
		self assert: self topEntity sourceCode equals: 'x.y'.
		self assert: (self topEntity isOfType: FASTPyAttributeAccess).
		
		"Testing value of monovalue relation attribute"
		self assert: self topEntity attribute isNotNil.

			stack push: self topEntity attribute.
			self assert: self topEntity sourceCode equals: 'y'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		
		"Testing value of monovalue relation object"
		self assert: self topEntity object isNotNil.

			stack push: self topEntity object.
			self assert: self topEntity sourceCode equals: 'x'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation right"
	self assert: self topEntity right isNotNil.

		stack push: self topEntity right.
		self assert: self topEntity sourceCode equals: '1'.
		self assert: (self topEntity isOfType: FASTPyInteger).
		self assert: (self topEntity isOfType: FASTTLiteral).
		self assert: (self topEntity isOfType: FASTTNumericalLiteral)
]

{ #category : 'tests - assignments' }
FASTPythonImporterTest >> testAssignmentInList [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testAssignmentInList 	"

	self parse: '[ remote ] = 1'.

	self assert: (fast allWithType: FASTPyAssignment) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyInteger) size equals: 1.
	self assert: (fast allWithType: FASTPyList) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: '[ remote ] = 1'.
	self assert: (self topEntity isOfType: FASTPyAssignment).
	
	"Testing value of monovalue relation left"
	self assert: self topEntity left isNotNil.

		stack push: self topEntity left.
		self assert: self topEntity sourceCode equals: '[ remote ]'.
		self assert: (self topEntity isOfType: FASTPyList).
		
		"Testing value of multivalued relation initializers"
		self assert: self topEntity initializers size equals: 1.
		
			stack push: (stack top initializers at: 1).
			self assert: self topEntity sourceCode equals: 'remote'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation right"
	self assert: self topEntity right isNotNil.

		stack push: self topEntity right.
		self assert: self topEntity sourceCode equals: '1'.
		self assert: (self topEntity isOfType: FASTPyInteger).
		self assert: (self topEntity isOfType: FASTTLiteral).
		self assert: (self topEntity isOfType: FASTTNumericalLiteral)
]

{ #category : 'tests - assignments' }
FASTPythonImporterTest >> testAssignmentInPatternList [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testAssignmentInPatternList 	"

	self parse: 'a, b = 1, 2'.

	self assert: (fast allWithType: FASTPyAssignment) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 2.
	self assert: (fast allWithType: FASTPyInteger) size equals: 2.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyTuple) size equals: 2.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'a, b = 1, 2'.
	self assert: (self topEntity isOfType: FASTPyAssignment).
	
	"Testing value of monovalue relation left"
	self assert: self topEntity left isNotNil.

		stack push: self topEntity left.
		self assert: self topEntity sourceCode equals: 'a, b'.
		self assert: (self topEntity isOfType: FASTPyTuple).
		
		"Testing value of multivalued relation initializers"
		self assert: self topEntity initializers size equals: 2.
		
			stack push: (stack top initializers at: 1).
			self assert: self topEntity sourceCode equals: 'a'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		
			stack push: (stack top initializers at: 2).
			self assert: self topEntity sourceCode equals: 'b'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation right"
	self assert: self topEntity right isNotNil.

		stack push: self topEntity right.
		self assert: self topEntity sourceCode equals: '1, 2'.
		self assert: (self topEntity isOfType: FASTPyTuple).
		
		"Testing value of multivalued relation initializers"
		self assert: self topEntity initializers size equals: 2.
		
			stack push: (stack top initializers at: 1).
			self assert: self topEntity sourceCode equals: '1'.
			self assert: (self topEntity isOfType: FASTPyInteger).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTNumericalLiteral).
			stack pop.
		
			stack push: (stack top initializers at: 2).
			self assert: self topEntity sourceCode equals: '2'.
			self assert: (self topEntity isOfType: FASTPyInteger).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTNumericalLiteral)
]

{ #category : 'tests - assignments' }
FASTPythonImporterTest >> testAssignmentInSlice [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testAssignmentInSlice 	"

	self parse: 'lst[i:j] = seq'.

	self assert: (fast allWithType: FASTPyAssignment) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 4.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPySlice) size equals: 1.
	self assert: (fast allWithType: FASTPySubscript) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'lst[i:j] = seq'.
	self assert: (self topEntity isOfType: FASTPyAssignment).
	
	"Testing value of monovalue relation left"
	self assert: self topEntity left isNotNil.

		stack push: self topEntity left.
		self assert: self topEntity sourceCode equals: 'lst[i:j]'.
		self assert: (self topEntity isOfType: FASTPySubscript).
		
		"Testing value of multivalued relation indices"
		self assert: self topEntity indices size equals: 1.
		
			stack push: (stack top indices at: 1).
			self assert: self topEntity sourceCode equals: 'i:j'.
			self assert: (self topEntity isOfType: FASTPySlice).
			
			"Testing value of multivalued relation components"
			self assert: self topEntity components size equals: 2.
			
				stack push: (stack top components at: 1).
				self assert: self topEntity sourceCode equals: 'i'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			
				stack push: (stack top components at: 2).
				self assert: self topEntity sourceCode equals: 'j'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			stack pop.
		
		"Testing value of monovalue relation value"
		self assert: self topEntity value isNotNil.

			stack push: self topEntity value.
			self assert: self topEntity sourceCode equals: 'lst'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation right"
	self assert: self topEntity right isNotNil.

		stack push: self topEntity right.
		self assert: self topEntity sourceCode equals: 'seq'.
		self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - assignments' }
FASTPythonImporterTest >> testAssignmentInSubscript [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testAssignmentInSubscript 	"

	self parse: 'variables[1] = 1'.

	self assert: (fast allWithType: FASTPyAssignment) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyInteger) size equals: 2.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPySubscript) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'variables[1] = 1'.
	self assert: (self topEntity isOfType: FASTPyAssignment).
	
	"Testing value of monovalue relation left"
	self assert: self topEntity left isNotNil.

		stack push: self topEntity left.
		self assert: self topEntity sourceCode equals: 'variables[1]'.
		self assert: (self topEntity isOfType: FASTPySubscript).
		
		"Testing value of multivalued relation indices"
		self assert: self topEntity indices size equals: 1.
		
			stack push: (stack top indices at: 1).
			self assert: self topEntity sourceCode equals: '1'.
			self assert: (self topEntity isOfType: FASTPyInteger).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTNumericalLiteral).
			stack pop.
		
		"Testing value of monovalue relation value"
		self assert: self topEntity value isNotNil.

			stack push: self topEntity value.
			self assert: self topEntity sourceCode equals: 'variables'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation right"
	self assert: self topEntity right isNotNil.

		stack push: self topEntity right.
		self assert: self topEntity sourceCode equals: '1'.
		self assert: (self topEntity isOfType: FASTPyInteger).
		self assert: (self topEntity isOfType: FASTTLiteral).
		self assert: (self topEntity isOfType: FASTTNumericalLiteral)
]

{ #category : 'tests - assignments' }
FASTPythonImporterTest >> testAssignmentInTuple [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testAssignmentInTuple 	"

	self parse: '(a, b) = x'.

	self assert: (fast allWithType: FASTPyAssignment) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 3.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyTuple) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: '(a, b) = x'.
	self assert: (self topEntity isOfType: FASTPyAssignment).
	
	"Testing value of monovalue relation left"
	self assert: self topEntity left isNotNil.

		stack push: self topEntity left.
		self assert: self topEntity sourceCode equals: '(a, b)'.
		self assert: (self topEntity isOfType: FASTPyTuple).
		
		"Testing value of multivalued relation initializers"
		self assert: self topEntity initializers size equals: 2.
		
			stack push: (stack top initializers at: 1).
			self assert: self topEntity sourceCode equals: 'a'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		
			stack push: (stack top initializers at: 2).
			self assert: self topEntity sourceCode equals: 'b'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation right"
	self assert: self topEntity right isNotNil.

		stack push: self topEntity right.
		self assert: self topEntity sourceCode equals: 'x'.
		self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - assignments' }
FASTPythonImporterTest >> testAssignmentToAttribute [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testAssignmentToAttribute 	"

	self parse: 'x = x.y'.

	self assert: (fast allWithType: FASTPyAssignment) size equals: 1.
	self assert: (fast allWithType: FASTPyAttributeAccess) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 2.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyVariable) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'x = x.y'.
	self assert: (self topEntity isOfType: FASTPyAssignment).
	
	"Testing value of monovalue relation left"
	self assert: self topEntity left isNotNil.

		stack push: self topEntity left.
		self assert: self topEntity sourceCode equals: 'x'.
		self assert: (self topEntity isOfType: FASTPyVariable).
		self assert: (self topEntity isOfType: FASTTVariableEntity).
		stack pop.
	
	"Testing value of monovalue relation right"
	self assert: self topEntity right isNotNil.

		stack push: self topEntity right.
		self assert: self topEntity sourceCode equals: 'x.y'.
		self assert: (self topEntity isOfType: FASTPyAttributeAccess).
		
		"Testing value of monovalue relation attribute"
		self assert: self topEntity attribute isNotNil.

			stack push: self topEntity attribute.
			self assert: self topEntity sourceCode equals: 'y'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		
		"Testing value of monovalue relation object"
		self assert: self topEntity object isNotNil.

			stack push: self topEntity object.
			self assert: self topEntity sourceCode equals: 'x'.
			self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - assignments' }
FASTPythonImporterTest >> testAssignmentToBinaryOperator [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testAssignmentToBinaryOperator 	"

	self parse: 'x = 10 / 2'.

	self assert: (fast allWithType: FASTPyAssignment) size equals: 1.
	self assert: (fast allWithType: FASTPyBinaryOperator) size equals: 1.
	self assert: (fast allWithType: FASTPyInteger) size equals: 2.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyVariable) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'x = 10 / 2'.
	self assert: (self topEntity isOfType: FASTPyAssignment).
	
	"Testing value of monovalue relation left"
	self assert: self topEntity left isNotNil.

		stack push: self topEntity left.
		self assert: self topEntity sourceCode equals: 'x'.
		self assert: (self topEntity isOfType: FASTPyVariable).
		self assert: (self topEntity isOfType: FASTTVariableEntity).
		stack pop.
	
	"Testing value of monovalue relation right"
	self assert: self topEntity right isNotNil.

		stack push: self topEntity right.
		self assert: self topEntity sourceCode equals: '10 / 2'.
		self assert: (self topEntity isOfType: FASTPyBinaryOperator).
		self assert: (self topEntity isOfType: FASTTBinaryExpression).
		
		"Testing value of monovalue relation leftOperand"
		self assert: self topEntity leftOperand isNotNil.

			stack push: self topEntity leftOperand.
			self assert: self topEntity sourceCode equals: '10'.
			self assert: (self topEntity isOfType: FASTPyInteger).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTNumericalLiteral).
			stack pop.
		self assert: self topEntity operator equals: '/'.
		
		"Testing value of monovalue relation rightOperand"
		self assert: self topEntity rightOperand isNotNil.

			stack push: self topEntity rightOperand.
			self assert: self topEntity sourceCode equals: '2'.
			self assert: (self topEntity isOfType: FASTPyInteger).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTNumericalLiteral)
]

{ #category : 'tests - assignments' }
FASTPythonImporterTest >> testAssignmentToBooleanOperator [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testAssignmentToBooleanOperator 	"

	self parse: 'x = x and y'.

	self assert: (fast allWithType: FASTPyAssignment) size equals: 1.
	self assert: (fast allWithType: FASTPyBooleanOperator) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 2.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyVariable) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'x = x and y'.
	self assert: (self topEntity isOfType: FASTPyAssignment).
	
	"Testing value of monovalue relation left"
	self assert: self topEntity left isNotNil.

		stack push: self topEntity left.
		self assert: self topEntity sourceCode equals: 'x'.
		self assert: (self topEntity isOfType: FASTPyVariable).
		self assert: (self topEntity isOfType: FASTTVariableEntity).
		stack pop.
	
	"Testing value of monovalue relation right"
	self assert: self topEntity right isNotNil.

		stack push: self topEntity right.
		self assert: self topEntity sourceCode equals: 'x and y'.
		self assert: (self topEntity isOfType: FASTPyBooleanOperator).
		self assert: (self topEntity isOfType: FASTTBinaryExpression).
		
		"Testing value of monovalue relation leftOperand"
		self assert: self topEntity leftOperand isNotNil.

			stack push: self topEntity leftOperand.
			self assert: self topEntity sourceCode equals: 'x'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		self assert: self topEntity operator equals: 'and'.
		
		"Testing value of monovalue relation rightOperand"
		self assert: self topEntity rightOperand isNotNil.

			stack push: self topEntity rightOperand.
			self assert: self topEntity sourceCode equals: 'y'.
			self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - assignments' }
FASTPythonImporterTest >> testAssignmentToCall [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testAssignmentToCall 	"

	self parse: 'x = obj()'.

	self assert: (fast allWithType: FASTPyAssignment) size equals: 1.
	self assert: (fast allWithType: FASTPyCall) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyVariable) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'x = obj()'.
	self assert: (self topEntity isOfType: FASTPyAssignment).
	
	"Testing value of monovalue relation left"
	self assert: self topEntity left isNotNil.

		stack push: self topEntity left.
		self assert: self topEntity sourceCode equals: 'x'.
		self assert: (self topEntity isOfType: FASTPyVariable).
		self assert: (self topEntity isOfType: FASTTVariableEntity).
		stack pop.
	
	"Testing value of monovalue relation right"
	self assert: self topEntity right isNotNil.

		stack push: self topEntity right.
		self assert: self topEntity sourceCode equals: 'obj()'.
		self assert: (self topEntity isOfType: FASTPyCall).
		self assert: (self topEntity isOfType: FASTTInvocation).
		self assert: (self topEntity isOfType: FASTTWithArguments).
		
		"Testing value of monovalue relation callee"
		self assert: self topEntity callee isNotNil.

			stack push: self topEntity callee.
			self assert: self topEntity sourceCode equals: 'obj'.
			self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - assignments' }
FASTPythonImporterTest >> testAssignmentToComparisonOperator [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testAssignmentToComparisonOperator 	"

	self parse: 'x = 1 > 3'.

	self assert: (fast allWithType: FASTPyAssignment) size equals: 1.
	self assert: (fast allWithType: FASTPyComparisonOperator) size equals: 1.
	self assert: (fast allWithType: FASTPyInteger) size equals: 2.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyVariable) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'x = 1 > 3'.
	self assert: (self topEntity isOfType: FASTPyAssignment).
	
	"Testing value of monovalue relation left"
	self assert: self topEntity left isNotNil.

		stack push: self topEntity left.
		self assert: self topEntity sourceCode equals: 'x'.
		self assert: (self topEntity isOfType: FASTPyVariable).
		self assert: (self topEntity isOfType: FASTTVariableEntity).
		stack pop.
	
	"Testing value of monovalue relation right"
	self assert: self topEntity right isNotNil.

		stack push: self topEntity right.
		self assert: self topEntity sourceCode equals: '1 > 3'.
		self assert: (self topEntity isOfType: FASTPyComparisonOperator).
		
		"Testing value of multivalued relation operands"
		self assert: self topEntity operands size equals: 2.
		
			stack push: (stack top operands at: 1).
			self assert: self topEntity sourceCode equals: '1'.
			self assert: (self topEntity isOfType: FASTPyInteger).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTNumericalLiteral).
			stack pop.
		
			stack push: (stack top operands at: 2).
			self assert: self topEntity sourceCode equals: '3'.
			self assert: (self topEntity isOfType: FASTPyInteger).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTNumericalLiteral).
			stack pop.
		self assert: self topEntity operators equals: #('>')
]

{ #category : 'tests - assignments' }
FASTPythonImporterTest >> testAssignmentToComplexe [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testAssignmentToComplexe 	"

	self parse: 'x = 1j'.

	self assert: (fast allWithType: FASTPyAssignment) size equals: 1.
	self assert: (fast allWithType: FASTPyComplex) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyVariable) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'x = 1j'.
	self assert: (self topEntity isOfType: FASTPyAssignment).
	
	"Testing value of monovalue relation left"
	self assert: self topEntity left isNotNil.

		stack push: self topEntity left.
		self assert: self topEntity sourceCode equals: 'x'.
		self assert: (self topEntity isOfType: FASTPyVariable).
		self assert: (self topEntity isOfType: FASTTVariableEntity).
		stack pop.
	
	"Testing value of monovalue relation right"
	self assert: self topEntity right isNotNil.

		stack push: self topEntity right.
		self assert: self topEntity sourceCode equals: '1j'.
		self assert: (self topEntity isOfType: FASTPyComplex).
		self assert: (self topEntity isOfType: FASTTLiteral).
		self assert: (self topEntity isOfType: FASTTNumericalLiteral)
]

{ #category : 'tests - assignments' }
FASTPythonImporterTest >> testAssignmentToConditionalExpression [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testAssignmentToConditionalExpression 	"

	self parse: 'x = x if True else y'.

	self assert: (fast allWithType: FASTPyAssignment) size equals: 1.
	self assert: (fast allWithType: FASTPyBoolean) size equals: 1.
	self assert: (fast allWithType: FASTPyConditionalExpression) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 2.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyVariable) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'x = x if True else y'.
	self assert: (self topEntity isOfType: FASTPyAssignment).
	
	"Testing value of monovalue relation left"
	self assert: self topEntity left isNotNil.

		stack push: self topEntity left.
		self assert: self topEntity sourceCode equals: 'x'.
		self assert: (self topEntity isOfType: FASTPyVariable).
		self assert: (self topEntity isOfType: FASTTVariableEntity).
		stack pop.
	
	"Testing value of monovalue relation right"
	self assert: self topEntity right isNotNil.

		stack push: self topEntity right.
		self assert: self topEntity sourceCode equals: 'x if True else y'.
		self assert: (self topEntity isOfType: FASTPyConditionalExpression).
		self assert: (self topEntity isOfType: FASTTWithCondition).
		
		"Testing value of monovalue relation condition"
		self assert: self topEntity condition isNotNil.

			stack push: self topEntity condition.
			self assert: self topEntity sourceCode equals: 'True'.
			self assert: (self topEntity isOfType: FASTPyBoolean).
			self assert: (self topEntity isOfType: FASTTBooleanLiteral).
			self assert: (self topEntity isOfType: FASTTLiteral).
			stack pop.
		
		"Testing value of monovalue relation elseExpression"
		self assert: self topEntity elseExpression isNotNil.

			stack push: self topEntity elseExpression.
			self assert: self topEntity sourceCode equals: 'y'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		
		"Testing value of monovalue relation thenExpression"
		self assert: self topEntity thenExpression isNotNil.

			stack push: self topEntity thenExpression.
			self assert: self topEntity sourceCode equals: 'x'.
			self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - assignments' }
FASTPythonImporterTest >> testAssignmentToDictionary [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testAssignmentToDictionary 	"

	self parse: 'x = { 1:1 }'.

	self assert: (fast allWithType: FASTPyAssignment) size equals: 1.
	self assert: (fast allWithType: FASTPyDictionary) size equals: 1.
	self assert: (fast allWithType: FASTPyInteger) size equals: 2.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPair) size equals: 1.
	self assert: (fast allWithType: FASTPyVariable) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'x = { 1:1 }'.
	self assert: (self topEntity isOfType: FASTPyAssignment).
	
	"Testing value of monovalue relation left"
	self assert: self topEntity left isNotNil.

		stack push: self topEntity left.
		self assert: self topEntity sourceCode equals: 'x'.
		self assert: (self topEntity isOfType: FASTPyVariable).
		self assert: (self topEntity isOfType: FASTTVariableEntity).
		stack pop.
	
	"Testing value of monovalue relation right"
	self assert: self topEntity right isNotNil.

		stack push: self topEntity right.
		self assert: self topEntity sourceCode equals: '{ 1:1 }'.
		self assert: (self topEntity isOfType: FASTPyDictionary).
		
		"Testing value of multivalued relation initializers"
		self assert: self topEntity initializers size equals: 1.
		
			stack push: (stack top initializers at: 1).
			self assert: self topEntity sourceCode equals: '1:1'.
			self assert: (self topEntity isOfType: FASTPyPair).
			
			"Testing value of monovalue relation key"
			self assert: self topEntity key isNotNil.

				stack push: self topEntity key.
				self assert: self topEntity sourceCode equals: '1'.
				self assert: (self topEntity isOfType: FASTPyInteger).
				self assert: (self topEntity isOfType: FASTTLiteral).
				self assert: (self topEntity isOfType: FASTTNumericalLiteral).
				stack pop.
			
			"Testing value of monovalue relation value"
			self assert: self topEntity value isNotNil.

				stack push: self topEntity value.
				self assert: self topEntity sourceCode equals: '1'.
				self assert: (self topEntity isOfType: FASTPyInteger).
				self assert: (self topEntity isOfType: FASTTLiteral).
				self assert: (self topEntity isOfType: FASTTNumericalLiteral)
]

{ #category : 'tests - assignments' }
FASTPythonImporterTest >> testAssignmentToEllipsis [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testAssignmentToEllipsis 	"

	self parse: 'x = ...'.

	self assert: (fast allWithType: FASTPyAssignment) size equals: 1.
	self assert: (fast allWithType: FASTPyEllipsis) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyVariable) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'x = ...'.
	self assert: (self topEntity isOfType: FASTPyAssignment).
	
	"Testing value of monovalue relation left"
	self assert: self topEntity left isNotNil.

		stack push: self topEntity left.
		self assert: self topEntity sourceCode equals: 'x'.
		self assert: (self topEntity isOfType: FASTPyVariable).
		self assert: (self topEntity isOfType: FASTTVariableEntity).
		stack pop.
	
	"Testing value of monovalue relation right"
	self assert: self topEntity right isNotNil.

		stack push: self topEntity right.
		self assert: self topEntity sourceCode equals: '...'.
		self assert: (self topEntity isOfType: FASTPyEllipsis).
		self assert: (self topEntity isOfType: FASTTLiteral)
]

{ #category : 'tests - assignments' }
FASTPythonImporterTest >> testAssignmentToFalse [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testAssignmentToFalse 	"

	self parse: 'x = False'.

	self assert: (fast allWithType: FASTPyAssignment) size equals: 1.
	self assert: (fast allWithType: FASTPyBoolean) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyVariable) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'x = False'.
	self assert: (self topEntity isOfType: FASTPyAssignment).
	
	"Testing value of monovalue relation left"
	self assert: self topEntity left isNotNil.

		stack push: self topEntity left.
		self assert: self topEntity sourceCode equals: 'x'.
		self assert: (self topEntity isOfType: FASTPyVariable).
		self assert: (self topEntity isOfType: FASTTVariableEntity).
		stack pop.
	
	"Testing value of monovalue relation right"
	self assert: self topEntity right isNotNil.

		stack push: self topEntity right.
		self assert: self topEntity sourceCode equals: 'False'.
		self assert: (self topEntity isOfType: FASTPyBoolean).
		self assert: (self topEntity isOfType: FASTTBooleanLiteral).
		self assert: (self topEntity isOfType: FASTTLiteral)
]

{ #category : 'tests - assignments' }
FASTPythonImporterTest >> testAssignmentToFloat [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testAssignmentToFloat 	"

	self parse: 'x = 0.1'.

	self assert: (fast allWithType: FASTPyAssignment) size equals: 1.
	self assert: (fast allWithType: FASTPyFloat) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyVariable) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'x = 0.1'.
	self assert: (self topEntity isOfType: FASTPyAssignment).
	
	"Testing value of monovalue relation left"
	self assert: self topEntity left isNotNil.

		stack push: self topEntity left.
		self assert: self topEntity sourceCode equals: 'x'.
		self assert: (self topEntity isOfType: FASTPyVariable).
		self assert: (self topEntity isOfType: FASTTVariableEntity).
		stack pop.
	
	"Testing value of monovalue relation right"
	self assert: self topEntity right isNotNil.

		stack push: self topEntity right.
		self assert: self topEntity sourceCode equals: '0.1'.
		self assert: (self topEntity isOfType: FASTPyFloat).
		self assert: (self topEntity isOfType: FASTTLiteral).
		self assert: (self topEntity isOfType: FASTTNumericalLiteral)
]

{ #category : 'tests - assignments' }
FASTPythonImporterTest >> testAssignmentToIdentifier [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testAssignmentToIdentifier 	"

	self parse: 'x = element'.

	self assert: (fast allWithType: FASTPyAssignment) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyVariable) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'x = element'.
	self assert: (self topEntity isOfType: FASTPyAssignment).
	
	"Testing value of monovalue relation left"
	self assert: self topEntity left isNotNil.

		stack push: self topEntity left.
		self assert: self topEntity sourceCode equals: 'x'.
		self assert: (self topEntity isOfType: FASTPyVariable).
		self assert: (self topEntity isOfType: FASTTVariableEntity).
		stack pop.
	
	"Testing value of monovalue relation right"
	self assert: self topEntity right isNotNil.

		stack push: self topEntity right.
		self assert: self topEntity sourceCode equals: 'element'.
		self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - assignments' }
FASTPythonImporterTest >> testAssignmentToInteger [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testAssignmentToInteger 	"

	self parse: 'x = 1'.

	self assert: (fast allWithType: FASTPyAssignment) size equals: 1.
	self assert: (fast allWithType: FASTPyInteger) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyVariable) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'x = 1'.
	self assert: (self topEntity isOfType: FASTPyAssignment).
	
	"Testing value of monovalue relation left"
	self assert: self topEntity left isNotNil.

		stack push: self topEntity left.
		self assert: self topEntity sourceCode equals: 'x'.
		self assert: (self topEntity isOfType: FASTPyVariable).
		self assert: (self topEntity isOfType: FASTTVariableEntity).
		stack pop.
	
	"Testing value of monovalue relation right"
	self assert: self topEntity right isNotNil.

		stack push: self topEntity right.
		self assert: self topEntity sourceCode equals: '1'.
		self assert: (self topEntity isOfType: FASTPyInteger).
		self assert: (self topEntity isOfType: FASTTLiteral).
		self assert: (self topEntity isOfType: FASTTNumericalLiteral)
]

{ #category : 'tests - assignments' }
FASTPythonImporterTest >> testAssignmentToLambda [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testAssignmentToLambda 	"

	self parse: 'x = lambda x:x'.

	self assert: (fast allWithType: FASTPyAssignment) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyLambda) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyParameter) size equals: 1.
	self assert: (fast allWithType: FASTPyVariable) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'x = lambda x:x'.
	self assert: (self topEntity isOfType: FASTPyAssignment).
	
	"Testing value of monovalue relation left"
	self assert: self topEntity left isNotNil.

		stack push: self topEntity left.
		self assert: self topEntity sourceCode equals: 'x'.
		self assert: (self topEntity isOfType: FASTPyVariable).
		self assert: (self topEntity isOfType: FASTTVariableEntity).
		stack pop.
	
	"Testing value of monovalue relation right"
	self assert: self topEntity right isNotNil.

		stack push: self topEntity right.
		self assert: self topEntity sourceCode equals: 'lambda x:x'.
		self assert: (self topEntity isOfType: FASTPyLambda).
		self assert: (self topEntity isOfType: FASTTWithParameters).
		
		"Testing value of monovalue relation expression"
		self assert: self topEntity expression isNotNil.

			stack push: self topEntity expression.
			self assert: self topEntity sourceCode equals: 'x'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		
		"Testing value of multivalued relation parameters"
		self assert: self topEntity parameters size equals: 1.
		
			stack push: (stack top parameters at: 1).
			self assert: self topEntity sourceCode equals: 'x'.
			self assert: (self topEntity isOfType: FASTPyParameter).
			self assert: (self topEntity isOfType: FASTTVariableEntity).
			self assert: self topEntity name equals: 'x'
]

{ #category : 'tests - assignments' }
FASTPythonImporterTest >> testAssignmentToList [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testAssignmentToList 	"

	self parse: 'x = [ remote ]'.

	self assert: (fast allWithType: FASTPyAssignment) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyList) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyVariable) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'x = [ remote ]'.
	self assert: (self topEntity isOfType: FASTPyAssignment).
	
	"Testing value of monovalue relation left"
	self assert: self topEntity left isNotNil.

		stack push: self topEntity left.
		self assert: self topEntity sourceCode equals: 'x'.
		self assert: (self topEntity isOfType: FASTPyVariable).
		self assert: (self topEntity isOfType: FASTTVariableEntity).
		stack pop.
	
	"Testing value of monovalue relation right"
	self assert: self topEntity right isNotNil.

		stack push: self topEntity right.
		self assert: self topEntity sourceCode equals: '[ remote ]'.
		self assert: (self topEntity isOfType: FASTPyList).
		
		"Testing value of multivalued relation initializers"
		self assert: self topEntity initializers size equals: 1.
		
			stack push: (stack top initializers at: 1).
			self assert: self topEntity sourceCode equals: 'remote'.
			self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - assignments' }
FASTPythonImporterTest >> testAssignmentToList_comprehension [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testAssignmentToList_comprehension 	"

	self parse: 'x = [random for i in range(3)]'.

	self assert: (fast allWithType: FASTPyAssignment) size equals: 1.
	self assert: (fast allWithType: FASTPyCall) size equals: 1.
	self assert: (fast allWithType: FASTPyForInClause) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 3.
	self assert: (fast allWithType: FASTPyInteger) size equals: 1.
	self assert: (fast allWithType: FASTPyListComprehension) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyVariable) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'x = [random for i in range(3)]'.
	self assert: (self topEntity isOfType: FASTPyAssignment).
	
	"Testing value of monovalue relation left"
	self assert: self topEntity left isNotNil.

		stack push: self topEntity left.
		self assert: self topEntity sourceCode equals: 'x'.
		self assert: (self topEntity isOfType: FASTPyVariable).
		self assert: (self topEntity isOfType: FASTTVariableEntity).
		stack pop.
	
	"Testing value of monovalue relation right"
	self assert: self topEntity right isNotNil.

		stack push: self topEntity right.
		self assert: self topEntity sourceCode equals: '[random for i in range(3)]'.
		self assert: (self topEntity isOfType: FASTPyListComprehension).
		self assert: (self topEntity isOfType: FASTPyTComprehension).
		self assert: (self topEntity isOfType: FASTTWithCondition).
		
		"Testing value of monovalue relation body"
		self assert: self topEntity body isNotNil.

			stack push: self topEntity body.
			self assert: self topEntity sourceCode equals: 'random'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		
		"Testing value of multivalued relation clauses"
		self assert: self topEntity clauses size equals: 1.
		
			stack push: (stack top clauses at: 1).
			self assert: self topEntity sourceCode equals: 'for i in range(3)'.
			self assert: (self topEntity isOfType: FASTPyForInClause).
			
			"Testing value of monovalue relation left"
			self assert: self topEntity left isNotNil.

				stack push: self topEntity left.
				self assert: self topEntity sourceCode equals: 'i'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			
			"Testing value of monovalue relation right"
			self assert: self topEntity right isNotNil.

				stack push: self topEntity right.
				self assert: self topEntity sourceCode equals: 'range(3)'.
				self assert: (self topEntity isOfType: FASTPyCall).
				self assert: (self topEntity isOfType: FASTTInvocation).
				self assert: (self topEntity isOfType: FASTTWithArguments).
				
				"Testing value of multivalued relation arguments"
				self assert: self topEntity arguments size equals: 1.
				
					stack push: (stack top arguments at: 1).
					self assert: self topEntity sourceCode equals: '3'.
					self assert: (self topEntity isOfType: FASTPyInteger).
					self assert: (self topEntity isOfType: FASTTLiteral).
					self assert: (self topEntity isOfType: FASTTNumericalLiteral).
					stack pop.
				
				"Testing value of monovalue relation callee"
				self assert: self topEntity callee isNotNil.

					stack push: self topEntity callee.
					self assert: self topEntity sourceCode equals: 'range'.
					self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - assignments' }
FASTPythonImporterTest >> testAssignmentToNone [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testAssignmentToNone 	"

	self parse: 'x = None'.

	self assert: (fast allWithType: FASTPyAssignment) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyNone) size equals: 1.
	self assert: (fast allWithType: FASTPyVariable) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'x = None'.
	self assert: (self topEntity isOfType: FASTPyAssignment).
	
	"Testing value of monovalue relation left"
	self assert: self topEntity left isNotNil.

		stack push: self topEntity left.
		self assert: self topEntity sourceCode equals: 'x'.
		self assert: (self topEntity isOfType: FASTPyVariable).
		self assert: (self topEntity isOfType: FASTTVariableEntity).
		stack pop.
	
	"Testing value of monovalue relation right"
	self assert: self topEntity right isNotNil.

		stack push: self topEntity right.
		self assert: self topEntity sourceCode equals: 'None'.
		self assert: (self topEntity isOfType: FASTPyNone).
		self assert: (self topEntity isOfType: FASTTLiteral)
]

{ #category : 'tests - assignments' }
FASTPythonImporterTest >> testAssignmentToNotOperator [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testAssignmentToNotOperator 	"

	self parse: 'x = not old'.

	self assert: (fast allWithType: FASTPyAssignment) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyNotOperator) size equals: 1.
	self assert: (fast allWithType: FASTPyVariable) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'x = not old'.
	self assert: (self topEntity isOfType: FASTPyAssignment).
	
	"Testing value of monovalue relation left"
	self assert: self topEntity left isNotNil.

		stack push: self topEntity left.
		self assert: self topEntity sourceCode equals: 'x'.
		self assert: (self topEntity isOfType: FASTPyVariable).
		self assert: (self topEntity isOfType: FASTTVariableEntity).
		stack pop.
	
	"Testing value of monovalue relation right"
	self assert: self topEntity right isNotNil.

		stack push: self topEntity right.
		self assert: self topEntity sourceCode equals: 'not old'.
		self assert: (self topEntity isOfType: FASTPyNotOperator).
		self assert: (self topEntity isOfType: FASTPyTUnaryOperator).
		
		"Testing value of monovalue relation argument"
		self assert: self topEntity argument isNotNil.

			stack push: self topEntity argument.
			self assert: self topEntity sourceCode equals: 'old'.
			self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - assignments' }
FASTPythonImporterTest >> testAssignmentToPatternList [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testAssignmentToPatternList 	"

	self parse: 'x = a, b'.

	self assert: (fast allWithType: FASTPyAssignment) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 2.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyTuple) size equals: 1.
	self assert: (fast allWithType: FASTPyVariable) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'x = a, b'.
	self assert: (self topEntity isOfType: FASTPyAssignment).
	
	"Testing value of monovalue relation left"
	self assert: self topEntity left isNotNil.

		stack push: self topEntity left.
		self assert: self topEntity sourceCode equals: 'x'.
		self assert: (self topEntity isOfType: FASTPyVariable).
		self assert: (self topEntity isOfType: FASTTVariableEntity).
		stack pop.
	
	"Testing value of monovalue relation right"
	self assert: self topEntity right isNotNil.

		stack push: self topEntity right.
		self assert: self topEntity sourceCode equals: 'a, b'.
		self assert: (self topEntity isOfType: FASTPyTuple).
		
		"Testing value of multivalued relation initializers"
		self assert: self topEntity initializers size equals: 2.
		
			stack push: (stack top initializers at: 1).
			self assert: self topEntity sourceCode equals: 'a'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		
			stack push: (stack top initializers at: 2).
			self assert: self topEntity sourceCode equals: 'b'.
			self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - assignments' }
FASTPythonImporterTest >> testAssignmentToSet [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testAssignmentToSet 	"

	self parse: 'x = { 1 }'.

	self assert: (fast allWithType: FASTPyAssignment) size equals: 1.
	self assert: (fast allWithType: FASTPyInteger) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPySet) size equals: 1.
	self assert: (fast allWithType: FASTPyVariable) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'x = { 1 }'.
	self assert: (self topEntity isOfType: FASTPyAssignment).
	
	"Testing value of monovalue relation left"
	self assert: self topEntity left isNotNil.

		stack push: self topEntity left.
		self assert: self topEntity sourceCode equals: 'x'.
		self assert: (self topEntity isOfType: FASTPyVariable).
		self assert: (self topEntity isOfType: FASTTVariableEntity).
		stack pop.
	
	"Testing value of monovalue relation right"
	self assert: self topEntity right isNotNil.

		stack push: self topEntity right.
		self assert: self topEntity sourceCode equals: '{ 1 }'.
		self assert: (self topEntity isOfType: FASTPySet).
		
		"Testing value of multivalued relation initializers"
		self assert: self topEntity initializers size equals: 1.
		
			stack push: (stack top initializers at: 1).
			self assert: self topEntity sourceCode equals: '1'.
			self assert: (self topEntity isOfType: FASTPyInteger).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTNumericalLiteral)
]

{ #category : 'tests - assignments' }
FASTPythonImporterTest >> testAssignmentToSetComprehension [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testAssignmentToSetComprehension 	"

	self parse: 'x = {random for i in range(3)}'.

	self assert: (fast allWithType: FASTPyAssignment) size equals: 1.
	self assert: (fast allWithType: FASTPyCall) size equals: 1.
	self assert: (fast allWithType: FASTPyForInClause) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 3.
	self assert: (fast allWithType: FASTPyInteger) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPySetComprehension) size equals: 1.
	self assert: (fast allWithType: FASTPyVariable) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'x = {random for i in range(3)}'.
	self assert: (self topEntity isOfType: FASTPyAssignment).
	
	"Testing value of monovalue relation left"
	self assert: self topEntity left isNotNil.

		stack push: self topEntity left.
		self assert: self topEntity sourceCode equals: 'x'.
		self assert: (self topEntity isOfType: FASTPyVariable).
		self assert: (self topEntity isOfType: FASTTVariableEntity).
		stack pop.
	
	"Testing value of monovalue relation right"
	self assert: self topEntity right isNotNil.

		stack push: self topEntity right.
		self assert: self topEntity sourceCode equals: '{random for i in range(3)}'.
		self assert: (self topEntity isOfType: FASTPySetComprehension).
		self assert: (self topEntity isOfType: FASTPyTComprehension).
		self assert: (self topEntity isOfType: FASTTWithCondition).
		
		"Testing value of monovalue relation body"
		self assert: self topEntity body isNotNil.

			stack push: self topEntity body.
			self assert: self topEntity sourceCode equals: 'random'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		
		"Testing value of multivalued relation clauses"
		self assert: self topEntity clauses size equals: 1.
		
			stack push: (stack top clauses at: 1).
			self assert: self topEntity sourceCode equals: 'for i in range(3)'.
			self assert: (self topEntity isOfType: FASTPyForInClause).
			
			"Testing value of monovalue relation left"
			self assert: self topEntity left isNotNil.

				stack push: self topEntity left.
				self assert: self topEntity sourceCode equals: 'i'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			
			"Testing value of monovalue relation right"
			self assert: self topEntity right isNotNil.

				stack push: self topEntity right.
				self assert: self topEntity sourceCode equals: 'range(3)'.
				self assert: (self topEntity isOfType: FASTPyCall).
				self assert: (self topEntity isOfType: FASTTInvocation).
				self assert: (self topEntity isOfType: FASTTWithArguments).
				
				"Testing value of multivalued relation arguments"
				self assert: self topEntity arguments size equals: 1.
				
					stack push: (stack top arguments at: 1).
					self assert: self topEntity sourceCode equals: '3'.
					self assert: (self topEntity isOfType: FASTPyInteger).
					self assert: (self topEntity isOfType: FASTTLiteral).
					self assert: (self topEntity isOfType: FASTTNumericalLiteral).
					stack pop.
				
				"Testing value of monovalue relation callee"
				self assert: self topEntity callee isNotNil.

					stack push: self topEntity callee.
					self assert: self topEntity sourceCode equals: 'range'.
					self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - assignments' }
FASTPythonImporterTest >> testAssignmentToString [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testAssignmentToString 	"

	self parse: 'x = "Hello"'.

	self assert: (fast allWithType: FASTPyAssignment) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyString) size equals: 1.
	self assert: (fast allWithType: FASTPyVariable) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'x = "Hello"'.
	self assert: (self topEntity isOfType: FASTPyAssignment).
	
	"Testing value of monovalue relation left"
	self assert: self topEntity left isNotNil.

		stack push: self topEntity left.
		self assert: self topEntity sourceCode equals: 'x'.
		self assert: (self topEntity isOfType: FASTPyVariable).
		self assert: (self topEntity isOfType: FASTTVariableEntity).
		stack pop.
	
	"Testing value of monovalue relation right"
	self assert: self topEntity right isNotNil.

		stack push: self topEntity right.
		self assert: self topEntity sourceCode equals: '"Hello"'.
		self assert: (self topEntity isOfType: FASTPyString).
		self assert: (self topEntity isOfType: FASTTLiteral).
		self assert: (self topEntity isOfType: FASTTStringLiteral)
]

{ #category : 'tests - assignments' }
FASTPythonImporterTest >> testAssignmentToSubscript [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testAssignmentToSubscript 	"

	self parse: 'x = attrs[2]'.

	self assert: (fast allWithType: FASTPyAssignment) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyInteger) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPySubscript) size equals: 1.
	self assert: (fast allWithType: FASTPyVariable) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'x = attrs[2]'.
	self assert: (self topEntity isOfType: FASTPyAssignment).
	
	"Testing value of monovalue relation left"
	self assert: self topEntity left isNotNil.

		stack push: self topEntity left.
		self assert: self topEntity sourceCode equals: 'x'.
		self assert: (self topEntity isOfType: FASTPyVariable).
		self assert: (self topEntity isOfType: FASTTVariableEntity).
		stack pop.
	
	"Testing value of monovalue relation right"
	self assert: self topEntity right isNotNil.

		stack push: self topEntity right.
		self assert: self topEntity sourceCode equals: 'attrs[2]'.
		self assert: (self topEntity isOfType: FASTPySubscript).
		
		"Testing value of multivalued relation indices"
		self assert: self topEntity indices size equals: 1.
		
			stack push: (stack top indices at: 1).
			self assert: self topEntity sourceCode equals: '2'.
			self assert: (self topEntity isOfType: FASTPyInteger).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTNumericalLiteral).
			stack pop.
		
		"Testing value of monovalue relation value"
		self assert: self topEntity value isNotNil.

			stack push: self topEntity value.
			self assert: self topEntity sourceCode equals: 'attrs'.
			self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - assignments' }
FASTPythonImporterTest >> testAssignmentToTrue [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testAssignmentToTrue 	"

	self parse: 'x = True'.

	self assert: (fast allWithType: FASTPyAssignment) size equals: 1.
	self assert: (fast allWithType: FASTPyBoolean) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyVariable) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'x = True'.
	self assert: (self topEntity isOfType: FASTPyAssignment).
	
	"Testing value of monovalue relation left"
	self assert: self topEntity left isNotNil.

		stack push: self topEntity left.
		self assert: self topEntity sourceCode equals: 'x'.
		self assert: (self topEntity isOfType: FASTPyVariable).
		self assert: (self topEntity isOfType: FASTTVariableEntity).
		stack pop.
	
	"Testing value of monovalue relation right"
	self assert: self topEntity right isNotNil.

		stack push: self topEntity right.
		self assert: self topEntity sourceCode equals: 'True'.
		self assert: (self topEntity isOfType: FASTPyBoolean).
		self assert: (self topEntity isOfType: FASTTBooleanLiteral).
		self assert: (self topEntity isOfType: FASTTLiteral)
]

{ #category : 'tests - assignments' }
FASTPythonImporterTest >> testAssignmentToTuple [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testAssignmentToTuple 	"

	self parse: 'x = (1,)'.

	self assert: (fast allWithType: FASTPyAssignment) size equals: 1.
	self assert: (fast allWithType: FASTPyInteger) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyTuple) size equals: 1.
	self assert: (fast allWithType: FASTPyVariable) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'x = (1,)'.
	self assert: (self topEntity isOfType: FASTPyAssignment).
	
	"Testing value of monovalue relation left"
	self assert: self topEntity left isNotNil.

		stack push: self topEntity left.
		self assert: self topEntity sourceCode equals: 'x'.
		self assert: (self topEntity isOfType: FASTPyVariable).
		self assert: (self topEntity isOfType: FASTTVariableEntity).
		stack pop.
	
	"Testing value of monovalue relation right"
	self assert: self topEntity right isNotNil.

		stack push: self topEntity right.
		self assert: self topEntity sourceCode equals: '(1,)'.
		self assert: (self topEntity isOfType: FASTPyTuple).
		
		"Testing value of multivalued relation initializers"
		self assert: self topEntity initializers size equals: 1.
		
			stack push: (stack top initializers at: 1).
			self assert: self topEntity sourceCode equals: '1'.
			self assert: (self topEntity isOfType: FASTPyInteger).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTNumericalLiteral)
]

{ #category : 'tests - assignments' }
FASTPythonImporterTest >> testAssignmentToUnaryOperator [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testAssignmentToUnaryOperator 	"

	self parse: 'x = -1'.

	self assert: (fast allWithType: FASTPyAssignment) size equals: 1.
	self assert: (fast allWithType: FASTPyInteger) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyUnaryOperator) size equals: 1.
	self assert: (fast allWithType: FASTPyVariable) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'x = -1'.
	self assert: (self topEntity isOfType: FASTPyAssignment).
	
	"Testing value of monovalue relation left"
	self assert: self topEntity left isNotNil.

		stack push: self topEntity left.
		self assert: self topEntity sourceCode equals: 'x'.
		self assert: (self topEntity isOfType: FASTPyVariable).
		self assert: (self topEntity isOfType: FASTTVariableEntity).
		stack pop.
	
	"Testing value of monovalue relation right"
	self assert: self topEntity right isNotNil.

		stack push: self topEntity right.
		self assert: self topEntity sourceCode equals: '-1'.
		self assert: (self topEntity isOfType: FASTPyUnaryOperator).
		self assert: (self topEntity isOfType: FASTPyTUnaryOperator).
		
		"Testing value of monovalue relation argument"
		self assert: self topEntity argument isNotNil.

			stack push: self topEntity argument.
			self assert: self topEntity sourceCode equals: '1'.
			self assert: (self topEntity isOfType: FASTPyInteger).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTNumericalLiteral).
			stack pop.
		self assert: self topEntity operator equals: '-'
]

{ #category : 'tests - assignments' }
FASTPythonImporterTest >> testAssignmentType [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testAssignmentType 	"

	self parse: 'int: x = 1'.

	self assert: (fast allWithType: FASTPyAssignment) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyInteger) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyType) size equals: 1.
	self assert: (fast allWithType: FASTPyVariable) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'int: x = 1'.
	self assert: (self topEntity isOfType: FASTPyAssignment).
	
	"Testing value of monovalue relation left"
	self assert: self topEntity left isNotNil.

		stack push: self topEntity left.
		self assert: self topEntity sourceCode equals: 'int'.
		self assert: (self topEntity isOfType: FASTPyVariable).
		self assert: (self topEntity isOfType: FASTTVariableEntity).
		stack pop.
	
	"Testing value of monovalue relation right"
	self assert: self topEntity right isNotNil.

		stack push: self topEntity right.
		self assert: self topEntity sourceCode equals: '1'.
		self assert: (self topEntity isOfType: FASTPyInteger).
		self assert: (self topEntity isOfType: FASTTLiteral).
		self assert: (self topEntity isOfType: FASTTNumericalLiteral).
		stack pop.
	
	"Testing value of monovalue relation type"
	self assert: self topEntity type isNotNil.

		stack push: self topEntity type.
		self assert: self topEntity sourceCode equals: 'x'.
		self assert: (self topEntity isOfType: FASTPyType).
		
		"Testing value of monovalue relation content"
		self assert: self topEntity content isNotNil.

			stack push: self topEntity content.
			self assert: self topEntity sourceCode equals: 'x'.
			self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - accesses' }
FASTPythonImporterTest >> testAttributeAccess [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testAttributeAccess 	"

	self parse: 'obj.count'.

	self assert: (fast allWithType: FASTPyAttributeAccess) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 2.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'obj.count'.
	self assert: (self topEntity isOfType: FASTPyAttributeAccess).
	
	"Testing value of monovalue relation attribute"
	self assert: self topEntity attribute isNotNil.

		stack push: self topEntity attribute.
		self assert: self topEntity sourceCode equals: 'count'.
		self assert: (self topEntity isOfType: FASTPyIdentifier).
		stack pop.
	
	"Testing value of monovalue relation object"
	self assert: self topEntity object isNotNil.

		stack push: self topEntity object.
		self assert: self topEntity sourceCode equals: 'obj'.
		self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - accesses' }
FASTPythonImporterTest >> testAttributeAccessNested [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testAttributeAccessNested 	"

	self parse: 'obj.toto.count'.

	self assert: (fast allWithType: FASTPyAttributeAccess) size equals: 2.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 3.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'obj.toto.count'.
	self assert: (self topEntity isOfType: FASTPyAttributeAccess).
	
	"Testing value of monovalue relation attribute"
	self assert: self topEntity attribute isNotNil.

		stack push: self topEntity attribute.
		self assert: self topEntity sourceCode equals: 'count'.
		self assert: (self topEntity isOfType: FASTPyIdentifier).
		stack pop.
	
	"Testing value of monovalue relation object"
	self assert: self topEntity object isNotNil.

		stack push: self topEntity object.
		self assert: self topEntity sourceCode equals: 'obj.toto'.
		self assert: (self topEntity isOfType: FASTPyAttributeAccess).
		
		"Testing value of monovalue relation attribute"
		self assert: self topEntity attribute isNotNil.

			stack push: self topEntity attribute.
			self assert: self topEntity sourceCode equals: 'toto'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		
		"Testing value of monovalue relation object"
		self assert: self topEntity object isNotNil.

			stack push: self topEntity object.
			self assert: self topEntity sourceCode equals: 'obj'.
			self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - accesses' }
FASTPythonImporterTest >> testAttributeAccessOnString [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testAttributeAccessOnString 	"

	self parse: ''' ''.join'.

	self assert: (fast allWithType: FASTPyAttributeAccess) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyString) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: ''' ''.join'.
	self assert: (self topEntity isOfType: FASTPyAttributeAccess).
	
	"Testing value of monovalue relation attribute"
	self assert: self topEntity attribute isNotNil.

		stack push: self topEntity attribute.
		self assert: self topEntity sourceCode equals: 'join'.
		self assert: (self topEntity isOfType: FASTPyIdentifier).
		stack pop.
	
	"Testing value of monovalue relation object"
	self assert: self topEntity object isNotNil.

		stack push: self topEntity object.
		self assert: self topEntity sourceCode equals: ''' '''.
		self assert: (self topEntity isOfType: FASTPyString).
		self assert: (self topEntity isOfType: FASTTLiteral).
		self assert: (self topEntity isOfType: FASTTStringLiteral)
]

{ #category : 'tests - accesses' }
FASTPythonImporterTest >> testAttributeAccessWithReceiverParenthesised [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testAttributeAccessWithReceiverParenthesised 	"

	self parse: '(obj).attribute'.

	self assert: (fast allWithType: FASTPyAttributeAccess) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 2.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: '(obj).attribute'.
	self assert: (self topEntity isOfType: FASTPyAttributeAccess).
	
	"Testing value of monovalue relation attribute"
	self assert: self topEntity attribute isNotNil.

		stack push: self topEntity attribute.
		self assert: self topEntity sourceCode equals: 'attribute'.
		self assert: (self topEntity isOfType: FASTPyIdentifier).
		stack pop.
	
	"Testing value of monovalue relation object"
	self assert: self topEntity object isNotNil.

		stack push: self topEntity object.
		self assert: self topEntity sourceCode equals: 'obj'.
		self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - assignments' }
FASTPythonImporterTest >> testAugmentedAssignmentInAttribute [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testAugmentedAssignmentInAttribute 	"

	self parse: 'x.y+=  1'.

	self assert: (fast allWithType: FASTPyAttributeAccess) size equals: 1.
	self assert: (fast allWithType: FASTPyAugmentedAssignment) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 2.
	self assert: (fast allWithType: FASTPyInteger) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'x.y+=  1'.
	self assert: (self topEntity isOfType: FASTPyAugmentedAssignment).
	
	"Testing value of monovalue relation left"
	self assert: self topEntity left isNotNil.

		stack push: self topEntity left.
		self assert: self topEntity sourceCode equals: 'x.y'.
		self assert: (self topEntity isOfType: FASTPyAttributeAccess).
		
		"Testing value of monovalue relation attribute"
		self assert: self topEntity attribute isNotNil.

			stack push: self topEntity attribute.
			self assert: self topEntity sourceCode equals: 'y'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		
		"Testing value of monovalue relation object"
		self assert: self topEntity object isNotNil.

			stack push: self topEntity object.
			self assert: self topEntity sourceCode equals: 'x'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		stack pop.
	self assert: self topEntity operator equals: '+='.
	
	"Testing value of monovalue relation right"
	self assert: self topEntity right isNotNil.

		stack push: self topEntity right.
		self assert: self topEntity sourceCode equals: '1'.
		self assert: (self topEntity isOfType: FASTPyInteger).
		self assert: (self topEntity isOfType: FASTTLiteral).
		self assert: (self topEntity isOfType: FASTTNumericalLiteral)
]

{ #category : 'tests - assignments' }
FASTPythonImporterTest >> testAugmentedAssignmentInIdentifier [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testAugmentedAssignmentInIdentifier 	"

	self parse: 'element+=  1'.

	self assert: (fast allWithType: FASTPyAugmentedAssignment) size equals: 1.
	self assert: (fast allWithType: FASTPyInteger) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyVariable) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'element+=  1'.
	self assert: (self topEntity isOfType: FASTPyAugmentedAssignment).
	
	"Testing value of monovalue relation left"
	self assert: self topEntity left isNotNil.

		stack push: self topEntity left.
		self assert: self topEntity sourceCode equals: 'element'.
		self assert: (self topEntity isOfType: FASTPyVariable).
		self assert: (self topEntity isOfType: FASTTVariableEntity).
		stack pop.
	self assert: self topEntity operator equals: '+='.
	
	"Testing value of monovalue relation right"
	self assert: self topEntity right isNotNil.

		stack push: self topEntity right.
		self assert: self topEntity sourceCode equals: '1'.
		self assert: (self topEntity isOfType: FASTPyInteger).
		self assert: (self topEntity isOfType: FASTTLiteral).
		self assert: (self topEntity isOfType: FASTTNumericalLiteral)
]

{ #category : 'tests - assignments' }
FASTPythonImporterTest >> testAugmentedAssignmentInSubscript [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testAugmentedAssignmentInSubscript 	"

	self parse: 'attrs[2]+=  1'.

	self assert: (fast allWithType: FASTPyAugmentedAssignment) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyInteger) size equals: 2.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPySubscript) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'attrs[2]+=  1'.
	self assert: (self topEntity isOfType: FASTPyAugmentedAssignment).
	
	"Testing value of monovalue relation left"
	self assert: self topEntity left isNotNil.

		stack push: self topEntity left.
		self assert: self topEntity sourceCode equals: 'attrs[2]'.
		self assert: (self topEntity isOfType: FASTPySubscript).
		
		"Testing value of multivalued relation indices"
		self assert: self topEntity indices size equals: 1.
		
			stack push: (stack top indices at: 1).
			self assert: self topEntity sourceCode equals: '2'.
			self assert: (self topEntity isOfType: FASTPyInteger).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTNumericalLiteral).
			stack pop.
		
		"Testing value of monovalue relation value"
		self assert: self topEntity value isNotNil.

			stack push: self topEntity value.
			self assert: self topEntity sourceCode equals: 'attrs'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		stack pop.
	self assert: self topEntity operator equals: '+='.
	
	"Testing value of monovalue relation right"
	self assert: self topEntity right isNotNil.

		stack push: self topEntity right.
		self assert: self topEntity sourceCode equals: '1'.
		self assert: (self topEntity isOfType: FASTPyInteger).
		self assert: (self topEntity isOfType: FASTTLiteral).
		self assert: (self topEntity isOfType: FASTTNumericalLiteral)
]

{ #category : 'tests - assignments' }
FASTPythonImporterTest >> testAugmentedAssignmentInTuple [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testAugmentedAssignmentInTuple 	"

	self parse: '(a,b)+=  1'.

	self assert: (fast allWithType: FASTPyAugmentedAssignment) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 2.
	self assert: (fast allWithType: FASTPyInteger) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyTuple) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: '(a,b)+=  1'.
	self assert: (self topEntity isOfType: FASTPyAugmentedAssignment).
	
	"Testing value of monovalue relation left"
	self assert: self topEntity left isNotNil.

		stack push: self topEntity left.
		self assert: self topEntity sourceCode equals: '(a,b)'.
		self assert: (self topEntity isOfType: FASTPyTuple).
		
		"Testing value of multivalued relation initializers"
		self assert: self topEntity initializers size equals: 2.
		
			stack push: (stack top initializers at: 1).
			self assert: self topEntity sourceCode equals: 'a'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		
			stack push: (stack top initializers at: 2).
			self assert: self topEntity sourceCode equals: 'b'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		stack pop.
	self assert: self topEntity operator equals: '+='.
	
	"Testing value of monovalue relation right"
	self assert: self topEntity right isNotNil.

		stack push: self topEntity right.
		self assert: self topEntity sourceCode equals: '1'.
		self assert: (self topEntity isOfType: FASTPyInteger).
		self assert: (self topEntity isOfType: FASTTLiteral).
		self assert: (self topEntity isOfType: FASTTNumericalLiteral)
]

{ #category : 'tests - assignments' }
FASTPythonImporterTest >> testAugmentedAssignmentToAttribute [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testAugmentedAssignmentToAttribute 	"

	self parse: 'a +=  x.y'.

	self assert: (fast allWithType: FASTPyAttributeAccess) size equals: 1.
	self assert: (fast allWithType: FASTPyAugmentedAssignment) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 2.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyVariable) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'a +=  x.y'.
	self assert: (self topEntity isOfType: FASTPyAugmentedAssignment).
	
	"Testing value of monovalue relation left"
	self assert: self topEntity left isNotNil.

		stack push: self topEntity left.
		self assert: self topEntity sourceCode equals: 'a'.
		self assert: (self topEntity isOfType: FASTPyVariable).
		self assert: (self topEntity isOfType: FASTTVariableEntity).
		stack pop.
	self assert: self topEntity operator equals: '+='.
	
	"Testing value of monovalue relation right"
	self assert: self topEntity right isNotNil.

		stack push: self topEntity right.
		self assert: self topEntity sourceCode equals: 'x.y'.
		self assert: (self topEntity isOfType: FASTPyAttributeAccess).
		
		"Testing value of monovalue relation attribute"
		self assert: self topEntity attribute isNotNil.

			stack push: self topEntity attribute.
			self assert: self topEntity sourceCode equals: 'y'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		
		"Testing value of monovalue relation object"
		self assert: self topEntity object isNotNil.

			stack push: self topEntity object.
			self assert: self topEntity sourceCode equals: 'x'.
			self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - assignments' }
FASTPythonImporterTest >> testAugmentedAssignmentToBinaryOperator [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testAugmentedAssignmentToBinaryOperator 	"

	self parse: 'a +=  10 / 2'.

	self assert: (fast allWithType: FASTPyAugmentedAssignment) size equals: 1.
	self assert: (fast allWithType: FASTPyBinaryOperator) size equals: 1.
	self assert: (fast allWithType: FASTPyInteger) size equals: 2.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyVariable) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'a +=  10 / 2'.
	self assert: (self topEntity isOfType: FASTPyAugmentedAssignment).
	
	"Testing value of monovalue relation left"
	self assert: self topEntity left isNotNil.

		stack push: self topEntity left.
		self assert: self topEntity sourceCode equals: 'a'.
		self assert: (self topEntity isOfType: FASTPyVariable).
		self assert: (self topEntity isOfType: FASTTVariableEntity).
		stack pop.
	self assert: self topEntity operator equals: '+='.
	
	"Testing value of monovalue relation right"
	self assert: self topEntity right isNotNil.

		stack push: self topEntity right.
		self assert: self topEntity sourceCode equals: '10 / 2'.
		self assert: (self topEntity isOfType: FASTPyBinaryOperator).
		self assert: (self topEntity isOfType: FASTTBinaryExpression).
		
		"Testing value of monovalue relation leftOperand"
		self assert: self topEntity leftOperand isNotNil.

			stack push: self topEntity leftOperand.
			self assert: self topEntity sourceCode equals: '10'.
			self assert: (self topEntity isOfType: FASTPyInteger).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTNumericalLiteral).
			stack pop.
		self assert: self topEntity operator equals: '/'.
		
		"Testing value of monovalue relation rightOperand"
		self assert: self topEntity rightOperand isNotNil.

			stack push: self topEntity rightOperand.
			self assert: self topEntity sourceCode equals: '2'.
			self assert: (self topEntity isOfType: FASTPyInteger).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTNumericalLiteral)
]

{ #category : 'tests - assignments' }
FASTPythonImporterTest >> testAugmentedAssignmentToCall [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testAugmentedAssignmentToCall 	"

	self parse: 'a +=  obj()'.

	self assert: (fast allWithType: FASTPyAugmentedAssignment) size equals: 1.
	self assert: (fast allWithType: FASTPyCall) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyVariable) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'a +=  obj()'.
	self assert: (self topEntity isOfType: FASTPyAugmentedAssignment).
	
	"Testing value of monovalue relation left"
	self assert: self topEntity left isNotNil.

		stack push: self topEntity left.
		self assert: self topEntity sourceCode equals: 'a'.
		self assert: (self topEntity isOfType: FASTPyVariable).
		self assert: (self topEntity isOfType: FASTTVariableEntity).
		stack pop.
	self assert: self topEntity operator equals: '+='.
	
	"Testing value of monovalue relation right"
	self assert: self topEntity right isNotNil.

		stack push: self topEntity right.
		self assert: self topEntity sourceCode equals: 'obj()'.
		self assert: (self topEntity isOfType: FASTPyCall).
		self assert: (self topEntity isOfType: FASTTInvocation).
		self assert: (self topEntity isOfType: FASTTWithArguments).
		
		"Testing value of monovalue relation callee"
		self assert: self topEntity callee isNotNil.

			stack push: self topEntity callee.
			self assert: self topEntity sourceCode equals: 'obj'.
			self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - assignments' }
FASTPythonImporterTest >> testAugmentedAssignmentToComplexe [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testAugmentedAssignmentToComplexe 	"

	self parse: 'a +=  1j'.

	self assert: (fast allWithType: FASTPyAugmentedAssignment) size equals: 1.
	self assert: (fast allWithType: FASTPyComplex) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyVariable) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'a +=  1j'.
	self assert: (self topEntity isOfType: FASTPyAugmentedAssignment).
	
	"Testing value of monovalue relation left"
	self assert: self topEntity left isNotNil.

		stack push: self topEntity left.
		self assert: self topEntity sourceCode equals: 'a'.
		self assert: (self topEntity isOfType: FASTPyVariable).
		self assert: (self topEntity isOfType: FASTTVariableEntity).
		stack pop.
	self assert: self topEntity operator equals: '+='.
	
	"Testing value of monovalue relation right"
	self assert: self topEntity right isNotNil.

		stack push: self topEntity right.
		self assert: self topEntity sourceCode equals: '1j'.
		self assert: (self topEntity isOfType: FASTPyComplex).
		self assert: (self topEntity isOfType: FASTTLiteral).
		self assert: (self topEntity isOfType: FASTTNumericalLiteral)
]

{ #category : 'tests - assignments' }
FASTPythonImporterTest >> testAugmentedAssignmentToConditionalExpression [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testAugmentedAssignmentToConditionalExpression 	"

	self parse: 'a +=  x if True else y'.

	self assert: (fast allWithType: FASTPyAugmentedAssignment) size equals: 1.
	self assert: (fast allWithType: FASTPyBoolean) size equals: 1.
	self assert: (fast allWithType: FASTPyConditionalExpression) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 2.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyVariable) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'a +=  x if True else y'.
	self assert: (self topEntity isOfType: FASTPyAugmentedAssignment).
	
	"Testing value of monovalue relation left"
	self assert: self topEntity left isNotNil.

		stack push: self topEntity left.
		self assert: self topEntity sourceCode equals: 'a'.
		self assert: (self topEntity isOfType: FASTPyVariable).
		self assert: (self topEntity isOfType: FASTTVariableEntity).
		stack pop.
	self assert: self topEntity operator equals: '+='.
	
	"Testing value of monovalue relation right"
	self assert: self topEntity right isNotNil.

		stack push: self topEntity right.
		self assert: self topEntity sourceCode equals: 'x if True else y'.
		self assert: (self topEntity isOfType: FASTPyConditionalExpression).
		self assert: (self topEntity isOfType: FASTTWithCondition).
		
		"Testing value of monovalue relation condition"
		self assert: self topEntity condition isNotNil.

			stack push: self topEntity condition.
			self assert: self topEntity sourceCode equals: 'True'.
			self assert: (self topEntity isOfType: FASTPyBoolean).
			self assert: (self topEntity isOfType: FASTTBooleanLiteral).
			self assert: (self topEntity isOfType: FASTTLiteral).
			stack pop.
		
		"Testing value of monovalue relation elseExpression"
		self assert: self topEntity elseExpression isNotNil.

			stack push: self topEntity elseExpression.
			self assert: self topEntity sourceCode equals: 'y'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		
		"Testing value of monovalue relation thenExpression"
		self assert: self topEntity thenExpression isNotNil.

			stack push: self topEntity thenExpression.
			self assert: self topEntity sourceCode equals: 'x'.
			self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - assignments' }
FASTPythonImporterTest >> testAugmentedAssignmentToFloat [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testAugmentedAssignmentToFloat 	"

	self parse: 'a +=  0.1'.

	self assert: (fast allWithType: FASTPyAugmentedAssignment) size equals: 1.
	self assert: (fast allWithType: FASTPyFloat) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyVariable) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'a +=  0.1'.
	self assert: (self topEntity isOfType: FASTPyAugmentedAssignment).
	
	"Testing value of monovalue relation left"
	self assert: self topEntity left isNotNil.

		stack push: self topEntity left.
		self assert: self topEntity sourceCode equals: 'a'.
		self assert: (self topEntity isOfType: FASTPyVariable).
		self assert: (self topEntity isOfType: FASTTVariableEntity).
		stack pop.
	self assert: self topEntity operator equals: '+='.
	
	"Testing value of monovalue relation right"
	self assert: self topEntity right isNotNil.

		stack push: self topEntity right.
		self assert: self topEntity sourceCode equals: '0.1'.
		self assert: (self topEntity isOfType: FASTPyFloat).
		self assert: (self topEntity isOfType: FASTTLiteral).
		self assert: (self topEntity isOfType: FASTTNumericalLiteral)
]

{ #category : 'tests - assignments' }
FASTPythonImporterTest >> testAugmentedAssignmentToIdentifier [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testAugmentedAssignmentToIdentifier 	"

	self parse: 'a +=  element'.

	self assert: (fast allWithType: FASTPyAugmentedAssignment) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyVariable) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'a +=  element'.
	self assert: (self topEntity isOfType: FASTPyAugmentedAssignment).
	
	"Testing value of monovalue relation left"
	self assert: self topEntity left isNotNil.

		stack push: self topEntity left.
		self assert: self topEntity sourceCode equals: 'a'.
		self assert: (self topEntity isOfType: FASTPyVariable).
		self assert: (self topEntity isOfType: FASTTVariableEntity).
		stack pop.
	self assert: self topEntity operator equals: '+='.
	
	"Testing value of monovalue relation right"
	self assert: self topEntity right isNotNil.

		stack push: self topEntity right.
		self assert: self topEntity sourceCode equals: 'element'.
		self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - assignments' }
FASTPythonImporterTest >> testAugmentedAssignmentToInteger [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testAugmentedAssignmentToInteger 	"

	self parse: 'a +=  1'.

	self assert: (fast allWithType: FASTPyAugmentedAssignment) size equals: 1.
	self assert: (fast allWithType: FASTPyInteger) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyVariable) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'a +=  1'.
	self assert: (self topEntity isOfType: FASTPyAugmentedAssignment).
	
	"Testing value of monovalue relation left"
	self assert: self topEntity left isNotNil.

		stack push: self topEntity left.
		self assert: self topEntity sourceCode equals: 'a'.
		self assert: (self topEntity isOfType: FASTPyVariable).
		self assert: (self topEntity isOfType: FASTTVariableEntity).
		stack pop.
	self assert: self topEntity operator equals: '+='.
	
	"Testing value of monovalue relation right"
	self assert: self topEntity right isNotNil.

		stack push: self topEntity right.
		self assert: self topEntity sourceCode equals: '1'.
		self assert: (self topEntity isOfType: FASTPyInteger).
		self assert: (self topEntity isOfType: FASTTLiteral).
		self assert: (self topEntity isOfType: FASTTNumericalLiteral)
]

{ #category : 'tests - assignments' }
FASTPythonImporterTest >> testAugmentedAssignmentToSubscript [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testAugmentedAssignmentToSubscript 	"

	self parse: 'a +=  attrs[2]'.

	self assert: (fast allWithType: FASTPyAugmentedAssignment) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyInteger) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPySubscript) size equals: 1.
	self assert: (fast allWithType: FASTPyVariable) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'a +=  attrs[2]'.
	self assert: (self topEntity isOfType: FASTPyAugmentedAssignment).
	
	"Testing value of monovalue relation left"
	self assert: self topEntity left isNotNil.

		stack push: self topEntity left.
		self assert: self topEntity sourceCode equals: 'a'.
		self assert: (self topEntity isOfType: FASTPyVariable).
		self assert: (self topEntity isOfType: FASTTVariableEntity).
		stack pop.
	self assert: self topEntity operator equals: '+='.
	
	"Testing value of monovalue relation right"
	self assert: self topEntity right isNotNil.

		stack push: self topEntity right.
		self assert: self topEntity sourceCode equals: 'attrs[2]'.
		self assert: (self topEntity isOfType: FASTPySubscript).
		
		"Testing value of multivalued relation indices"
		self assert: self topEntity indices size equals: 1.
		
			stack push: (stack top indices at: 1).
			self assert: self topEntity sourceCode equals: '2'.
			self assert: (self topEntity isOfType: FASTPyInteger).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTNumericalLiteral).
			stack pop.
		
		"Testing value of monovalue relation value"
		self assert: self topEntity value isNotNil.

			stack push: self topEntity value.
			self assert: self topEntity sourceCode equals: 'attrs'.
			self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - assignments' }
FASTPythonImporterTest >> testAugmentedAssignmentToUnaryOperator [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testAugmentedAssignmentToUnaryOperator 	"

	self parse: 'a +=  -1'.

	self assert: (fast allWithType: FASTPyAugmentedAssignment) size equals: 1.
	self assert: (fast allWithType: FASTPyInteger) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyUnaryOperator) size equals: 1.
	self assert: (fast allWithType: FASTPyVariable) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'a +=  -1'.
	self assert: (self topEntity isOfType: FASTPyAugmentedAssignment).
	
	"Testing value of monovalue relation left"
	self assert: self topEntity left isNotNil.

		stack push: self topEntity left.
		self assert: self topEntity sourceCode equals: 'a'.
		self assert: (self topEntity isOfType: FASTPyVariable).
		self assert: (self topEntity isOfType: FASTTVariableEntity).
		stack pop.
	self assert: self topEntity operator equals: '+='.
	
	"Testing value of monovalue relation right"
	self assert: self topEntity right isNotNil.

		stack push: self topEntity right.
		self assert: self topEntity sourceCode equals: '-1'.
		self assert: (self topEntity isOfType: FASTPyUnaryOperator).
		self assert: (self topEntity isOfType: FASTPyTUnaryOperator).
		
		"Testing value of monovalue relation argument"
		self assert: self topEntity argument isNotNil.

			stack push: self topEntity argument.
			self assert: self topEntity sourceCode equals: '1'.
			self assert: (self topEntity isOfType: FASTPyInteger).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTNumericalLiteral).
			stack pop.
		self assert: self topEntity operator equals: '-'
]

{ #category : 'tests - await' }
FASTPythonImporterTest >> testAwait [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testAwait 	"

	self parse: 'await future'.

	self assert: (fast allWithType: FASTPyAwait) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'await future'.
	self assert: (self topEntity isOfType: FASTPyAwait).
	
	"Testing value of monovalue relation expression"
	self assert: self topEntity expression isNotNil.

		stack push: self topEntity expression.
		self assert: self topEntity sourceCode equals: 'future'.
		self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - await' }
FASTPythonImporterTest >> testAwaitCall [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testAwaitCall 	"

	self parse: 'await coro_func()'.

	self assert: (fast allWithType: FASTPyAwait) size equals: 1.
	self assert: (fast allWithType: FASTPyCall) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'await coro_func()'.
	self assert: (self topEntity isOfType: FASTPyAwait).
	
	"Testing value of monovalue relation expression"
	self assert: self topEntity expression isNotNil.

		stack push: self topEntity expression.
		self assert: self topEntity sourceCode equals: 'coro_func()'.
		self assert: (self topEntity isOfType: FASTPyCall).
		self assert: (self topEntity isOfType: FASTTInvocation).
		self assert: (self topEntity isOfType: FASTTWithArguments).
		
		"Testing value of monovalue relation callee"
		self assert: self topEntity callee isNotNil.

			stack push: self topEntity callee.
			self assert: self topEntity sourceCode equals: 'coro_func'.
			self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - operators' }
FASTPythonImporterTest >> testBinaryOperator [

	| operator |
	self parse: '40 + 2'.

	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyBinaryOperator) size equals: 1.

	operator := (fast allWithType: FASTPyBinaryOperator) anyOne.
	self assert: operator sourceCode equals: '40 + 2'.
	self assert: (operator isOfType: FASTTBinaryExpression).
	self assert: operator leftOperand sourceCode equals: '40'.
	self assert: (operator leftOperand isOfType: FASTPyInteger).
	self assert: operator rightOperand sourceCode equals: '2'.
	self assert: (operator rightOperand isOfType: FASTPyInteger).
	self assert: operator operator equals: '+'
]

{ #category : 'tests - operators' }
FASTPythonImporterTest >> testBinaryOperatorInBinaryOperator [

	| operator left |
	self parse: '40 + 2 - 0'.

	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyBinaryOperator) size equals: 2.

	operator := (fast allWithType: FASTPyBinaryOperator) anyOne.
	self assert: operator sourceCode equals: '40 + 2 - 0'.
	self assert: (operator isOfType: FASTTBinaryExpression).

	self assert: operator operator equals: '-'.
	self assert: operator rightOperand sourceCode equals: '0'.
	self assert: (operator rightOperand isOfType: FASTPyInteger).

	left := operator leftOperand.
	self assert: left sourceCode equals: '40 + 2'.
	self assert: (left isOfType: FASTPyBinaryOperator).
	self assert: (left leftOperand isOfType: FASTPyInteger).
	self assert: (left leftOperand sourceCode) equals: '40'.
	self assert: (left rightOperand sourceCode) equals: '2'.
	self assert: left operator equals: '+'
]

{ #category : 'tests - operators' }
FASTPythonImporterTest >> testBitwiseOperator [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testBitwiseOperator 	"

	self parse: '0b00101101 & 0b00011011'.

	self assert: (fast allWithType: FASTPyBinaryOperator) size equals: 1.
	self assert: (fast allWithType: FASTPyInteger) size equals: 2.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: '0b00101101 & 0b00011011'.
	self assert: (self topEntity isOfType: FASTPyBinaryOperator).
	self assert: (self topEntity isOfType: FASTTBinaryExpression).
	
	"Testing value of monovalue relation leftOperand"
	self assert: self topEntity leftOperand isNotNil.

		stack push: self topEntity leftOperand.
		self assert: self topEntity sourceCode equals: '0b00101101'.
		self assert: (self topEntity isOfType: FASTPyInteger).
		self assert: (self topEntity isOfType: FASTTLiteral).
		self assert: (self topEntity isOfType: FASTTNumericalLiteral).
		stack pop.
	self assert: self topEntity operator equals: '&'.
	
	"Testing value of monovalue relation rightOperand"
	self assert: self topEntity rightOperand isNotNil.

		stack push: self topEntity rightOperand.
		self assert: self topEntity sourceCode equals: '0b00011011'.
		self assert: (self topEntity isOfType: FASTPyInteger).
		self assert: (self topEntity isOfType: FASTTLiteral).
		self assert: (self topEntity isOfType: FASTTNumericalLiteral)
]

{ #category : 'tests - operators' }
FASTPythonImporterTest >> testBooleanOperator [

	| operator |
	self parse: 'True and False'.

	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyBooleanOperator) size equals: 1.

	operator := (fast allWithType: FASTPyBooleanOperator) anyOne.
	self assert: operator sourceCode equals: 'True and False'.
	self assert: (operator isOfType: FASTTBinaryExpression).
	self assert: operator leftOperand sourceCode equals: 'True'.
	self assert: (operator leftOperand isOfType: FASTPyBoolean).
	self assert: operator rightOperand sourceCode equals: 'False'.
	self assert: (operator rightOperand isOfType: FASTPyBoolean).
	self assert: operator operator equals: 'and'
]

{ #category : 'tests - operators' }
FASTPythonImporterTest >> testBooleanOperatorInBooleanOperator [

	| operator left |
	self parse: 'True and True or False'.

	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyBooleanOperator) size equals: 2.

	operator := (fast allWithType: FASTPyBooleanOperator) anyOne.
	self assert: operator sourceCode equals: 'True and True or False'.
	self assert: (operator isOfType: FASTTBinaryExpression).

	self assert: operator operator equals: 'or'.
	self assert: operator rightOperand sourceCode equals: 'False'.
	self assert: (operator rightOperand isOfType: FASTPyBoolean).

	left := operator leftOperand.
	self assert: left sourceCode equals: 'True and True'.
	self assert: (left isOfType: FASTPyBooleanOperator).
	self assert: (left leftOperand isOfType: FASTPyBoolean).
	self assert: (left leftOperand sourceCode) equals: 'True'.
	self assert: (left rightOperand sourceCode) equals: 'True'.
	self assert: left operator equals: 'and'
]

{ #category : 'tests - statements' }
FASTPythonImporterTest >> testBreakStatement [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testBreakStatement 	"

	self parse: 'while True:
	break'.

	self assert: (fast allWithType: FASTPyBlock) size equals: 1.
	self assert: (fast allWithType: FASTPyBoolean) size equals: 1.
	self assert: (fast allWithType: FASTPyBreakStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyWhileStatement) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'while True:
	break' withPlatformLineEndings.
	self assert: (self topEntity isOfType: FASTPyWhileStatement).
	self assert: (self topEntity isOfType: FASTPyTWithElseClause).
	self assert: (self topEntity isOfType: FASTTConditionalStatement).
	self assert: (self topEntity isOfType: FASTTLoopStatement).
	self assert: (self topEntity isOfType: FASTTWithCondition).
	
	"Testing value of monovalue relation body"
	self assert: self topEntity body isNotNil.

		stack push: self topEntity body.
		self assert: self topEntity sourceCode equals: 'break'.
		self assert: (self topEntity isOfType: FASTPyBlock).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'break'.
			self assert: (self topEntity isOfType: FASTPyBreakStatement).
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation condition"
	self assert: self topEntity condition isNotNil.

		stack push: self topEntity condition.
		self assert: self topEntity sourceCode equals: 'True'.
		self assert: (self topEntity isOfType: FASTPyBoolean).
		self assert: (self topEntity isOfType: FASTTBooleanLiteral).
		self assert: (self topEntity isOfType: FASTTLiteral)
]

{ #category : 'tests - calls' }
FASTPythonImporterTest >> testCall [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testCall 	"

	self parse: 'print("Hello")'.

	self assert: (fast allWithType: FASTPyCall) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyString) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'print("Hello")'.
	self assert: (self topEntity isOfType: FASTPyCall).
	self assert: (self topEntity isOfType: FASTTInvocation).
	self assert: (self topEntity isOfType: FASTTWithArguments).
	
	"Testing value of multivalued relation arguments"
	self assert: self topEntity arguments size equals: 1.
	
		stack push: (stack top arguments at: 1).
		self assert: self topEntity sourceCode equals: '"Hello"'.
		self assert: (self topEntity isOfType: FASTPyString).
		self assert: (self topEntity isOfType: FASTTLiteral).
		self assert: (self topEntity isOfType: FASTTStringLiteral).
		stack pop.
	
	"Testing value of monovalue relation callee"
	self assert: self topEntity callee isNotNil.

		stack push: self topEntity callee.
		self assert: self topEntity sourceCode equals: 'print'.
		self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - calls' }
FASTPythonImporterTest >> testCallArgumentWithAttribute [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testCallArgumentWithAttribute 	"

	self parse: 'function(CFG.seed)'.

	self assert: (fast allWithType: FASTPyAttributeAccess) size equals: 1.
	self assert: (fast allWithType: FASTPyCall) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 3.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'function(CFG.seed)'.
	self assert: (self topEntity isOfType: FASTPyCall).
	self assert: (self topEntity isOfType: FASTTInvocation).
	self assert: (self topEntity isOfType: FASTTWithArguments).
	
	"Testing value of multivalued relation arguments"
	self assert: self topEntity arguments size equals: 1.
	
		stack push: (stack top arguments at: 1).
		self assert: self topEntity sourceCode equals: 'CFG.seed'.
		self assert: (self topEntity isOfType: FASTPyAttributeAccess).
		
		"Testing value of monovalue relation attribute"
		self assert: self topEntity attribute isNotNil.

			stack push: self topEntity attribute.
			self assert: self topEntity sourceCode equals: 'seed'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		
		"Testing value of monovalue relation object"
		self assert: self topEntity object isNotNil.

			stack push: self topEntity object.
			self assert: self topEntity sourceCode equals: 'CFG'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation callee"
	self assert: self topEntity callee isNotNil.

		stack push: self topEntity callee.
		self assert: self topEntity sourceCode equals: 'function'.
		self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - calls' }
FASTPythonImporterTest >> testCallArgumentWithBinaryOperator [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testCallArgumentWithBinaryOperator 	"

	self parse: 'function(30000/train_bs*epochs)'.

	self assert: (fast allWithType: FASTPyBinaryOperator) size equals: 2.
	self assert: (fast allWithType: FASTPyCall) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 3.
	self assert: (fast allWithType: FASTPyInteger) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'function(30000/train_bs*epochs)'.
	self assert: (self topEntity isOfType: FASTPyCall).
	self assert: (self topEntity isOfType: FASTTInvocation).
	self assert: (self topEntity isOfType: FASTTWithArguments).
	
	"Testing value of multivalued relation arguments"
	self assert: self topEntity arguments size equals: 1.
	
		stack push: (stack top arguments at: 1).
		self assert: self topEntity sourceCode equals: '30000/train_bs*epochs'.
		self assert: (self topEntity isOfType: FASTPyBinaryOperator).
		self assert: (self topEntity isOfType: FASTTBinaryExpression).
		
		"Testing value of monovalue relation leftOperand"
		self assert: self topEntity leftOperand isNotNil.

			stack push: self topEntity leftOperand.
			self assert: self topEntity sourceCode equals: '30000/train_bs'.
			self assert: (self topEntity isOfType: FASTPyBinaryOperator).
			self assert: (self topEntity isOfType: FASTTBinaryExpression).
			
			"Testing value of monovalue relation leftOperand"
			self assert: self topEntity leftOperand isNotNil.

				stack push: self topEntity leftOperand.
				self assert: self topEntity sourceCode equals: '30000'.
				self assert: (self topEntity isOfType: FASTPyInteger).
				self assert: (self topEntity isOfType: FASTTLiteral).
				self assert: (self topEntity isOfType: FASTTNumericalLiteral).
				stack pop.
			self assert: self topEntity operator equals: '/'.
			
			"Testing value of monovalue relation rightOperand"
			self assert: self topEntity rightOperand isNotNil.

				stack push: self topEntity rightOperand.
				self assert: self topEntity sourceCode equals: 'train_bs'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			stack pop.
		self assert: self topEntity operator equals: '*'.
		
		"Testing value of monovalue relation rightOperand"
		self assert: self topEntity rightOperand isNotNil.

			stack push: self topEntity rightOperand.
			self assert: self topEntity sourceCode equals: 'epochs'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation callee"
	self assert: self topEntity callee isNotNil.

		stack push: self topEntity callee.
		self assert: self topEntity sourceCode equals: 'function'.
		self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - calls' }
FASTPythonImporterTest >> testCallArgumentWithCall [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testCallArgumentWithCall 	"

	self parse: 'function(data[0].replace(''case'',''''))'.

	self assert: (fast allWithType: FASTPyAttributeAccess) size equals: 1.
	self assert: (fast allWithType: FASTPyCall) size equals: 2.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 3.
	self assert: (fast allWithType: FASTPyInteger) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyString) size equals: 2.
	self assert: (fast allWithType: FASTPySubscript) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'function(data[0].replace(''case'',''''))'.
	self assert: (self topEntity isOfType: FASTPyCall).
	self assert: (self topEntity isOfType: FASTTInvocation).
	self assert: (self topEntity isOfType: FASTTWithArguments).
	
	"Testing value of multivalued relation arguments"
	self assert: self topEntity arguments size equals: 1.
	
		stack push: (stack top arguments at: 1).
		self assert: self topEntity sourceCode equals: 'data[0].replace(''case'','''')'.
		self assert: (self topEntity isOfType: FASTPyCall).
		self assert: (self topEntity isOfType: FASTTInvocation).
		self assert: (self topEntity isOfType: FASTTWithArguments).
		
		"Testing value of multivalued relation arguments"
		self assert: self topEntity arguments size equals: 2.
		
			stack push: (stack top arguments at: 1).
			self assert: self topEntity sourceCode equals: '''case'''.
			self assert: (self topEntity isOfType: FASTPyString).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTStringLiteral).
			stack pop.
		
			stack push: (stack top arguments at: 2).
			self assert: self topEntity sourceCode equals: ''''''.
			self assert: (self topEntity isOfType: FASTPyString).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTStringLiteral).
			stack pop.
		
		"Testing value of monovalue relation callee"
		self assert: self topEntity callee isNotNil.

			stack push: self topEntity callee.
			self assert: self topEntity sourceCode equals: 'data[0].replace'.
			self assert: (self topEntity isOfType: FASTPyAttributeAccess).
			
			"Testing value of monovalue relation attribute"
			self assert: self topEntity attribute isNotNil.

				stack push: self topEntity attribute.
				self assert: self topEntity sourceCode equals: 'replace'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			
			"Testing value of monovalue relation object"
			self assert: self topEntity object isNotNil.

				stack push: self topEntity object.
				self assert: self topEntity sourceCode equals: 'data[0]'.
				self assert: (self topEntity isOfType: FASTPySubscript).
				
				"Testing value of multivalued relation indices"
				self assert: self topEntity indices size equals: 1.
				
					stack push: (stack top indices at: 1).
					self assert: self topEntity sourceCode equals: '0'.
					self assert: (self topEntity isOfType: FASTPyInteger).
					self assert: (self topEntity isOfType: FASTTLiteral).
					self assert: (self topEntity isOfType: FASTTNumericalLiteral).
					stack pop.
				
				"Testing value of monovalue relation value"
				self assert: self topEntity value isNotNil.

					stack push: self topEntity value.
					self assert: self topEntity sourceCode equals: 'data'.
					self assert: (self topEntity isOfType: FASTPyIdentifier).
					stack pop.
				stack pop.
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation callee"
	self assert: self topEntity callee isNotNil.

		stack push: self topEntity callee.
		self assert: self topEntity sourceCode equals: 'function'.
		self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - calls' }
FASTPythonImporterTest >> testCallArgumentWithComparisonOperator [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testCallArgumentWithComparisonOperator 	"

	self parse: 'function(shape0!=resize)'.

	self assert: (fast allWithType: FASTPyCall) size equals: 1.
	self assert: (fast allWithType: FASTPyComparisonOperator) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 3.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'function(shape0!=resize)'.
	self assert: (self topEntity isOfType: FASTPyCall).
	self assert: (self topEntity isOfType: FASTTInvocation).
	self assert: (self topEntity isOfType: FASTTWithArguments).
	
	"Testing value of multivalued relation arguments"
	self assert: self topEntity arguments size equals: 1.
	
		stack push: (stack top arguments at: 1).
		self assert: self topEntity sourceCode equals: 'shape0!=resize'.
		self assert: (self topEntity isOfType: FASTPyComparisonOperator).
		
		"Testing value of multivalued relation operands"
		self assert: self topEntity operands size equals: 2.
		
			stack push: (stack top operands at: 1).
			self assert: self topEntity sourceCode equals: 'shape0'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		
			stack push: (stack top operands at: 2).
			self assert: self topEntity sourceCode equals: 'resize'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		self assert: self topEntity operators equals: #('!=').
		stack pop.
	
	"Testing value of monovalue relation callee"
	self assert: self topEntity callee isNotNil.

		stack push: self topEntity callee.
		self assert: self topEntity sourceCode equals: 'function'.
		self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - calls' }
FASTPythonImporterTest >> testCallArgumentWithDictionary [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testCallArgumentWithDictionary 	"

	self parse: 'function({
    "id":pred_ids,
    "class":pred_classes
})'.

	self assert: (fast allWithType: FASTPyCall) size equals: 1.
	self assert: (fast allWithType: FASTPyDictionary) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 3.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPair) size equals: 2.
	self assert: (fast allWithType: FASTPyString) size equals: 2.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'function({
    "id":pred_ids,
    "class":pred_classes
})' withPlatformLineEndings.
	self assert: (self topEntity isOfType: FASTPyCall).
	self assert: (self topEntity isOfType: FASTTInvocation).
	self assert: (self topEntity isOfType: FASTTWithArguments).
	
	"Testing value of multivalued relation arguments"
	self assert: self topEntity arguments size equals: 1.
	
		stack push: (stack top arguments at: 1).
		self assert: self topEntity sourceCode equals: '{
    "id":pred_ids,
    "class":pred_classes
}' withPlatformLineEndings.
		self assert: (self topEntity isOfType: FASTPyDictionary).
		
		"Testing value of multivalued relation initializers"
		self assert: self topEntity initializers size equals: 2.
		
			stack push: (stack top initializers at: 1).
			self assert: self topEntity sourceCode equals: '"id":pred_ids'.
			self assert: (self topEntity isOfType: FASTPyPair).
			
			"Testing value of monovalue relation key"
			self assert: self topEntity key isNotNil.

				stack push: self topEntity key.
				self assert: self topEntity sourceCode equals: '"id"'.
				self assert: (self topEntity isOfType: FASTPyString).
				self assert: (self topEntity isOfType: FASTTLiteral).
				self assert: (self topEntity isOfType: FASTTStringLiteral).
				stack pop.
			
			"Testing value of monovalue relation value"
			self assert: self topEntity value isNotNil.

				stack push: self topEntity value.
				self assert: self topEntity sourceCode equals: 'pred_ids'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			stack pop.
		
			stack push: (stack top initializers at: 2).
			self assert: self topEntity sourceCode equals: '"class":pred_classes'.
			self assert: (self topEntity isOfType: FASTPyPair).
			
			"Testing value of monovalue relation key"
			self assert: self topEntity key isNotNil.

				stack push: self topEntity key.
				self assert: self topEntity sourceCode equals: '"class"'.
				self assert: (self topEntity isOfType: FASTPyString).
				self assert: (self topEntity isOfType: FASTTLiteral).
				self assert: (self topEntity isOfType: FASTTStringLiteral).
				stack pop.
			
			"Testing value of monovalue relation value"
			self assert: self topEntity value isNotNil.

				stack push: self topEntity value.
				self assert: self topEntity sourceCode equals: 'pred_classes'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation callee"
	self assert: self topEntity callee isNotNil.

		stack push: self topEntity callee.
		self assert: self topEntity sourceCode equals: 'function'.
		self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - calls' }
FASTPythonImporterTest >> testCallArgumentWithEllipsis [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testCallArgumentWithEllipsis 	"

	self parse: 'function(3, ...)'.

	self assert: (fast allWithType: FASTPyCall) size equals: 1.
	self assert: (fast allWithType: FASTPyEllipsis) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyInteger) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'function(3, ...)'.
	self assert: (self topEntity isOfType: FASTPyCall).
	self assert: (self topEntity isOfType: FASTTInvocation).
	self assert: (self topEntity isOfType: FASTTWithArguments).
	
	"Testing value of multivalued relation arguments"
	self assert: self topEntity arguments size equals: 2.
	
		stack push: (stack top arguments at: 1).
		self assert: self topEntity sourceCode equals: '3'.
		self assert: (self topEntity isOfType: FASTPyInteger).
		self assert: (self topEntity isOfType: FASTTLiteral).
		self assert: (self topEntity isOfType: FASTTNumericalLiteral).
		stack pop.
	
		stack push: (stack top arguments at: 2).
		self assert: self topEntity sourceCode equals: '...'.
		self assert: (self topEntity isOfType: FASTPyEllipsis).
		self assert: (self topEntity isOfType: FASTTLiteral).
		stack pop.
	
	"Testing value of monovalue relation callee"
	self assert: self topEntity callee isNotNil.

		stack push: self topEntity callee.
		self assert: self topEntity sourceCode equals: 'function'.
		self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - calls' }
FASTPythonImporterTest >> testCallArgumentWithKeywordArgument [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testCallArgumentWithKeywordArgument 	"

	self parse: 'function("/root/.cache/torch/hub/checkpoints/", exist_ok=True)'.

	self assert: (fast allWithType: FASTPyBoolean) size equals: 1.
	self assert: (fast allWithType: FASTPyCall) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyKeywordArgument) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyString) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'function("/root/.cache/torch/hub/checkpoints/", exist_ok=True)'.
	self assert: (self topEntity isOfType: FASTPyCall).
	self assert: (self topEntity isOfType: FASTTInvocation).
	self assert: (self topEntity isOfType: FASTTWithArguments).
	
	"Testing value of multivalued relation arguments"
	self assert: self topEntity arguments size equals: 2.
	
		stack push: (stack top arguments at: 1).
		self assert: self topEntity sourceCode equals: '"/root/.cache/torch/hub/checkpoints/"'.
		self assert: (self topEntity isOfType: FASTPyString).
		self assert: (self topEntity isOfType: FASTTLiteral).
		self assert: (self topEntity isOfType: FASTTStringLiteral).
		stack pop.
	
		stack push: (stack top arguments at: 2).
		self assert: self topEntity sourceCode equals: 'exist_ok=True'.
		self assert: (self topEntity isOfType: FASTPyKeywordArgument).
		self assert: self topEntity name equals: 'exist_ok'.
		
		"Testing value of monovalue relation value"
		self assert: self topEntity value isNotNil.

			stack push: self topEntity value.
			self assert: self topEntity sourceCode equals: 'True'.
			self assert: (self topEntity isOfType: FASTPyBoolean).
			self assert: (self topEntity isOfType: FASTTBooleanLiteral).
			self assert: (self topEntity isOfType: FASTTLiteral).
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation callee"
	self assert: self topEntity callee isNotNil.

		stack push: self topEntity callee.
		self assert: self topEntity sourceCode equals: 'function'.
		self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - calls' }
FASTPythonImporterTest >> testCallArgumentWithList [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testCallArgumentWithList 	"

	self parse: 'function(img, [pady, padx])'.

	self assert: (fast allWithType: FASTPyCall) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 4.
	self assert: (fast allWithType: FASTPyList) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'function(img, [pady, padx])'.
	self assert: (self topEntity isOfType: FASTPyCall).
	self assert: (self topEntity isOfType: FASTTInvocation).
	self assert: (self topEntity isOfType: FASTTWithArguments).
	
	"Testing value of multivalued relation arguments"
	self assert: self topEntity arguments size equals: 2.
	
		stack push: (stack top arguments at: 1).
		self assert: self topEntity sourceCode equals: 'img'.
		self assert: (self topEntity isOfType: FASTPyIdentifier).
		stack pop.
	
		stack push: (stack top arguments at: 2).
		self assert: self topEntity sourceCode equals: '[pady, padx]'.
		self assert: (self topEntity isOfType: FASTPyList).
		
		"Testing value of multivalued relation initializers"
		self assert: self topEntity initializers size equals: 2.
		
			stack push: (stack top initializers at: 1).
			self assert: self topEntity sourceCode equals: 'pady'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		
			stack push: (stack top initializers at: 2).
			self assert: self topEntity sourceCode equals: 'padx'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation callee"
	self assert: self topEntity callee isNotNil.

		stack push: self topEntity callee.
		self assert: self topEntity sourceCode equals: 'function'.
		self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - calls' }
FASTPythonImporterTest >> testCallChained [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testCallChained 	"

	self parse: 'function()(13)'.

	self assert: (fast allWithType: FASTPyCall) size equals: 2.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyInteger) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'function()(13)'.
	self assert: (self topEntity isOfType: FASTPyCall).
	self assert: (self topEntity isOfType: FASTTInvocation).
	self assert: (self topEntity isOfType: FASTTWithArguments).
	
	"Testing value of multivalued relation arguments"
	self assert: self topEntity arguments size equals: 1.
	
		stack push: (stack top arguments at: 1).
		self assert: self topEntity sourceCode equals: '13'.
		self assert: (self topEntity isOfType: FASTPyInteger).
		self assert: (self topEntity isOfType: FASTTLiteral).
		self assert: (self topEntity isOfType: FASTTNumericalLiteral).
		stack pop.
	
	"Testing value of monovalue relation callee"
	self assert: self topEntity callee isNotNil.

		stack push: self topEntity callee.
		self assert: self topEntity sourceCode equals: 'function()'.
		self assert: (self topEntity isOfType: FASTPyCall).
		self assert: (self topEntity isOfType: FASTTInvocation).
		self assert: (self topEntity isOfType: FASTTWithArguments).
		
		"Testing value of monovalue relation callee"
		self assert: self topEntity callee isNotNil.

			stack push: self topEntity callee.
			self assert: self topEntity sourceCode equals: 'function'.
			self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - calls' }
FASTPythonImporterTest >> testCallLambda [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testCallLambda 	"

	self parse: '(lambda x: x > 0)(10)'.

	self assert: (fast allWithType: FASTPyCall) size equals: 1.
	self assert: (fast allWithType: FASTPyComparisonOperator) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyInteger) size equals: 2.
	self assert: (fast allWithType: FASTPyLambda) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyParameter) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: '(lambda x: x > 0)(10)'.
	self assert: (self topEntity isOfType: FASTPyCall).
	self assert: (self topEntity isOfType: FASTTInvocation).
	self assert: (self topEntity isOfType: FASTTWithArguments).
	
	"Testing value of multivalued relation arguments"
	self assert: self topEntity arguments size equals: 1.
	
		stack push: (stack top arguments at: 1).
		self assert: self topEntity sourceCode equals: '10'.
		self assert: (self topEntity isOfType: FASTPyInteger).
		self assert: (self topEntity isOfType: FASTTLiteral).
		self assert: (self topEntity isOfType: FASTTNumericalLiteral).
		stack pop.
	
	"Testing value of monovalue relation callee"
	self assert: self topEntity callee isNotNil.

		stack push: self topEntity callee.
		self assert: self topEntity sourceCode equals: 'lambda x: x > 0'.
		self assert: (self topEntity isOfType: FASTPyLambda).
		self assert: (self topEntity isOfType: FASTTWithParameters).
		
		"Testing value of monovalue relation expression"
		self assert: self topEntity expression isNotNil.

			stack push: self topEntity expression.
			self assert: self topEntity sourceCode equals: 'x > 0'.
			self assert: (self topEntity isOfType: FASTPyComparisonOperator).
			
			"Testing value of multivalued relation operands"
			self assert: self topEntity operands size equals: 2.
			
				stack push: (stack top operands at: 1).
				self assert: self topEntity sourceCode equals: 'x'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			
				stack push: (stack top operands at: 2).
				self assert: self topEntity sourceCode equals: '0'.
				self assert: (self topEntity isOfType: FASTPyInteger).
				self assert: (self topEntity isOfType: FASTTLiteral).
				self assert: (self topEntity isOfType: FASTTNumericalLiteral).
				stack pop.
			self assert: self topEntity operators equals: #('>').
			stack pop.
		
		"Testing value of multivalued relation parameters"
		self assert: self topEntity parameters size equals: 1.
		
			stack push: (stack top parameters at: 1).
			self assert: self topEntity sourceCode equals: 'x'.
			self assert: (self topEntity isOfType: FASTPyParameter).
			self assert: (self topEntity isOfType: FASTTVariableEntity).
			self assert: self topEntity name equals: 'x'
]

{ #category : 'tests - calls' }
FASTPythonImporterTest >> testCallSubscript [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testCallSubscript 	"

	self parse: 'coll[1](True)'.

	self assert: (fast allWithType: FASTPyBoolean) size equals: 1.
	self assert: (fast allWithType: FASTPyCall) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyInteger) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPySubscript) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'coll[1](True)'.
	self assert: (self topEntity isOfType: FASTPyCall).
	self assert: (self topEntity isOfType: FASTTInvocation).
	self assert: (self topEntity isOfType: FASTTWithArguments).
	
	"Testing value of multivalued relation arguments"
	self assert: self topEntity arguments size equals: 1.
	
		stack push: (stack top arguments at: 1).
		self assert: self topEntity sourceCode equals: 'True'.
		self assert: (self topEntity isOfType: FASTPyBoolean).
		self assert: (self topEntity isOfType: FASTTBooleanLiteral).
		self assert: (self topEntity isOfType: FASTTLiteral).
		stack pop.
	
	"Testing value of monovalue relation callee"
	self assert: self topEntity callee isNotNil.

		stack push: self topEntity callee.
		self assert: self topEntity sourceCode equals: 'coll[1]'.
		self assert: (self topEntity isOfType: FASTPySubscript).
		
		"Testing value of multivalued relation indices"
		self assert: self topEntity indices size equals: 1.
		
			stack push: (stack top indices at: 1).
			self assert: self topEntity sourceCode equals: '1'.
			self assert: (self topEntity isOfType: FASTPyInteger).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTNumericalLiteral).
			stack pop.
		
		"Testing value of monovalue relation value"
		self assert: self topEntity value isNotNil.

			stack push: self topEntity value.
			self assert: self topEntity sourceCode equals: 'coll'.
			self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - calls' }
FASTPythonImporterTest >> testCallWithAttribute [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testCallWithAttribute 	"

	self parse: 'tqdm.pandas()'.

	self assert: (fast allWithType: FASTPyAttributeAccess) size equals: 1.
	self assert: (fast allWithType: FASTPyCall) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 2.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'tqdm.pandas()'.
	self assert: (self topEntity isOfType: FASTPyCall).
	self assert: (self topEntity isOfType: FASTTInvocation).
	self assert: (self topEntity isOfType: FASTTWithArguments).
	
	"Testing value of monovalue relation callee"
	self assert: self topEntity callee isNotNil.

		stack push: self topEntity callee.
		self assert: self topEntity sourceCode equals: 'tqdm.pandas'.
		self assert: (self topEntity isOfType: FASTPyAttributeAccess).
		
		"Testing value of monovalue relation attribute"
		self assert: self topEntity attribute isNotNil.

			stack push: self topEntity attribute.
			self assert: self topEntity sourceCode equals: 'pandas'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		
		"Testing value of monovalue relation object"
		self assert: self topEntity object isNotNil.

			stack push: self topEntity object.
			self assert: self topEntity sourceCode equals: 'tqdm'.
			self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - operators' }
FASTPythonImporterTest >> testChainedComparisonOperator [

	| operator |
	self parse: '14 > 13 < 15'.

	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyComparisonOperator) size equals: 1.

	operator := (fast allWithType: FASTPyComparisonOperator) anyOne.
	self assert: operator sourceCode equals: '14 > 13 < 15'.
	self assert: (operator isOfType: FASTTExpression).
	self assert: operator operands size equals: 3.
	self assert: (operator operands first isOfType: FASTPyInteger).
	self assert: (operator operands second isOfType: FASTPyInteger).
	self assert: (operator operands third isOfType: FASTPyInteger).
	self assert: operator operands first sourceCode equals: '14'.
	self assert: operator operands second sourceCode equals: '13'.
	self assert: operator operands third sourceCode equals: '15'.
	self assert: operator operators size equals: 2.
	self assert: operator operators first equals: '>'.
	self assert: operator operators second equals: '<'
]

{ #category : 'tests - classes' }
FASTPythonImporterTest >> testClassDefinition [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testClassDefinition 	"

	self parse: 'class AClass():
	pass'.

	self assert: (fast allWithType: FASTPyBlock) size equals: 1.
	self assert: (fast allWithType: FASTPyClassDefinition) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPassStatement) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'class AClass():
	pass' withPlatformLineEndings.
	self assert: (self topEntity isOfType: FASTPyClassDefinition).
	self assert: (self topEntity isOfType: FASTPyTDefinition).
	self assert: (self topEntity isOfType: FASTTWithParameters).
	self assert: (self topEntity isOfType: FASTTWithStatements).
	self assert: self topEntity name equals: 'AClass'.
	
	"Testing value of monovalue relation statementBlock"
	self assert: self topEntity statementBlock isNotNil.

		stack push: self topEntity statementBlock.
		self assert: self topEntity sourceCode equals: 'pass'.
		self assert: (self topEntity isOfType: FASTPyBlock).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'pass'.
			self assert: (self topEntity isOfType: FASTPyPassStatement)
]

{ #category : 'tests - comments' }
FASTPythonImporterTest >> testComment [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testComment 	"

	self parse: '#There is only one comment'.

	self assert: (fast allWithType: FASTPyComment) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: '#There is only one comment'.
	self assert: (self topEntity isOfType: FASTPyComment).
	self assert: (self topEntity isOfType: FASTTComment)
]

{ #category : 'tests - comments' }
FASTPythonImporterTest >> testCommentInArgumentList [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testCommentInArgumentList 	"

	self parse: 'funct(
    10  # Process/thread ID
)'.

	self assert: (fast allWithType: FASTPyCall) size equals: 1.
	self assert: (fast allWithType: FASTPyComment) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyInteger) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'funct(
    10  # Process/thread ID
)' withPlatformLineEndings.
	self assert: (self topEntity isOfType: FASTPyCall).
	self assert: (self topEntity isOfType: FASTTInvocation).
	self assert: (self topEntity isOfType: FASTTWithArguments).
	
	"Testing value of multivalued relation arguments"
	self assert: self topEntity arguments size equals: 1.
	
		stack push: (stack top arguments at: 1).
		self assert: self topEntity sourceCode equals: '10'.
		self assert: (self topEntity isOfType: FASTPyInteger).
		self assert: (self topEntity isOfType: FASTTLiteral).
		self assert: (self topEntity isOfType: FASTTNumericalLiteral).
		stack pop.
	
	"Testing value of monovalue relation callee"
	self assert: self topEntity callee isNotNil.

		stack push: self topEntity callee.
		self assert: self topEntity sourceCode equals: 'funct'.
		self assert: (self topEntity isOfType: FASTPyIdentifier).
		stack pop.
	
	"Testing value of multivalued relation comments"
	self assert: self topEntity comments size equals: 1.
	
		stack push: (stack top comments at: 1).
		self assert: self topEntity sourceCode equals: '# Process/thread ID'.
		self assert: (self topEntity isOfType: FASTPyComment).
		self assert: (self topEntity isOfType: FASTTComment)
]

{ #category : 'tests - operators' }
FASTPythonImporterTest >> testComparisonOperator [

	| operator |
	self parse: '10 >= 1'.

	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyComparisonOperator) size equals: 1.

	operator := (fast allWithType: FASTPyComparisonOperator) anyOne.
	self assert: operator sourceCode equals: '10 >= 1'.
	self assert: (operator isOfType: FASTTExpression).
	self assert: operator operands size equals: 2.
	self assert: (operator operands first isOfType: FASTPyInteger).
	self assert: (operator operands second isOfType: FASTPyInteger).
	self assert: operator operands first sourceCode equals: '10'.
	self assert: operator operands second sourceCode equals: '1'.
	self assert: operator operators size equals: 1.
	self assert: operator operators anyOne equals: '>='
]

{ #category : 'tests - operators' }
FASTPythonImporterTest >> testComparisonOperatorWithEllipsis [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testComparisonOperatorWithEllipsis 	"

	self parse: 'obj is ...'.

	self assert: (fast allWithType: FASTPyComparisonOperator) size equals: 1.
	self assert: (fast allWithType: FASTPyEllipsis) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'obj is ...'.
	self assert: (self topEntity isOfType: FASTPyComparisonOperator).
	
	"Testing value of multivalued relation operands"
	self assert: self topEntity operands size equals: 2.
	
		stack push: (stack top operands at: 1).
		self assert: self topEntity sourceCode equals: 'obj'.
		self assert: (self topEntity isOfType: FASTPyIdentifier).
		stack pop.
	
		stack push: (stack top operands at: 2).
		self assert: self topEntity sourceCode equals: '...'.
		self assert: (self topEntity isOfType: FASTPyEllipsis).
		self assert: (self topEntity isOfType: FASTTLiteral).
		stack pop.
	self assert: self topEntity operators equals: #('is')
]

{ #category : 'tests - literals' }
FASTPythonImporterTest >> testComplex [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testComplex 	"

	self parse: '42j'.

	self assert: (fast allWithType: FASTPyComplex) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: '42j'.
	self assert: (self topEntity isOfType: FASTPyComplex).
	self assert: (self topEntity isOfType: FASTTLiteral).
	self assert: (self topEntity isOfType: FASTTNumericalLiteral)
]

{ #category : 'tests - literals' }
FASTPythonImporterTest >> testConcatenatedString [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testConcatenatedString 	"

	self parse: 'f"Hello {name}" "Have a nice day"'.

	self assert: (fast allWithType: FASTPyConcatenatedString) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyInterpolation) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyString) size equals: 2.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'f"Hello {name}" "Have a nice day"'.
	self assert: (self topEntity isOfType: FASTPyConcatenatedString).
	
	"Testing value of multivalued relation strings"
	self assert: self topEntity strings size equals: 2.
	
		stack push: (stack top strings at: 1).
		self assert: self topEntity sourceCode equals: 'f"Hello {name}"'.
		self assert: (self topEntity isOfType: FASTPyString).
		self assert: (self topEntity isOfType: FASTTLiteral).
		self assert: (self topEntity isOfType: FASTTStringLiteral).
		
		"Testing value of multivalued relation interpolations"
		self assert: self topEntity interpolations size equals: 1.
		
			stack push: (stack top interpolations at: 1).
			self assert: self topEntity sourceCode equals: '{name}'.
			self assert: (self topEntity isOfType: FASTPyInterpolation).
			
			"Testing value of monovalue relation expression"
			self assert: self topEntity expression isNotNil.

				stack push: self topEntity expression.
				self assert: self topEntity sourceCode equals: 'name'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			stack pop.
		stack pop.
	
		stack push: (stack top strings at: 2).
		self assert: self topEntity sourceCode equals: '"Have a nice day"'.
		self assert: (self topEntity isOfType: FASTPyString).
		self assert: (self topEntity isOfType: FASTTLiteral).
		self assert: (self topEntity isOfType: FASTTStringLiteral)
]

{ #category : 'tests' }
FASTPythonImporterTest >> testConditionalExpression [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testConditionalExpression 	"

	self parse: '"even" if x % 2 == 0 else "odd"'.

	self assert: (fast allWithType: FASTPyBinaryOperator) size equals: 1.
	self assert: (fast allWithType: FASTPyComparisonOperator) size equals: 1.
	self assert: (fast allWithType: FASTPyConditionalExpression) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyInteger) size equals: 2.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyString) size equals: 2.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: '"even" if x % 2 == 0 else "odd"'.
	self assert: (self topEntity isOfType: FASTPyConditionalExpression).
	self assert: (self topEntity isOfType: FASTTWithCondition).
	
	"Testing value of monovalue relation condition"
	self assert: self topEntity condition isNotNil.

		stack push: self topEntity condition.
		self assert: self topEntity sourceCode equals: 'x % 2 == 0'.
		self assert: (self topEntity isOfType: FASTPyComparisonOperator).
		
		"Testing value of multivalued relation operands"
		self assert: self topEntity operands size equals: 2.
		
			stack push: (stack top operands at: 1).
			self assert: self topEntity sourceCode equals: 'x % 2'.
			self assert: (self topEntity isOfType: FASTPyBinaryOperator).
			self assert: (self topEntity isOfType: FASTTBinaryExpression).
			
			"Testing value of monovalue relation leftOperand"
			self assert: self topEntity leftOperand isNotNil.

				stack push: self topEntity leftOperand.
				self assert: self topEntity sourceCode equals: 'x'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			self assert: self topEntity operator equals: '%'.
			
			"Testing value of monovalue relation rightOperand"
			self assert: self topEntity rightOperand isNotNil.

				stack push: self topEntity rightOperand.
				self assert: self topEntity sourceCode equals: '2'.
				self assert: (self topEntity isOfType: FASTPyInteger).
				self assert: (self topEntity isOfType: FASTTLiteral).
				self assert: (self topEntity isOfType: FASTTNumericalLiteral).
				stack pop.
			stack pop.
		
			stack push: (stack top operands at: 2).
			self assert: self topEntity sourceCode equals: '0'.
			self assert: (self topEntity isOfType: FASTPyInteger).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTNumericalLiteral).
			stack pop.
		self assert: self topEntity operators equals: #('==').
		stack pop.
	
	"Testing value of monovalue relation elseExpression"
	self assert: self topEntity elseExpression isNotNil.

		stack push: self topEntity elseExpression.
		self assert: self topEntity sourceCode equals: '"odd"'.
		self assert: (self topEntity isOfType: FASTPyString).
		self assert: (self topEntity isOfType: FASTTLiteral).
		self assert: (self topEntity isOfType: FASTTStringLiteral).
		stack pop.
	
	"Testing value of monovalue relation thenExpression"
	self assert: self topEntity thenExpression isNotNil.

		stack push: self topEntity thenExpression.
		self assert: self topEntity sourceCode equals: '"even"'.
		self assert: (self topEntity isOfType: FASTPyString).
		self assert: (self topEntity isOfType: FASTTLiteral).
		self assert: (self topEntity isOfType: FASTTStringLiteral)
]

{ #category : 'tests - types' }
FASTPythonImporterTest >> testConstrainedType [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testConstrainedType 	"

	self parse: 'def f[T: int](): pass'.

	self assert: (fast allWithType: FASTPyBlock) size equals: 1.
	self assert: (fast allWithType: FASTPyConstrainedType) size equals: 1.
	self assert: (fast allWithType: FASTPyFunctionDefinition) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 2.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPassStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyType) size equals: 3.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'def f[T: int](): pass'.
	self assert: (self topEntity isOfType: FASTPyFunctionDefinition).
	self assert: (self topEntity isOfType: FASTPyTDefinition).
	self assert: (self topEntity isOfType: FASTTWithParameters).
	self assert: (self topEntity isOfType: FASTTWithStatements).
	self assert: self topEntity name equals: 'f'.
	
	"Testing value of monovalue relation statementBlock"
	self assert: self topEntity statementBlock isNotNil.

		stack push: self topEntity statementBlock.
		self assert: self topEntity sourceCode equals: 'pass'.
		self assert: (self topEntity isOfType: FASTPyBlock).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'pass'.
			self assert: (self topEntity isOfType: FASTPyPassStatement).
			stack pop.
		stack pop.
	
	"Testing value of multivalued relation typeParameters"
	self assert: self topEntity typeParameters size equals: 1.
	
		stack push: (stack top typeParameters at: 1).
		self assert: self topEntity sourceCode equals: 'T: int'.
		self assert: (self topEntity isOfType: FASTPyType).
		
		"Testing value of monovalue relation content"
		self assert: self topEntity content isNotNil.

			stack push: self topEntity content.
			self assert: self topEntity sourceCode equals: 'T: int'.
			self assert: (self topEntity isOfType: FASTPyConstrainedType).
			
			"Testing value of monovalue relation bound"
			self assert: self topEntity bound isNotNil.

				stack push: self topEntity bound.
				self assert: self topEntity sourceCode equals: 'int'.
				self assert: (self topEntity isOfType: FASTPyType).
				
				"Testing value of monovalue relation content"
				self assert: self topEntity content isNotNil.

					stack push: self topEntity content.
					self assert: self topEntity sourceCode equals: 'int'.
					self assert: (self topEntity isOfType: FASTPyIdentifier).
					stack pop.
				stack pop.
			
			"Testing value of monovalue relation type"
			self assert: self topEntity type isNotNil.

				stack push: self topEntity type.
				self assert: self topEntity sourceCode equals: 'T'.
				self assert: (self topEntity isOfType: FASTPyType).
				
				"Testing value of monovalue relation content"
				self assert: self topEntity content isNotNil.

					stack push: self topEntity content.
					self assert: self topEntity sourceCode equals: 'T'.
					self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - statements' }
FASTPythonImporterTest >> testContinueStatement [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testContinueStatement 	"

	self parse: 'while True:
	continue'.

	self assert: (fast allWithType: FASTPyBlock) size equals: 1.
	self assert: (fast allWithType: FASTPyBoolean) size equals: 1.
	self assert: (fast allWithType: FASTPyContinueStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyWhileStatement) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'while True:
	continue' withPlatformLineEndings.
	self assert: (self topEntity isOfType: FASTPyWhileStatement).
	self assert: (self topEntity isOfType: FASTPyTWithElseClause).
	self assert: (self topEntity isOfType: FASTTConditionalStatement).
	self assert: (self topEntity isOfType: FASTTLoopStatement).
	self assert: (self topEntity isOfType: FASTTWithCondition).
	
	"Testing value of monovalue relation body"
	self assert: self topEntity body isNotNil.

		stack push: self topEntity body.
		self assert: self topEntity sourceCode equals: 'continue'.
		self assert: (self topEntity isOfType: FASTPyBlock).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'continue'.
			self assert: (self topEntity isOfType: FASTPyContinueStatement).
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation condition"
	self assert: self topEntity condition isNotNil.

		stack push: self topEntity condition.
		self assert: self topEntity sourceCode equals: 'True'.
		self assert: (self topEntity isOfType: FASTPyBoolean).
		self assert: (self topEntity isOfType: FASTTBooleanLiteral).
		self assert: (self topEntity isOfType: FASTTLiteral)
]

{ #category : 'tests - decorators' }
FASTPythonImporterTest >> testDecorator [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testDecorator 	"

	self parse: '@decorator
def funct():
	pass'.

	self assert: (fast allWithType: FASTPyBlock) size equals: 1.
	self assert: (fast allWithType: FASTPyDecorator) size equals: 1.
	self assert: (fast allWithType: FASTPyFunctionDefinition) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPassStatement) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'def funct():
	pass' withPlatformLineEndings.
	self assert: (self topEntity isOfType: FASTPyFunctionDefinition).
	self assert: (self topEntity isOfType: FASTPyTDefinition).
	self assert: (self topEntity isOfType: FASTTWithParameters).
	self assert: (self topEntity isOfType: FASTTWithStatements).
	
	"Testing value of multivalued relation decorators"
	self assert: self topEntity decorators size equals: 1.
	
		stack push: (stack top decorators at: 1).
		self assert: self topEntity sourceCode equals: '@decorator'.
		self assert: (self topEntity isOfType: FASTPyDecorator).
		
		"Testing value of monovalue relation expression"
		self assert: self topEntity expression isNotNil.

			stack push: self topEntity expression.
			self assert: self topEntity sourceCode equals: 'decorator'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		stack pop.
	self assert: self topEntity name equals: 'funct'.
	
	"Testing value of monovalue relation statementBlock"
	self assert: self topEntity statementBlock isNotNil.

		stack push: self topEntity statementBlock.
		self assert: self topEntity sourceCode equals: 'pass'.
		self assert: (self topEntity isOfType: FASTPyBlock).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'pass'.
			self assert: (self topEntity isOfType: FASTPyPassStatement)
]

{ #category : 'tests - decorators' }
FASTPythonImporterTest >> testDecoratorOnClassDefinition [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testDecoratorOnClassDefinition 	"

	self parse: '@register_component
class MyComponent: pass'.

	self assert: (fast allWithType: FASTPyBlock) size equals: 1.
	self assert: (fast allWithType: FASTPyClassDefinition) size equals: 1.
	self assert: (fast allWithType: FASTPyDecorator) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPassStatement) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'class MyComponent: pass'.
	self assert: (self topEntity isOfType: FASTPyClassDefinition).
	self assert: (self topEntity isOfType: FASTPyTDefinition).
	self assert: (self topEntity isOfType: FASTTWithParameters).
	self assert: (self topEntity isOfType: FASTTWithStatements).
	
	"Testing value of multivalued relation decorators"
	self assert: self topEntity decorators size equals: 1.
	
		stack push: (stack top decorators at: 1).
		self assert: self topEntity sourceCode equals: '@register_component'.
		self assert: (self topEntity isOfType: FASTPyDecorator).
		
		"Testing value of monovalue relation expression"
		self assert: self topEntity expression isNotNil.

			stack push: self topEntity expression.
			self assert: self topEntity sourceCode equals: 'register_component'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		stack pop.
	self assert: self topEntity name equals: 'MyComponent'.
	
	"Testing value of monovalue relation statementBlock"
	self assert: self topEntity statementBlock isNotNil.

		stack push: self topEntity statementBlock.
		self assert: self topEntity sourceCode equals: 'pass'.
		self assert: (self topEntity isOfType: FASTPyBlock).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'pass'.
			self assert: (self topEntity isOfType: FASTPyPassStatement)
]

{ #category : 'tests - decorators' }
FASTPythonImporterTest >> testDecoratorWithAttributeAccess [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testDecoratorWithAttributeAccess 	"

	self parse: '@functools.lru_cache
def funct(): pass'.

	self assert: (fast allWithType: FASTPyAttributeAccess) size equals: 1.
	self assert: (fast allWithType: FASTPyBlock) size equals: 1.
	self assert: (fast allWithType: FASTPyDecorator) size equals: 1.
	self assert: (fast allWithType: FASTPyFunctionDefinition) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 2.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPassStatement) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'def funct(): pass'.
	self assert: (self topEntity isOfType: FASTPyFunctionDefinition).
	self assert: (self topEntity isOfType: FASTPyTDefinition).
	self assert: (self topEntity isOfType: FASTTWithParameters).
	self assert: (self topEntity isOfType: FASTTWithStatements).
	
	"Testing value of multivalued relation decorators"
	self assert: self topEntity decorators size equals: 1.
	
		stack push: (stack top decorators at: 1).
		self assert: self topEntity sourceCode equals: '@functools.lru_cache'.
		self assert: (self topEntity isOfType: FASTPyDecorator).
		
		"Testing value of monovalue relation expression"
		self assert: self topEntity expression isNotNil.

			stack push: self topEntity expression.
			self assert: self topEntity sourceCode equals: 'functools.lru_cache'.
			self assert: (self topEntity isOfType: FASTPyAttributeAccess).
			
			"Testing value of monovalue relation attribute"
			self assert: self topEntity attribute isNotNil.

				stack push: self topEntity attribute.
				self assert: self topEntity sourceCode equals: 'lru_cache'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			
			"Testing value of monovalue relation object"
			self assert: self topEntity object isNotNil.

				stack push: self topEntity object.
				self assert: self topEntity sourceCode equals: 'functools'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			stack pop.
		stack pop.
	self assert: self topEntity name equals: 'funct'.
	
	"Testing value of monovalue relation statementBlock"
	self assert: self topEntity statementBlock isNotNil.

		stack push: self topEntity statementBlock.
		self assert: self topEntity sourceCode equals: 'pass'.
		self assert: (self topEntity isOfType: FASTPyBlock).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'pass'.
			self assert: (self topEntity isOfType: FASTPyPassStatement)
]

{ #category : 'tests - decorators' }
FASTPythonImporterTest >> testDecoratorWithCall [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testDecoratorWithCall 	"

	self parse: '@factory.make_decorator()
def funct(): pass'.

	self assert: (fast allWithType: FASTPyAttributeAccess) size equals: 1.
	self assert: (fast allWithType: FASTPyBlock) size equals: 1.
	self assert: (fast allWithType: FASTPyCall) size equals: 1.
	self assert: (fast allWithType: FASTPyDecorator) size equals: 1.
	self assert: (fast allWithType: FASTPyFunctionDefinition) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 2.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPassStatement) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'def funct(): pass'.
	self assert: (self topEntity isOfType: FASTPyFunctionDefinition).
	self assert: (self topEntity isOfType: FASTPyTDefinition).
	self assert: (self topEntity isOfType: FASTTWithParameters).
	self assert: (self topEntity isOfType: FASTTWithStatements).
	
	"Testing value of multivalued relation decorators"
	self assert: self topEntity decorators size equals: 1.
	
		stack push: (stack top decorators at: 1).
		self assert: self topEntity sourceCode equals: '@factory.make_decorator()'.
		self assert: (self topEntity isOfType: FASTPyDecorator).
		
		"Testing value of monovalue relation expression"
		self assert: self topEntity expression isNotNil.

			stack push: self topEntity expression.
			self assert: self topEntity sourceCode equals: 'factory.make_decorator()'.
			self assert: (self topEntity isOfType: FASTPyCall).
			self assert: (self topEntity isOfType: FASTTInvocation).
			self assert: (self topEntity isOfType: FASTTWithArguments).
			
			"Testing value of monovalue relation callee"
			self assert: self topEntity callee isNotNil.

				stack push: self topEntity callee.
				self assert: self topEntity sourceCode equals: 'factory.make_decorator'.
				self assert: (self topEntity isOfType: FASTPyAttributeAccess).
				
				"Testing value of monovalue relation attribute"
				self assert: self topEntity attribute isNotNil.

					stack push: self topEntity attribute.
					self assert: self topEntity sourceCode equals: 'make_decorator'.
					self assert: (self topEntity isOfType: FASTPyIdentifier).
					stack pop.
				
				"Testing value of monovalue relation object"
				self assert: self topEntity object isNotNil.

					stack push: self topEntity object.
					self assert: self topEntity sourceCode equals: 'factory'.
					self assert: (self topEntity isOfType: FASTPyIdentifier).
					stack pop.
				stack pop.
			stack pop.
		stack pop.
	self assert: self topEntity name equals: 'funct'.
	
	"Testing value of monovalue relation statementBlock"
	self assert: self topEntity statementBlock isNotNil.

		stack push: self topEntity statementBlock.
		self assert: self topEntity sourceCode equals: 'pass'.
		self assert: (self topEntity isOfType: FASTPyBlock).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'pass'.
			self assert: (self topEntity isOfType: FASTPyPassStatement)
]

{ #category : 'tests - decorators' }
FASTPythonImporterTest >> testDecoratorWithLambda [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testDecoratorWithLambda 	"

	self parse: '@(lambda f: f)
def funct(): pass'.

	self assert: (fast allWithType: FASTPyBlock) size equals: 1.
	self assert: (fast allWithType: FASTPyDecorator) size equals: 1.
	self assert: (fast allWithType: FASTPyFunctionDefinition) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyLambda) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyParameter) size equals: 1.
	self assert: (fast allWithType: FASTPyPassStatement) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'def funct(): pass'.
	self assert: (self topEntity isOfType: FASTPyFunctionDefinition).
	self assert: (self topEntity isOfType: FASTPyTDefinition).
	self assert: (self topEntity isOfType: FASTTWithParameters).
	self assert: (self topEntity isOfType: FASTTWithStatements).
	
	"Testing value of multivalued relation decorators"
	self assert: self topEntity decorators size equals: 1.
	
		stack push: (stack top decorators at: 1).
		self assert: self topEntity sourceCode equals: '@(lambda f: f)'.
		self assert: (self topEntity isOfType: FASTPyDecorator).
		
		"Testing value of monovalue relation expression"
		self assert: self topEntity expression isNotNil.

			stack push: self topEntity expression.
			self assert: self topEntity sourceCode equals: 'lambda f: f'.
			self assert: (self topEntity isOfType: FASTPyLambda).
			self assert: (self topEntity isOfType: FASTTWithParameters).
			
			"Testing value of monovalue relation expression"
			self assert: self topEntity expression isNotNil.

				stack push: self topEntity expression.
				self assert: self topEntity sourceCode equals: 'f'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			
			"Testing value of multivalued relation parameters"
			self assert: self topEntity parameters size equals: 1.
			
				stack push: (stack top parameters at: 1).
				self assert: self topEntity sourceCode equals: 'f'.
				self assert: (self topEntity isOfType: FASTPyParameter).
				self assert: (self topEntity isOfType: FASTTVariableEntity).
				self assert: self topEntity name equals: 'f'.
				stack pop.
			stack pop.
		stack pop.
	self assert: self topEntity name equals: 'funct'.
	
	"Testing value of monovalue relation statementBlock"
	self assert: self topEntity statementBlock isNotNil.

		stack push: self topEntity statementBlock.
		self assert: self topEntity sourceCode equals: 'pass'.
		self assert: (self topEntity isOfType: FASTPyBlock).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'pass'.
			self assert: (self topEntity isOfType: FASTPyPassStatement)
]

{ #category : 'tests - decorators' }
FASTPythonImporterTest >> testDecoratorWithSubscript [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testDecoratorWithSubscript 	"

	self parse: '@decorators[''auth'']
def funct(): pass'.

	self assert: (fast allWithType: FASTPyBlock) size equals: 1.
	self assert: (fast allWithType: FASTPyDecorator) size equals: 1.
	self assert: (fast allWithType: FASTPyFunctionDefinition) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPassStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyString) size equals: 1.
	self assert: (fast allWithType: FASTPySubscript) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'def funct(): pass'.
	self assert: (self topEntity isOfType: FASTPyFunctionDefinition).
	self assert: (self topEntity isOfType: FASTPyTDefinition).
	self assert: (self topEntity isOfType: FASTTWithParameters).
	self assert: (self topEntity isOfType: FASTTWithStatements).
	
	"Testing value of multivalued relation decorators"
	self assert: self topEntity decorators size equals: 1.
	
		stack push: (stack top decorators at: 1).
		self assert: self topEntity sourceCode equals: '@decorators[''auth'']'.
		self assert: (self topEntity isOfType: FASTPyDecorator).
		
		"Testing value of monovalue relation expression"
		self assert: self topEntity expression isNotNil.

			stack push: self topEntity expression.
			self assert: self topEntity sourceCode equals: 'decorators[''auth'']'.
			self assert: (self topEntity isOfType: FASTPySubscript).
			
			"Testing value of multivalued relation indices"
			self assert: self topEntity indices size equals: 1.
			
				stack push: (stack top indices at: 1).
				self assert: self topEntity sourceCode equals: '''auth'''.
				self assert: (self topEntity isOfType: FASTPyString).
				self assert: (self topEntity isOfType: FASTTLiteral).
				self assert: (self topEntity isOfType: FASTTStringLiteral).
				stack pop.
			
			"Testing value of monovalue relation value"
			self assert: self topEntity value isNotNil.

				stack push: self topEntity value.
				self assert: self topEntity sourceCode equals: 'decorators'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			stack pop.
		stack pop.
	self assert: self topEntity name equals: 'funct'.
	
	"Testing value of monovalue relation statementBlock"
	self assert: self topEntity statementBlock isNotNil.

		stack push: self topEntity statementBlock.
		self assert: self topEntity sourceCode equals: 'pass'.
		self assert: (self topEntity isOfType: FASTPyBlock).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'pass'.
			self assert: (self topEntity isOfType: FASTPyPassStatement)
]

{ #category : 'tests - decorators' }
FASTPythonImporterTest >> testDecorators [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testDecorators 	"

	self parse: '@decoratorA
@decoratorB
def funct():
	pass'.

	self assert: (fast allWithType: FASTPyBlock) size equals: 1.
	self assert: (fast allWithType: FASTPyDecorator) size equals: 2.
	self assert: (fast allWithType: FASTPyFunctionDefinition) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 2.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPassStatement) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'def funct():
	pass' withPlatformLineEndings.
	self assert: (self topEntity isOfType: FASTPyFunctionDefinition).
	self assert: (self topEntity isOfType: FASTPyTDefinition).
	self assert: (self topEntity isOfType: FASTTWithParameters).
	self assert: (self topEntity isOfType: FASTTWithStatements).
	
	"Testing value of multivalued relation decorators"
	self assert: self topEntity decorators size equals: 2.
	
		stack push: (stack top decorators at: 1).
		self assert: self topEntity sourceCode equals: '@decoratorA'.
		self assert: (self topEntity isOfType: FASTPyDecorator).
		
		"Testing value of monovalue relation expression"
		self assert: self topEntity expression isNotNil.

			stack push: self topEntity expression.
			self assert: self topEntity sourceCode equals: 'decoratorA'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		stack pop.
	
		stack push: (stack top decorators at: 2).
		self assert: self topEntity sourceCode equals: '@decoratorB'.
		self assert: (self topEntity isOfType: FASTPyDecorator).
		
		"Testing value of monovalue relation expression"
		self assert: self topEntity expression isNotNil.

			stack push: self topEntity expression.
			self assert: self topEntity sourceCode equals: 'decoratorB'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		stack pop.
	self assert: self topEntity name equals: 'funct'.
	
	"Testing value of monovalue relation statementBlock"
	self assert: self topEntity statementBlock isNotNil.

		stack push: self topEntity statementBlock.
		self assert: self topEntity sourceCode equals: 'pass'.
		self assert: (self topEntity isOfType: FASTPyBlock).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'pass'.
			self assert: (self topEntity isOfType: FASTPyPassStatement)
]

{ #category : 'tests - function definitions' }
FASTPythonImporterTest >> testDefaultParameter [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testDefaultParameter 	"

	self parse: 'def f(x=1): pass'.

	self assert: (fast allWithType: FASTPyBlock) size equals: 1.
	self assert: (fast allWithType: FASTPyFunctionDefinition) size equals: 1.
	self assert: (fast allWithType: FASTPyInteger) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyParameter) size equals: 1.
	self assert: (fast allWithType: FASTPyPassStatement) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'def f(x=1): pass'.
	self assert: (self topEntity isOfType: FASTPyFunctionDefinition).
	self assert: (self topEntity isOfType: FASTPyTDefinition).
	self assert: (self topEntity isOfType: FASTTWithParameters).
	self assert: (self topEntity isOfType: FASTTWithStatements).
	self assert: self topEntity name equals: 'f'.
	
	"Testing value of multivalued relation parameters"
	self assert: self topEntity parameters size equals: 1.
	
		stack push: (stack top parameters at: 1).
		self assert: self topEntity sourceCode equals: 'x=1'.
		self assert: (self topEntity isOfType: FASTPyParameter).
		self assert: (self topEntity isOfType: FASTTVariableEntity).
		
		"Testing value of monovalue relation defaultValue"
		self assert: self topEntity defaultValue isNotNil.

			stack push: self topEntity defaultValue.
			self assert: self topEntity sourceCode equals: '1'.
			self assert: (self topEntity isOfType: FASTPyInteger).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTNumericalLiteral).
			stack pop.
		self assert: self topEntity name equals: 'x'.
		stack pop.
	
	"Testing value of monovalue relation statementBlock"
	self assert: self topEntity statementBlock isNotNil.

		stack push: self topEntity statementBlock.
		self assert: self topEntity sourceCode equals: 'pass'.
		self assert: (self topEntity isOfType: FASTPyBlock).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'pass'.
			self assert: (self topEntity isOfType: FASTPyPassStatement)
]

{ #category : 'tests - function definitions' }
FASTPythonImporterTest >> testDefaultTypedParameter [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testDefaultTypedParameter 	"

	self parse: 'def f(c:1={}): pass'.

	self assert: (fast allWithType: FASTPyBlock) size equals: 1.
	self assert: (fast allWithType: FASTPyDictionary) size equals: 1.
	self assert: (fast allWithType: FASTPyFunctionDefinition) size equals: 1.
	self assert: (fast allWithType: FASTPyInteger) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyParameter) size equals: 1.
	self assert: (fast allWithType: FASTPyPassStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyType) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'def f(c:1={}): pass'.
	self assert: (self topEntity isOfType: FASTPyFunctionDefinition).
	self assert: (self topEntity isOfType: FASTPyTDefinition).
	self assert: (self topEntity isOfType: FASTTWithParameters).
	self assert: (self topEntity isOfType: FASTTWithStatements).
	self assert: self topEntity name equals: 'f'.
	
	"Testing value of multivalued relation parameters"
	self assert: self topEntity parameters size equals: 1.
	
		stack push: (stack top parameters at: 1).
		self assert: self topEntity sourceCode equals: 'c:1={}'.
		self assert: (self topEntity isOfType: FASTPyParameter).
		self assert: (self topEntity isOfType: FASTTVariableEntity).
		
		"Testing value of monovalue relation defaultValue"
		self assert: self topEntity defaultValue isNotNil.

			stack push: self topEntity defaultValue.
			self assert: self topEntity sourceCode equals: '{}'.
			self assert: (self topEntity isOfType: FASTPyDictionary).
			stack pop.
		self assert: self topEntity name equals: 'c'.
		
		"Testing value of monovalue relation type"
		self assert: self topEntity type isNotNil.

			stack push: self topEntity type.
			self assert: self topEntity sourceCode equals: '1'.
			self assert: (self topEntity isOfType: FASTPyType).
			
			"Testing value of monovalue relation content"
			self assert: self topEntity content isNotNil.

				stack push: self topEntity content.
				self assert: self topEntity sourceCode equals: '1'.
				self assert: (self topEntity isOfType: FASTPyInteger).
				self assert: (self topEntity isOfType: FASTTLiteral).
				self assert: (self topEntity isOfType: FASTTNumericalLiteral).
				stack pop.
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation statementBlock"
	self assert: self topEntity statementBlock isNotNil.

		stack push: self topEntity statementBlock.
		self assert: self topEntity sourceCode equals: 'pass'.
		self assert: (self topEntity isOfType: FASTPyBlock).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'pass'.
			self assert: (self topEntity isOfType: FASTPyPassStatement)
]

{ #category : 'tests - statements' }
FASTPythonImporterTest >> testDelStatement [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testDelStatement 	"

	self parse: 'del coll'.

	self assert: (fast allWithType: FASTPyDeleteStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'del coll'.
	self assert: (self topEntity isOfType: FASTPyDeleteStatement).
	
	"Testing value of monovalue relation expression"
	self assert: self topEntity expression isNotNil.

		stack push: self topEntity expression.
		self assert: self topEntity sourceCode equals: 'coll'.
		self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - statements' }
FASTPythonImporterTest >> testDelStatementAttribute [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testDelStatementAttribute 	"

	self parse: 'del obj.attr'.

	self assert: (fast allWithType: FASTPyAttributeAccess) size equals: 1.
	self assert: (fast allWithType: FASTPyDeleteStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 2.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'del obj.attr'.
	self assert: (self topEntity isOfType: FASTPyDeleteStatement).
	
	"Testing value of monovalue relation expression"
	self assert: self topEntity expression isNotNil.

		stack push: self topEntity expression.
		self assert: self topEntity sourceCode equals: 'obj.attr'.
		self assert: (self topEntity isOfType: FASTPyAttributeAccess).
		
		"Testing value of monovalue relation attribute"
		self assert: self topEntity attribute isNotNil.

			stack push: self topEntity attribute.
			self assert: self topEntity sourceCode equals: 'attr'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		
		"Testing value of monovalue relation object"
		self assert: self topEntity object isNotNil.

			stack push: self topEntity object.
			self assert: self topEntity sourceCode equals: 'obj'.
			self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - statements' }
FASTPythonImporterTest >> testDelStatementMultiple [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testDelStatementMultiple 	"

	self parse: 'del obj1, obj2, obj.attr'.

	self assert: (fast allWithType: FASTPyAttributeAccess) size equals: 1.
	self assert: (fast allWithType: FASTPyDeleteStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 4.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyTuple) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'del obj1, obj2, obj.attr'.
	self assert: (self topEntity isOfType: FASTPyDeleteStatement).
	
	"Testing value of monovalue relation expression"
	self assert: self topEntity expression isNotNil.

		stack push: self topEntity expression.
		self assert: self topEntity sourceCode equals: 'obj1, obj2, obj.attr'.
		self assert: (self topEntity isOfType: FASTPyTuple).
		
		"Testing value of multivalued relation initializers"
		self assert: self topEntity initializers size equals: 3.
		
			stack push: (stack top initializers at: 1).
			self assert: self topEntity sourceCode equals: 'obj1'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		
			stack push: (stack top initializers at: 2).
			self assert: self topEntity sourceCode equals: 'obj2'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		
			stack push: (stack top initializers at: 3).
			self assert: self topEntity sourceCode equals: 'obj.attr'.
			self assert: (self topEntity isOfType: FASTPyAttributeAccess).
			
			"Testing value of monovalue relation attribute"
			self assert: self topEntity attribute isNotNil.

				stack push: self topEntity attribute.
				self assert: self topEntity sourceCode equals: 'attr'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			
			"Testing value of monovalue relation object"
			self assert: self topEntity object isNotNil.

				stack push: self topEntity object.
				self assert: self topEntity sourceCode equals: 'obj'.
				self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - statements' }
FASTPythonImporterTest >> testDelStatementSubscript [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testDelStatementSubscript 	"

	self parse: 'del obj.attrs[1]'.

	self assert: (fast allWithType: FASTPyAttributeAccess) size equals: 1.
	self assert: (fast allWithType: FASTPyDeleteStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 2.
	self assert: (fast allWithType: FASTPyInteger) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPySubscript) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'del obj.attrs[1]'.
	self assert: (self topEntity isOfType: FASTPyDeleteStatement).
	
	"Testing value of monovalue relation expression"
	self assert: self topEntity expression isNotNil.

		stack push: self topEntity expression.
		self assert: self topEntity sourceCode equals: 'obj.attrs[1]'.
		self assert: (self topEntity isOfType: FASTPySubscript).
		
		"Testing value of multivalued relation indices"
		self assert: self topEntity indices size equals: 1.
		
			stack push: (stack top indices at: 1).
			self assert: self topEntity sourceCode equals: '1'.
			self assert: (self topEntity isOfType: FASTPyInteger).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTNumericalLiteral).
			stack pop.
		
		"Testing value of monovalue relation value"
		self assert: self topEntity value isNotNil.

			stack push: self topEntity value.
			self assert: self topEntity sourceCode equals: 'obj.attrs'.
			self assert: (self topEntity isOfType: FASTPyAttributeAccess).
			
			"Testing value of monovalue relation attribute"
			self assert: self topEntity attribute isNotNil.

				stack push: self topEntity attribute.
				self assert: self topEntity sourceCode equals: 'attrs'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			
			"Testing value of monovalue relation object"
			self assert: self topEntity object isNotNil.

				stack push: self topEntity object.
				self assert: self topEntity sourceCode equals: 'obj'.
				self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - statements' }
FASTPythonImporterTest >> testDelStatementTuple [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testDelStatementTuple 	"

	self parse: 'del ()'.

	self assert: (fast allWithType: FASTPyDeleteStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyTuple) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'del ()'.
	self assert: (self topEntity isOfType: FASTPyDeleteStatement).
	
	"Testing value of monovalue relation expression"
	self assert: self topEntity expression isNotNil.

		stack push: self topEntity expression.
		self assert: self topEntity sourceCode equals: '()'.
		self assert: (self topEntity isOfType: FASTPyTuple)
]

{ #category : 'tests - collections' }
FASTPythonImporterTest >> testDictionary [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testDictionary 	"

	self parse: ' { "brand": "Ford", "model": "Mustang", "year": 1964 }'.

	self assert: (fast allWithType: FASTPyDictionary) size equals: 1.
	self assert: (fast allWithType: FASTPyInteger) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPair) size equals: 3.
	self assert: (fast allWithType: FASTPyString) size equals: 5.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: '{ "brand": "Ford", "model": "Mustang", "year": 1964 }'.
	self assert: (self topEntity isOfType: FASTPyDictionary).
	
	"Testing value of multivalued relation initializers"
	self assert: self topEntity initializers size equals: 3.
	
		stack push: (stack top initializers at: 1).
		self assert: self topEntity sourceCode equals: '"brand": "Ford"'.
		self assert: (self topEntity isOfType: FASTPyPair).
		
		"Testing value of monovalue relation key"
		self assert: self topEntity key isNotNil.

			stack push: self topEntity key.
			self assert: self topEntity sourceCode equals: '"brand"'.
			self assert: (self topEntity isOfType: FASTPyString).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTStringLiteral).
			stack pop.
		
		"Testing value of monovalue relation value"
		self assert: self topEntity value isNotNil.

			stack push: self topEntity value.
			self assert: self topEntity sourceCode equals: '"Ford"'.
			self assert: (self topEntity isOfType: FASTPyString).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTStringLiteral).
			stack pop.
		stack pop.
	
		stack push: (stack top initializers at: 2).
		self assert: self topEntity sourceCode equals: '"model": "Mustang"'.
		self assert: (self topEntity isOfType: FASTPyPair).
		
		"Testing value of monovalue relation key"
		self assert: self topEntity key isNotNil.

			stack push: self topEntity key.
			self assert: self topEntity sourceCode equals: '"model"'.
			self assert: (self topEntity isOfType: FASTPyString).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTStringLiteral).
			stack pop.
		
		"Testing value of monovalue relation value"
		self assert: self topEntity value isNotNil.

			stack push: self topEntity value.
			self assert: self topEntity sourceCode equals: '"Mustang"'.
			self assert: (self topEntity isOfType: FASTPyString).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTStringLiteral).
			stack pop.
		stack pop.
	
		stack push: (stack top initializers at: 3).
		self assert: self topEntity sourceCode equals: '"year": 1964'.
		self assert: (self topEntity isOfType: FASTPyPair).
		
		"Testing value of monovalue relation key"
		self assert: self topEntity key isNotNil.

			stack push: self topEntity key.
			self assert: self topEntity sourceCode equals: '"year"'.
			self assert: (self topEntity isOfType: FASTPyString).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTStringLiteral).
			stack pop.
		
		"Testing value of monovalue relation value"
		self assert: self topEntity value isNotNil.

			stack push: self topEntity value.
			self assert: self topEntity sourceCode equals: '1964'.
			self assert: (self topEntity isOfType: FASTPyInteger).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTNumericalLiteral)
]

{ #category : 'tests - comprehension' }
FASTPythonImporterTest >> testDictionaryComprehension [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testDictionaryComprehension 	"

	self parse: '{ v:k for k,v in class_indices.items() }'.

	self assert: (fast allWithType: FASTPyAttributeAccess) size equals: 1.
	self assert: (fast allWithType: FASTPyCall) size equals: 1.
	self assert: (fast allWithType: FASTPyDictionaryComprehension) size equals: 1.
	self assert: (fast allWithType: FASTPyForInClause) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 6.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPair) size equals: 1.
	self assert: (fast allWithType: FASTPyTuple) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: '{ v:k for k,v in class_indices.items() }'.
	self assert: (self topEntity isOfType: FASTPyDictionaryComprehension).
	self assert: (self topEntity isOfType: FASTPyTComprehension).
	self assert: (self topEntity isOfType: FASTTWithCondition).
	
	"Testing value of monovalue relation body"
	self assert: self topEntity body isNotNil.

		stack push: self topEntity body.
		self assert: self topEntity sourceCode equals: 'v:k'.
		self assert: (self topEntity isOfType: FASTPyPair).
		
		"Testing value of monovalue relation key"
		self assert: self topEntity key isNotNil.

			stack push: self topEntity key.
			self assert: self topEntity sourceCode equals: 'v'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		
		"Testing value of monovalue relation value"
		self assert: self topEntity value isNotNil.

			stack push: self topEntity value.
			self assert: self topEntity sourceCode equals: 'k'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		stack pop.
	
	"Testing value of multivalued relation clauses"
	self assert: self topEntity clauses size equals: 1.
	
		stack push: (stack top clauses at: 1).
		self assert: self topEntity sourceCode equals: 'for k,v in class_indices.items()'.
		self assert: (self topEntity isOfType: FASTPyForInClause).
		
		"Testing value of monovalue relation left"
		self assert: self topEntity left isNotNil.

			stack push: self topEntity left.
			self assert: self topEntity sourceCode equals: 'k,v'.
			self assert: (self topEntity isOfType: FASTPyTuple).
			
			"Testing value of multivalued relation initializers"
			self assert: self topEntity initializers size equals: 2.
			
				stack push: (stack top initializers at: 1).
				self assert: self topEntity sourceCode equals: 'k'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			
				stack push: (stack top initializers at: 2).
				self assert: self topEntity sourceCode equals: 'v'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			stack pop.
		
		"Testing value of monovalue relation right"
		self assert: self topEntity right isNotNil.

			stack push: self topEntity right.
			self assert: self topEntity sourceCode equals: 'class_indices.items()'.
			self assert: (self topEntity isOfType: FASTPyCall).
			self assert: (self topEntity isOfType: FASTTInvocation).
			self assert: (self topEntity isOfType: FASTTWithArguments).
			
			"Testing value of monovalue relation callee"
			self assert: self topEntity callee isNotNil.

				stack push: self topEntity callee.
				self assert: self topEntity sourceCode equals: 'class_indices.items'.
				self assert: (self topEntity isOfType: FASTPyAttributeAccess).
				
				"Testing value of monovalue relation attribute"
				self assert: self topEntity attribute isNotNil.

					stack push: self topEntity attribute.
					self assert: self topEntity sourceCode equals: 'items'.
					self assert: (self topEntity isOfType: FASTPyIdentifier).
					stack pop.
				
				"Testing value of monovalue relation object"
				self assert: self topEntity object isNotNil.

					stack push: self topEntity object.
					self assert: self topEntity sourceCode equals: 'class_indices'.
					self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - comprehension' }
FASTPythonImporterTest >> testDictionaryComprehensionWithIfClause [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testDictionaryComprehensionWithIfClause 	"

	self parse: '{ v:k for k,v in class_indices.items() if v > 3 }'.

	self assert: (fast allWithType: FASTPyAttributeAccess) size equals: 1.
	self assert: (fast allWithType: FASTPyCall) size equals: 1.
	self assert: (fast allWithType: FASTPyComparisonOperator) size equals: 1.
	self assert: (fast allWithType: FASTPyDictionaryComprehension) size equals: 1.
	self assert: (fast allWithType: FASTPyForInClause) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 7.
	self assert: (fast allWithType: FASTPyInteger) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPair) size equals: 1.
	self assert: (fast allWithType: FASTPyTuple) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: '{ v:k for k,v in class_indices.items() if v > 3 }'.
	self assert: (self topEntity isOfType: FASTPyDictionaryComprehension).
	self assert: (self topEntity isOfType: FASTPyTComprehension).
	self assert: (self topEntity isOfType: FASTTWithCondition).
	
	"Testing value of monovalue relation body"
	self assert: self topEntity body isNotNil.

		stack push: self topEntity body.
		self assert: self topEntity sourceCode equals: 'v:k'.
		self assert: (self topEntity isOfType: FASTPyPair).
		
		"Testing value of monovalue relation key"
		self assert: self topEntity key isNotNil.

			stack push: self topEntity key.
			self assert: self topEntity sourceCode equals: 'v'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		
		"Testing value of monovalue relation value"
		self assert: self topEntity value isNotNil.

			stack push: self topEntity value.
			self assert: self topEntity sourceCode equals: 'k'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		stack pop.
	
	"Testing value of multivalued relation clauses"
	self assert: self topEntity clauses size equals: 1.
	
		stack push: (stack top clauses at: 1).
		self assert: self topEntity sourceCode equals: 'for k,v in class_indices.items()'.
		self assert: (self topEntity isOfType: FASTPyForInClause).
		
		"Testing value of monovalue relation left"
		self assert: self topEntity left isNotNil.

			stack push: self topEntity left.
			self assert: self topEntity sourceCode equals: 'k,v'.
			self assert: (self topEntity isOfType: FASTPyTuple).
			
			"Testing value of multivalued relation initializers"
			self assert: self topEntity initializers size equals: 2.
			
				stack push: (stack top initializers at: 1).
				self assert: self topEntity sourceCode equals: 'k'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			
				stack push: (stack top initializers at: 2).
				self assert: self topEntity sourceCode equals: 'v'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			stack pop.
		
		"Testing value of monovalue relation right"
		self assert: self topEntity right isNotNil.

			stack push: self topEntity right.
			self assert: self topEntity sourceCode equals: 'class_indices.items()'.
			self assert: (self topEntity isOfType: FASTPyCall).
			self assert: (self topEntity isOfType: FASTTInvocation).
			self assert: (self topEntity isOfType: FASTTWithArguments).
			
			"Testing value of monovalue relation callee"
			self assert: self topEntity callee isNotNil.

				stack push: self topEntity callee.
				self assert: self topEntity sourceCode equals: 'class_indices.items'.
				self assert: (self topEntity isOfType: FASTPyAttributeAccess).
				
				"Testing value of monovalue relation attribute"
				self assert: self topEntity attribute isNotNil.

					stack push: self topEntity attribute.
					self assert: self topEntity sourceCode equals: 'items'.
					self assert: (self topEntity isOfType: FASTPyIdentifier).
					stack pop.
				
				"Testing value of monovalue relation object"
				self assert: self topEntity object isNotNil.

					stack push: self topEntity object.
					self assert: self topEntity sourceCode equals: 'class_indices'.
					self assert: (self topEntity isOfType: FASTPyIdentifier).
					stack pop.
				stack pop.
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation condition"
	self assert: self topEntity condition isNotNil.

		stack push: self topEntity condition.
		self assert: self topEntity sourceCode equals: 'v > 3'.
		self assert: (self topEntity isOfType: FASTPyComparisonOperator).
		
		"Testing value of multivalued relation operands"
		self assert: self topEntity operands size equals: 2.
		
			stack push: (stack top operands at: 1).
			self assert: self topEntity sourceCode equals: 'v'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		
			stack push: (stack top operands at: 2).
			self assert: self topEntity sourceCode equals: '3'.
			self assert: (self topEntity isOfType: FASTPyInteger).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTNumericalLiteral).
			stack pop.
		self assert: self topEntity operators equals: #('>')
]

{ #category : 'tests - splats' }
FASTPythonImporterTest >> testDictionarySplatInCall [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testDictionarySplatInCall 	"

	self parse: 'func(**kwargs)'.

	self assert: (fast allWithType: FASTPyCall) size equals: 1.
	self assert: (fast allWithType: FASTPyDictionarySplat) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 2.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'func(**kwargs)'.
	self assert: (self topEntity isOfType: FASTPyCall).
	self assert: (self topEntity isOfType: FASTTInvocation).
	self assert: (self topEntity isOfType: FASTTWithArguments).
	
	"Testing value of multivalued relation arguments"
	self assert: self topEntity arguments size equals: 1.
	
		stack push: (stack top arguments at: 1).
		self assert: self topEntity sourceCode equals: '**kwargs'.
		self assert: (self topEntity isOfType: FASTPyDictionarySplat).
		
		"Testing value of monovalue relation expression"
		self assert: self topEntity expression isNotNil.

			stack push: self topEntity expression.
			self assert: self topEntity sourceCode equals: 'kwargs'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation callee"
	self assert: self topEntity callee isNotNil.

		stack push: self topEntity callee.
		self assert: self topEntity sourceCode equals: 'func'.
		self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - splats' }
FASTPythonImporterTest >> testDictionarySplatInDictionary [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testDictionarySplatInDictionary 	"

	self parse: '{**d1, "c": 3}'.

	self assert: (fast allWithType: FASTPyDictionary) size equals: 1.
	self assert: (fast allWithType: FASTPyDictionarySplat) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyInteger) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPair) size equals: 1.
	self assert: (fast allWithType: FASTPyString) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: '{**d1, "c": 3}'.
	self assert: (self topEntity isOfType: FASTPyDictionary).
	
	"Testing value of multivalued relation initializers"
	self assert: self topEntity initializers size equals: 2.
	
		stack push: (stack top initializers at: 1).
		self assert: self topEntity sourceCode equals: '**d1'.
		self assert: (self topEntity isOfType: FASTPyDictionarySplat).
		
		"Testing value of monovalue relation expression"
		self assert: self topEntity expression isNotNil.

			stack push: self topEntity expression.
			self assert: self topEntity sourceCode equals: 'd1'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		stack pop.
	
		stack push: (stack top initializers at: 2).
		self assert: self topEntity sourceCode equals: '"c": 3'.
		self assert: (self topEntity isOfType: FASTPyPair).
		
		"Testing value of monovalue relation key"
		self assert: self topEntity key isNotNil.

			stack push: self topEntity key.
			self assert: self topEntity sourceCode equals: '"c"'.
			self assert: (self topEntity isOfType: FASTPyString).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTStringLiteral).
			stack pop.
		
		"Testing value of monovalue relation value"
		self assert: self topEntity value isNotNil.

			stack push: self topEntity value.
			self assert: self topEntity sourceCode equals: '3'.
			self assert: (self topEntity isOfType: FASTPyInteger).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTNumericalLiteral)
]

{ #category : 'tests - splats' }
FASTPythonImporterTest >> testDictionarySplatOnAttributeAccess [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testDictionarySplatOnAttributeAccess 	"

	self parse: 'funct(**obj.settings)'.

	self assert: (fast allWithType: FASTPyAttributeAccess) size equals: 1.
	self assert: (fast allWithType: FASTPyCall) size equals: 1.
	self assert: (fast allWithType: FASTPyDictionarySplat) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 3.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'funct(**obj.settings)'.
	self assert: (self topEntity isOfType: FASTPyCall).
	self assert: (self topEntity isOfType: FASTTInvocation).
	self assert: (self topEntity isOfType: FASTTWithArguments).
	
	"Testing value of multivalued relation arguments"
	self assert: self topEntity arguments size equals: 1.
	
		stack push: (stack top arguments at: 1).
		self assert: self topEntity sourceCode equals: '**obj.settings'.
		self assert: (self topEntity isOfType: FASTPyDictionarySplat).
		
		"Testing value of monovalue relation expression"
		self assert: self topEntity expression isNotNil.

			stack push: self topEntity expression.
			self assert: self topEntity sourceCode equals: 'obj.settings'.
			self assert: (self topEntity isOfType: FASTPyAttributeAccess).
			
			"Testing value of monovalue relation attribute"
			self assert: self topEntity attribute isNotNil.

				stack push: self topEntity attribute.
				self assert: self topEntity sourceCode equals: 'settings'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			
			"Testing value of monovalue relation object"
			self assert: self topEntity object isNotNil.

				stack push: self topEntity object.
				self assert: self topEntity sourceCode equals: 'obj'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation callee"
	self assert: self topEntity callee isNotNil.

		stack push: self topEntity callee.
		self assert: self topEntity sourceCode equals: 'funct'.
		self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - splats' }
FASTPythonImporterTest >> testDictionarySplatOnBinaryOperator [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testDictionarySplatOnBinaryOperator 	"

	self parse: 'func(**default_args | subprocess_args)'.

	self assert: (fast allWithType: FASTPyBinaryOperator) size equals: 1.
	self assert: (fast allWithType: FASTPyCall) size equals: 1.
	self assert: (fast allWithType: FASTPyDictionarySplat) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 3.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'func(**default_args | subprocess_args)'.
	self assert: (self topEntity isOfType: FASTPyCall).
	self assert: (self topEntity isOfType: FASTTInvocation).
	self assert: (self topEntity isOfType: FASTTWithArguments).
	
	"Testing value of multivalued relation arguments"
	self assert: self topEntity arguments size equals: 1.
	
		stack push: (stack top arguments at: 1).
		self assert: self topEntity sourceCode equals: '**default_args | subprocess_args'.
		self assert: (self topEntity isOfType: FASTPyDictionarySplat).
		
		"Testing value of monovalue relation expression"
		self assert: self topEntity expression isNotNil.

			stack push: self topEntity expression.
			self assert: self topEntity sourceCode equals: 'default_args | subprocess_args'.
			self assert: (self topEntity isOfType: FASTPyBinaryOperator).
			self assert: (self topEntity isOfType: FASTTBinaryExpression).
			
			"Testing value of monovalue relation leftOperand"
			self assert: self topEntity leftOperand isNotNil.

				stack push: self topEntity leftOperand.
				self assert: self topEntity sourceCode equals: 'default_args'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			self assert: self topEntity operator equals: '|'.
			
			"Testing value of monovalue relation rightOperand"
			self assert: self topEntity rightOperand isNotNil.

				stack push: self topEntity rightOperand.
				self assert: self topEntity sourceCode equals: 'subprocess_args'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation callee"
	self assert: self topEntity callee isNotNil.

		stack push: self topEntity callee.
		self assert: self topEntity sourceCode equals: 'func'.
		self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - splats' }
FASTPythonImporterTest >> testDictionarySplatOnBooleanOperator [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testDictionarySplatOnBooleanOperator 	"

	self parse: 'func(**extra or {}) '.

	self assert: (fast allWithType: FASTPyBooleanOperator) size equals: 1.
	self assert: (fast allWithType: FASTPyCall) size equals: 1.
	self assert: (fast allWithType: FASTPyDictionary) size equals: 1.
	self assert: (fast allWithType: FASTPyDictionarySplat) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 2.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'func(**extra or {})'.
	self assert: (self topEntity isOfType: FASTPyCall).
	self assert: (self topEntity isOfType: FASTTInvocation).
	self assert: (self topEntity isOfType: FASTTWithArguments).
	
	"Testing value of multivalued relation arguments"
	self assert: self topEntity arguments size equals: 1.
	
		stack push: (stack top arguments at: 1).
		self assert: self topEntity sourceCode equals: '**extra or {}'.
		self assert: (self topEntity isOfType: FASTPyDictionarySplat).
		
		"Testing value of monovalue relation expression"
		self assert: self topEntity expression isNotNil.

			stack push: self topEntity expression.
			self assert: self topEntity sourceCode equals: 'extra or {}'.
			self assert: (self topEntity isOfType: FASTPyBooleanOperator).
			self assert: (self topEntity isOfType: FASTTBinaryExpression).
			
			"Testing value of monovalue relation leftOperand"
			self assert: self topEntity leftOperand isNotNil.

				stack push: self topEntity leftOperand.
				self assert: self topEntity sourceCode equals: 'extra'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			self assert: self topEntity operator equals: 'or'.
			
			"Testing value of monovalue relation rightOperand"
			self assert: self topEntity rightOperand isNotNil.

				stack push: self topEntity rightOperand.
				self assert: self topEntity sourceCode equals: '{}'.
				self assert: (self topEntity isOfType: FASTPyDictionary).
				stack pop.
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation callee"
	self assert: self topEntity callee isNotNil.

		stack push: self topEntity callee.
		self assert: self topEntity sourceCode equals: 'func'.
		self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - splats' }
FASTPythonImporterTest >> testDictionarySplatOnCall [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testDictionarySplatOnCall 	"

	self parse: 'funct(**get_config())'.

	self assert: (fast allWithType: FASTPyCall) size equals: 2.
	self assert: (fast allWithType: FASTPyDictionarySplat) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 2.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'funct(**get_config())'.
	self assert: (self topEntity isOfType: FASTPyCall).
	self assert: (self topEntity isOfType: FASTTInvocation).
	self assert: (self topEntity isOfType: FASTTWithArguments).
	
	"Testing value of multivalued relation arguments"
	self assert: self topEntity arguments size equals: 1.
	
		stack push: (stack top arguments at: 1).
		self assert: self topEntity sourceCode equals: '**get_config()'.
		self assert: (self topEntity isOfType: FASTPyDictionarySplat).
		
		"Testing value of monovalue relation expression"
		self assert: self topEntity expression isNotNil.

			stack push: self topEntity expression.
			self assert: self topEntity sourceCode equals: 'get_config()'.
			self assert: (self topEntity isOfType: FASTPyCall).
			self assert: (self topEntity isOfType: FASTTInvocation).
			self assert: (self topEntity isOfType: FASTTWithArguments).
			
			"Testing value of monovalue relation callee"
			self assert: self topEntity callee isNotNil.

				stack push: self topEntity callee.
				self assert: self topEntity sourceCode equals: 'get_config'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation callee"
	self assert: self topEntity callee isNotNil.

		stack push: self topEntity callee.
		self assert: self topEntity sourceCode equals: 'funct'.
		self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - splats' }
FASTPythonImporterTest >> testDictionarySplatOnDictionaryComprehension [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testDictionarySplatOnDictionaryComprehension 	"

	self parse: 'funct(**{k: v * 2 for k, v in items})'.

	self assert: (fast allWithType: FASTPyBinaryOperator) size equals: 1.
	self assert: (fast allWithType: FASTPyCall) size equals: 1.
	self assert: (fast allWithType: FASTPyDictionaryComprehension) size equals: 1.
	self assert: (fast allWithType: FASTPyDictionarySplat) size equals: 1.
	self assert: (fast allWithType: FASTPyForInClause) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 6.
	self assert: (fast allWithType: FASTPyInteger) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPair) size equals: 1.
	self assert: (fast allWithType: FASTPyTuple) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'funct(**{k: v * 2 for k, v in items})'.
	self assert: (self topEntity isOfType: FASTPyCall).
	self assert: (self topEntity isOfType: FASTTInvocation).
	self assert: (self topEntity isOfType: FASTTWithArguments).
	
	"Testing value of multivalued relation arguments"
	self assert: self topEntity arguments size equals: 1.
	
		stack push: (stack top arguments at: 1).
		self assert: self topEntity sourceCode equals: '**{k: v * 2 for k, v in items}'.
		self assert: (self topEntity isOfType: FASTPyDictionarySplat).
		
		"Testing value of monovalue relation expression"
		self assert: self topEntity expression isNotNil.

			stack push: self topEntity expression.
			self assert: self topEntity sourceCode equals: '{k: v * 2 for k, v in items}'.
			self assert: (self topEntity isOfType: FASTPyDictionaryComprehension).
			self assert: (self topEntity isOfType: FASTPyTComprehension).
			self assert: (self topEntity isOfType: FASTTWithCondition).
			
			"Testing value of monovalue relation body"
			self assert: self topEntity body isNotNil.

				stack push: self topEntity body.
				self assert: self topEntity sourceCode equals: 'k: v * 2'.
				self assert: (self topEntity isOfType: FASTPyPair).
				
				"Testing value of monovalue relation key"
				self assert: self topEntity key isNotNil.

					stack push: self topEntity key.
					self assert: self topEntity sourceCode equals: 'k'.
					self assert: (self topEntity isOfType: FASTPyIdentifier).
					stack pop.
				
				"Testing value of monovalue relation value"
				self assert: self topEntity value isNotNil.

					stack push: self topEntity value.
					self assert: self topEntity sourceCode equals: 'v * 2'.
					self assert: (self topEntity isOfType: FASTPyBinaryOperator).
					self assert: (self topEntity isOfType: FASTTBinaryExpression).
					
					"Testing value of monovalue relation leftOperand"
					self assert: self topEntity leftOperand isNotNil.

						stack push: self topEntity leftOperand.
						self assert: self topEntity sourceCode equals: 'v'.
						self assert: (self topEntity isOfType: FASTPyIdentifier).
						stack pop.
					self assert: self topEntity operator equals: '*'.
					
					"Testing value of monovalue relation rightOperand"
					self assert: self topEntity rightOperand isNotNil.

						stack push: self topEntity rightOperand.
						self assert: self topEntity sourceCode equals: '2'.
						self assert: (self topEntity isOfType: FASTPyInteger).
						self assert: (self topEntity isOfType: FASTTLiteral).
						self assert: (self topEntity isOfType: FASTTNumericalLiteral).
						stack pop.
					stack pop.
				stack pop.
			
			"Testing value of multivalued relation clauses"
			self assert: self topEntity clauses size equals: 1.
			
				stack push: (stack top clauses at: 1).
				self assert: self topEntity sourceCode equals: 'for k, v in items'.
				self assert: (self topEntity isOfType: FASTPyForInClause).
				
				"Testing value of monovalue relation left"
				self assert: self topEntity left isNotNil.

					stack push: self topEntity left.
					self assert: self topEntity sourceCode equals: 'k, v'.
					self assert: (self topEntity isOfType: FASTPyTuple).
					
					"Testing value of multivalued relation initializers"
					self assert: self topEntity initializers size equals: 2.
					
						stack push: (stack top initializers at: 1).
						self assert: self topEntity sourceCode equals: 'k'.
						self assert: (self topEntity isOfType: FASTPyIdentifier).
						stack pop.
					
						stack push: (stack top initializers at: 2).
						self assert: self topEntity sourceCode equals: 'v'.
						self assert: (self topEntity isOfType: FASTPyIdentifier).
						stack pop.
					stack pop.
				
				"Testing value of monovalue relation right"
				self assert: self topEntity right isNotNil.

					stack push: self topEntity right.
					self assert: self topEntity sourceCode equals: 'items'.
					self assert: (self topEntity isOfType: FASTPyIdentifier).
					stack pop.
				stack pop.
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation callee"
	self assert: self topEntity callee isNotNil.

		stack push: self topEntity callee.
		self assert: self topEntity sourceCode equals: 'funct'.
		self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - splats' }
FASTPythonImporterTest >> testDictionarySplatOnParenthesis [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testDictionarySplatOnParenthesis 	"

	self parse: 'func(**(kwargs)) '.

	self assert: (fast allWithType: FASTPyCall) size equals: 1.
	self assert: (fast allWithType: FASTPyDictionarySplat) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 2.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'func(**(kwargs))'.
	self assert: (self topEntity isOfType: FASTPyCall).
	self assert: (self topEntity isOfType: FASTTInvocation).
	self assert: (self topEntity isOfType: FASTTWithArguments).
	
	"Testing value of multivalued relation arguments"
	self assert: self topEntity arguments size equals: 1.
	
		stack push: (stack top arguments at: 1).
		self assert: self topEntity sourceCode equals: '**(kwargs)'.
		self assert: (self topEntity isOfType: FASTPyDictionarySplat).
		
		"Testing value of monovalue relation expression"
		self assert: self topEntity expression isNotNil.

			stack push: self topEntity expression.
			self assert: self topEntity sourceCode equals: 'kwargs'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation callee"
	self assert: self topEntity callee isNotNil.

		stack push: self topEntity callee.
		self assert: self topEntity sourceCode equals: 'func'.
		self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - splats' }
FASTPythonImporterTest >> testDictionarySplatOnSubscript [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testDictionarySplatOnSubscript 	"

	self parse: 'funct(**d["nested"])'.

	self assert: (fast allWithType: FASTPyCall) size equals: 1.
	self assert: (fast allWithType: FASTPyDictionarySplat) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 2.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyString) size equals: 1.
	self assert: (fast allWithType: FASTPySubscript) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'funct(**d["nested"])'.
	self assert: (self topEntity isOfType: FASTPyCall).
	self assert: (self topEntity isOfType: FASTTInvocation).
	self assert: (self topEntity isOfType: FASTTWithArguments).
	
	"Testing value of multivalued relation arguments"
	self assert: self topEntity arguments size equals: 1.
	
		stack push: (stack top arguments at: 1).
		self assert: self topEntity sourceCode equals: '**d["nested"]'.
		self assert: (self topEntity isOfType: FASTPyDictionarySplat).
		
		"Testing value of monovalue relation expression"
		self assert: self topEntity expression isNotNil.

			stack push: self topEntity expression.
			self assert: self topEntity sourceCode equals: 'd["nested"]'.
			self assert: (self topEntity isOfType: FASTPySubscript).
			
			"Testing value of multivalued relation indices"
			self assert: self topEntity indices size equals: 1.
			
				stack push: (stack top indices at: 1).
				self assert: self topEntity sourceCode equals: '"nested"'.
				self assert: (self topEntity isOfType: FASTPyString).
				self assert: (self topEntity isOfType: FASTTLiteral).
				self assert: (self topEntity isOfType: FASTTStringLiteral).
				stack pop.
			
			"Testing value of monovalue relation value"
			self assert: self topEntity value isNotNil.

				stack push: self topEntity value.
				self assert: self topEntity sourceCode equals: 'd'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation callee"
	self assert: self topEntity callee isNotNil.

		stack push: self topEntity callee.
		self assert: self topEntity sourceCode equals: 'funct'.
		self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - function definitions' }
FASTPythonImporterTest >> testDictionarySplatParameter [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testDictionarySplatParameter 	"

	self parse: 'def function(**kwargs): pass'.

	self assert: (fast allWithType: FASTPyBlock) size equals: 1.
	self assert: (fast allWithType: FASTPyDictionarySplatParameter) size equals: 1.
	self assert: (fast allWithType: FASTPyFunctionDefinition) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPassStatement) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'def function(**kwargs): pass'.
	self assert: (self topEntity isOfType: FASTPyFunctionDefinition).
	self assert: (self topEntity isOfType: FASTPyTDefinition).
	self assert: (self topEntity isOfType: FASTTWithParameters).
	self assert: (self topEntity isOfType: FASTTWithStatements).
	self assert: self topEntity name equals: 'function'.
	
	"Testing value of multivalued relation parameters"
	self assert: self topEntity parameters size equals: 1.
	
		stack push: (stack top parameters at: 1).
		self assert: self topEntity sourceCode equals: '**kwargs'.
		self assert: (self topEntity isOfType: FASTPyDictionarySplatParameter).
		self assert: (self topEntity isOfType: FASTTVariableEntity).
		self assert: self topEntity name equals: 'kwargs'.
		stack pop.
	
	"Testing value of monovalue relation statementBlock"
	self assert: self topEntity statementBlock isNotNil.

		stack push: self topEntity statementBlock.
		self assert: self topEntity sourceCode equals: 'pass'.
		self assert: (self topEntity isOfType: FASTPyBlock).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'pass'.
			self assert: (self topEntity isOfType: FASTPyPassStatement)
]

{ #category : 'tests - collections' }
FASTPythonImporterTest >> testDictionaryWithAttribute [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testDictionaryWithAttribute 	"

	self parse: '{ "key": obj.attr }'.

	self assert: (fast allWithType: FASTPyAttributeAccess) size equals: 1.
	self assert: (fast allWithType: FASTPyDictionary) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 2.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPair) size equals: 1.
	self assert: (fast allWithType: FASTPyString) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: '{ "key": obj.attr }'.
	self assert: (self topEntity isOfType: FASTPyDictionary).
	
	"Testing value of multivalued relation initializers"
	self assert: self topEntity initializers size equals: 1.
	
		stack push: (stack top initializers at: 1).
		self assert: self topEntity sourceCode equals: '"key": obj.attr'.
		self assert: (self topEntity isOfType: FASTPyPair).
		
		"Testing value of monovalue relation key"
		self assert: self topEntity key isNotNil.

			stack push: self topEntity key.
			self assert: self topEntity sourceCode equals: '"key"'.
			self assert: (self topEntity isOfType: FASTPyString).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTStringLiteral).
			stack pop.
		
		"Testing value of monovalue relation value"
		self assert: self topEntity value isNotNil.

			stack push: self topEntity value.
			self assert: self topEntity sourceCode equals: 'obj.attr'.
			self assert: (self topEntity isOfType: FASTPyAttributeAccess).
			
			"Testing value of monovalue relation attribute"
			self assert: self topEntity attribute isNotNil.

				stack push: self topEntity attribute.
				self assert: self topEntity sourceCode equals: 'attr'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			
			"Testing value of monovalue relation object"
			self assert: self topEntity object isNotNil.

				stack push: self topEntity object.
				self assert: self topEntity sourceCode equals: 'obj'.
				self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - collections' }
FASTPythonImporterTest >> testDictionaryWithBinaryOperator [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testDictionaryWithBinaryOperator 	"

	self parse: '{ "key": 10 / 2 }'.

	self assert: (fast allWithType: FASTPyBinaryOperator) size equals: 1.
	self assert: (fast allWithType: FASTPyDictionary) size equals: 1.
	self assert: (fast allWithType: FASTPyInteger) size equals: 2.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPair) size equals: 1.
	self assert: (fast allWithType: FASTPyString) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: '{ "key": 10 / 2 }'.
	self assert: (self topEntity isOfType: FASTPyDictionary).
	
	"Testing value of multivalued relation initializers"
	self assert: self topEntity initializers size equals: 1.
	
		stack push: (stack top initializers at: 1).
		self assert: self topEntity sourceCode equals: '"key": 10 / 2'.
		self assert: (self topEntity isOfType: FASTPyPair).
		
		"Testing value of monovalue relation key"
		self assert: self topEntity key isNotNil.

			stack push: self topEntity key.
			self assert: self topEntity sourceCode equals: '"key"'.
			self assert: (self topEntity isOfType: FASTPyString).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTStringLiteral).
			stack pop.
		
		"Testing value of monovalue relation value"
		self assert: self topEntity value isNotNil.

			stack push: self topEntity value.
			self assert: self topEntity sourceCode equals: '10 / 2'.
			self assert: (self topEntity isOfType: FASTPyBinaryOperator).
			self assert: (self topEntity isOfType: FASTTBinaryExpression).
			
			"Testing value of monovalue relation leftOperand"
			self assert: self topEntity leftOperand isNotNil.

				stack push: self topEntity leftOperand.
				self assert: self topEntity sourceCode equals: '10'.
				self assert: (self topEntity isOfType: FASTPyInteger).
				self assert: (self topEntity isOfType: FASTTLiteral).
				self assert: (self topEntity isOfType: FASTTNumericalLiteral).
				stack pop.
			self assert: self topEntity operator equals: '/'.
			
			"Testing value of monovalue relation rightOperand"
			self assert: self topEntity rightOperand isNotNil.

				stack push: self topEntity rightOperand.
				self assert: self topEntity sourceCode equals: '2'.
				self assert: (self topEntity isOfType: FASTPyInteger).
				self assert: (self topEntity isOfType: FASTTLiteral).
				self assert: (self topEntity isOfType: FASTTNumericalLiteral)
]

{ #category : 'tests - collections' }
FASTPythonImporterTest >> testDictionaryWithBooleanOperator [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testDictionaryWithBooleanOperator 	"

	self parse: '{ "key": x and y }'.

	self assert: (fast allWithType: FASTPyBooleanOperator) size equals: 1.
	self assert: (fast allWithType: FASTPyDictionary) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 2.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPair) size equals: 1.
	self assert: (fast allWithType: FASTPyString) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: '{ "key": x and y }'.
	self assert: (self topEntity isOfType: FASTPyDictionary).
	
	"Testing value of multivalued relation initializers"
	self assert: self topEntity initializers size equals: 1.
	
		stack push: (stack top initializers at: 1).
		self assert: self topEntity sourceCode equals: '"key": x and y'.
		self assert: (self topEntity isOfType: FASTPyPair).
		
		"Testing value of monovalue relation key"
		self assert: self topEntity key isNotNil.

			stack push: self topEntity key.
			self assert: self topEntity sourceCode equals: '"key"'.
			self assert: (self topEntity isOfType: FASTPyString).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTStringLiteral).
			stack pop.
		
		"Testing value of monovalue relation value"
		self assert: self topEntity value isNotNil.

			stack push: self topEntity value.
			self assert: self topEntity sourceCode equals: 'x and y'.
			self assert: (self topEntity isOfType: FASTPyBooleanOperator).
			self assert: (self topEntity isOfType: FASTTBinaryExpression).
			
			"Testing value of monovalue relation leftOperand"
			self assert: self topEntity leftOperand isNotNil.

				stack push: self topEntity leftOperand.
				self assert: self topEntity sourceCode equals: 'x'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			self assert: self topEntity operator equals: 'and'.
			
			"Testing value of monovalue relation rightOperand"
			self assert: self topEntity rightOperand isNotNil.

				stack push: self topEntity rightOperand.
				self assert: self topEntity sourceCode equals: 'y'.
				self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - collections' }
FASTPythonImporterTest >> testDictionaryWithCall [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testDictionaryWithCall 	"

	self parse: '{ "key": obj() }'.

	self assert: (fast allWithType: FASTPyCall) size equals: 1.
	self assert: (fast allWithType: FASTPyDictionary) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPair) size equals: 1.
	self assert: (fast allWithType: FASTPyString) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: '{ "key": obj() }'.
	self assert: (self topEntity isOfType: FASTPyDictionary).
	
	"Testing value of multivalued relation initializers"
	self assert: self topEntity initializers size equals: 1.
	
		stack push: (stack top initializers at: 1).
		self assert: self topEntity sourceCode equals: '"key": obj()'.
		self assert: (self topEntity isOfType: FASTPyPair).
		
		"Testing value of monovalue relation key"
		self assert: self topEntity key isNotNil.

			stack push: self topEntity key.
			self assert: self topEntity sourceCode equals: '"key"'.
			self assert: (self topEntity isOfType: FASTPyString).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTStringLiteral).
			stack pop.
		
		"Testing value of monovalue relation value"
		self assert: self topEntity value isNotNil.

			stack push: self topEntity value.
			self assert: self topEntity sourceCode equals: 'obj()'.
			self assert: (self topEntity isOfType: FASTPyCall).
			self assert: (self topEntity isOfType: FASTTInvocation).
			self assert: (self topEntity isOfType: FASTTWithArguments).
			
			"Testing value of monovalue relation callee"
			self assert: self topEntity callee isNotNil.

				stack push: self topEntity callee.
				self assert: self topEntity sourceCode equals: 'obj'.
				self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - collections' }
FASTPythonImporterTest >> testDictionaryWithComparisonOperator [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testDictionaryWithComparisonOperator 	"

	self parse: '{ "key": 1 > 3 }'.

	self assert: (fast allWithType: FASTPyComparisonOperator) size equals: 1.
	self assert: (fast allWithType: FASTPyDictionary) size equals: 1.
	self assert: (fast allWithType: FASTPyInteger) size equals: 2.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPair) size equals: 1.
	self assert: (fast allWithType: FASTPyString) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: '{ "key": 1 > 3 }'.
	self assert: (self topEntity isOfType: FASTPyDictionary).
	
	"Testing value of multivalued relation initializers"
	self assert: self topEntity initializers size equals: 1.
	
		stack push: (stack top initializers at: 1).
		self assert: self topEntity sourceCode equals: '"key": 1 > 3'.
		self assert: (self topEntity isOfType: FASTPyPair).
		
		"Testing value of monovalue relation key"
		self assert: self topEntity key isNotNil.

			stack push: self topEntity key.
			self assert: self topEntity sourceCode equals: '"key"'.
			self assert: (self topEntity isOfType: FASTPyString).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTStringLiteral).
			stack pop.
		
		"Testing value of monovalue relation value"
		self assert: self topEntity value isNotNil.

			stack push: self topEntity value.
			self assert: self topEntity sourceCode equals: '1 > 3'.
			self assert: (self topEntity isOfType: FASTPyComparisonOperator).
			
			"Testing value of multivalued relation operands"
			self assert: self topEntity operands size equals: 2.
			
				stack push: (stack top operands at: 1).
				self assert: self topEntity sourceCode equals: '1'.
				self assert: (self topEntity isOfType: FASTPyInteger).
				self assert: (self topEntity isOfType: FASTTLiteral).
				self assert: (self topEntity isOfType: FASTTNumericalLiteral).
				stack pop.
			
				stack push: (stack top operands at: 2).
				self assert: self topEntity sourceCode equals: '3'.
				self assert: (self topEntity isOfType: FASTPyInteger).
				self assert: (self topEntity isOfType: FASTTLiteral).
				self assert: (self topEntity isOfType: FASTTNumericalLiteral).
				stack pop.
			self assert: self topEntity operators equals: #('>')
]

{ #category : 'tests - collections' }
FASTPythonImporterTest >> testDictionaryWithComplexe [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testDictionaryWithComplexe 	"

	self parse: '{ "key": 1j }'.

	self assert: (fast allWithType: FASTPyComplex) size equals: 1.
	self assert: (fast allWithType: FASTPyDictionary) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPair) size equals: 1.
	self assert: (fast allWithType: FASTPyString) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: '{ "key": 1j }'.
	self assert: (self topEntity isOfType: FASTPyDictionary).
	
	"Testing value of multivalued relation initializers"
	self assert: self topEntity initializers size equals: 1.
	
		stack push: (stack top initializers at: 1).
		self assert: self topEntity sourceCode equals: '"key": 1j'.
		self assert: (self topEntity isOfType: FASTPyPair).
		
		"Testing value of monovalue relation key"
		self assert: self topEntity key isNotNil.

			stack push: self topEntity key.
			self assert: self topEntity sourceCode equals: '"key"'.
			self assert: (self topEntity isOfType: FASTPyString).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTStringLiteral).
			stack pop.
		
		"Testing value of monovalue relation value"
		self assert: self topEntity value isNotNil.

			stack push: self topEntity value.
			self assert: self topEntity sourceCode equals: '1j'.
			self assert: (self topEntity isOfType: FASTPyComplex).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTNumericalLiteral)
]

{ #category : 'tests - collections' }
FASTPythonImporterTest >> testDictionaryWithConditionalExpression [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testDictionaryWithConditionalExpression 	"

	self parse: '{ "key": x if True else y }'.

	self assert: (fast allWithType: FASTPyBoolean) size equals: 1.
	self assert: (fast allWithType: FASTPyConditionalExpression) size equals: 1.
	self assert: (fast allWithType: FASTPyDictionary) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 2.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPair) size equals: 1.
	self assert: (fast allWithType: FASTPyString) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: '{ "key": x if True else y }'.
	self assert: (self topEntity isOfType: FASTPyDictionary).
	
	"Testing value of multivalued relation initializers"
	self assert: self topEntity initializers size equals: 1.
	
		stack push: (stack top initializers at: 1).
		self assert: self topEntity sourceCode equals: '"key": x if True else y'.
		self assert: (self topEntity isOfType: FASTPyPair).
		
		"Testing value of monovalue relation key"
		self assert: self topEntity key isNotNil.

			stack push: self topEntity key.
			self assert: self topEntity sourceCode equals: '"key"'.
			self assert: (self topEntity isOfType: FASTPyString).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTStringLiteral).
			stack pop.
		
		"Testing value of monovalue relation value"
		self assert: self topEntity value isNotNil.

			stack push: self topEntity value.
			self assert: self topEntity sourceCode equals: 'x if True else y'.
			self assert: (self topEntity isOfType: FASTPyConditionalExpression).
			self assert: (self topEntity isOfType: FASTTWithCondition).
			
			"Testing value of monovalue relation condition"
			self assert: self topEntity condition isNotNil.

				stack push: self topEntity condition.
				self assert: self topEntity sourceCode equals: 'True'.
				self assert: (self topEntity isOfType: FASTPyBoolean).
				self assert: (self topEntity isOfType: FASTTBooleanLiteral).
				self assert: (self topEntity isOfType: FASTTLiteral).
				stack pop.
			
			"Testing value of monovalue relation elseExpression"
			self assert: self topEntity elseExpression isNotNil.

				stack push: self topEntity elseExpression.
				self assert: self topEntity sourceCode equals: 'y'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			
			"Testing value of monovalue relation thenExpression"
			self assert: self topEntity thenExpression isNotNil.

				stack push: self topEntity thenExpression.
				self assert: self topEntity sourceCode equals: 'x'.
				self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - collections' }
FASTPythonImporterTest >> testDictionaryWithDictionary [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testDictionaryWithDictionary 	"

	self parse: '{ "key": { 1:1 } }'.

	self assert: (fast allWithType: FASTPyDictionary) size equals: 2.
	self assert: (fast allWithType: FASTPyInteger) size equals: 2.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPair) size equals: 2.
	self assert: (fast allWithType: FASTPyString) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: '{ "key": { 1:1 } }'.
	self assert: (self topEntity isOfType: FASTPyDictionary).
	
	"Testing value of multivalued relation initializers"
	self assert: self topEntity initializers size equals: 1.
	
		stack push: (stack top initializers at: 1).
		self assert: self topEntity sourceCode equals: '"key": { 1:1 }'.
		self assert: (self topEntity isOfType: FASTPyPair).
		
		"Testing value of monovalue relation key"
		self assert: self topEntity key isNotNil.

			stack push: self topEntity key.
			self assert: self topEntity sourceCode equals: '"key"'.
			self assert: (self topEntity isOfType: FASTPyString).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTStringLiteral).
			stack pop.
		
		"Testing value of monovalue relation value"
		self assert: self topEntity value isNotNil.

			stack push: self topEntity value.
			self assert: self topEntity sourceCode equals: '{ 1:1 }'.
			self assert: (self topEntity isOfType: FASTPyDictionary).
			
			"Testing value of multivalued relation initializers"
			self assert: self topEntity initializers size equals: 1.
			
				stack push: (stack top initializers at: 1).
				self assert: self topEntity sourceCode equals: '1:1'.
				self assert: (self topEntity isOfType: FASTPyPair).
				
				"Testing value of monovalue relation key"
				self assert: self topEntity key isNotNil.

					stack push: self topEntity key.
					self assert: self topEntity sourceCode equals: '1'.
					self assert: (self topEntity isOfType: FASTPyInteger).
					self assert: (self topEntity isOfType: FASTTLiteral).
					self assert: (self topEntity isOfType: FASTTNumericalLiteral).
					stack pop.
				
				"Testing value of monovalue relation value"
				self assert: self topEntity value isNotNil.

					stack push: self topEntity value.
					self assert: self topEntity sourceCode equals: '1'.
					self assert: (self topEntity isOfType: FASTPyInteger).
					self assert: (self topEntity isOfType: FASTTLiteral).
					self assert: (self topEntity isOfType: FASTTNumericalLiteral)
]

{ #category : 'tests - collections' }
FASTPythonImporterTest >> testDictionaryWithEllipsis [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testDictionaryWithEllipsis 	"

	self parse: '{ "key": ... }'.

	self assert: (fast allWithType: FASTPyDictionary) size equals: 1.
	self assert: (fast allWithType: FASTPyEllipsis) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPair) size equals: 1.
	self assert: (fast allWithType: FASTPyString) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: '{ "key": ... }'.
	self assert: (self topEntity isOfType: FASTPyDictionary).
	
	"Testing value of multivalued relation initializers"
	self assert: self topEntity initializers size equals: 1.
	
		stack push: (stack top initializers at: 1).
		self assert: self topEntity sourceCode equals: '"key": ...'.
		self assert: (self topEntity isOfType: FASTPyPair).
		
		"Testing value of monovalue relation key"
		self assert: self topEntity key isNotNil.

			stack push: self topEntity key.
			self assert: self topEntity sourceCode equals: '"key"'.
			self assert: (self topEntity isOfType: FASTPyString).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTStringLiteral).
			stack pop.
		
		"Testing value of monovalue relation value"
		self assert: self topEntity value isNotNil.

			stack push: self topEntity value.
			self assert: self topEntity sourceCode equals: '...'.
			self assert: (self topEntity isOfType: FASTPyEllipsis).
			self assert: (self topEntity isOfType: FASTTLiteral)
]

{ #category : 'tests - collections' }
FASTPythonImporterTest >> testDictionaryWithFalse [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testDictionaryWithFalse 	"

	self parse: '{ "key": False }'.

	self assert: (fast allWithType: FASTPyBoolean) size equals: 1.
	self assert: (fast allWithType: FASTPyDictionary) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPair) size equals: 1.
	self assert: (fast allWithType: FASTPyString) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: '{ "key": False }'.
	self assert: (self topEntity isOfType: FASTPyDictionary).
	
	"Testing value of multivalued relation initializers"
	self assert: self topEntity initializers size equals: 1.
	
		stack push: (stack top initializers at: 1).
		self assert: self topEntity sourceCode equals: '"key": False'.
		self assert: (self topEntity isOfType: FASTPyPair).
		
		"Testing value of monovalue relation key"
		self assert: self topEntity key isNotNil.

			stack push: self topEntity key.
			self assert: self topEntity sourceCode equals: '"key"'.
			self assert: (self topEntity isOfType: FASTPyString).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTStringLiteral).
			stack pop.
		
		"Testing value of monovalue relation value"
		self assert: self topEntity value isNotNil.

			stack push: self topEntity value.
			self assert: self topEntity sourceCode equals: 'False'.
			self assert: (self topEntity isOfType: FASTPyBoolean).
			self assert: (self topEntity isOfType: FASTTBooleanLiteral).
			self assert: (self topEntity isOfType: FASTTLiteral)
]

{ #category : 'tests - collections' }
FASTPythonImporterTest >> testDictionaryWithFloat [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testDictionaryWithFloat 	"

	self parse: '{ "key": 0.1 }'.

	self assert: (fast allWithType: FASTPyDictionary) size equals: 1.
	self assert: (fast allWithType: FASTPyFloat) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPair) size equals: 1.
	self assert: (fast allWithType: FASTPyString) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: '{ "key": 0.1 }'.
	self assert: (self topEntity isOfType: FASTPyDictionary).
	
	"Testing value of multivalued relation initializers"
	self assert: self topEntity initializers size equals: 1.
	
		stack push: (stack top initializers at: 1).
		self assert: self topEntity sourceCode equals: '"key": 0.1'.
		self assert: (self topEntity isOfType: FASTPyPair).
		
		"Testing value of monovalue relation key"
		self assert: self topEntity key isNotNil.

			stack push: self topEntity key.
			self assert: self topEntity sourceCode equals: '"key"'.
			self assert: (self topEntity isOfType: FASTPyString).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTStringLiteral).
			stack pop.
		
		"Testing value of monovalue relation value"
		self assert: self topEntity value isNotNil.

			stack push: self topEntity value.
			self assert: self topEntity sourceCode equals: '0.1'.
			self assert: (self topEntity isOfType: FASTPyFloat).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTNumericalLiteral)
]

{ #category : 'tests - collections' }
FASTPythonImporterTest >> testDictionaryWithIdentifier [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testDictionaryWithIdentifier 	"

	self parse: '{ "key": element }'.

	self assert: (fast allWithType: FASTPyDictionary) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPair) size equals: 1.
	self assert: (fast allWithType: FASTPyString) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: '{ "key": element }'.
	self assert: (self topEntity isOfType: FASTPyDictionary).
	
	"Testing value of multivalued relation initializers"
	self assert: self topEntity initializers size equals: 1.
	
		stack push: (stack top initializers at: 1).
		self assert: self topEntity sourceCode equals: '"key": element'.
		self assert: (self topEntity isOfType: FASTPyPair).
		
		"Testing value of monovalue relation key"
		self assert: self topEntity key isNotNil.

			stack push: self topEntity key.
			self assert: self topEntity sourceCode equals: '"key"'.
			self assert: (self topEntity isOfType: FASTPyString).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTStringLiteral).
			stack pop.
		
		"Testing value of monovalue relation value"
		self assert: self topEntity value isNotNil.

			stack push: self topEntity value.
			self assert: self topEntity sourceCode equals: 'element'.
			self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - collections' }
FASTPythonImporterTest >> testDictionaryWithInteger [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testDictionaryWithInteger 	"

	self parse: '{ "key": 1 }'.

	self assert: (fast allWithType: FASTPyDictionary) size equals: 1.
	self assert: (fast allWithType: FASTPyInteger) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPair) size equals: 1.
	self assert: (fast allWithType: FASTPyString) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: '{ "key": 1 }'.
	self assert: (self topEntity isOfType: FASTPyDictionary).
	
	"Testing value of multivalued relation initializers"
	self assert: self topEntity initializers size equals: 1.
	
		stack push: (stack top initializers at: 1).
		self assert: self topEntity sourceCode equals: '"key": 1'.
		self assert: (self topEntity isOfType: FASTPyPair).
		
		"Testing value of monovalue relation key"
		self assert: self topEntity key isNotNil.

			stack push: self topEntity key.
			self assert: self topEntity sourceCode equals: '"key"'.
			self assert: (self topEntity isOfType: FASTPyString).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTStringLiteral).
			stack pop.
		
		"Testing value of monovalue relation value"
		self assert: self topEntity value isNotNil.

			stack push: self topEntity value.
			self assert: self topEntity sourceCode equals: '1'.
			self assert: (self topEntity isOfType: FASTPyInteger).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTNumericalLiteral)
]

{ #category : 'tests - collections' }
FASTPythonImporterTest >> testDictionaryWithLambda [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testDictionaryWithLambda 	"

	self parse: '{ "key": lambda x:x }'.

	self assert: (fast allWithType: FASTPyDictionary) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyLambda) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPair) size equals: 1.
	self assert: (fast allWithType: FASTPyParameter) size equals: 1.
	self assert: (fast allWithType: FASTPyString) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: '{ "key": lambda x:x }'.
	self assert: (self topEntity isOfType: FASTPyDictionary).
	
	"Testing value of multivalued relation initializers"
	self assert: self topEntity initializers size equals: 1.
	
		stack push: (stack top initializers at: 1).
		self assert: self topEntity sourceCode equals: '"key": lambda x:x'.
		self assert: (self topEntity isOfType: FASTPyPair).
		
		"Testing value of monovalue relation key"
		self assert: self topEntity key isNotNil.

			stack push: self topEntity key.
			self assert: self topEntity sourceCode equals: '"key"'.
			self assert: (self topEntity isOfType: FASTPyString).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTStringLiteral).
			stack pop.
		
		"Testing value of monovalue relation value"
		self assert: self topEntity value isNotNil.

			stack push: self topEntity value.
			self assert: self topEntity sourceCode equals: 'lambda x:x'.
			self assert: (self topEntity isOfType: FASTPyLambda).
			self assert: (self topEntity isOfType: FASTTWithParameters).
			
			"Testing value of monovalue relation expression"
			self assert: self topEntity expression isNotNil.

				stack push: self topEntity expression.
				self assert: self topEntity sourceCode equals: 'x'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			
			"Testing value of multivalued relation parameters"
			self assert: self topEntity parameters size equals: 1.
			
				stack push: (stack top parameters at: 1).
				self assert: self topEntity sourceCode equals: 'x'.
				self assert: (self topEntity isOfType: FASTPyParameter).
				self assert: (self topEntity isOfType: FASTTVariableEntity).
				self assert: self topEntity name equals: 'x'
]

{ #category : 'tests - collections' }
FASTPythonImporterTest >> testDictionaryWithList [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testDictionaryWithList 	"

	self parse: '{ "key": [ 1 ] }'.

	self assert: (fast allWithType: FASTPyDictionary) size equals: 1.
	self assert: (fast allWithType: FASTPyInteger) size equals: 1.
	self assert: (fast allWithType: FASTPyList) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPair) size equals: 1.
	self assert: (fast allWithType: FASTPyString) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: '{ "key": [ 1 ] }'.
	self assert: (self topEntity isOfType: FASTPyDictionary).
	
	"Testing value of multivalued relation initializers"
	self assert: self topEntity initializers size equals: 1.
	
		stack push: (stack top initializers at: 1).
		self assert: self topEntity sourceCode equals: '"key": [ 1 ]'.
		self assert: (self topEntity isOfType: FASTPyPair).
		
		"Testing value of monovalue relation key"
		self assert: self topEntity key isNotNil.

			stack push: self topEntity key.
			self assert: self topEntity sourceCode equals: '"key"'.
			self assert: (self topEntity isOfType: FASTPyString).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTStringLiteral).
			stack pop.
		
		"Testing value of monovalue relation value"
		self assert: self topEntity value isNotNil.

			stack push: self topEntity value.
			self assert: self topEntity sourceCode equals: '[ 1 ]'.
			self assert: (self topEntity isOfType: FASTPyList).
			
			"Testing value of multivalued relation initializers"
			self assert: self topEntity initializers size equals: 1.
			
				stack push: (stack top initializers at: 1).
				self assert: self topEntity sourceCode equals: '1'.
				self assert: (self topEntity isOfType: FASTPyInteger).
				self assert: (self topEntity isOfType: FASTTLiteral).
				self assert: (self topEntity isOfType: FASTTNumericalLiteral)
]

{ #category : 'tests - collections' }
FASTPythonImporterTest >> testDictionaryWithListSplat [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testDictionaryWithListSplat 	"

	self parse: '{ "key": *args }'.

	self assert: (fast allWithType: FASTPyDictionary) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyListSplat) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPair) size equals: 1.
	self assert: (fast allWithType: FASTPyString) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: '{ "key": *args }'.
	self assert: (self topEntity isOfType: FASTPyDictionary).
	
	"Testing value of multivalued relation initializers"
	self assert: self topEntity initializers size equals: 1.
	
		stack push: (stack top initializers at: 1).
		self assert: self topEntity sourceCode equals: '"key": *args'.
		self assert: (self topEntity isOfType: FASTPyPair).
		
		"Testing value of monovalue relation key"
		self assert: self topEntity key isNotNil.

			stack push: self topEntity key.
			self assert: self topEntity sourceCode equals: '"key"'.
			self assert: (self topEntity isOfType: FASTPyString).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTStringLiteral).
			stack pop.
		
		"Testing value of monovalue relation value"
		self assert: self topEntity value isNotNil.

			stack push: self topEntity value.
			self assert: self topEntity sourceCode equals: '*args'.
			self assert: (self topEntity isOfType: FASTPyListSplat).
			
			"Testing value of monovalue relation expression"
			self assert: self topEntity expression isNotNil.

				stack push: self topEntity expression.
				self assert: self topEntity sourceCode equals: 'args'.
				self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - collections' }
FASTPythonImporterTest >> testDictionaryWithList_comprehension [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testDictionaryWithList_comprehension 	"

	self parse: '{ "key": [random for i in range(3)] }'.

	self assert: (fast allWithType: FASTPyCall) size equals: 1.
	self assert: (fast allWithType: FASTPyDictionary) size equals: 1.
	self assert: (fast allWithType: FASTPyForInClause) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 3.
	self assert: (fast allWithType: FASTPyInteger) size equals: 1.
	self assert: (fast allWithType: FASTPyListComprehension) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPair) size equals: 1.
	self assert: (fast allWithType: FASTPyString) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: '{ "key": [random for i in range(3)] }'.
	self assert: (self topEntity isOfType: FASTPyDictionary).
	
	"Testing value of multivalued relation initializers"
	self assert: self topEntity initializers size equals: 1.
	
		stack push: (stack top initializers at: 1).
		self assert: self topEntity sourceCode equals: '"key": [random for i in range(3)]'.
		self assert: (self topEntity isOfType: FASTPyPair).
		
		"Testing value of monovalue relation key"
		self assert: self topEntity key isNotNil.

			stack push: self topEntity key.
			self assert: self topEntity sourceCode equals: '"key"'.
			self assert: (self topEntity isOfType: FASTPyString).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTStringLiteral).
			stack pop.
		
		"Testing value of monovalue relation value"
		self assert: self topEntity value isNotNil.

			stack push: self topEntity value.
			self assert: self topEntity sourceCode equals: '[random for i in range(3)]'.
			self assert: (self topEntity isOfType: FASTPyListComprehension).
			self assert: (self topEntity isOfType: FASTPyTComprehension).
			self assert: (self topEntity isOfType: FASTTWithCondition).
			
			"Testing value of monovalue relation body"
			self assert: self topEntity body isNotNil.

				stack push: self topEntity body.
				self assert: self topEntity sourceCode equals: 'random'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			
			"Testing value of multivalued relation clauses"
			self assert: self topEntity clauses size equals: 1.
			
				stack push: (stack top clauses at: 1).
				self assert: self topEntity sourceCode equals: 'for i in range(3)'.
				self assert: (self topEntity isOfType: FASTPyForInClause).
				
				"Testing value of monovalue relation left"
				self assert: self topEntity left isNotNil.

					stack push: self topEntity left.
					self assert: self topEntity sourceCode equals: 'i'.
					self assert: (self topEntity isOfType: FASTPyIdentifier).
					stack pop.
				
				"Testing value of monovalue relation right"
				self assert: self topEntity right isNotNil.

					stack push: self topEntity right.
					self assert: self topEntity sourceCode equals: 'range(3)'.
					self assert: (self topEntity isOfType: FASTPyCall).
					self assert: (self topEntity isOfType: FASTTInvocation).
					self assert: (self topEntity isOfType: FASTTWithArguments).
					
					"Testing value of multivalued relation arguments"
					self assert: self topEntity arguments size equals: 1.
					
						stack push: (stack top arguments at: 1).
						self assert: self topEntity sourceCode equals: '3'.
						self assert: (self topEntity isOfType: FASTPyInteger).
						self assert: (self topEntity isOfType: FASTTLiteral).
						self assert: (self topEntity isOfType: FASTTNumericalLiteral).
						stack pop.
					
					"Testing value of monovalue relation callee"
					self assert: self topEntity callee isNotNil.

						stack push: self topEntity callee.
						self assert: self topEntity sourceCode equals: 'range'.
						self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - collections' }
FASTPythonImporterTest >> testDictionaryWithNone [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testDictionaryWithNone 	"

	self parse: '{ "key": None }'.

	self assert: (fast allWithType: FASTPyDictionary) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyNone) size equals: 1.
	self assert: (fast allWithType: FASTPyPair) size equals: 1.
	self assert: (fast allWithType: FASTPyString) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: '{ "key": None }'.
	self assert: (self topEntity isOfType: FASTPyDictionary).
	
	"Testing value of multivalued relation initializers"
	self assert: self topEntity initializers size equals: 1.
	
		stack push: (stack top initializers at: 1).
		self assert: self topEntity sourceCode equals: '"key": None'.
		self assert: (self topEntity isOfType: FASTPyPair).
		
		"Testing value of monovalue relation key"
		self assert: self topEntity key isNotNil.

			stack push: self topEntity key.
			self assert: self topEntity sourceCode equals: '"key"'.
			self assert: (self topEntity isOfType: FASTPyString).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTStringLiteral).
			stack pop.
		
		"Testing value of monovalue relation value"
		self assert: self topEntity value isNotNil.

			stack push: self topEntity value.
			self assert: self topEntity sourceCode equals: 'None'.
			self assert: (self topEntity isOfType: FASTPyNone).
			self assert: (self topEntity isOfType: FASTTLiteral)
]

{ #category : 'tests - collections' }
FASTPythonImporterTest >> testDictionaryWithNotOperator [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testDictionaryWithNotOperator 	"

	self parse: '{ "key": not old }'.

	self assert: (fast allWithType: FASTPyDictionary) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyNotOperator) size equals: 1.
	self assert: (fast allWithType: FASTPyPair) size equals: 1.
	self assert: (fast allWithType: FASTPyString) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: '{ "key": not old }'.
	self assert: (self topEntity isOfType: FASTPyDictionary).
	
	"Testing value of multivalued relation initializers"
	self assert: self topEntity initializers size equals: 1.
	
		stack push: (stack top initializers at: 1).
		self assert: self topEntity sourceCode equals: '"key": not old'.
		self assert: (self topEntity isOfType: FASTPyPair).
		
		"Testing value of monovalue relation key"
		self assert: self topEntity key isNotNil.

			stack push: self topEntity key.
			self assert: self topEntity sourceCode equals: '"key"'.
			self assert: (self topEntity isOfType: FASTPyString).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTStringLiteral).
			stack pop.
		
		"Testing value of monovalue relation value"
		self assert: self topEntity value isNotNil.

			stack push: self topEntity value.
			self assert: self topEntity sourceCode equals: 'not old'.
			self assert: (self topEntity isOfType: FASTPyNotOperator).
			self assert: (self topEntity isOfType: FASTPyTUnaryOperator).
			
			"Testing value of monovalue relation argument"
			self assert: self topEntity argument isNotNil.

				stack push: self topEntity argument.
				self assert: self topEntity sourceCode equals: 'old'.
				self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - collections' }
FASTPythonImporterTest >> testDictionaryWithSet [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testDictionaryWithSet 	"

	self parse: '{ "key": { 1 } }'.

	self assert: (fast allWithType: FASTPyDictionary) size equals: 1.
	self assert: (fast allWithType: FASTPyInteger) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPair) size equals: 1.
	self assert: (fast allWithType: FASTPySet) size equals: 1.
	self assert: (fast allWithType: FASTPyString) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: '{ "key": { 1 } }'.
	self assert: (self topEntity isOfType: FASTPyDictionary).
	
	"Testing value of multivalued relation initializers"
	self assert: self topEntity initializers size equals: 1.
	
		stack push: (stack top initializers at: 1).
		self assert: self topEntity sourceCode equals: '"key": { 1 }'.
		self assert: (self topEntity isOfType: FASTPyPair).
		
		"Testing value of monovalue relation key"
		self assert: self topEntity key isNotNil.

			stack push: self topEntity key.
			self assert: self topEntity sourceCode equals: '"key"'.
			self assert: (self topEntity isOfType: FASTPyString).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTStringLiteral).
			stack pop.
		
		"Testing value of monovalue relation value"
		self assert: self topEntity value isNotNil.

			stack push: self topEntity value.
			self assert: self topEntity sourceCode equals: '{ 1 }'.
			self assert: (self topEntity isOfType: FASTPySet).
			
			"Testing value of multivalued relation initializers"
			self assert: self topEntity initializers size equals: 1.
			
				stack push: (stack top initializers at: 1).
				self assert: self topEntity sourceCode equals: '1'.
				self assert: (self topEntity isOfType: FASTPyInteger).
				self assert: (self topEntity isOfType: FASTTLiteral).
				self assert: (self topEntity isOfType: FASTTNumericalLiteral)
]

{ #category : 'tests - collections' }
FASTPythonImporterTest >> testDictionaryWithSetComprehension [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testDictionaryWithSetComprehension 	"

	self parse: '{ "key": {random for i in range(3)} }'.

	self assert: (fast allWithType: FASTPyCall) size equals: 1.
	self assert: (fast allWithType: FASTPyDictionary) size equals: 1.
	self assert: (fast allWithType: FASTPyForInClause) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 3.
	self assert: (fast allWithType: FASTPyInteger) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPair) size equals: 1.
	self assert: (fast allWithType: FASTPySetComprehension) size equals: 1.
	self assert: (fast allWithType: FASTPyString) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: '{ "key": {random for i in range(3)} }'.
	self assert: (self topEntity isOfType: FASTPyDictionary).
	
	"Testing value of multivalued relation initializers"
	self assert: self topEntity initializers size equals: 1.
	
		stack push: (stack top initializers at: 1).
		self assert: self topEntity sourceCode equals: '"key": {random for i in range(3)}'.
		self assert: (self topEntity isOfType: FASTPyPair).
		
		"Testing value of monovalue relation key"
		self assert: self topEntity key isNotNil.

			stack push: self topEntity key.
			self assert: self topEntity sourceCode equals: '"key"'.
			self assert: (self topEntity isOfType: FASTPyString).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTStringLiteral).
			stack pop.
		
		"Testing value of monovalue relation value"
		self assert: self topEntity value isNotNil.

			stack push: self topEntity value.
			self assert: self topEntity sourceCode equals: '{random for i in range(3)}'.
			self assert: (self topEntity isOfType: FASTPySetComprehension).
			self assert: (self topEntity isOfType: FASTPyTComprehension).
			self assert: (self topEntity isOfType: FASTTWithCondition).
			
			"Testing value of monovalue relation body"
			self assert: self topEntity body isNotNil.

				stack push: self topEntity body.
				self assert: self topEntity sourceCode equals: 'random'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			
			"Testing value of multivalued relation clauses"
			self assert: self topEntity clauses size equals: 1.
			
				stack push: (stack top clauses at: 1).
				self assert: self topEntity sourceCode equals: 'for i in range(3)'.
				self assert: (self topEntity isOfType: FASTPyForInClause).
				
				"Testing value of monovalue relation left"
				self assert: self topEntity left isNotNil.

					stack push: self topEntity left.
					self assert: self topEntity sourceCode equals: 'i'.
					self assert: (self topEntity isOfType: FASTPyIdentifier).
					stack pop.
				
				"Testing value of monovalue relation right"
				self assert: self topEntity right isNotNil.

					stack push: self topEntity right.
					self assert: self topEntity sourceCode equals: 'range(3)'.
					self assert: (self topEntity isOfType: FASTPyCall).
					self assert: (self topEntity isOfType: FASTTInvocation).
					self assert: (self topEntity isOfType: FASTTWithArguments).
					
					"Testing value of multivalued relation arguments"
					self assert: self topEntity arguments size equals: 1.
					
						stack push: (stack top arguments at: 1).
						self assert: self topEntity sourceCode equals: '3'.
						self assert: (self topEntity isOfType: FASTPyInteger).
						self assert: (self topEntity isOfType: FASTTLiteral).
						self assert: (self topEntity isOfType: FASTTNumericalLiteral).
						stack pop.
					
					"Testing value of monovalue relation callee"
					self assert: self topEntity callee isNotNil.

						stack push: self topEntity callee.
						self assert: self topEntity sourceCode equals: 'range'.
						self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - collections' }
FASTPythonImporterTest >> testDictionaryWithSplat [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testDictionaryWithSplat 	"

	self parse: '{**base, 1:2}'.

	self assert: (fast allWithType: FASTPyDictionary) size equals: 1.
	self assert: (fast allWithType: FASTPyDictionarySplat) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyInteger) size equals: 2.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPair) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: '{**base, 1:2}'.
	self assert: (self topEntity isOfType: FASTPyDictionary).
	
	"Testing value of multivalued relation initializers"
	self assert: self topEntity initializers size equals: 2.
	
		stack push: (stack top initializers at: 1).
		self assert: self topEntity sourceCode equals: '**base'.
		self assert: (self topEntity isOfType: FASTPyDictionarySplat).
		
		"Testing value of monovalue relation expression"
		self assert: self topEntity expression isNotNil.

			stack push: self topEntity expression.
			self assert: self topEntity sourceCode equals: 'base'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		stack pop.
	
		stack push: (stack top initializers at: 2).
		self assert: self topEntity sourceCode equals: '1:2'.
		self assert: (self topEntity isOfType: FASTPyPair).
		
		"Testing value of monovalue relation key"
		self assert: self topEntity key isNotNil.

			stack push: self topEntity key.
			self assert: self topEntity sourceCode equals: '1'.
			self assert: (self topEntity isOfType: FASTPyInteger).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTNumericalLiteral).
			stack pop.
		
		"Testing value of monovalue relation value"
		self assert: self topEntity value isNotNil.

			stack push: self topEntity value.
			self assert: self topEntity sourceCode equals: '2'.
			self assert: (self topEntity isOfType: FASTPyInteger).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTNumericalLiteral)
]

{ #category : 'tests - collections' }
FASTPythonImporterTest >> testDictionaryWithString [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testDictionaryWithString 	"

	self parse: '{ "key": "Hello" }'.

	self assert: (fast allWithType: FASTPyDictionary) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPair) size equals: 1.
	self assert: (fast allWithType: FASTPyString) size equals: 2.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: '{ "key": "Hello" }'.
	self assert: (self topEntity isOfType: FASTPyDictionary).
	
	"Testing value of multivalued relation initializers"
	self assert: self topEntity initializers size equals: 1.
	
		stack push: (stack top initializers at: 1).
		self assert: self topEntity sourceCode equals: '"key": "Hello"'.
		self assert: (self topEntity isOfType: FASTPyPair).
		
		"Testing value of monovalue relation key"
		self assert: self topEntity key isNotNil.

			stack push: self topEntity key.
			self assert: self topEntity sourceCode equals: '"key"'.
			self assert: (self topEntity isOfType: FASTPyString).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTStringLiteral).
			stack pop.
		
		"Testing value of monovalue relation value"
		self assert: self topEntity value isNotNil.

			stack push: self topEntity value.
			self assert: self topEntity sourceCode equals: '"Hello"'.
			self assert: (self topEntity isOfType: FASTPyString).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTStringLiteral)
]

{ #category : 'tests - collections' }
FASTPythonImporterTest >> testDictionaryWithSubscript [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testDictionaryWithSubscript 	"

	self parse: '{ "key": attrs[2] }'.

	self assert: (fast allWithType: FASTPyDictionary) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyInteger) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPair) size equals: 1.
	self assert: (fast allWithType: FASTPyString) size equals: 1.
	self assert: (fast allWithType: FASTPySubscript) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: '{ "key": attrs[2] }'.
	self assert: (self topEntity isOfType: FASTPyDictionary).
	
	"Testing value of multivalued relation initializers"
	self assert: self topEntity initializers size equals: 1.
	
		stack push: (stack top initializers at: 1).
		self assert: self topEntity sourceCode equals: '"key": attrs[2]'.
		self assert: (self topEntity isOfType: FASTPyPair).
		
		"Testing value of monovalue relation key"
		self assert: self topEntity key isNotNil.

			stack push: self topEntity key.
			self assert: self topEntity sourceCode equals: '"key"'.
			self assert: (self topEntity isOfType: FASTPyString).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTStringLiteral).
			stack pop.
		
		"Testing value of monovalue relation value"
		self assert: self topEntity value isNotNil.

			stack push: self topEntity value.
			self assert: self topEntity sourceCode equals: 'attrs[2]'.
			self assert: (self topEntity isOfType: FASTPySubscript).
			
			"Testing value of multivalued relation indices"
			self assert: self topEntity indices size equals: 1.
			
				stack push: (stack top indices at: 1).
				self assert: self topEntity sourceCode equals: '2'.
				self assert: (self topEntity isOfType: FASTPyInteger).
				self assert: (self topEntity isOfType: FASTTLiteral).
				self assert: (self topEntity isOfType: FASTTNumericalLiteral).
				stack pop.
			
			"Testing value of monovalue relation value"
			self assert: self topEntity value isNotNil.

				stack push: self topEntity value.
				self assert: self topEntity sourceCode equals: 'attrs'.
				self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - collections' }
FASTPythonImporterTest >> testDictionaryWithTrue [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testDictionaryWithTrue 	"

	self parse: '{ "key": True }'.

	self assert: (fast allWithType: FASTPyBoolean) size equals: 1.
	self assert: (fast allWithType: FASTPyDictionary) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPair) size equals: 1.
	self assert: (fast allWithType: FASTPyString) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: '{ "key": True }'.
	self assert: (self topEntity isOfType: FASTPyDictionary).
	
	"Testing value of multivalued relation initializers"
	self assert: self topEntity initializers size equals: 1.
	
		stack push: (stack top initializers at: 1).
		self assert: self topEntity sourceCode equals: '"key": True'.
		self assert: (self topEntity isOfType: FASTPyPair).
		
		"Testing value of monovalue relation key"
		self assert: self topEntity key isNotNil.

			stack push: self topEntity key.
			self assert: self topEntity sourceCode equals: '"key"'.
			self assert: (self topEntity isOfType: FASTPyString).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTStringLiteral).
			stack pop.
		
		"Testing value of monovalue relation value"
		self assert: self topEntity value isNotNil.

			stack push: self topEntity value.
			self assert: self topEntity sourceCode equals: 'True'.
			self assert: (self topEntity isOfType: FASTPyBoolean).
			self assert: (self topEntity isOfType: FASTTBooleanLiteral).
			self assert: (self topEntity isOfType: FASTTLiteral)
]

{ #category : 'tests - collections' }
FASTPythonImporterTest >> testDictionaryWithTuple [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testDictionaryWithTuple 	"

	self parse: '{ "key": (1,) }'.

	self assert: (fast allWithType: FASTPyDictionary) size equals: 1.
	self assert: (fast allWithType: FASTPyInteger) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPair) size equals: 1.
	self assert: (fast allWithType: FASTPyString) size equals: 1.
	self assert: (fast allWithType: FASTPyTuple) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: '{ "key": (1,) }'.
	self assert: (self topEntity isOfType: FASTPyDictionary).
	
	"Testing value of multivalued relation initializers"
	self assert: self topEntity initializers size equals: 1.
	
		stack push: (stack top initializers at: 1).
		self assert: self topEntity sourceCode equals: '"key": (1,)'.
		self assert: (self topEntity isOfType: FASTPyPair).
		
		"Testing value of monovalue relation key"
		self assert: self topEntity key isNotNil.

			stack push: self topEntity key.
			self assert: self topEntity sourceCode equals: '"key"'.
			self assert: (self topEntity isOfType: FASTPyString).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTStringLiteral).
			stack pop.
		
		"Testing value of monovalue relation value"
		self assert: self topEntity value isNotNil.

			stack push: self topEntity value.
			self assert: self topEntity sourceCode equals: '(1,)'.
			self assert: (self topEntity isOfType: FASTPyTuple).
			
			"Testing value of multivalued relation initializers"
			self assert: self topEntity initializers size equals: 1.
			
				stack push: (stack top initializers at: 1).
				self assert: self topEntity sourceCode equals: '1'.
				self assert: (self topEntity isOfType: FASTPyInteger).
				self assert: (self topEntity isOfType: FASTTLiteral).
				self assert: (self topEntity isOfType: FASTTNumericalLiteral)
]

{ #category : 'tests - collections' }
FASTPythonImporterTest >> testDictionaryWithUnaryOperator [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testDictionaryWithUnaryOperator 	"

	self parse: '{ "key": -1 }'.

	self assert: (fast allWithType: FASTPyDictionary) size equals: 1.
	self assert: (fast allWithType: FASTPyInteger) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPair) size equals: 1.
	self assert: (fast allWithType: FASTPyString) size equals: 1.
	self assert: (fast allWithType: FASTPyUnaryOperator) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: '{ "key": -1 }'.
	self assert: (self topEntity isOfType: FASTPyDictionary).
	
	"Testing value of multivalued relation initializers"
	self assert: self topEntity initializers size equals: 1.
	
		stack push: (stack top initializers at: 1).
		self assert: self topEntity sourceCode equals: '"key": -1'.
		self assert: (self topEntity isOfType: FASTPyPair).
		
		"Testing value of monovalue relation key"
		self assert: self topEntity key isNotNil.

			stack push: self topEntity key.
			self assert: self topEntity sourceCode equals: '"key"'.
			self assert: (self topEntity isOfType: FASTPyString).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTStringLiteral).
			stack pop.
		
		"Testing value of monovalue relation value"
		self assert: self topEntity value isNotNil.

			stack push: self topEntity value.
			self assert: self topEntity sourceCode equals: '-1'.
			self assert: (self topEntity isOfType: FASTPyUnaryOperator).
			self assert: (self topEntity isOfType: FASTPyTUnaryOperator).
			
			"Testing value of monovalue relation argument"
			self assert: self topEntity argument isNotNil.

				stack push: self topEntity argument.
				self assert: self topEntity sourceCode equals: '1'.
				self assert: (self topEntity isOfType: FASTPyInteger).
				self assert: (self topEntity isOfType: FASTTLiteral).
				self assert: (self topEntity isOfType: FASTTNumericalLiteral).
				stack pop.
			self assert: self topEntity operator equals: '-'
]

{ #category : 'tests - literals' }
FASTPythonImporterTest >> testDoubleQuotedString [

	| string |
	self parse: '"This is a string"'.

	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyString) size equals: 1.

	string := (fast allWithType: FASTPyString) anyOne.
	self assert: string sourceCode equals: '"This is a string"'.
	self assert: (string isOfType: FASTTStringLiteral).
	self assertEmpty: string allContainedEntities "This is because TreeSitter has nodes inside a string but we do not care about that in FAST. This is to avoid a regression"
]

{ #category : 'tests - literals' }
FASTPythonImporterTest >> testEllipsis [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testEllipsis 	"

	self parse: '...'.

	self assert: (fast allWithType: FASTPyEllipsis) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: '...'.
	self assert: (self topEntity isOfType: FASTPyEllipsis).
	self assert: (self topEntity isOfType: FASTTLiteral)
]

{ #category : 'tests - ellipsis' }
FASTPythonImporterTest >> testEllipsisInBlock [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testEllipsisInBlock 	"

	self parse: 'if True:
	...'.

	self assert: (fast allWithType: FASTPyBoolean) size equals: 1.
	self assert: (fast allWithType: FASTPyEllipsis) size equals: 1.
	self assert: (fast allWithType: FASTPyIfStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyThenClause) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'if True:
	...' withPlatformLineEndings.
	self assert: (self topEntity isOfType: FASTPyIfStatement).
	self assert: (self topEntity isOfType: FASTPyTWithElseClause).
	self assert: (self topEntity isOfType: FASTTConditionalStatement).
	self assert: (self topEntity isOfType: FASTTWithCondition).
	
	"Testing value of monovalue relation condition"
	self assert: self topEntity condition isNotNil.

		stack push: self topEntity condition.
		self assert: self topEntity sourceCode equals: 'True'.
		self assert: (self topEntity isOfType: FASTPyBoolean).
		self assert: (self topEntity isOfType: FASTTBooleanLiteral).
		self assert: (self topEntity isOfType: FASTTLiteral).
		stack pop.
	
	"Testing value of monovalue relation thenClause"
	self assert: self topEntity thenClause isNotNil.

		stack push: self topEntity thenClause.
		self assert: self topEntity sourceCode equals: '...'.
		self assert: (self topEntity isOfType: FASTPyThenClause).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: '...'.
			self assert: (self topEntity isOfType: FASTPyEllipsis).
			self assert: (self topEntity isOfType: FASTTLiteral)
]

{ #category : 'tests - try statement' }
FASTPythonImporterTest >> testExceptAttribute [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testExceptAttribute 	"

	self parse: 'try: pass
except exceptions.Exception: pass '.

	self assert: (fast allWithType: FASTPyAttributeAccess) size equals: 1.
	self assert: (fast allWithType: FASTPyExceptClause) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 2.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPassStatement) size equals: 2.
	self assert: (fast allWithType: FASTPyTryStatement) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'try: pass
except exceptions.Exception: pass' withPlatformLineEndings.
	self assert: (self topEntity isOfType: FASTPyTryStatement).
	self assert: (self topEntity isOfType: FASTPyTWithElseClause).
	self assert: (self topEntity isOfType: FASTTStatementBlock).
	
	"Testing value of multivalued relation excepts"
	self assert: self topEntity excepts size equals: 1.
	
		stack push: (stack top excepts at: 1).
		self assert: self topEntity sourceCode equals: 'except exceptions.Exception: pass'.
		self assert: (self topEntity isOfType: FASTPyExceptClause).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		
		"Testing value of multivalued relation expressions"
		self assert: self topEntity expressions size equals: 1.
		
			stack push: (stack top expressions at: 1).
			self assert: self topEntity sourceCode equals: 'exceptions.Exception'.
			self assert: (self topEntity isOfType: FASTPyAttributeAccess).
			
			"Testing value of monovalue relation attribute"
			self assert: self topEntity attribute isNotNil.

				stack push: self topEntity attribute.
				self assert: self topEntity sourceCode equals: 'Exception'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			
			"Testing value of monovalue relation object"
			self assert: self topEntity object isNotNil.

				stack push: self topEntity object.
				self assert: self topEntity sourceCode equals: 'exceptions'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			stack pop.
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'pass'.
			self assert: (self topEntity isOfType: FASTPyPassStatement).
			stack pop.
		stack pop.
	
	"Testing value of multivalued relation statements"
	self assert: self topEntity statements size equals: 1.
	
		stack push: (stack top statements at: 1).
		self assert: self topEntity sourceCode equals: 'pass'.
		self assert: (self topEntity isOfType: FASTPyPassStatement)
]

{ #category : 'tests - try statement' }
FASTPythonImporterTest >> testExceptCall [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testExceptCall 	"

	self parse: 'try: pass
except Exception(): pass '.

	self assert: (fast allWithType: FASTPyCall) size equals: 1.
	self assert: (fast allWithType: FASTPyExceptClause) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPassStatement) size equals: 2.
	self assert: (fast allWithType: FASTPyTryStatement) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'try: pass
except Exception(): pass' withPlatformLineEndings.
	self assert: (self topEntity isOfType: FASTPyTryStatement).
	self assert: (self topEntity isOfType: FASTPyTWithElseClause).
	self assert: (self topEntity isOfType: FASTTStatementBlock).
	
	"Testing value of multivalued relation excepts"
	self assert: self topEntity excepts size equals: 1.
	
		stack push: (stack top excepts at: 1).
		self assert: self topEntity sourceCode equals: 'except Exception(): pass'.
		self assert: (self topEntity isOfType: FASTPyExceptClause).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		
		"Testing value of multivalued relation expressions"
		self assert: self topEntity expressions size equals: 1.
		
			stack push: (stack top expressions at: 1).
			self assert: self topEntity sourceCode equals: 'Exception()'.
			self assert: (self topEntity isOfType: FASTPyCall).
			self assert: (self topEntity isOfType: FASTTInvocation).
			self assert: (self topEntity isOfType: FASTTWithArguments).
			
			"Testing value of monovalue relation callee"
			self assert: self topEntity callee isNotNil.

				stack push: self topEntity callee.
				self assert: self topEntity sourceCode equals: 'Exception'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			stack pop.
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'pass'.
			self assert: (self topEntity isOfType: FASTPyPassStatement).
			stack pop.
		stack pop.
	
	"Testing value of multivalued relation statements"
	self assert: self topEntity statements size equals: 1.
	
		stack push: (stack top statements at: 1).
		self assert: self topEntity sourceCode equals: 'pass'.
		self assert: (self topEntity isOfType: FASTPyPassStatement)
]

{ #category : 'tests - try statement' }
FASTPythonImporterTest >> testExceptConditionalExpression [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testExceptConditionalExpression 	"

	self parse: 'try: pass
except Exception if n > 1 else Error: pass '.

	self assert: (fast allWithType: FASTPyComparisonOperator) size equals: 1.
	self assert: (fast allWithType: FASTPyConditionalExpression) size equals: 1.
	self assert: (fast allWithType: FASTPyExceptClause) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 3.
	self assert: (fast allWithType: FASTPyInteger) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPassStatement) size equals: 2.
	self assert: (fast allWithType: FASTPyTryStatement) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'try: pass
except Exception if n > 1 else Error: pass' withPlatformLineEndings.
	self assert: (self topEntity isOfType: FASTPyTryStatement).
	self assert: (self topEntity isOfType: FASTPyTWithElseClause).
	self assert: (self topEntity isOfType: FASTTStatementBlock).
	
	"Testing value of multivalued relation excepts"
	self assert: self topEntity excepts size equals: 1.
	
		stack push: (stack top excepts at: 1).
		self assert: self topEntity sourceCode equals: 'except Exception if n > 1 else Error: pass'.
		self assert: (self topEntity isOfType: FASTPyExceptClause).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		
		"Testing value of multivalued relation expressions"
		self assert: self topEntity expressions size equals: 1.
		
			stack push: (stack top expressions at: 1).
			self assert: self topEntity sourceCode equals: 'Exception if n > 1 else Error'.
			self assert: (self topEntity isOfType: FASTPyConditionalExpression).
			self assert: (self topEntity isOfType: FASTTWithCondition).
			
			"Testing value of monovalue relation condition"
			self assert: self topEntity condition isNotNil.

				stack push: self topEntity condition.
				self assert: self topEntity sourceCode equals: 'n > 1'.
				self assert: (self topEntity isOfType: FASTPyComparisonOperator).
				
				"Testing value of multivalued relation operands"
				self assert: self topEntity operands size equals: 2.
				
					stack push: (stack top operands at: 1).
					self assert: self topEntity sourceCode equals: 'n'.
					self assert: (self topEntity isOfType: FASTPyIdentifier).
					stack pop.
				
					stack push: (stack top operands at: 2).
					self assert: self topEntity sourceCode equals: '1'.
					self assert: (self topEntity isOfType: FASTPyInteger).
					self assert: (self topEntity isOfType: FASTTLiteral).
					self assert: (self topEntity isOfType: FASTTNumericalLiteral).
					stack pop.
				self assert: self topEntity operators equals: #('>').
				stack pop.
			
			"Testing value of monovalue relation elseExpression"
			self assert: self topEntity elseExpression isNotNil.

				stack push: self topEntity elseExpression.
				self assert: self topEntity sourceCode equals: 'Error'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			
			"Testing value of monovalue relation thenExpression"
			self assert: self topEntity thenExpression isNotNil.

				stack push: self topEntity thenExpression.
				self assert: self topEntity sourceCode equals: 'Exception'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			stack pop.
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'pass'.
			self assert: (self topEntity isOfType: FASTPyPassStatement).
			stack pop.
		stack pop.
	
	"Testing value of multivalued relation statements"
	self assert: self topEntity statements size equals: 1.
	
		stack push: (stack top statements at: 1).
		self assert: self topEntity sourceCode equals: 'pass'.
		self assert: (self topEntity isOfType: FASTPyPassStatement)
]

{ #category : 'tests - try statement' }
FASTPythonImporterTest >> testExceptIdentifier [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testExceptIdentifier 	"

	self parse: 'try: pass
except Exception: pass '.

	self assert: (fast allWithType: FASTPyExceptClause) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPassStatement) size equals: 2.
	self assert: (fast allWithType: FASTPyTryStatement) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'try: pass
except Exception: pass' withPlatformLineEndings.
	self assert: (self topEntity isOfType: FASTPyTryStatement).
	self assert: (self topEntity isOfType: FASTPyTWithElseClause).
	self assert: (self topEntity isOfType: FASTTStatementBlock).
	
	"Testing value of multivalued relation excepts"
	self assert: self topEntity excepts size equals: 1.
	
		stack push: (stack top excepts at: 1).
		self assert: self topEntity sourceCode equals: 'except Exception: pass'.
		self assert: (self topEntity isOfType: FASTPyExceptClause).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		
		"Testing value of multivalued relation expressions"
		self assert: self topEntity expressions size equals: 1.
		
			stack push: (stack top expressions at: 1).
			self assert: self topEntity sourceCode equals: 'Exception'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'pass'.
			self assert: (self topEntity isOfType: FASTPyPassStatement).
			stack pop.
		stack pop.
	
	"Testing value of multivalued relation statements"
	self assert: self topEntity statements size equals: 1.
	
		stack push: (stack top statements at: 1).
		self assert: self topEntity sourceCode equals: 'pass'.
		self assert: (self topEntity isOfType: FASTPyPassStatement)
]

{ #category : 'tests - try statement' }
FASTPythonImporterTest >> testExceptSubscript [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testExceptSubscript 	"

	self parse: 'try: pass
except exceptions[2]: pass '.

	self assert: (fast allWithType: FASTPyExceptClause) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyInteger) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPassStatement) size equals: 2.
	self assert: (fast allWithType: FASTPySubscript) size equals: 1.
	self assert: (fast allWithType: FASTPyTryStatement) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'try: pass
except exceptions[2]: pass' withPlatformLineEndings.
	self assert: (self topEntity isOfType: FASTPyTryStatement).
	self assert: (self topEntity isOfType: FASTPyTWithElseClause).
	self assert: (self topEntity isOfType: FASTTStatementBlock).
	
	"Testing value of multivalued relation excepts"
	self assert: self topEntity excepts size equals: 1.
	
		stack push: (stack top excepts at: 1).
		self assert: self topEntity sourceCode equals: 'except exceptions[2]: pass'.
		self assert: (self topEntity isOfType: FASTPyExceptClause).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		
		"Testing value of multivalued relation expressions"
		self assert: self topEntity expressions size equals: 1.
		
			stack push: (stack top expressions at: 1).
			self assert: self topEntity sourceCode equals: 'exceptions[2]'.
			self assert: (self topEntity isOfType: FASTPySubscript).
			
			"Testing value of multivalued relation indices"
			self assert: self topEntity indices size equals: 1.
			
				stack push: (stack top indices at: 1).
				self assert: self topEntity sourceCode equals: '2'.
				self assert: (self topEntity isOfType: FASTPyInteger).
				self assert: (self topEntity isOfType: FASTTLiteral).
				self assert: (self topEntity isOfType: FASTTNumericalLiteral).
				stack pop.
			
			"Testing value of monovalue relation value"
			self assert: self topEntity value isNotNil.

				stack push: self topEntity value.
				self assert: self topEntity sourceCode equals: 'exceptions'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			stack pop.
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'pass'.
			self assert: (self topEntity isOfType: FASTPyPassStatement).
			stack pop.
		stack pop.
	
	"Testing value of multivalued relation statements"
	self assert: self topEntity statements size equals: 1.
	
		stack push: (stack top statements at: 1).
		self assert: self topEntity sourceCode equals: 'pass'.
		self assert: (self topEntity isOfType: FASTPyPassStatement)
]

{ #category : 'tests - try statements' }
FASTPythonImporterTest >> testExceptWithAlias [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testExceptWithAlias 	"

	self parse: 'try: pass
except Exception as e: pass'.

	self assert: (fast allWithType: FASTPyExceptClause) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPassStatement) size equals: 2.
	self assert: (fast allWithType: FASTPyTryStatement) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'try: pass
except Exception as e: pass' withPlatformLineEndings.
	self assert: (self topEntity isOfType: FASTPyTryStatement).
	self assert: (self topEntity isOfType: FASTPyTWithElseClause).
	self assert: (self topEntity isOfType: FASTTStatementBlock).
	
	"Testing value of multivalued relation excepts"
	self assert: self topEntity excepts size equals: 1.
	
		stack push: (stack top excepts at: 1).
		self assert: self topEntity sourceCode equals: 'except Exception as e: pass'.
		self assert: (self topEntity isOfType: FASTPyExceptClause).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		self assert: self topEntity alias equals: 'e'.
		
		"Testing value of multivalued relation expressions"
		self assert: self topEntity expressions size equals: 1.
		
			stack push: (stack top expressions at: 1).
			self assert: self topEntity sourceCode equals: 'Exception'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'pass'.
			self assert: (self topEntity isOfType: FASTPyPassStatement).
			stack pop.
		stack pop.
	
	"Testing value of multivalued relation statements"
	self assert: self topEntity statements size equals: 1.
	
		stack push: (stack top statements at: 1).
		self assert: self topEntity sourceCode equals: 'pass'.
		self assert: (self topEntity isOfType: FASTPyPassStatement)
]

{ #category : 'tests - literals' }
FASTPythonImporterTest >> testFalse [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testFalse 	"

	self parse: 'False'.

	self assert: (fast allWithType: FASTPyBoolean) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'False'.
	self assert: (self topEntity isOfType: FASTPyBoolean).
	self assert: (self topEntity isOfType: FASTTBooleanLiteral).
	self assert: (self topEntity isOfType: FASTTLiteral)
]

{ #category : 'tests - literals' }
FASTPythonImporterTest >> testFloat [

	| boolean |
	self parse: '42.0'.

	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyFloat) size equals: 1.

	boolean := (fast allWithType: FASTPyFloat) anyOne.
	self assert: boolean sourceCode equals: '42.0'.
	self assert: (boolean isOfType: FASTTLiteral)
]

{ #category : 'tests - advanced statements' }
FASTPythonImporterTest >> testForStatement [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testForStatement 	"

	self parse: 'for i in [ ]:
    continue'.

	self assert: (fast allWithType: FASTPyBlock) size equals: 1.
	self assert: (fast allWithType: FASTPyContinueStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyForStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyList) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'for i in [ ]:
    continue' withPlatformLineEndings.
	self assert: (self topEntity isOfType: FASTPyForStatement).
	self assert: (self topEntity isOfType: FASTPyTWithElseClause).
	self assert: (self topEntity isOfType: FASTTLoopStatement).
	
	"Testing value of monovalue relation body"
	self assert: self topEntity body isNotNil.

		stack push: self topEntity body.
		self assert: self topEntity sourceCode equals: 'continue'.
		self assert: (self topEntity isOfType: FASTPyBlock).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'continue'.
			self assert: (self topEntity isOfType: FASTPyContinueStatement).
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation left"
	self assert: self topEntity left isNotNil.

		stack push: self topEntity left.
		self assert: self topEntity sourceCode equals: 'i'.
		self assert: (self topEntity isOfType: FASTPyIdentifier).
		stack pop.
	
	"Testing value of monovalue relation right"
	self assert: self topEntity right isNotNil.

		stack push: self topEntity right.
		self assert: self topEntity sourceCode equals: '[ ]'.
		self assert: (self topEntity isOfType: FASTPyList)
]

{ #category : 'tests - advanced statements' }
FASTPythonImporterTest >> testForStatementWithABreak [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testForStatementWithABreak 	"

	self parse: 'for c in [1, 3, 5]:
    fun(c)
    break'.

	self assert: (fast allWithType: FASTPyBlock) size equals: 1.
	self assert: (fast allWithType: FASTPyBreakStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyCall) size equals: 1.
	self assert: (fast allWithType: FASTPyForStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 3.
	self assert: (fast allWithType: FASTPyInteger) size equals: 3.
	self assert: (fast allWithType: FASTPyList) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'for c in [1, 3, 5]:
    fun(c)
    break' withPlatformLineEndings.
	self assert: (self topEntity isOfType: FASTPyForStatement).
	self assert: (self topEntity isOfType: FASTPyTWithElseClause).
	self assert: (self topEntity isOfType: FASTTLoopStatement).
	
	"Testing value of monovalue relation body"
	self assert: self topEntity body isNotNil.

		stack push: self topEntity body.
		self assert: self topEntity sourceCode equals: 'fun(c)
    break' withPlatformLineEndings.
		self assert: (self topEntity isOfType: FASTPyBlock).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 2.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'fun(c)'.
			self assert: (self topEntity isOfType: FASTPyCall).
			self assert: (self topEntity isOfType: FASTTInvocation).
			self assert: (self topEntity isOfType: FASTTWithArguments).
			
			"Testing value of multivalued relation arguments"
			self assert: self topEntity arguments size equals: 1.
			
				stack push: (stack top arguments at: 1).
				self assert: self topEntity sourceCode equals: 'c'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			
			"Testing value of monovalue relation callee"
			self assert: self topEntity callee isNotNil.

				stack push: self topEntity callee.
				self assert: self topEntity sourceCode equals: 'fun'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			stack pop.
		
			stack push: (stack top statements at: 2).
			self assert: self topEntity sourceCode equals: 'break'.
			self assert: (self topEntity isOfType: FASTPyBreakStatement).
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation left"
	self assert: self topEntity left isNotNil.

		stack push: self topEntity left.
		self assert: self topEntity sourceCode equals: 'c'.
		self assert: (self topEntity isOfType: FASTPyIdentifier).
		stack pop.
	
	"Testing value of monovalue relation right"
	self assert: self topEntity right isNotNil.

		stack push: self topEntity right.
		self assert: self topEntity sourceCode equals: '[1, 3, 5]'.
		self assert: (self topEntity isOfType: FASTPyList).
		
		"Testing value of multivalued relation initializers"
		self assert: self topEntity initializers size equals: 3.
		
			stack push: (stack top initializers at: 1).
			self assert: self topEntity sourceCode equals: '1'.
			self assert: (self topEntity isOfType: FASTPyInteger).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTNumericalLiteral).
			stack pop.
		
			stack push: (stack top initializers at: 2).
			self assert: self topEntity sourceCode equals: '3'.
			self assert: (self topEntity isOfType: FASTPyInteger).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTNumericalLiteral).
			stack pop.
		
			stack push: (stack top initializers at: 3).
			self assert: self topEntity sourceCode equals: '5'.
			self assert: (self topEntity isOfType: FASTPyInteger).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTNumericalLiteral)
]

{ #category : 'tests - advanced statements' }
FASTPythonImporterTest >> testForStatementWithAContinue [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testForStatementWithAContinue 	"

	self parse: 'for c in [1, 3, 5]:
    fun(c)
    continue'.

	self assert: (fast allWithType: FASTPyBlock) size equals: 1.
	self assert: (fast allWithType: FASTPyCall) size equals: 1.
	self assert: (fast allWithType: FASTPyContinueStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyForStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 3.
	self assert: (fast allWithType: FASTPyInteger) size equals: 3.
	self assert: (fast allWithType: FASTPyList) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'for c in [1, 3, 5]:
    fun(c)
    continue' withPlatformLineEndings.
	self assert: (self topEntity isOfType: FASTPyForStatement).
	self assert: (self topEntity isOfType: FASTPyTWithElseClause).
	self assert: (self topEntity isOfType: FASTTLoopStatement).
	
	"Testing value of monovalue relation body"
	self assert: self topEntity body isNotNil.

		stack push: self topEntity body.
		self assert: self topEntity sourceCode equals: 'fun(c)
    continue' withPlatformLineEndings.
		self assert: (self topEntity isOfType: FASTPyBlock).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 2.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'fun(c)'.
			self assert: (self topEntity isOfType: FASTPyCall).
			self assert: (self topEntity isOfType: FASTTInvocation).
			self assert: (self topEntity isOfType: FASTTWithArguments).
			
			"Testing value of multivalued relation arguments"
			self assert: self topEntity arguments size equals: 1.
			
				stack push: (stack top arguments at: 1).
				self assert: self topEntity sourceCode equals: 'c'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			
			"Testing value of monovalue relation callee"
			self assert: self topEntity callee isNotNil.

				stack push: self topEntity callee.
				self assert: self topEntity sourceCode equals: 'fun'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			stack pop.
		
			stack push: (stack top statements at: 2).
			self assert: self topEntity sourceCode equals: 'continue'.
			self assert: (self topEntity isOfType: FASTPyContinueStatement).
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation left"
	self assert: self topEntity left isNotNil.

		stack push: self topEntity left.
		self assert: self topEntity sourceCode equals: 'c'.
		self assert: (self topEntity isOfType: FASTPyIdentifier).
		stack pop.
	
	"Testing value of monovalue relation right"
	self assert: self topEntity right isNotNil.

		stack push: self topEntity right.
		self assert: self topEntity sourceCode equals: '[1, 3, 5]'.
		self assert: (self topEntity isOfType: FASTPyList).
		
		"Testing value of multivalued relation initializers"
		self assert: self topEntity initializers size equals: 3.
		
			stack push: (stack top initializers at: 1).
			self assert: self topEntity sourceCode equals: '1'.
			self assert: (self topEntity isOfType: FASTPyInteger).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTNumericalLiteral).
			stack pop.
		
			stack push: (stack top initializers at: 2).
			self assert: self topEntity sourceCode equals: '3'.
			self assert: (self topEntity isOfType: FASTPyInteger).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTNumericalLiteral).
			stack pop.
		
			stack push: (stack top initializers at: 3).
			self assert: self topEntity sourceCode equals: '5'.
			self assert: (self topEntity isOfType: FASTPyInteger).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTNumericalLiteral)
]

{ #category : 'tests - advanced statements' }
FASTPythonImporterTest >> testForStatementWithElseClause [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testForStatementWithElseClause 	"

	self parse: 'for c in [1, 3, 5]:
    fun(c)
else: print("Hello")'.

	self assert: (fast allWithType: FASTPyBlock) size equals: 1.
	self assert: (fast allWithType: FASTPyCall) size equals: 2.
	self assert: (fast allWithType: FASTPyElseClause) size equals: 1.
	self assert: (fast allWithType: FASTPyForStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 4.
	self assert: (fast allWithType: FASTPyInteger) size equals: 3.
	self assert: (fast allWithType: FASTPyList) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyString) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'for c in [1, 3, 5]:
    fun(c)
else: print("Hello")' withPlatformLineEndings.
	self assert: (self topEntity isOfType: FASTPyForStatement).
	self assert: (self topEntity isOfType: FASTPyTWithElseClause).
	self assert: (self topEntity isOfType: FASTTLoopStatement).
	
	"Testing value of monovalue relation body"
	self assert: self topEntity body isNotNil.

		stack push: self topEntity body.
		self assert: self topEntity sourceCode equals: 'fun(c)'.
		self assert: (self topEntity isOfType: FASTPyBlock).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'fun(c)'.
			self assert: (self topEntity isOfType: FASTPyCall).
			self assert: (self topEntity isOfType: FASTTInvocation).
			self assert: (self topEntity isOfType: FASTTWithArguments).
			
			"Testing value of multivalued relation arguments"
			self assert: self topEntity arguments size equals: 1.
			
				stack push: (stack top arguments at: 1).
				self assert: self topEntity sourceCode equals: 'c'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			
			"Testing value of monovalue relation callee"
			self assert: self topEntity callee isNotNil.

				stack push: self topEntity callee.
				self assert: self topEntity sourceCode equals: 'fun'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation elseClause"
	self assert: self topEntity elseClause isNotNil.

		stack push: self topEntity elseClause.
		self assert: self topEntity sourceCode equals: 'print("Hello")'.
		self assert: (self topEntity isOfType: FASTPyElseClause).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'print("Hello")'.
			self assert: (self topEntity isOfType: FASTPyCall).
			self assert: (self topEntity isOfType: FASTTInvocation).
			self assert: (self topEntity isOfType: FASTTWithArguments).
			
			"Testing value of multivalued relation arguments"
			self assert: self topEntity arguments size equals: 1.
			
				stack push: (stack top arguments at: 1).
				self assert: self topEntity sourceCode equals: '"Hello"'.
				self assert: (self topEntity isOfType: FASTPyString).
				self assert: (self topEntity isOfType: FASTTLiteral).
				self assert: (self topEntity isOfType: FASTTStringLiteral).
				stack pop.
			
			"Testing value of monovalue relation callee"
			self assert: self topEntity callee isNotNil.

				stack push: self topEntity callee.
				self assert: self topEntity sourceCode equals: 'print'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation left"
	self assert: self topEntity left isNotNil.

		stack push: self topEntity left.
		self assert: self topEntity sourceCode equals: 'c'.
		self assert: (self topEntity isOfType: FASTPyIdentifier).
		stack pop.
	
	"Testing value of monovalue relation right"
	self assert: self topEntity right isNotNil.

		stack push: self topEntity right.
		self assert: self topEntity sourceCode equals: '[1, 3, 5]'.
		self assert: (self topEntity isOfType: FASTPyList).
		
		"Testing value of multivalued relation initializers"
		self assert: self topEntity initializers size equals: 3.
		
			stack push: (stack top initializers at: 1).
			self assert: self topEntity sourceCode equals: '1'.
			self assert: (self topEntity isOfType: FASTPyInteger).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTNumericalLiteral).
			stack pop.
		
			stack push: (stack top initializers at: 2).
			self assert: self topEntity sourceCode equals: '3'.
			self assert: (self topEntity isOfType: FASTPyInteger).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTNumericalLiteral).
			stack pop.
		
			stack push: (stack top initializers at: 3).
			self assert: self topEntity sourceCode equals: '5'.
			self assert: (self topEntity isOfType: FASTPyInteger).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTNumericalLiteral)
]

{ #category : 'tests - advanced statements' }
FASTPythonImporterTest >> testForStatementWithForInClauseAndTuplePattern [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testForStatementWithForInClauseAndTuplePattern 	"

	self parse: 'for idx, (heights, widths) in data:
    print(idx, heights, widths)'.

	self assert: (fast allWithType: FASTPyBlock) size equals: 1.
	self assert: (fast allWithType: FASTPyCall) size equals: 1.
	self assert: (fast allWithType: FASTPyForStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 8.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyTuple) size equals: 2.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'for idx, (heights, widths) in data:
    print(idx, heights, widths)' withPlatformLineEndings.
	self assert: (self topEntity isOfType: FASTPyForStatement).
	self assert: (self topEntity isOfType: FASTPyTWithElseClause).
	self assert: (self topEntity isOfType: FASTTLoopStatement).
	
	"Testing value of monovalue relation body"
	self assert: self topEntity body isNotNil.

		stack push: self topEntity body.
		self assert: self topEntity sourceCode equals: 'print(idx, heights, widths)'.
		self assert: (self topEntity isOfType: FASTPyBlock).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'print(idx, heights, widths)'.
			self assert: (self topEntity isOfType: FASTPyCall).
			self assert: (self topEntity isOfType: FASTTInvocation).
			self assert: (self topEntity isOfType: FASTTWithArguments).
			
			"Testing value of multivalued relation arguments"
			self assert: self topEntity arguments size equals: 3.
			
				stack push: (stack top arguments at: 1).
				self assert: self topEntity sourceCode equals: 'idx'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			
				stack push: (stack top arguments at: 2).
				self assert: self topEntity sourceCode equals: 'heights'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			
				stack push: (stack top arguments at: 3).
				self assert: self topEntity sourceCode equals: 'widths'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			
			"Testing value of monovalue relation callee"
			self assert: self topEntity callee isNotNil.

				stack push: self topEntity callee.
				self assert: self topEntity sourceCode equals: 'print'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation left"
	self assert: self topEntity left isNotNil.

		stack push: self topEntity left.
		self assert: self topEntity sourceCode equals: 'idx, (heights, widths)'.
		self assert: (self topEntity isOfType: FASTPyTuple).
		
		"Testing value of multivalued relation initializers"
		self assert: self topEntity initializers size equals: 2.
		
			stack push: (stack top initializers at: 1).
			self assert: self topEntity sourceCode equals: 'idx'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		
			stack push: (stack top initializers at: 2).
			self assert: self topEntity sourceCode equals: '(heights, widths)'.
			self assert: (self topEntity isOfType: FASTPyTuple).
			
			"Testing value of multivalued relation initializers"
			self assert: self topEntity initializers size equals: 2.
			
				stack push: (stack top initializers at: 1).
				self assert: self topEntity sourceCode equals: 'heights'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			
				stack push: (stack top initializers at: 2).
				self assert: self topEntity sourceCode equals: 'widths'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation right"
	self assert: self topEntity right isNotNil.

		stack push: self topEntity right.
		self assert: self topEntity sourceCode equals: 'data'.
		self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - imports' }
FASTPythonImporterTest >> testFromImport [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testFromImport 	"

	self parse: 'from datetime import date'.

	self assert: (fast allWithType: FASTPyFromModule) size equals: 1.
	self assert: (fast allWithType: FASTPyImportFromStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyImportedEntity) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'from datetime import date'.
	self assert: (self topEntity isOfType: FASTPyImportFromStatement).
	
	"Testing value of monovalue relation fromModule"
	self assert: self topEntity fromModule isNotNil.

		stack push: self topEntity fromModule.
		self assert: self topEntity sourceCode equals: 'datetime'.
		self assert: (self topEntity isOfType: FASTPyFromModule).
		self assert: self topEntity segments equals: #('datetime').
		stack pop.
	
	"Testing value of multivalued relation importedEntities"
	self assert: self topEntity importedEntities size equals: 1.
	
		stack push: (stack top importedEntities at: 1).
		self assert: self topEntity sourceCode equals: 'date'.
		self assert: (self topEntity isOfType: FASTPyImportedEntity).
		self assert: self topEntity segments equals: #('date')
]

{ #category : 'tests - imports' }
FASTPythonImporterTest >> testFromImportMultiple [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testFromImportMultiple 	"

	self parse: 'from collections import Counter, defaultdic'.

	self assert: (fast allWithType: FASTPyFromModule) size equals: 1.
	self assert: (fast allWithType: FASTPyImportFromStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyImportedEntity) size equals: 2.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'from collections import Counter, defaultdic'.
	self assert: (self topEntity isOfType: FASTPyImportFromStatement).
	
	"Testing value of monovalue relation fromModule"
	self assert: self topEntity fromModule isNotNil.

		stack push: self topEntity fromModule.
		self assert: self topEntity sourceCode equals: 'collections'.
		self assert: (self topEntity isOfType: FASTPyFromModule).
		self assert: self topEntity segments equals: #('collections').
		stack pop.
	
	"Testing value of multivalued relation importedEntities"
	self assert: self topEntity importedEntities size equals: 2.
	
		stack push: (stack top importedEntities at: 1).
		self assert: self topEntity sourceCode equals: 'Counter'.
		self assert: (self topEntity isOfType: FASTPyImportedEntity).
		self assert: self topEntity segments equals: #('Counter').
		stack pop.
	
		stack push: (stack top importedEntities at: 2).
		self assert: self topEntity sourceCode equals: 'defaultdic'.
		self assert: (self topEntity isOfType: FASTPyImportedEntity).
		self assert: self topEntity segments equals: #('defaultdic')
]

{ #category : 'tests - imports' }
FASTPythonImporterTest >> testFromImportMultipleWithAlias [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testFromImportMultipleWithAlias 	"

	self parse: 'from math import sin, cos as cosinus, pi'.

	self assert: (fast allWithType: FASTPyFromModule) size equals: 1.
	self assert: (fast allWithType: FASTPyImportFromStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyImportedEntity) size equals: 3.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'from math import sin, cos as cosinus, pi'.
	self assert: (self topEntity isOfType: FASTPyImportFromStatement).
	
	"Testing value of monovalue relation fromModule"
	self assert: self topEntity fromModule isNotNil.

		stack push: self topEntity fromModule.
		self assert: self topEntity sourceCode equals: 'math'.
		self assert: (self topEntity isOfType: FASTPyFromModule).
		self assert: self topEntity segments equals: #('math').
		stack pop.
	
	"Testing value of multivalued relation importedEntities"
	self assert: self topEntity importedEntities size equals: 3.
	
		stack push: (stack top importedEntities at: 1).
		self assert: self topEntity sourceCode equals: 'sin'.
		self assert: (self topEntity isOfType: FASTPyImportedEntity).
		self assert: self topEntity segments equals: #('sin').
		stack pop.
	
		stack push: (stack top importedEntities at: 2).
		self assert: self topEntity sourceCode equals: 'cos as cosinus'.
		self assert: (self topEntity isOfType: FASTPyImportedEntity).
		self assert: self topEntity alias equals: 'cosinus'.
		self assert: self topEntity segments equals: #('cos').
		stack pop.
	
		stack push: (stack top importedEntities at: 3).
		self assert: self topEntity sourceCode equals: 'pi'.
		self assert: (self topEntity isOfType: FASTPyImportedEntity).
		self assert: self topEntity segments equals: #('pi')
]

{ #category : 'tests - imports' }
FASTPythonImporterTest >> testFromImportMultipleWithParenthesis [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testFromImportMultipleWithParenthesis 	"

	self parse: 'from math import (count, cycle, repeat,)'.

	self assert: (fast allWithType: FASTPyFromModule) size equals: 1.
	self assert: (fast allWithType: FASTPyImportFromStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyImportedEntity) size equals: 3.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'from math import (count, cycle, repeat,)'.
	self assert: (self topEntity isOfType: FASTPyImportFromStatement).
	
	"Testing value of monovalue relation fromModule"
	self assert: self topEntity fromModule isNotNil.

		stack push: self topEntity fromModule.
		self assert: self topEntity sourceCode equals: 'math'.
		self assert: (self topEntity isOfType: FASTPyFromModule).
		self assert: self topEntity segments equals: #('math').
		stack pop.
	
	"Testing value of multivalued relation importedEntities"
	self assert: self topEntity importedEntities size equals: 3.
	
		stack push: (stack top importedEntities at: 1).
		self assert: self topEntity sourceCode equals: 'count'.
		self assert: (self topEntity isOfType: FASTPyImportedEntity).
		self assert: self topEntity segments equals: #('count').
		stack pop.
	
		stack push: (stack top importedEntities at: 2).
		self assert: self topEntity sourceCode equals: 'cycle'.
		self assert: (self topEntity isOfType: FASTPyImportedEntity).
		self assert: self topEntity segments equals: #('cycle').
		stack pop.
	
		stack push: (stack top importedEntities at: 3).
		self assert: self topEntity sourceCode equals: 'repeat'.
		self assert: (self topEntity isOfType: FASTPyImportedEntity).
		self assert: self topEntity segments equals: #('repeat')
]

{ #category : 'tests - imports' }
FASTPythonImporterTest >> testFromImportMultipleWithParenthesisAndAlias [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testFromImportMultipleWithParenthesisAndAlias 	"

	self parse: 'from math import (count, cycle as c, repeat,)'.

	self assert: (fast allWithType: FASTPyFromModule) size equals: 1.
	self assert: (fast allWithType: FASTPyImportFromStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyImportedEntity) size equals: 3.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'from math import (count, cycle as c, repeat,)'.
	self assert: (self topEntity isOfType: FASTPyImportFromStatement).
	
	"Testing value of monovalue relation fromModule"
	self assert: self topEntity fromModule isNotNil.

		stack push: self topEntity fromModule.
		self assert: self topEntity sourceCode equals: 'math'.
		self assert: (self topEntity isOfType: FASTPyFromModule).
		self assert: self topEntity segments equals: #('math').
		stack pop.
	
	"Testing value of multivalued relation importedEntities"
	self assert: self topEntity importedEntities size equals: 3.
	
		stack push: (stack top importedEntities at: 1).
		self assert: self topEntity sourceCode equals: 'count'.
		self assert: (self topEntity isOfType: FASTPyImportedEntity).
		self assert: self topEntity segments equals: #('count').
		stack pop.
	
		stack push: (stack top importedEntities at: 2).
		self assert: self topEntity sourceCode equals: 'cycle as c'.
		self assert: (self topEntity isOfType: FASTPyImportedEntity).
		self assert: self topEntity alias equals: 'c'.
		self assert: self topEntity segments equals: #('cycle').
		stack pop.
	
		stack push: (stack top importedEntities at: 3).
		self assert: self topEntity sourceCode equals: 'repeat'.
		self assert: (self topEntity isOfType: FASTPyImportedEntity).
		self assert: self topEntity segments equals: #('repeat')
]

{ #category : 'tests - imports' }
FASTPythonImporterTest >> testFromImportRelative [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testFromImportRelative 	"

	self parse: 'from ..utils.extra import module'.

	self assert: (fast allWithType: FASTPyFromModule) size equals: 1.
	self assert: (fast allWithType: FASTPyImportFromStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyImportedEntity) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'from ..utils.extra import module'.
	self assert: (self topEntity isOfType: FASTPyImportFromStatement).
	
	"Testing value of monovalue relation fromModule"
	self assert: self topEntity fromModule isNotNil.

		stack push: self topEntity fromModule.
		self assert: self topEntity sourceCode equals: '..'.
		self assert: (self topEntity isOfType: FASTPyFromModule).
		self assert: self topEntity segments equals: #('..' 'utils' 'extra').
		stack pop.
	
	"Testing value of multivalued relation importedEntities"
	self assert: self topEntity importedEntities size equals: 1.
	
		stack push: (stack top importedEntities at: 1).
		self assert: self topEntity sourceCode equals: 'module'.
		self assert: (self topEntity isOfType: FASTPyImportedEntity).
		self assert: self topEntity segments equals: #('module')
]

{ #category : 'tests - imports' }
FASTPythonImporterTest >> testFromImportRelativeWithAlias [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testFromImportRelativeWithAlias 	"

	self parse: 'from ..utils import module as mod'.

	self assert: (fast allWithType: FASTPyFromModule) size equals: 1.
	self assert: (fast allWithType: FASTPyImportFromStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyImportedEntity) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'from ..utils import module as mod'.
	self assert: (self topEntity isOfType: FASTPyImportFromStatement).
	
	"Testing value of monovalue relation fromModule"
	self assert: self topEntity fromModule isNotNil.

		stack push: self topEntity fromModule.
		self assert: self topEntity sourceCode equals: '..'.
		self assert: (self topEntity isOfType: FASTPyFromModule).
		self assert: self topEntity segments equals: #('..' 'utils').
		stack pop.
	
	"Testing value of multivalued relation importedEntities"
	self assert: self topEntity importedEntities size equals: 1.
	
		stack push: (stack top importedEntities at: 1).
		self assert: self topEntity sourceCode equals: 'module as mod'.
		self assert: (self topEntity isOfType: FASTPyImportedEntity).
		self assert: self topEntity alias equals: 'mod'.
		self assert: self topEntity segments equals: #('module')
]

{ #category : 'tests - imports' }
FASTPythonImporterTest >> testFromImportRelativeWithDoubleDot [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testFromImportRelativeWithDoubleDot 	"

	self parse: 'from .. import module'.

	self assert: (fast allWithType: FASTPyFromModule) size equals: 1.
	self assert: (fast allWithType: FASTPyImportFromStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyImportedEntity) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'from .. import module'.
	self assert: (self topEntity isOfType: FASTPyImportFromStatement).
	
	"Testing value of monovalue relation fromModule"
	self assert: self topEntity fromModule isNotNil.

		stack push: self topEntity fromModule.
		self assert: self topEntity sourceCode equals: '..'.
		self assert: (self topEntity isOfType: FASTPyFromModule).
		self assert: self topEntity segments equals: #('..').
		stack pop.
	
	"Testing value of multivalued relation importedEntities"
	self assert: self topEntity importedEntities size equals: 1.
	
		stack push: (stack top importedEntities at: 1).
		self assert: self topEntity sourceCode equals: 'module'.
		self assert: (self topEntity isOfType: FASTPyImportedEntity).
		self assert: self topEntity segments equals: #('module')
]

{ #category : 'tests - imports' }
FASTPythonImporterTest >> testFromImportRelativeWithOnlyOneDot [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testFromImportRelativeWithOnlyOneDot 	"

	self parse: 'from . import module'.

	self assert: (fast allWithType: FASTPyFromModule) size equals: 1.
	self assert: (fast allWithType: FASTPyImportFromStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyImportedEntity) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'from . import module'.
	self assert: (self topEntity isOfType: FASTPyImportFromStatement).
	
	"Testing value of monovalue relation fromModule"
	self assert: self topEntity fromModule isNotNil.

		stack push: self topEntity fromModule.
		self assert: self topEntity sourceCode equals: '.'.
		self assert: (self topEntity isOfType: FASTPyFromModule).
		self assert: self topEntity segments equals: #('.').
		stack pop.
	
	"Testing value of multivalued relation importedEntities"
	self assert: self topEntity importedEntities size equals: 1.
	
		stack push: (stack top importedEntities at: 1).
		self assert: self topEntity sourceCode equals: 'module'.
		self assert: (self topEntity isOfType: FASTPyImportedEntity).
		self assert: self topEntity segments equals: #('module')
]

{ #category : 'tests - imports' }
FASTPythonImporterTest >> testFromImportWithAlias [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testFromImportWithAlias 	"

	self parse: 'from datetime import date as d'.

	self assert: (fast allWithType: FASTPyFromModule) size equals: 1.
	self assert: (fast allWithType: FASTPyImportFromStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyImportedEntity) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'from datetime import date as d'.
	self assert: (self topEntity isOfType: FASTPyImportFromStatement).
	
	"Testing value of monovalue relation fromModule"
	self assert: self topEntity fromModule isNotNil.

		stack push: self topEntity fromModule.
		self assert: self topEntity sourceCode equals: 'datetime'.
		self assert: (self topEntity isOfType: FASTPyFromModule).
		self assert: self topEntity segments equals: #('datetime').
		stack pop.
	
	"Testing value of multivalued relation importedEntities"
	self assert: self topEntity importedEntities size equals: 1.
	
		stack push: (stack top importedEntities at: 1).
		self assert: self topEntity sourceCode equals: 'date as d'.
		self assert: (self topEntity isOfType: FASTPyImportedEntity).
		self assert: self topEntity alias equals: 'd'.
		self assert: self topEntity segments equals: #('date')
]

{ #category : 'tests - imports' }
FASTPythonImporterTest >> testFromImportWithWildcard [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testFromImportWithWildcard 	"

	self parse: 'from math import *'.

	self assert: (fast allWithType: FASTPyFromModule) size equals: 1.
	self assert: (fast allWithType: FASTPyImportFromStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyImportedEntity) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'from math import *'.
	self assert: (self topEntity isOfType: FASTPyImportFromStatement).
	
	"Testing value of monovalue relation fromModule"
	self assert: self topEntity fromModule isNotNil.

		stack push: self topEntity fromModule.
		self assert: self topEntity sourceCode equals: 'math'.
		self assert: (self topEntity isOfType: FASTPyFromModule).
		self assert: self topEntity segments equals: #('math').
		stack pop.
	
	"Testing value of multivalued relation importedEntities"
	self assert: self topEntity importedEntities size equals: 1.
	
		stack push: (stack top importedEntities at: 1).
		self assert: self topEntity sourceCode equals: '*'.
		self assert: (self topEntity isOfType: FASTPyImportedEntity).
		self assert: self topEntity segments equals: #('*')
]

{ #category : 'tests - function definitions' }
FASTPythonImporterTest >> testFunctionDefinition [

	| function |
	self parse: 'def test():
	return 1'.

	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyFunctionDefinition) size equals: 1.

	function := (fast allWithType: FASTPyFunctionDefinition) anyOne.
	self assert: function name equals: 'test'
]

{ #category : 'tests - parameters' }
FASTPythonImporterTest >> testFunctionParameters [

	| function |
	self parse: 'def add(a, b):
	return a + b'.

	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyFunctionDefinition) size equals: 1.
	function := (fast allWithType: FASTPyFunctionDefinition) anyOne.

	self assert: (fast allWithType: FASTPyParameter) size equals: 2.
	self assertCollection: (fast allWithType: FASTPyParameter) hasSameElements: function parameters.
	function parameters do: [ :parameter | self assert: (parameter isOfType: FASTPyParameter) ].
	self assertCollection: (function parameters collect: #name) hasSameElements: #( 'a' 'b' )
]

{ #category : 'tests - imports' }
FASTPythonImporterTest >> testFutureImport [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testFutureImport 	"

	self parse: 'from __future__ import annotiations as ann, division'.

	self assert: (fast allWithType: FASTPyFromModule) size equals: 1.
	self assert: (fast allWithType: FASTPyImportFromStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyImportedEntity) size equals: 2.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'from __future__ import annotiations as ann, division'.
	self assert: (self topEntity isOfType: FASTPyImportFromStatement).
	
	"Testing value of monovalue relation fromModule"
	self assert: self topEntity fromModule isNotNil.

		stack push: self topEntity fromModule.
		self assert: self topEntity sourceCode equals: 'from __future__ import annotiations as ann, division'.
		self assert: (self topEntity isOfType: FASTPyFromModule).
		self assert: self topEntity segments equals: #('__future__').
		stack pop.
	
	"Testing value of multivalued relation importedEntities"
	self assert: self topEntity importedEntities size equals: 2.
	
		stack push: (stack top importedEntities at: 1).
		self assert: self topEntity sourceCode equals: 'annotiations as ann'.
		self assert: (self topEntity isOfType: FASTPyImportedEntity).
		self assert: self topEntity alias equals: 'ann'.
		self assert: self topEntity segments equals: #('annotiations').
		stack pop.
	
		stack push: (stack top importedEntities at: 2).
		self assert: self topEntity sourceCode equals: 'division'.
		self assert: (self topEntity isOfType: FASTPyImportedEntity).
		self assert: self topEntity segments equals: #('division')
]

{ #category : 'tests - comprehension' }
FASTPythonImporterTest >> testGeneratorExpression [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testGeneratorExpression 	"

	self parse: '(x * x for x in range(10))'.

	self assert: (fast allWithType: FASTPyBinaryOperator) size equals: 1.
	self assert: (fast allWithType: FASTPyCall) size equals: 1.
	self assert: (fast allWithType: FASTPyForInClause) size equals: 1.
	self assert: (fast allWithType: FASTPyGeneratorExpression) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 4.
	self assert: (fast allWithType: FASTPyInteger) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: '(x * x for x in range(10))'.
	self assert: (self topEntity isOfType: FASTPyGeneratorExpression).
	self assert: (self topEntity isOfType: FASTPyTComprehension).
	self assert: (self topEntity isOfType: FASTTWithCondition).
	
	"Testing value of monovalue relation body"
	self assert: self topEntity body isNotNil.

		stack push: self topEntity body.
		self assert: self topEntity sourceCode equals: 'x * x'.
		self assert: (self topEntity isOfType: FASTPyBinaryOperator).
		self assert: (self topEntity isOfType: FASTTBinaryExpression).
		
		"Testing value of monovalue relation leftOperand"
		self assert: self topEntity leftOperand isNotNil.

			stack push: self topEntity leftOperand.
			self assert: self topEntity sourceCode equals: 'x'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		self assert: self topEntity operator equals: '*'.
		
		"Testing value of monovalue relation rightOperand"
		self assert: self topEntity rightOperand isNotNil.

			stack push: self topEntity rightOperand.
			self assert: self topEntity sourceCode equals: 'x'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		stack pop.
	
	"Testing value of multivalued relation clauses"
	self assert: self topEntity clauses size equals: 1.
	
		stack push: (stack top clauses at: 1).
		self assert: self topEntity sourceCode equals: 'for x in range(10)'.
		self assert: (self topEntity isOfType: FASTPyForInClause).
		
		"Testing value of monovalue relation left"
		self assert: self topEntity left isNotNil.

			stack push: self topEntity left.
			self assert: self topEntity sourceCode equals: 'x'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		
		"Testing value of monovalue relation right"
		self assert: self topEntity right isNotNil.

			stack push: self topEntity right.
			self assert: self topEntity sourceCode equals: 'range(10)'.
			self assert: (self topEntity isOfType: FASTPyCall).
			self assert: (self topEntity isOfType: FASTTInvocation).
			self assert: (self topEntity isOfType: FASTTWithArguments).
			
			"Testing value of multivalued relation arguments"
			self assert: self topEntity arguments size equals: 1.
			
				stack push: (stack top arguments at: 1).
				self assert: self topEntity sourceCode equals: '10'.
				self assert: (self topEntity isOfType: FASTPyInteger).
				self assert: (self topEntity isOfType: FASTTLiteral).
				self assert: (self topEntity isOfType: FASTTNumericalLiteral).
				stack pop.
			
			"Testing value of monovalue relation callee"
			self assert: self topEntity callee isNotNil.

				stack push: self topEntity callee.
				self assert: self topEntity sourceCode equals: 'range'.
				self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - types' }
FASTPythonImporterTest >> testGenericType [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testGenericType 	"

	self parse: 'def f(items: List[T]): pass'.

	self assert: (fast allWithType: FASTPyBlock) size equals: 1.
	self assert: (fast allWithType: FASTPyFunctionDefinition) size equals: 1.
	self assert: (fast allWithType: FASTPyGenericType) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyParameter) size equals: 1.
	self assert: (fast allWithType: FASTPyPassStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyType) size equals: 2.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'def f(items: List[T]): pass'.
	self assert: (self topEntity isOfType: FASTPyFunctionDefinition).
	self assert: (self topEntity isOfType: FASTPyTDefinition).
	self assert: (self topEntity isOfType: FASTTWithParameters).
	self assert: (self topEntity isOfType: FASTTWithStatements).
	self assert: self topEntity name equals: 'f'.
	
	"Testing value of multivalued relation parameters"
	self assert: self topEntity parameters size equals: 1.
	
		stack push: (stack top parameters at: 1).
		self assert: self topEntity sourceCode equals: 'items: List[T]'.
		self assert: (self topEntity isOfType: FASTPyParameter).
		self assert: (self topEntity isOfType: FASTTVariableEntity).
		self assert: self topEntity name equals: 'items'.
		
		"Testing value of monovalue relation type"
		self assert: self topEntity type isNotNil.

			stack push: self topEntity type.
			self assert: self topEntity sourceCode equals: 'List[T]'.
			self assert: (self topEntity isOfType: FASTPyType).
			
			"Testing value of monovalue relation content"
			self assert: self topEntity content isNotNil.

				stack push: self topEntity content.
				self assert: self topEntity sourceCode equals: 'List[T]'.
				self assert: (self topEntity isOfType: FASTPyGenericType).
				self assert: self topEntity name equals: 'List'.
				
				"Testing value of multivalued relation typeParameters"
				self assert: self topEntity typeParameters size equals: 1.
				
					stack push: (stack top typeParameters at: 1).
					self assert: self topEntity sourceCode equals: 'T'.
					self assert: (self topEntity isOfType: FASTPyType).
					
					"Testing value of monovalue relation content"
					self assert: self topEntity content isNotNil.

						stack push: self topEntity content.
						self assert: self topEntity sourceCode equals: 'T'.
						self assert: (self topEntity isOfType: FASTPyIdentifier).
						stack pop.
					stack pop.
				stack pop.
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation statementBlock"
	self assert: self topEntity statementBlock isNotNil.

		stack push: self topEntity statementBlock.
		self assert: self topEntity sourceCode equals: 'pass'.
		self assert: (self topEntity isOfType: FASTPyBlock).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'pass'.
			self assert: (self topEntity isOfType: FASTPyPassStatement)
]

{ #category : 'tests - types' }
FASTPythonImporterTest >> testGenericTypeWithMultipleParameters [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testGenericTypeWithMultipleParameters 	"

	self parse: 'def f(items: Dictionary[str, T]): pass'.

	self assert: (fast allWithType: FASTPyBlock) size equals: 1.
	self assert: (fast allWithType: FASTPyFunctionDefinition) size equals: 1.
	self assert: (fast allWithType: FASTPyGenericType) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 2.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyParameter) size equals: 1.
	self assert: (fast allWithType: FASTPyPassStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyType) size equals: 3.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'def f(items: Dictionary[str, T]): pass'.
	self assert: (self topEntity isOfType: FASTPyFunctionDefinition).
	self assert: (self topEntity isOfType: FASTPyTDefinition).
	self assert: (self topEntity isOfType: FASTTWithParameters).
	self assert: (self topEntity isOfType: FASTTWithStatements).
	self assert: self topEntity name equals: 'f'.
	
	"Testing value of multivalued relation parameters"
	self assert: self topEntity parameters size equals: 1.
	
		stack push: (stack top parameters at: 1).
		self assert: self topEntity sourceCode equals: 'items: Dictionary[str, T]'.
		self assert: (self topEntity isOfType: FASTPyParameter).
		self assert: (self topEntity isOfType: FASTTVariableEntity).
		self assert: self topEntity name equals: 'items'.
		
		"Testing value of monovalue relation type"
		self assert: self topEntity type isNotNil.

			stack push: self topEntity type.
			self assert: self topEntity sourceCode equals: 'Dictionary[str, T]'.
			self assert: (self topEntity isOfType: FASTPyType).
			
			"Testing value of monovalue relation content"
			self assert: self topEntity content isNotNil.

				stack push: self topEntity content.
				self assert: self topEntity sourceCode equals: 'Dictionary[str, T]'.
				self assert: (self topEntity isOfType: FASTPyGenericType).
				self assert: self topEntity name equals: 'Dictionary'.
				
				"Testing value of multivalued relation typeParameters"
				self assert: self topEntity typeParameters size equals: 2.
				
					stack push: (stack top typeParameters at: 1).
					self assert: self topEntity sourceCode equals: 'str'.
					self assert: (self topEntity isOfType: FASTPyType).
					
					"Testing value of monovalue relation content"
					self assert: self topEntity content isNotNil.

						stack push: self topEntity content.
						self assert: self topEntity sourceCode equals: 'str'.
						self assert: (self topEntity isOfType: FASTPyIdentifier).
						stack pop.
					stack pop.
				
					stack push: (stack top typeParameters at: 2).
					self assert: self topEntity sourceCode equals: 'T'.
					self assert: (self topEntity isOfType: FASTPyType).
					
					"Testing value of monovalue relation content"
					self assert: self topEntity content isNotNil.

						stack push: self topEntity content.
						self assert: self topEntity sourceCode equals: 'T'.
						self assert: (self topEntity isOfType: FASTPyIdentifier).
						stack pop.
					stack pop.
				stack pop.
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation statementBlock"
	self assert: self topEntity statementBlock isNotNil.

		stack push: self topEntity statementBlock.
		self assert: self topEntity sourceCode equals: 'pass'.
		self assert: (self topEntity isOfType: FASTPyBlock).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'pass'.
			self assert: (self topEntity isOfType: FASTPyPassStatement)
]

{ #category : 'tests - statements' }
FASTPythonImporterTest >> testGlobalStatement [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testGlobalStatement 	"

	self parse: 'def aFunction():
	global counter
	return counter'.

	self assert: (fast allWithType: FASTPyBlock) size equals: 1.
	self assert: (fast allWithType: FASTPyFunctionDefinition) size equals: 1.
	self assert: (fast allWithType: FASTPyGlobalStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyReturnStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyVariable) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'def aFunction():
	global counter
	return counter' withPlatformLineEndings.
	self assert: (self topEntity isOfType: FASTPyFunctionDefinition).
	self assert: (self topEntity isOfType: FASTPyTDefinition).
	self assert: (self topEntity isOfType: FASTTWithParameters).
	self assert: (self topEntity isOfType: FASTTWithStatements).
	self assert: self topEntity name equals: 'aFunction'.
	
	"Testing value of monovalue relation statementBlock"
	self assert: self topEntity statementBlock isNotNil.

		stack push: self topEntity statementBlock.
		self assert: self topEntity sourceCode equals: 'global counter
	return counter' withPlatformLineEndings.
		self assert: (self topEntity isOfType: FASTPyBlock).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 2.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'global counter'.
			self assert: (self topEntity isOfType: FASTPyGlobalStatement).
			
			"Testing value of multivalued relation variables"
			self assert: self topEntity variables size equals: 1.
			
				stack push: (stack top variables at: 1).
				self assert: self topEntity sourceCode equals: 'counter'.
				self assert: (self topEntity isOfType: FASTPyVariable).
				self assert: (self topEntity isOfType: FASTTVariableEntity).
				stack pop.
			stack pop.
		
			stack push: (stack top statements at: 2).
			self assert: self topEntity sourceCode equals: 'return counter'.
			self assert: (self topEntity isOfType: FASTPyReturnStatement).
			self assert: (self topEntity isOfType: FASTTReturnStatement).
			
			"Testing value of monovalue relation expression"
			self assert: self topEntity expression isNotNil.

				stack push: self topEntity expression.
				self assert: self topEntity sourceCode equals: 'counter'.
				self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - statements' }
FASTPythonImporterTest >> testGlobalStatementMultiple [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testGlobalStatementMultiple 	"

	self parse: 'def function ():
	global x, y, z
	return (x, y, z)'.

	self assert: (fast allWithType: FASTPyBlock) size equals: 1.
	self assert: (fast allWithType: FASTPyFunctionDefinition) size equals: 1.
	self assert: (fast allWithType: FASTPyGlobalStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 3.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyReturnStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyTuple) size equals: 1.
	self assert: (fast allWithType: FASTPyVariable) size equals: 3.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'def function ():
	global x, y, z
	return (x, y, z)' withPlatformLineEndings.
	self assert: (self topEntity isOfType: FASTPyFunctionDefinition).
	self assert: (self topEntity isOfType: FASTPyTDefinition).
	self assert: (self topEntity isOfType: FASTTWithParameters).
	self assert: (self topEntity isOfType: FASTTWithStatements).
	self assert: self topEntity name equals: 'function'.
	
	"Testing value of monovalue relation statementBlock"
	self assert: self topEntity statementBlock isNotNil.

		stack push: self topEntity statementBlock.
		self assert: self topEntity sourceCode equals: 'global x, y, z
	return (x, y, z)' withPlatformLineEndings.
		self assert: (self topEntity isOfType: FASTPyBlock).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 2.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'global x, y, z'.
			self assert: (self topEntity isOfType: FASTPyGlobalStatement).
			
			"Testing value of multivalued relation variables"
			self assert: self topEntity variables size equals: 3.
			
				stack push: (stack top variables at: 1).
				self assert: self topEntity sourceCode equals: 'x'.
				self assert: (self topEntity isOfType: FASTPyVariable).
				self assert: (self topEntity isOfType: FASTTVariableEntity).
				stack pop.
			
				stack push: (stack top variables at: 2).
				self assert: self topEntity sourceCode equals: 'y'.
				self assert: (self topEntity isOfType: FASTPyVariable).
				self assert: (self topEntity isOfType: FASTTVariableEntity).
				stack pop.
			
				stack push: (stack top variables at: 3).
				self assert: self topEntity sourceCode equals: 'z'.
				self assert: (self topEntity isOfType: FASTPyVariable).
				self assert: (self topEntity isOfType: FASTTVariableEntity).
				stack pop.
			stack pop.
		
			stack push: (stack top statements at: 2).
			self assert: self topEntity sourceCode equals: 'return (x, y, z)'.
			self assert: (self topEntity isOfType: FASTPyReturnStatement).
			self assert: (self topEntity isOfType: FASTTReturnStatement).
			
			"Testing value of monovalue relation expression"
			self assert: self topEntity expression isNotNil.

				stack push: self topEntity expression.
				self assert: self topEntity sourceCode equals: '(x, y, z)'.
				self assert: (self topEntity isOfType: FASTPyTuple).
				
				"Testing value of multivalued relation initializers"
				self assert: self topEntity initializers size equals: 3.
				
					stack push: (stack top initializers at: 1).
					self assert: self topEntity sourceCode equals: 'x'.
					self assert: (self topEntity isOfType: FASTPyIdentifier).
					stack pop.
				
					stack push: (stack top initializers at: 2).
					self assert: self topEntity sourceCode equals: 'y'.
					self assert: (self topEntity isOfType: FASTPyIdentifier).
					stack pop.
				
					stack push: (stack top initializers at: 3).
					self assert: self topEntity sourceCode equals: 'z'.
					self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - advanced statements' }
FASTPythonImporterTest >> testIf [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testIf 	"

	self parse: 'if x > 0:
    3'.

	self assert: (fast allWithType: FASTPyComparisonOperator) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyIfStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyInteger) size equals: 2.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyThenClause) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'if x > 0:
    3' withPlatformLineEndings.
	self assert: (self topEntity isOfType: FASTPyIfStatement).
	self assert: (self topEntity isOfType: FASTPyTWithElseClause).
	self assert: (self topEntity isOfType: FASTTConditionalStatement).
	self assert: (self topEntity isOfType: FASTTWithCondition).
	
	"Testing value of monovalue relation condition"
	self assert: self topEntity condition isNotNil.

		stack push: self topEntity condition.
		self assert: self topEntity sourceCode equals: 'x > 0'.
		self assert: (self topEntity isOfType: FASTPyComparisonOperator).
		
		"Testing value of multivalued relation operands"
		self assert: self topEntity operands size equals: 2.
		
			stack push: (stack top operands at: 1).
			self assert: self topEntity sourceCode equals: 'x'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		
			stack push: (stack top operands at: 2).
			self assert: self topEntity sourceCode equals: '0'.
			self assert: (self topEntity isOfType: FASTPyInteger).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTNumericalLiteral).
			stack pop.
		self assert: self topEntity operators equals: #('>').
		stack pop.
	
	"Testing value of monovalue relation thenClause"
	self assert: self topEntity thenClause isNotNil.

		stack push: self topEntity thenClause.
		self assert: self topEntity sourceCode equals: '3'.
		self assert: (self topEntity isOfType: FASTPyThenClause).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: '3'.
			self assert: (self topEntity isOfType: FASTPyInteger).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTNumericalLiteral)
]

{ #category : 'tests - advanced statements' }
FASTPythonImporterTest >> testIfElse [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testIfElse 	"

	self parse: 'if x > 0:
    True
else:
	False'.

	self assert: (fast allWithType: FASTPyBoolean) size equals: 2.
	self assert: (fast allWithType: FASTPyComparisonOperator) size equals: 1.
	self assert: (fast allWithType: FASTPyElseClause) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyIfStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyInteger) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyThenClause) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'if x > 0:
    True
else:
	False' withPlatformLineEndings.
	self assert: (self topEntity isOfType: FASTPyIfStatement).
	self assert: (self topEntity isOfType: FASTPyTWithElseClause).
	self assert: (self topEntity isOfType: FASTTConditionalStatement).
	self assert: (self topEntity isOfType: FASTTWithCondition).
	
	"Testing value of monovalue relation condition"
	self assert: self topEntity condition isNotNil.

		stack push: self topEntity condition.
		self assert: self topEntity sourceCode equals: 'x > 0'.
		self assert: (self topEntity isOfType: FASTPyComparisonOperator).
		
		"Testing value of multivalued relation operands"
		self assert: self topEntity operands size equals: 2.
		
			stack push: (stack top operands at: 1).
			self assert: self topEntity sourceCode equals: 'x'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		
			stack push: (stack top operands at: 2).
			self assert: self topEntity sourceCode equals: '0'.
			self assert: (self topEntity isOfType: FASTPyInteger).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTNumericalLiteral).
			stack pop.
		self assert: self topEntity operators equals: #('>').
		stack pop.
	
	"Testing value of monovalue relation elseClause"
	self assert: self topEntity elseClause isNotNil.

		stack push: self topEntity elseClause.
		self assert: self topEntity sourceCode equals: 'False'.
		self assert: (self topEntity isOfType: FASTPyElseClause).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'False'.
			self assert: (self topEntity isOfType: FASTPyBoolean).
			self assert: (self topEntity isOfType: FASTTBooleanLiteral).
			self assert: (self topEntity isOfType: FASTTLiteral).
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation thenClause"
	self assert: self topEntity thenClause isNotNil.

		stack push: self topEntity thenClause.
		self assert: self topEntity sourceCode equals: 'True'.
		self assert: (self topEntity isOfType: FASTPyThenClause).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'True'.
			self assert: (self topEntity isOfType: FASTPyBoolean).
			self assert: (self topEntity isOfType: FASTTBooleanLiteral).
			self assert: (self topEntity isOfType: FASTTLiteral)
]

{ #category : 'tests - advanced statements' }
FASTPythonImporterTest >> testIfElseIf [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testIfElseIf 	"

	self parse: 'if x > 0:
    True
elif x < 10:
	False'.

	self assert: (fast allWithType: FASTPyBoolean) size equals: 2.
	self assert: (fast allWithType: FASTPyComparisonOperator) size equals: 2.
	self assert: (fast allWithType: FASTPyElifClause) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 2.
	self assert: (fast allWithType: FASTPyIfStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyInteger) size equals: 2.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyThenClause) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'if x > 0:
    True
elif x < 10:
	False' withPlatformLineEndings.
	self assert: (self topEntity isOfType: FASTPyIfStatement).
	self assert: (self topEntity isOfType: FASTPyTWithElseClause).
	self assert: (self topEntity isOfType: FASTTConditionalStatement).
	self assert: (self topEntity isOfType: FASTTWithCondition).
	
	"Testing value of monovalue relation condition"
	self assert: self topEntity condition isNotNil.

		stack push: self topEntity condition.
		self assert: self topEntity sourceCode equals: 'x > 0'.
		self assert: (self topEntity isOfType: FASTPyComparisonOperator).
		
		"Testing value of multivalued relation operands"
		self assert: self topEntity operands size equals: 2.
		
			stack push: (stack top operands at: 1).
			self assert: self topEntity sourceCode equals: 'x'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		
			stack push: (stack top operands at: 2).
			self assert: self topEntity sourceCode equals: '0'.
			self assert: (self topEntity isOfType: FASTPyInteger).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTNumericalLiteral).
			stack pop.
		self assert: self topEntity operators equals: #('>').
		stack pop.
	
	"Testing value of multivalued relation elifClauses"
	self assert: self topEntity elifClauses size equals: 1.
	
		stack push: (stack top elifClauses at: 1).
		self assert: self topEntity sourceCode equals: 'elif x < 10:
	False' withPlatformLineEndings.
		self assert: (self topEntity isOfType: FASTPyElifClause).
		self assert: (self topEntity isOfType: FASTTConditionalStatement).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		self assert: (self topEntity isOfType: FASTTWithCondition).
		
		"Testing value of monovalue relation condition"
		self assert: self topEntity condition isNotNil.

			stack push: self topEntity condition.
			self assert: self topEntity sourceCode equals: 'x < 10'.
			self assert: (self topEntity isOfType: FASTPyComparisonOperator).
			
			"Testing value of multivalued relation operands"
			self assert: self topEntity operands size equals: 2.
			
				stack push: (stack top operands at: 1).
				self assert: self topEntity sourceCode equals: 'x'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			
				stack push: (stack top operands at: 2).
				self assert: self topEntity sourceCode equals: '10'.
				self assert: (self topEntity isOfType: FASTPyInteger).
				self assert: (self topEntity isOfType: FASTTLiteral).
				self assert: (self topEntity isOfType: FASTTNumericalLiteral).
				stack pop.
			self assert: self topEntity operators equals: #('<').
			stack pop.
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'False'.
			self assert: (self topEntity isOfType: FASTPyBoolean).
			self assert: (self topEntity isOfType: FASTTBooleanLiteral).
			self assert: (self topEntity isOfType: FASTTLiteral).
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation thenClause"
	self assert: self topEntity thenClause isNotNil.

		stack push: self topEntity thenClause.
		self assert: self topEntity sourceCode equals: 'True'.
		self assert: (self topEntity isOfType: FASTPyThenClause).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'True'.
			self assert: (self topEntity isOfType: FASTPyBoolean).
			self assert: (self topEntity isOfType: FASTTBooleanLiteral).
			self assert: (self topEntity isOfType: FASTTLiteral)
]

{ #category : 'tests - advanced statements' }
FASTPythonImporterTest >> testIfElseIf2 [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testIfElseIf2 	"

	self parse: 'if x > 0:
    True
elif x < 10:
	False
elif x == 60:
	True'.

	self assert: (fast allWithType: FASTPyBoolean) size equals: 3.
	self assert: (fast allWithType: FASTPyComparisonOperator) size equals: 3.
	self assert: (fast allWithType: FASTPyElifClause) size equals: 2.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 3.
	self assert: (fast allWithType: FASTPyIfStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyInteger) size equals: 3.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyThenClause) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'if x > 0:
    True
elif x < 10:
	False
elif x == 60:
	True' withPlatformLineEndings.
	self assert: (self topEntity isOfType: FASTPyIfStatement).
	self assert: (self topEntity isOfType: FASTPyTWithElseClause).
	self assert: (self topEntity isOfType: FASTTConditionalStatement).
	self assert: (self topEntity isOfType: FASTTWithCondition).
	
	"Testing value of monovalue relation condition"
	self assert: self topEntity condition isNotNil.

		stack push: self topEntity condition.
		self assert: self topEntity sourceCode equals: 'x > 0'.
		self assert: (self topEntity isOfType: FASTPyComparisonOperator).
		
		"Testing value of multivalued relation operands"
		self assert: self topEntity operands size equals: 2.
		
			stack push: (stack top operands at: 1).
			self assert: self topEntity sourceCode equals: 'x'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		
			stack push: (stack top operands at: 2).
			self assert: self topEntity sourceCode equals: '0'.
			self assert: (self topEntity isOfType: FASTPyInteger).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTNumericalLiteral).
			stack pop.
		self assert: self topEntity operators equals: #('>').
		stack pop.
	
	"Testing value of multivalued relation elifClauses"
	self assert: self topEntity elifClauses size equals: 2.
	
		stack push: (stack top elifClauses at: 1).
		self assert: self topEntity sourceCode equals: 'elif x < 10:
	False' withPlatformLineEndings.
		self assert: (self topEntity isOfType: FASTPyElifClause).
		self assert: (self topEntity isOfType: FASTTConditionalStatement).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		self assert: (self topEntity isOfType: FASTTWithCondition).
		
		"Testing value of monovalue relation condition"
		self assert: self topEntity condition isNotNil.

			stack push: self topEntity condition.
			self assert: self topEntity sourceCode equals: 'x < 10'.
			self assert: (self topEntity isOfType: FASTPyComparisonOperator).
			
			"Testing value of multivalued relation operands"
			self assert: self topEntity operands size equals: 2.
			
				stack push: (stack top operands at: 1).
				self assert: self topEntity sourceCode equals: 'x'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			
				stack push: (stack top operands at: 2).
				self assert: self topEntity sourceCode equals: '10'.
				self assert: (self topEntity isOfType: FASTPyInteger).
				self assert: (self topEntity isOfType: FASTTLiteral).
				self assert: (self topEntity isOfType: FASTTNumericalLiteral).
				stack pop.
			self assert: self topEntity operators equals: #('<').
			stack pop.
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'False'.
			self assert: (self topEntity isOfType: FASTPyBoolean).
			self assert: (self topEntity isOfType: FASTTBooleanLiteral).
			self assert: (self topEntity isOfType: FASTTLiteral).
			stack pop.
		stack pop.
	
		stack push: (stack top elifClauses at: 2).
		self assert: self topEntity sourceCode equals: 'elif x == 60:
	True' withPlatformLineEndings.
		self assert: (self topEntity isOfType: FASTPyElifClause).
		self assert: (self topEntity isOfType: FASTTConditionalStatement).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		self assert: (self topEntity isOfType: FASTTWithCondition).
		
		"Testing value of monovalue relation condition"
		self assert: self topEntity condition isNotNil.

			stack push: self topEntity condition.
			self assert: self topEntity sourceCode equals: 'x == 60'.
			self assert: (self topEntity isOfType: FASTPyComparisonOperator).
			
			"Testing value of multivalued relation operands"
			self assert: self topEntity operands size equals: 2.
			
				stack push: (stack top operands at: 1).
				self assert: self topEntity sourceCode equals: 'x'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			
				stack push: (stack top operands at: 2).
				self assert: self topEntity sourceCode equals: '60'.
				self assert: (self topEntity isOfType: FASTPyInteger).
				self assert: (self topEntity isOfType: FASTTLiteral).
				self assert: (self topEntity isOfType: FASTTNumericalLiteral).
				stack pop.
			self assert: self topEntity operators equals: #('==').
			stack pop.
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'True'.
			self assert: (self topEntity isOfType: FASTPyBoolean).
			self assert: (self topEntity isOfType: FASTTBooleanLiteral).
			self assert: (self topEntity isOfType: FASTTLiteral).
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation thenClause"
	self assert: self topEntity thenClause isNotNil.

		stack push: self topEntity thenClause.
		self assert: self topEntity sourceCode equals: 'True'.
		self assert: (self topEntity isOfType: FASTPyThenClause).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'True'.
			self assert: (self topEntity isOfType: FASTPyBoolean).
			self assert: (self topEntity isOfType: FASTTBooleanLiteral).
			self assert: (self topEntity isOfType: FASTTLiteral)
]

{ #category : 'tests - advanced statements' }
FASTPythonImporterTest >> testIfElseIfElse [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testIfElseIfElse 	"

	self parse: 'if x > 0:
    True
elif x < 10:
	False
elif x == 60:
	True
else:
	False'.

	self assert: (fast allWithType: FASTPyBoolean) size equals: 4.
	self assert: (fast allWithType: FASTPyComparisonOperator) size equals: 3.
	self assert: (fast allWithType: FASTPyElifClause) size equals: 2.
	self assert: (fast allWithType: FASTPyElseClause) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 3.
	self assert: (fast allWithType: FASTPyIfStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyInteger) size equals: 3.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyThenClause) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'if x > 0:
    True
elif x < 10:
	False
elif x == 60:
	True
else:
	False' withPlatformLineEndings.
	self assert: (self topEntity isOfType: FASTPyIfStatement).
	self assert: (self topEntity isOfType: FASTPyTWithElseClause).
	self assert: (self topEntity isOfType: FASTTConditionalStatement).
	self assert: (self topEntity isOfType: FASTTWithCondition).
	
	"Testing value of monovalue relation condition"
	self assert: self topEntity condition isNotNil.

		stack push: self topEntity condition.
		self assert: self topEntity sourceCode equals: 'x > 0'.
		self assert: (self topEntity isOfType: FASTPyComparisonOperator).
		
		"Testing value of multivalued relation operands"
		self assert: self topEntity operands size equals: 2.
		
			stack push: (stack top operands at: 1).
			self assert: self topEntity sourceCode equals: 'x'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		
			stack push: (stack top operands at: 2).
			self assert: self topEntity sourceCode equals: '0'.
			self assert: (self topEntity isOfType: FASTPyInteger).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTNumericalLiteral).
			stack pop.
		self assert: self topEntity operators equals: #('>').
		stack pop.
	
	"Testing value of multivalued relation elifClauses"
	self assert: self topEntity elifClauses size equals: 2.
	
		stack push: (stack top elifClauses at: 1).
		self assert: self topEntity sourceCode equals: 'elif x < 10:
	False' withPlatformLineEndings.
		self assert: (self topEntity isOfType: FASTPyElifClause).
		self assert: (self topEntity isOfType: FASTTConditionalStatement).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		self assert: (self topEntity isOfType: FASTTWithCondition).
		
		"Testing value of monovalue relation condition"
		self assert: self topEntity condition isNotNil.

			stack push: self topEntity condition.
			self assert: self topEntity sourceCode equals: 'x < 10'.
			self assert: (self topEntity isOfType: FASTPyComparisonOperator).
			
			"Testing value of multivalued relation operands"
			self assert: self topEntity operands size equals: 2.
			
				stack push: (stack top operands at: 1).
				self assert: self topEntity sourceCode equals: 'x'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			
				stack push: (stack top operands at: 2).
				self assert: self topEntity sourceCode equals: '10'.
				self assert: (self topEntity isOfType: FASTPyInteger).
				self assert: (self topEntity isOfType: FASTTLiteral).
				self assert: (self topEntity isOfType: FASTTNumericalLiteral).
				stack pop.
			self assert: self topEntity operators equals: #('<').
			stack pop.
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'False'.
			self assert: (self topEntity isOfType: FASTPyBoolean).
			self assert: (self topEntity isOfType: FASTTBooleanLiteral).
			self assert: (self topEntity isOfType: FASTTLiteral).
			stack pop.
		stack pop.
	
		stack push: (stack top elifClauses at: 2).
		self assert: self topEntity sourceCode equals: 'elif x == 60:
	True' withPlatformLineEndings.
		self assert: (self topEntity isOfType: FASTPyElifClause).
		self assert: (self topEntity isOfType: FASTTConditionalStatement).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		self assert: (self topEntity isOfType: FASTTWithCondition).
		
		"Testing value of monovalue relation condition"
		self assert: self topEntity condition isNotNil.

			stack push: self topEntity condition.
			self assert: self topEntity sourceCode equals: 'x == 60'.
			self assert: (self topEntity isOfType: FASTPyComparisonOperator).
			
			"Testing value of multivalued relation operands"
			self assert: self topEntity operands size equals: 2.
			
				stack push: (stack top operands at: 1).
				self assert: self topEntity sourceCode equals: 'x'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			
				stack push: (stack top operands at: 2).
				self assert: self topEntity sourceCode equals: '60'.
				self assert: (self topEntity isOfType: FASTPyInteger).
				self assert: (self topEntity isOfType: FASTTLiteral).
				self assert: (self topEntity isOfType: FASTTNumericalLiteral).
				stack pop.
			self assert: self topEntity operators equals: #('==').
			stack pop.
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'True'.
			self assert: (self topEntity isOfType: FASTPyBoolean).
			self assert: (self topEntity isOfType: FASTTBooleanLiteral).
			self assert: (self topEntity isOfType: FASTTLiteral).
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation elseClause"
	self assert: self topEntity elseClause isNotNil.

		stack push: self topEntity elseClause.
		self assert: self topEntity sourceCode equals: 'False'.
		self assert: (self topEntity isOfType: FASTPyElseClause).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'False'.
			self assert: (self topEntity isOfType: FASTPyBoolean).
			self assert: (self topEntity isOfType: FASTTBooleanLiteral).
			self assert: (self topEntity isOfType: FASTTLiteral).
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation thenClause"
	self assert: self topEntity thenClause isNotNil.

		stack push: self topEntity thenClause.
		self assert: self topEntity sourceCode equals: 'True'.
		self assert: (self topEntity isOfType: FASTPyThenClause).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'True'.
			self assert: (self topEntity isOfType: FASTPyBoolean).
			self assert: (self topEntity isOfType: FASTTBooleanLiteral).
			self assert: (self topEntity isOfType: FASTTLiteral)
]

{ #category : 'tests - advanced statements' }
FASTPythonImporterTest >> testIfStatementWithABreak [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testIfStatementWithABreak 	"

	self parse: 'if c > 3: break'.

	self assert: (fast allWithType: FASTPyBreakStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyComparisonOperator) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyIfStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyInteger) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyThenClause) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'if c > 3: break'.
	self assert: (self topEntity isOfType: FASTPyIfStatement).
	self assert: (self topEntity isOfType: FASTPyTWithElseClause).
	self assert: (self topEntity isOfType: FASTTConditionalStatement).
	self assert: (self topEntity isOfType: FASTTWithCondition).
	
	"Testing value of monovalue relation condition"
	self assert: self topEntity condition isNotNil.

		stack push: self topEntity condition.
		self assert: self topEntity sourceCode equals: 'c > 3'.
		self assert: (self topEntity isOfType: FASTPyComparisonOperator).
		
		"Testing value of multivalued relation operands"
		self assert: self topEntity operands size equals: 2.
		
			stack push: (stack top operands at: 1).
			self assert: self topEntity sourceCode equals: 'c'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		
			stack push: (stack top operands at: 2).
			self assert: self topEntity sourceCode equals: '3'.
			self assert: (self topEntity isOfType: FASTPyInteger).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTNumericalLiteral).
			stack pop.
		self assert: self topEntity operators equals: #('>').
		stack pop.
	
	"Testing value of monovalue relation thenClause"
	self assert: self topEntity thenClause isNotNil.

		stack push: self topEntity thenClause.
		self assert: self topEntity sourceCode equals: 'break'.
		self assert: (self topEntity isOfType: FASTPyThenClause).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'break'.
			self assert: (self topEntity isOfType: FASTPyBreakStatement)
]

{ #category : 'tests - advanced statements' }
FASTPythonImporterTest >> testIfStatementWithAContinue [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testIfStatementWithAContinue 	"

	self parse: 'if c > 3: continue'.

	self assert: (fast allWithType: FASTPyComparisonOperator) size equals: 1.
	self assert: (fast allWithType: FASTPyContinueStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyIfStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyInteger) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyThenClause) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'if c > 3: continue'.
	self assert: (self topEntity isOfType: FASTPyIfStatement).
	self assert: (self topEntity isOfType: FASTPyTWithElseClause).
	self assert: (self topEntity isOfType: FASTTConditionalStatement).
	self assert: (self topEntity isOfType: FASTTWithCondition).
	
	"Testing value of monovalue relation condition"
	self assert: self topEntity condition isNotNil.

		stack push: self topEntity condition.
		self assert: self topEntity sourceCode equals: 'c > 3'.
		self assert: (self topEntity isOfType: FASTPyComparisonOperator).
		
		"Testing value of multivalued relation operands"
		self assert: self topEntity operands size equals: 2.
		
			stack push: (stack top operands at: 1).
			self assert: self topEntity sourceCode equals: 'c'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		
			stack push: (stack top operands at: 2).
			self assert: self topEntity sourceCode equals: '3'.
			self assert: (self topEntity isOfType: FASTPyInteger).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTNumericalLiteral).
			stack pop.
		self assert: self topEntity operators equals: #('>').
		stack pop.
	
	"Testing value of monovalue relation thenClause"
	self assert: self topEntity thenClause isNotNil.

		stack push: self topEntity thenClause.
		self assert: self topEntity sourceCode equals: 'continue'.
		self assert: (self topEntity isOfType: FASTPyThenClause).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'continue'.
			self assert: (self topEntity isOfType: FASTPyContinueStatement)
]

{ #category : 'tests - imports' }
FASTPythonImporterTest >> testImport [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testImport 	"

	self parse: 'import random'.

	self assert: (fast allWithType: FASTPyImportStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyImportedEntity) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'import random'.
	self assert: (self topEntity isOfType: FASTPyImportStatement).
	
	"Testing value of multivalued relation importedEntities"
	self assert: self topEntity importedEntities size equals: 1.
	
		stack push: (stack top importedEntities at: 1).
		self assert: self topEntity sourceCode equals: 'random'.
		self assert: (self topEntity isOfType: FASTPyImportedEntity).
		self assert: self topEntity segments equals: #('random')
]

{ #category : 'tests - imports' }
FASTPythonImporterTest >> testImportDottedImport [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testImportDottedImport 	"

	self parse: 'import xml.etree.ElementTree'.

	self assert: (fast allWithType: FASTPyImportStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyImportedEntity) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'import xml.etree.ElementTree'.
	self assert: (self topEntity isOfType: FASTPyImportStatement).
	
	"Testing value of multivalued relation importedEntities"
	self assert: self topEntity importedEntities size equals: 1.
	
		stack push: (stack top importedEntities at: 1).
		self assert: self topEntity sourceCode equals: 'xml.etree.ElementTree'.
		self assert: (self topEntity isOfType: FASTPyImportedEntity).
		self assert: self topEntity segments equals: #('xml' 'etree' 'ElementTree')
]

{ #category : 'tests - imports' }
FASTPythonImporterTest >> testImportDottedImportAndAlias [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testImportDottedImportAndAlias 	"

	self parse: 'import xml.etree.ElementTree as ET'.

	self assert: (fast allWithType: FASTPyImportStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyImportedEntity) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'import xml.etree.ElementTree as ET'.
	self assert: (self topEntity isOfType: FASTPyImportStatement).
	
	"Testing value of multivalued relation importedEntities"
	self assert: self topEntity importedEntities size equals: 1.
	
		stack push: (stack top importedEntities at: 1).
		self assert: self topEntity sourceCode equals: 'xml.etree.ElementTree as ET'.
		self assert: (self topEntity isOfType: FASTPyImportedEntity).
		self assert: self topEntity alias equals: 'ET'.
		self assert: self topEntity segments equals: #('xml' 'etree' 'ElementTree')
]

{ #category : 'tests - imports' }
FASTPythonImporterTest >> testImportFromWithLineContinuation [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testImportFromWithLineContinuation 	"

	self parse: 'from distutils.ccompiler import \
     CCompiler, gen_preprocess_options'.

	self assert: (fast allWithType: FASTPyFromModule) size equals: 1.
	self assert: (fast allWithType: FASTPyImportFromStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyImportedEntity) size equals: 2.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'from distutils.ccompiler import \
     CCompiler, gen_preprocess_options' withPlatformLineEndings.
	self assert: (self topEntity isOfType: FASTPyImportFromStatement).
	
	"Testing value of monovalue relation fromModule"
	self assert: self topEntity fromModule isNotNil.

		stack push: self topEntity fromModule.
		self assert: self topEntity sourceCode equals: 'distutils.ccompiler'.
		self assert: (self topEntity isOfType: FASTPyFromModule).
		self assert: self topEntity segments equals: #('distutils' 'ccompiler').
		stack pop.
	
	"Testing value of multivalued relation importedEntities"
	self assert: self topEntity importedEntities size equals: 2.
	
		stack push: (stack top importedEntities at: 1).
		self assert: self topEntity sourceCode equals: 'CCompiler'.
		self assert: (self topEntity isOfType: FASTPyImportedEntity).
		self assert: self topEntity segments equals: #('CCompiler').
		stack pop.
	
		stack push: (stack top importedEntities at: 2).
		self assert: self topEntity sourceCode equals: 'gen_preprocess_options'.
		self assert: (self topEntity isOfType: FASTPyImportedEntity).
		self assert: self topEntity segments equals: #('gen_preprocess_options')
]

{ #category : 'tests - imports' }
FASTPythonImporterTest >> testImportInFunction [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testImportInFunction 	"

	self parse: 'def fun():
	import numpy as np
	np.fit()'.

	self assert: (fast allWithType: FASTPyAttributeAccess) size equals: 1.
	self assert: (fast allWithType: FASTPyBlock) size equals: 1.
	self assert: (fast allWithType: FASTPyCall) size equals: 1.
	self assert: (fast allWithType: FASTPyFunctionDefinition) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 2.
	self assert: (fast allWithType: FASTPyImportStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyImportedEntity) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'def fun():
	import numpy as np
	np.fit()' withPlatformLineEndings.
	self assert: (self topEntity isOfType: FASTPyFunctionDefinition).
	self assert: (self topEntity isOfType: FASTPyTDefinition).
	self assert: (self topEntity isOfType: FASTTWithParameters).
	self assert: (self topEntity isOfType: FASTTWithStatements).
	self assert: self topEntity name equals: 'fun'.
	
	"Testing value of monovalue relation statementBlock"
	self assert: self topEntity statementBlock isNotNil.

		stack push: self topEntity statementBlock.
		self assert: self topEntity sourceCode equals: 'import numpy as np
	np.fit()' withPlatformLineEndings.
		self assert: (self topEntity isOfType: FASTPyBlock).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 2.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'import numpy as np'.
			self assert: (self topEntity isOfType: FASTPyImportStatement).
			
			"Testing value of multivalued relation importedEntities"
			self assert: self topEntity importedEntities size equals: 1.
			
				stack push: (stack top importedEntities at: 1).
				self assert: self topEntity sourceCode equals: 'numpy as np'.
				self assert: (self topEntity isOfType: FASTPyImportedEntity).
				self assert: self topEntity alias equals: 'np'.
				self assert: self topEntity segments equals: #('numpy').
				stack pop.
			stack pop.
		
			stack push: (stack top statements at: 2).
			self assert: self topEntity sourceCode equals: 'np.fit()'.
			self assert: (self topEntity isOfType: FASTPyCall).
			self assert: (self topEntity isOfType: FASTTInvocation).
			self assert: (self topEntity isOfType: FASTTWithArguments).
			
			"Testing value of monovalue relation callee"
			self assert: self topEntity callee isNotNil.

				stack push: self topEntity callee.
				self assert: self topEntity sourceCode equals: 'np.fit'.
				self assert: (self topEntity isOfType: FASTPyAttributeAccess).
				
				"Testing value of monovalue relation attribute"
				self assert: self topEntity attribute isNotNil.

					stack push: self topEntity attribute.
					self assert: self topEntity sourceCode equals: 'fit'.
					self assert: (self topEntity isOfType: FASTPyIdentifier).
					stack pop.
				
				"Testing value of monovalue relation object"
				self assert: self topEntity object isNotNil.

					stack push: self topEntity object.
					self assert: self topEntity sourceCode equals: 'np'.
					self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - imports' }
FASTPythonImporterTest >> testImportMultiple [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testImportMultiple 	"

	self parse: 'import random, collections'.

	self assert: (fast allWithType: FASTPyImportStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyImportedEntity) size equals: 2.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'import random, collections'.
	self assert: (self topEntity isOfType: FASTPyImportStatement).
	
	"Testing value of multivalued relation importedEntities"
	self assert: self topEntity importedEntities size equals: 2.
	
		stack push: (stack top importedEntities at: 1).
		self assert: self topEntity sourceCode equals: 'random'.
		self assert: (self topEntity isOfType: FASTPyImportedEntity).
		self assert: self topEntity segments equals: #('random').
		stack pop.
	
		stack push: (stack top importedEntities at: 2).
		self assert: self topEntity sourceCode equals: 'collections'.
		self assert: (self topEntity isOfType: FASTPyImportedEntity).
		self assert: self topEntity segments equals: #('collections')
]

{ #category : 'tests - imports' }
FASTPythonImporterTest >> testImportMultipleAndAliases [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testImportMultipleAndAliases 	"

	self parse: 'import random as rand, collections'.

	self assert: (fast allWithType: FASTPyImportStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyImportedEntity) size equals: 2.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'import random as rand, collections'.
	self assert: (self topEntity isOfType: FASTPyImportStatement).
	
	"Testing value of multivalued relation importedEntities"
	self assert: self topEntity importedEntities size equals: 2.
	
		stack push: (stack top importedEntities at: 1).
		self assert: self topEntity sourceCode equals: 'random as rand'.
		self assert: (self topEntity isOfType: FASTPyImportedEntity).
		self assert: self topEntity alias equals: 'rand'.
		self assert: self topEntity segments equals: #('random').
		stack pop.
	
		stack push: (stack top importedEntities at: 2).
		self assert: self topEntity sourceCode equals: 'collections'.
		self assert: (self topEntity isOfType: FASTPyImportedEntity).
		self assert: self topEntity segments equals: #('collections')
]

{ #category : 'tests - imports' }
FASTPythonImporterTest >> testImportWithAlias [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testImportWithAlias 	"

	self parse: 'import random as rand'.

	self assert: (fast allWithType: FASTPyImportStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyImportedEntity) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'import random as rand'.
	self assert: (self topEntity isOfType: FASTPyImportStatement).
	
	"Testing value of multivalued relation importedEntities"
	self assert: self topEntity importedEntities size equals: 1.
	
		stack push: (stack top importedEntities at: 1).
		self assert: self topEntity sourceCode equals: 'random as rand'.
		self assert: (self topEntity isOfType: FASTPyImportedEntity).
		self assert: self topEntity alias equals: 'rand'.
		self assert: self topEntity segments equals: #('random')
]

{ #category : 'tests - imports' }
FASTPythonImporterTest >> testImportWithComment [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testImportWithComment 	"

	self parse: 'import #For visu
mathplotlib'.

	self assert: (fast allWithType: FASTPyComment) size equals: 1.
	self assert: (fast allWithType: FASTPyImportStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyImportedEntity) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'import #For visu
mathplotlib' withPlatformLineEndings.
	self assert: (self topEntity isOfType: FASTPyImportStatement).
	
	"Testing value of multivalued relation comments"
	self assert: self topEntity comments size equals: 1.
	
		stack push: (stack top comments at: 1).
		self assert: self topEntity sourceCode equals: '#For visu'.
		self assert: (self topEntity isOfType: FASTPyComment).
		self assert: (self topEntity isOfType: FASTTComment).
		stack pop.
	
	"Testing value of multivalued relation importedEntities"
	self assert: self topEntity importedEntities size equals: 1.
	
		stack push: (stack top importedEntities at: 1).
		self assert: self topEntity sourceCode equals: 'mathplotlib'.
		self assert: (self topEntity isOfType: FASTPyImportedEntity).
		self assert: self topEntity segments equals: #('mathplotlib')
]

{ #category : 'tests - literals' }
FASTPythonImporterTest >> testInteger [

	| boolean |
	self parse: '42'.

	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyInteger) size equals: 1.

	boolean := (fast allWithType: FASTPyInteger) anyOne.
	self assert: boolean sourceCode equals: '42'.
	self assert: (boolean isOfType: FASTTLiteral)
]

{ #category : 'tests - interpolations' }
FASTPythonImporterTest >> testInterpolation [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testInterpolation 	"

	self parse: 'f"User {name} has {value} items"'.

	self assert: (fast allWithType: FASTPyIdentifier) size equals: 2.
	self assert: (fast allWithType: FASTPyInterpolation) size equals: 2.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyString) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'f"User {name} has {value} items"'.
	self assert: (self topEntity isOfType: FASTPyString).
	self assert: (self topEntity isOfType: FASTTLiteral).
	self assert: (self topEntity isOfType: FASTTStringLiteral).
	
	"Testing value of multivalued relation interpolations"
	self assert: self topEntity interpolations size equals: 2.
	
		stack push: (stack top interpolations at: 1).
		self assert: self topEntity sourceCode equals: '{name}'.
		self assert: (self topEntity isOfType: FASTPyInterpolation).
		
		"Testing value of monovalue relation expression"
		self assert: self topEntity expression isNotNil.

			stack push: self topEntity expression.
			self assert: self topEntity sourceCode equals: 'name'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		stack pop.
	
		stack push: (stack top interpolations at: 2).
		self assert: self topEntity sourceCode equals: '{value}'.
		self assert: (self topEntity isOfType: FASTPyInterpolation).
		
		"Testing value of monovalue relation expression"
		self assert: self topEntity expression isNotNil.

			stack push: self topEntity expression.
			self assert: self topEntity sourceCode equals: 'value'.
			self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - interpolations' }
FASTPythonImporterTest >> testInterpolationWithAttributeAccess [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testInterpolationWithAttributeAccess 	"

	self parse: 'f"{CFG.model_name}"'.

	self assert: (fast allWithType: FASTPyAttributeAccess) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 2.
	self assert: (fast allWithType: FASTPyInterpolation) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyString) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'f"{CFG.model_name}"'.
	self assert: (self topEntity isOfType: FASTPyString).
	self assert: (self topEntity isOfType: FASTTLiteral).
	self assert: (self topEntity isOfType: FASTTStringLiteral).
	
	"Testing value of multivalued relation interpolations"
	self assert: self topEntity interpolations size equals: 1.
	
		stack push: (stack top interpolations at: 1).
		self assert: self topEntity sourceCode equals: '{CFG.model_name}'.
		self assert: (self topEntity isOfType: FASTPyInterpolation).
		
		"Testing value of monovalue relation expression"
		self assert: self topEntity expression isNotNil.

			stack push: self topEntity expression.
			self assert: self topEntity sourceCode equals: 'CFG.model_name'.
			self assert: (self topEntity isOfType: FASTPyAttributeAccess).
			
			"Testing value of monovalue relation attribute"
			self assert: self topEntity attribute isNotNil.

				stack push: self topEntity attribute.
				self assert: self topEntity sourceCode equals: 'model_name'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			
			"Testing value of monovalue relation object"
			self assert: self topEntity object isNotNil.

				stack push: self topEntity object.
				self assert: self topEntity sourceCode equals: 'CFG'.
				self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - interpolations' }
FASTPythonImporterTest >> testInterpolationWithBinaryOperator [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testInterpolationWithBinaryOperator 	"

	self parse: 'f"{k+1}"'.

	self assert: (fast allWithType: FASTPyBinaryOperator) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyInteger) size equals: 1.
	self assert: (fast allWithType: FASTPyInterpolation) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyString) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'f"{k+1}"'.
	self assert: (self topEntity isOfType: FASTPyString).
	self assert: (self topEntity isOfType: FASTTLiteral).
	self assert: (self topEntity isOfType: FASTTStringLiteral).
	
	"Testing value of multivalued relation interpolations"
	self assert: self topEntity interpolations size equals: 1.
	
		stack push: (stack top interpolations at: 1).
		self assert: self topEntity sourceCode equals: '{k+1}'.
		self assert: (self topEntity isOfType: FASTPyInterpolation).
		
		"Testing value of monovalue relation expression"
		self assert: self topEntity expression isNotNil.

			stack push: self topEntity expression.
			self assert: self topEntity sourceCode equals: 'k+1'.
			self assert: (self topEntity isOfType: FASTPyBinaryOperator).
			self assert: (self topEntity isOfType: FASTTBinaryExpression).
			
			"Testing value of monovalue relation leftOperand"
			self assert: self topEntity leftOperand isNotNil.

				stack push: self topEntity leftOperand.
				self assert: self topEntity sourceCode equals: 'k'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			self assert: self topEntity operator equals: '+'.
			
			"Testing value of monovalue relation rightOperand"
			self assert: self topEntity rightOperand isNotNil.

				stack push: self topEntity rightOperand.
				self assert: self topEntity sourceCode equals: '1'.
				self assert: (self topEntity isOfType: FASTPyInteger).
				self assert: (self topEntity isOfType: FASTTLiteral).
				self assert: (self topEntity isOfType: FASTTNumericalLiteral)
]

{ #category : 'tests - interpolations' }
FASTPythonImporterTest >> testInterpolationWithBooleanOperator [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testInterpolationWithBooleanOperator 	"

	self parse: 'f"{x and y}"'.

	self assert: (fast allWithType: FASTPyBooleanOperator) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 2.
	self assert: (fast allWithType: FASTPyInterpolation) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyString) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'f"{x and y}"'.
	self assert: (self topEntity isOfType: FASTPyString).
	self assert: (self topEntity isOfType: FASTTLiteral).
	self assert: (self topEntity isOfType: FASTTStringLiteral).
	
	"Testing value of multivalued relation interpolations"
	self assert: self topEntity interpolations size equals: 1.
	
		stack push: (stack top interpolations at: 1).
		self assert: self topEntity sourceCode equals: '{x and y}'.
		self assert: (self topEntity isOfType: FASTPyInterpolation).
		
		"Testing value of monovalue relation expression"
		self assert: self topEntity expression isNotNil.

			stack push: self topEntity expression.
			self assert: self topEntity sourceCode equals: 'x and y'.
			self assert: (self topEntity isOfType: FASTPyBooleanOperator).
			self assert: (self topEntity isOfType: FASTTBinaryExpression).
			
			"Testing value of monovalue relation leftOperand"
			self assert: self topEntity leftOperand isNotNil.

				stack push: self topEntity leftOperand.
				self assert: self topEntity sourceCode equals: 'x'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			self assert: self topEntity operator equals: 'and'.
			
			"Testing value of monovalue relation rightOperand"
			self assert: self topEntity rightOperand isNotNil.

				stack push: self topEntity rightOperand.
				self assert: self topEntity sourceCode equals: 'y'.
				self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - interpolations' }
FASTPythonImporterTest >> testInterpolationWithCall [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testInterpolationWithCall 	"

	self parse: 'f"{myMethod(2)}"'.

	self assert: (fast allWithType: FASTPyCall) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyInteger) size equals: 1.
	self assert: (fast allWithType: FASTPyInterpolation) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyString) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'f"{myMethod(2)}"'.
	self assert: (self topEntity isOfType: FASTPyString).
	self assert: (self topEntity isOfType: FASTTLiteral).
	self assert: (self topEntity isOfType: FASTTStringLiteral).
	
	"Testing value of multivalued relation interpolations"
	self assert: self topEntity interpolations size equals: 1.
	
		stack push: (stack top interpolations at: 1).
		self assert: self topEntity sourceCode equals: '{myMethod(2)}'.
		self assert: (self topEntity isOfType: FASTPyInterpolation).
		
		"Testing value of monovalue relation expression"
		self assert: self topEntity expression isNotNil.

			stack push: self topEntity expression.
			self assert: self topEntity sourceCode equals: 'myMethod(2)'.
			self assert: (self topEntity isOfType: FASTPyCall).
			self assert: (self topEntity isOfType: FASTTInvocation).
			self assert: (self topEntity isOfType: FASTTWithArguments).
			
			"Testing value of multivalued relation arguments"
			self assert: self topEntity arguments size equals: 1.
			
				stack push: (stack top arguments at: 1).
				self assert: self topEntity sourceCode equals: '2'.
				self assert: (self topEntity isOfType: FASTPyInteger).
				self assert: (self topEntity isOfType: FASTTLiteral).
				self assert: (self topEntity isOfType: FASTTNumericalLiteral).
				stack pop.
			
			"Testing value of monovalue relation callee"
			self assert: self topEntity callee isNotNil.

				stack push: self topEntity callee.
				self assert: self topEntity sourceCode equals: 'myMethod'.
				self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - interpolations' }
FASTPythonImporterTest >> testInterpolationWithComparisonOperator [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testInterpolationWithComparisonOperator 	"

	self parse: 'f"{x == y}"'.

	self assert: (fast allWithType: FASTPyComparisonOperator) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 2.
	self assert: (fast allWithType: FASTPyInterpolation) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyString) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'f"{x == y}"'.
	self assert: (self topEntity isOfType: FASTPyString).
	self assert: (self topEntity isOfType: FASTTLiteral).
	self assert: (self topEntity isOfType: FASTTStringLiteral).
	
	"Testing value of multivalued relation interpolations"
	self assert: self topEntity interpolations size equals: 1.
	
		stack push: (stack top interpolations at: 1).
		self assert: self topEntity sourceCode equals: '{x == y}'.
		self assert: (self topEntity isOfType: FASTPyInterpolation).
		
		"Testing value of monovalue relation expression"
		self assert: self topEntity expression isNotNil.

			stack push: self topEntity expression.
			self assert: self topEntity sourceCode equals: 'x == y'.
			self assert: (self topEntity isOfType: FASTPyComparisonOperator).
			
			"Testing value of multivalued relation operands"
			self assert: self topEntity operands size equals: 2.
			
				stack push: (stack top operands at: 1).
				self assert: self topEntity sourceCode equals: 'x'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			
				stack push: (stack top operands at: 2).
				self assert: self topEntity sourceCode equals: 'y'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			self assert: self topEntity operators equals: #('==')
]

{ #category : 'tests - interpolations' }
FASTPythonImporterTest >> testInterpolationWithComprehension [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testInterpolationWithComprehension 	"

	self parse: 'f"{[x for x in items]}"'.

	self assert: (fast allWithType: FASTPyForInClause) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 3.
	self assert: (fast allWithType: FASTPyInterpolation) size equals: 1.
	self assert: (fast allWithType: FASTPyListComprehension) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyString) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'f"{[x for x in items]}"'.
	self assert: (self topEntity isOfType: FASTPyString).
	self assert: (self topEntity isOfType: FASTTLiteral).
	self assert: (self topEntity isOfType: FASTTStringLiteral).
	
	"Testing value of multivalued relation interpolations"
	self assert: self topEntity interpolations size equals: 1.
	
		stack push: (stack top interpolations at: 1).
		self assert: self topEntity sourceCode equals: '{[x for x in items]}'.
		self assert: (self topEntity isOfType: FASTPyInterpolation).
		
		"Testing value of monovalue relation expression"
		self assert: self topEntity expression isNotNil.

			stack push: self topEntity expression.
			self assert: self topEntity sourceCode equals: '[x for x in items]'.
			self assert: (self topEntity isOfType: FASTPyListComprehension).
			self assert: (self topEntity isOfType: FASTPyTComprehension).
			self assert: (self topEntity isOfType: FASTTWithCondition).
			
			"Testing value of monovalue relation body"
			self assert: self topEntity body isNotNil.

				stack push: self topEntity body.
				self assert: self topEntity sourceCode equals: 'x'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			
			"Testing value of multivalued relation clauses"
			self assert: self topEntity clauses size equals: 1.
			
				stack push: (stack top clauses at: 1).
				self assert: self topEntity sourceCode equals: 'for x in items'.
				self assert: (self topEntity isOfType: FASTPyForInClause).
				
				"Testing value of monovalue relation left"
				self assert: self topEntity left isNotNil.

					stack push: self topEntity left.
					self assert: self topEntity sourceCode equals: 'x'.
					self assert: (self topEntity isOfType: FASTPyIdentifier).
					stack pop.
				
				"Testing value of monovalue relation right"
				self assert: self topEntity right isNotNil.

					stack push: self topEntity right.
					self assert: self topEntity sourceCode equals: 'items'.
					self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - interpolations' }
FASTPythonImporterTest >> testInterpolationWithConditionalExpression [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testInterpolationWithConditionalExpression 	"

	self parse: 'f"x{ if cond else y}"'.

	self assert: (fast allWithType: FASTPyConditionalExpression) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 3.
	self assert: (fast allWithType: FASTPyInterpolation) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyString) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'f"x{ if cond else y}"'.
	self assert: (self topEntity isOfType: FASTPyString).
	self assert: (self topEntity isOfType: FASTTLiteral).
	self assert: (self topEntity isOfType: FASTTStringLiteral).
	
	"Testing value of multivalued relation interpolations"
	self assert: self topEntity interpolations size equals: 1.
	
		stack push: (stack top interpolations at: 1).
		self assert: self topEntity sourceCode equals: '{ if cond else y}'.
		self assert: (self topEntity isOfType: FASTPyInterpolation).
		
		"Testing value of monovalue relation expression"
		self assert: self topEntity expression isNotNil.

			stack push: self topEntity expression.
			self assert: self topEntity sourceCode equals: ' if cond else y'.
			self assert: (self topEntity isOfType: FASTPyConditionalExpression).
			self assert: (self topEntity isOfType: FASTTWithCondition).
			
			"Testing value of monovalue relation condition"
			self assert: self topEntity condition isNotNil.

				stack push: self topEntity condition.
				self assert: self topEntity sourceCode equals: 'cond'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			
			"Testing value of monovalue relation elseExpression"
			self assert: self topEntity elseExpression isNotNil.

				stack push: self topEntity elseExpression.
				self assert: self topEntity sourceCode equals: 'y'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			
			"Testing value of monovalue relation thenExpression"
			self assert: self topEntity thenExpression isNotNil.

				stack push: self topEntity thenExpression.
				self assert: self topEntity sourceCode equals: ''.
				self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - interpolations' }
FASTPythonImporterTest >> testInterpolationWithFormatSpecifier [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testInterpolationWithFormatSpecifier 	"

	self parse: 'f"{255:X}"'.

	self assert: (fast allWithType: FASTPyInteger) size equals: 1.
	self assert: (fast allWithType: FASTPyInterpolation) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyString) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'f"{255:X}"'.
	self assert: (self topEntity isOfType: FASTPyString).
	self assert: (self topEntity isOfType: FASTTLiteral).
	self assert: (self topEntity isOfType: FASTTStringLiteral).
	
	"Testing value of multivalued relation interpolations"
	self assert: self topEntity interpolations size equals: 1.
	
		stack push: (stack top interpolations at: 1).
		self assert: self topEntity sourceCode equals: '{255:X}'.
		self assert: (self topEntity isOfType: FASTPyInterpolation).
		
		"Testing value of monovalue relation expression"
		self assert: self topEntity expression isNotNil.

			stack push: self topEntity expression.
			self assert: self topEntity sourceCode equals: '255'.
			self assert: (self topEntity isOfType: FASTPyInteger).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTNumericalLiteral).
			stack pop.
		self assert: self topEntity formatSpecifier equals: 'X'
]

{ #category : 'tests - interpolations' }
FASTPythonImporterTest >> testInterpolationWithLambda [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testInterpolationWithLambda 	"

	self parse: 'f"{ (lambda x: x*2) }"'.

	self assert: (fast allWithType: FASTPyBinaryOperator) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyInteger) size equals: 1.
	self assert: (fast allWithType: FASTPyInterpolation) size equals: 1.
	self assert: (fast allWithType: FASTPyLambda) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyParameter) size equals: 1.
	self assert: (fast allWithType: FASTPyString) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'f"{ (lambda x: x*2) }"'.
	self assert: (self topEntity isOfType: FASTPyString).
	self assert: (self topEntity isOfType: FASTTLiteral).
	self assert: (self topEntity isOfType: FASTTStringLiteral).
	
	"Testing value of multivalued relation interpolations"
	self assert: self topEntity interpolations size equals: 1.
	
		stack push: (stack top interpolations at: 1).
		self assert: self topEntity sourceCode equals: '{ (lambda x: x*2) }'.
		self assert: (self topEntity isOfType: FASTPyInterpolation).
		
		"Testing value of monovalue relation expression"
		self assert: self topEntity expression isNotNil.

			stack push: self topEntity expression.
			self assert: self topEntity sourceCode equals: 'lambda x: x*2'.
			self assert: (self topEntity isOfType: FASTPyLambda).
			self assert: (self topEntity isOfType: FASTTWithParameters).
			
			"Testing value of monovalue relation expression"
			self assert: self topEntity expression isNotNil.

				stack push: self topEntity expression.
				self assert: self topEntity sourceCode equals: 'x*2'.
				self assert: (self topEntity isOfType: FASTPyBinaryOperator).
				self assert: (self topEntity isOfType: FASTTBinaryExpression).
				
				"Testing value of monovalue relation leftOperand"
				self assert: self topEntity leftOperand isNotNil.

					stack push: self topEntity leftOperand.
					self assert: self topEntity sourceCode equals: 'x'.
					self assert: (self topEntity isOfType: FASTPyIdentifier).
					stack pop.
				self assert: self topEntity operator equals: '*'.
				
				"Testing value of monovalue relation rightOperand"
				self assert: self topEntity rightOperand isNotNil.

					stack push: self topEntity rightOperand.
					self assert: self topEntity sourceCode equals: '2'.
					self assert: (self topEntity isOfType: FASTPyInteger).
					self assert: (self topEntity isOfType: FASTTLiteral).
					self assert: (self topEntity isOfType: FASTTNumericalLiteral).
					stack pop.
				stack pop.
			
			"Testing value of multivalued relation parameters"
			self assert: self topEntity parameters size equals: 1.
			
				stack push: (stack top parameters at: 1).
				self assert: self topEntity sourceCode equals: 'x'.
				self assert: (self topEntity isOfType: FASTPyParameter).
				self assert: (self topEntity isOfType: FASTTVariableEntity).
				self assert: self topEntity name equals: 'x'
]

{ #category : 'tests - interpolations' }
FASTPythonImporterTest >> testInterpolationWithSubscript [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testInterpolationWithSubscript 	"

	self parse: 'f"{label_order[i]}"'.

	self assert: (fast allWithType: FASTPyIdentifier) size equals: 2.
	self assert: (fast allWithType: FASTPyInterpolation) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyString) size equals: 1.
	self assert: (fast allWithType: FASTPySubscript) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'f"{label_order[i]}"'.
	self assert: (self topEntity isOfType: FASTPyString).
	self assert: (self topEntity isOfType: FASTTLiteral).
	self assert: (self topEntity isOfType: FASTTStringLiteral).
	
	"Testing value of multivalued relation interpolations"
	self assert: self topEntity interpolations size equals: 1.
	
		stack push: (stack top interpolations at: 1).
		self assert: self topEntity sourceCode equals: '{label_order[i]}'.
		self assert: (self topEntity isOfType: FASTPyInterpolation).
		
		"Testing value of monovalue relation expression"
		self assert: self topEntity expression isNotNil.

			stack push: self topEntity expression.
			self assert: self topEntity sourceCode equals: 'label_order[i]'.
			self assert: (self topEntity isOfType: FASTPySubscript).
			
			"Testing value of multivalued relation indices"
			self assert: self topEntity indices size equals: 1.
			
				stack push: (stack top indices at: 1).
				self assert: self topEntity sourceCode equals: 'i'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			
			"Testing value of monovalue relation value"
			self assert: self topEntity value isNotNil.

				stack push: self topEntity value.
				self assert: self topEntity sourceCode equals: 'label_order'.
				self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - interpolations' }
FASTPythonImporterTest >> testInterpolationWithTypeConversion [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testInterpolationWithTypeConversion 	"

	self parse: 'f"{text!r}" '.

	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyInterpolation) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyString) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'f"{text!r}"'.
	self assert: (self topEntity isOfType: FASTPyString).
	self assert: (self topEntity isOfType: FASTTLiteral).
	self assert: (self topEntity isOfType: FASTTStringLiteral).
	
	"Testing value of multivalued relation interpolations"
	self assert: self topEntity interpolations size equals: 1.
	
		stack push: (stack top interpolations at: 1).
		self assert: self topEntity sourceCode equals: '{text!r}'.
		self assert: (self topEntity isOfType: FASTPyInterpolation).
		
		"Testing value of monovalue relation expression"
		self assert: self topEntity expression isNotNil.

			stack push: self topEntity expression.
			self assert: self topEntity sourceCode equals: 'text'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		self assert: self topEntity typeConversion equals: 'r'
]

{ #category : 'tests - function definitions' }
FASTPythonImporterTest >> testKeywordSeparator [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testKeywordSeparator 	"

	self parse: 'def function(fp, *, x): pass'.

	self assert: (fast allWithType: FASTPyBlock) size equals: 1.
	self assert: (fast allWithType: FASTPyFunctionDefinition) size equals: 1.
	self assert: (fast allWithType: FASTPyKeywordSeparator) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyParameter) size equals: 2.
	self assert: (fast allWithType: FASTPyPassStatement) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'def function(fp, *, x): pass'.
	self assert: (self topEntity isOfType: FASTPyFunctionDefinition).
	self assert: (self topEntity isOfType: FASTPyTDefinition).
	self assert: (self topEntity isOfType: FASTTWithParameters).
	self assert: (self topEntity isOfType: FASTTWithStatements).
	self assert: self topEntity name equals: 'function'.
	
	"Testing value of multivalued relation parameters"
	self assert: self topEntity parameters size equals: 3.
	
		stack push: (stack top parameters at: 1).
		self assert: self topEntity sourceCode equals: 'fp'.
		self assert: (self topEntity isOfType: FASTPyParameter).
		self assert: (self topEntity isOfType: FASTTVariableEntity).
		self assert: self topEntity name equals: 'fp'.
		stack pop.
	
		stack push: (stack top parameters at: 2).
		self assert: self topEntity sourceCode equals: '*'.
		self assert: (self topEntity isOfType: FASTPyKeywordSeparator).
		self assert: (self topEntity isOfType: FASTTVariableEntity).
		stack pop.
	
		stack push: (stack top parameters at: 3).
		self assert: self topEntity sourceCode equals: 'x'.
		self assert: (self topEntity isOfType: FASTPyParameter).
		self assert: (self topEntity isOfType: FASTTVariableEntity).
		self assert: self topEntity name equals: 'x'.
		stack pop.
	
	"Testing value of monovalue relation statementBlock"
	self assert: self topEntity statementBlock isNotNil.

		stack push: self topEntity statementBlock.
		self assert: self topEntity sourceCode equals: 'pass'.
		self assert: (self topEntity isOfType: FASTPyBlock).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'pass'.
			self assert: (self topEntity isOfType: FASTPyPassStatement)
]

{ #category : 'tests - lambdas' }
FASTPythonImporterTest >> testLambda [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testLambda 	"

	self parse: 'lambda : 3'.

	self assert: (fast allWithType: FASTPyInteger) size equals: 1.
	self assert: (fast allWithType: FASTPyLambda) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'lambda : 3'.
	self assert: (self topEntity isOfType: FASTPyLambda).
	self assert: (self topEntity isOfType: FASTTWithParameters).
	
	"Testing value of monovalue relation expression"
	self assert: self topEntity expression isNotNil.

		stack push: self topEntity expression.
		self assert: self topEntity sourceCode equals: '3'.
		self assert: (self topEntity isOfType: FASTPyInteger).
		self assert: (self topEntity isOfType: FASTTLiteral).
		self assert: (self topEntity isOfType: FASTTNumericalLiteral)
]

{ #category : 'tests - lambdas' }
FASTPythonImporterTest >> testLambdaWithDefaultParameter [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testLambdaWithDefaultParameter 	"

	self parse: 'lambda x = 1: x'.

	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyInteger) size equals: 1.
	self assert: (fast allWithType: FASTPyLambda) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyParameter) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'lambda x = 1: x'.
	self assert: (self topEntity isOfType: FASTPyLambda).
	self assert: (self topEntity isOfType: FASTTWithParameters).
	
	"Testing value of monovalue relation expression"
	self assert: self topEntity expression isNotNil.

		stack push: self topEntity expression.
		self assert: self topEntity sourceCode equals: 'x'.
		self assert: (self topEntity isOfType: FASTPyIdentifier).
		stack pop.
	
	"Testing value of multivalued relation parameters"
	self assert: self topEntity parameters size equals: 1.
	
		stack push: (stack top parameters at: 1).
		self assert: self topEntity sourceCode equals: 'x = 1'.
		self assert: (self topEntity isOfType: FASTPyParameter).
		self assert: (self topEntity isOfType: FASTTVariableEntity).
		
		"Testing value of monovalue relation defaultValue"
		self assert: self topEntity defaultValue isNotNil.

			stack push: self topEntity defaultValue.
			self assert: self topEntity sourceCode equals: '1'.
			self assert: (self topEntity isOfType: FASTPyInteger).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTNumericalLiteral).
			stack pop.
		self assert: self topEntity name equals: 'x'
]

{ #category : 'tests - lambdas' }
FASTPythonImporterTest >> testLambdaWithDictionarySplatParameter [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testLambdaWithDictionarySplatParameter 	"

	self parse: 'lambda **fp, x: x'.

	self assert: (fast allWithType: FASTPyDictionarySplatParameter) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyLambda) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyParameter) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'lambda **fp, x: x'.
	self assert: (self topEntity isOfType: FASTPyLambda).
	self assert: (self topEntity isOfType: FASTTWithParameters).
	
	"Testing value of monovalue relation expression"
	self assert: self topEntity expression isNotNil.

		stack push: self topEntity expression.
		self assert: self topEntity sourceCode equals: 'x'.
		self assert: (self topEntity isOfType: FASTPyIdentifier).
		stack pop.
	
	"Testing value of multivalued relation parameters"
	self assert: self topEntity parameters size equals: 2.
	
		stack push: (stack top parameters at: 1).
		self assert: self topEntity sourceCode equals: '**fp'.
		self assert: (self topEntity isOfType: FASTPyDictionarySplatParameter).
		self assert: (self topEntity isOfType: FASTTVariableEntity).
		self assert: self topEntity name equals: 'fp'.
		stack pop.
	
		stack push: (stack top parameters at: 2).
		self assert: self topEntity sourceCode equals: 'x'.
		self assert: (self topEntity isOfType: FASTPyParameter).
		self assert: (self topEntity isOfType: FASTTVariableEntity).
		self assert: self topEntity name equals: 'x'
]

{ #category : 'tests - lambdas' }
FASTPythonImporterTest >> testLambdaWithKeywordSeparator [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testLambdaWithKeywordSeparator 	"

	self parse: 'lambda fp, *, x: fp + x'.

	self assert: (fast allWithType: FASTPyBinaryOperator) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 2.
	self assert: (fast allWithType: FASTPyKeywordSeparator) size equals: 1.
	self assert: (fast allWithType: FASTPyLambda) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyParameter) size equals: 2.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'lambda fp, *, x: fp + x'.
	self assert: (self topEntity isOfType: FASTPyLambda).
	self assert: (self topEntity isOfType: FASTTWithParameters).
	
	"Testing value of monovalue relation expression"
	self assert: self topEntity expression isNotNil.

		stack push: self topEntity expression.
		self assert: self topEntity sourceCode equals: 'fp + x'.
		self assert: (self topEntity isOfType: FASTPyBinaryOperator).
		self assert: (self topEntity isOfType: FASTTBinaryExpression).
		
		"Testing value of monovalue relation leftOperand"
		self assert: self topEntity leftOperand isNotNil.

			stack push: self topEntity leftOperand.
			self assert: self topEntity sourceCode equals: 'fp'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		self assert: self topEntity operator equals: '+'.
		
		"Testing value of monovalue relation rightOperand"
		self assert: self topEntity rightOperand isNotNil.

			stack push: self topEntity rightOperand.
			self assert: self topEntity sourceCode equals: 'x'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		stack pop.
	
	"Testing value of multivalued relation parameters"
	self assert: self topEntity parameters size equals: 3.
	
		stack push: (stack top parameters at: 1).
		self assert: self topEntity sourceCode equals: 'fp'.
		self assert: (self topEntity isOfType: FASTPyParameter).
		self assert: (self topEntity isOfType: FASTTVariableEntity).
		self assert: self topEntity name equals: 'fp'.
		stack pop.
	
		stack push: (stack top parameters at: 2).
		self assert: self topEntity sourceCode equals: '*'.
		self assert: (self topEntity isOfType: FASTPyKeywordSeparator).
		self assert: (self topEntity isOfType: FASTTVariableEntity).
		stack pop.
	
		stack push: (stack top parameters at: 3).
		self assert: self topEntity sourceCode equals: 'x'.
		self assert: (self topEntity isOfType: FASTPyParameter).
		self assert: (self topEntity isOfType: FASTTVariableEntity).
		self assert: self topEntity name equals: 'x'
]

{ #category : 'tests - lambdas' }
FASTPythonImporterTest >> testLambdaWithListSplatParameter [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testLambdaWithListSplatParameter 	"

	self parse: 'lambda *fp, x: x'.

	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyLambda) size equals: 1.
	self assert: (fast allWithType: FASTPyListSplatParameter) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyParameter) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'lambda *fp, x: x'.
	self assert: (self topEntity isOfType: FASTPyLambda).
	self assert: (self topEntity isOfType: FASTTWithParameters).
	
	"Testing value of monovalue relation expression"
	self assert: self topEntity expression isNotNil.

		stack push: self topEntity expression.
		self assert: self topEntity sourceCode equals: 'x'.
		self assert: (self topEntity isOfType: FASTPyIdentifier).
		stack pop.
	
	"Testing value of multivalued relation parameters"
	self assert: self topEntity parameters size equals: 2.
	
		stack push: (stack top parameters at: 1).
		self assert: self topEntity sourceCode equals: '*fp'.
		self assert: (self topEntity isOfType: FASTPyListSplatParameter).
		self assert: (self topEntity isOfType: FASTTVariableEntity).
		self assert: self topEntity name equals: 'fp'.
		stack pop.
	
		stack push: (stack top parameters at: 2).
		self assert: self topEntity sourceCode equals: 'x'.
		self assert: (self topEntity isOfType: FASTPyParameter).
		self assert: (self topEntity isOfType: FASTTVariableEntity).
		self assert: self topEntity name equals: 'x'
]

{ #category : 'tests - lambdas' }
FASTPythonImporterTest >> testLambdaWithParameter [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testLambdaWithParameter 	"

	self parse: 'lambda x: x * x'.

	self assert: (fast allWithType: FASTPyBinaryOperator) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 2.
	self assert: (fast allWithType: FASTPyLambda) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyParameter) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'lambda x: x * x'.
	self assert: (self topEntity isOfType: FASTPyLambda).
	self assert: (self topEntity isOfType: FASTTWithParameters).
	
	"Testing value of monovalue relation expression"
	self assert: self topEntity expression isNotNil.

		stack push: self topEntity expression.
		self assert: self topEntity sourceCode equals: 'x * x'.
		self assert: (self topEntity isOfType: FASTPyBinaryOperator).
		self assert: (self topEntity isOfType: FASTTBinaryExpression).
		
		"Testing value of monovalue relation leftOperand"
		self assert: self topEntity leftOperand isNotNil.

			stack push: self topEntity leftOperand.
			self assert: self topEntity sourceCode equals: 'x'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		self assert: self topEntity operator equals: '*'.
		
		"Testing value of monovalue relation rightOperand"
		self assert: self topEntity rightOperand isNotNil.

			stack push: self topEntity rightOperand.
			self assert: self topEntity sourceCode equals: 'x'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		stack pop.
	
	"Testing value of multivalued relation parameters"
	self assert: self topEntity parameters size equals: 1.
	
		stack push: (stack top parameters at: 1).
		self assert: self topEntity sourceCode equals: 'x'.
		self assert: (self topEntity isOfType: FASTPyParameter).
		self assert: (self topEntity isOfType: FASTTVariableEntity).
		self assert: self topEntity name equals: 'x'
]

{ #category : 'tests - lambdas' }
FASTPythonImporterTest >> testLambdaWithParameters [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testLambdaWithParameters 	"

	self parse: 'lambda a, b: a + b'.

	self assert: (fast allWithType: FASTPyBinaryOperator) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 2.
	self assert: (fast allWithType: FASTPyLambda) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyParameter) size equals: 2.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'lambda a, b: a + b'.
	self assert: (self topEntity isOfType: FASTPyLambda).
	self assert: (self topEntity isOfType: FASTTWithParameters).
	
	"Testing value of monovalue relation expression"
	self assert: self topEntity expression isNotNil.

		stack push: self topEntity expression.
		self assert: self topEntity sourceCode equals: 'a + b'.
		self assert: (self topEntity isOfType: FASTPyBinaryOperator).
		self assert: (self topEntity isOfType: FASTTBinaryExpression).
		
		"Testing value of monovalue relation leftOperand"
		self assert: self topEntity leftOperand isNotNil.

			stack push: self topEntity leftOperand.
			self assert: self topEntity sourceCode equals: 'a'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		self assert: self topEntity operator equals: '+'.
		
		"Testing value of monovalue relation rightOperand"
		self assert: self topEntity rightOperand isNotNil.

			stack push: self topEntity rightOperand.
			self assert: self topEntity sourceCode equals: 'b'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		stack pop.
	
	"Testing value of multivalued relation parameters"
	self assert: self topEntity parameters size equals: 2.
	
		stack push: (stack top parameters at: 1).
		self assert: self topEntity sourceCode equals: 'a'.
		self assert: (self topEntity isOfType: FASTPyParameter).
		self assert: (self topEntity isOfType: FASTTVariableEntity).
		self assert: self topEntity name equals: 'a'.
		stack pop.
	
		stack push: (stack top parameters at: 2).
		self assert: self topEntity sourceCode equals: 'b'.
		self assert: (self topEntity isOfType: FASTPyParameter).
		self assert: (self topEntity isOfType: FASTTVariableEntity).
		self assert: self topEntity name equals: 'b'
]

{ #category : 'tests - lambdas' }
FASTPythonImporterTest >> testLambdaWithPositionalSeparator [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testLambdaWithPositionalSeparator 	"

	self parse: 'lambda fp, /, x: fp + x'.

	self assert: (fast allWithType: FASTPyBinaryOperator) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 2.
	self assert: (fast allWithType: FASTPyLambda) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyParameter) size equals: 2.
	self assert: (fast allWithType: FASTPyPositionalSeparator) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'lambda fp, /, x: fp + x'.
	self assert: (self topEntity isOfType: FASTPyLambda).
	self assert: (self topEntity isOfType: FASTTWithParameters).
	
	"Testing value of monovalue relation expression"
	self assert: self topEntity expression isNotNil.

		stack push: self topEntity expression.
		self assert: self topEntity sourceCode equals: 'fp + x'.
		self assert: (self topEntity isOfType: FASTPyBinaryOperator).
		self assert: (self topEntity isOfType: FASTTBinaryExpression).
		
		"Testing value of monovalue relation leftOperand"
		self assert: self topEntity leftOperand isNotNil.

			stack push: self topEntity leftOperand.
			self assert: self topEntity sourceCode equals: 'fp'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		self assert: self topEntity operator equals: '+'.
		
		"Testing value of monovalue relation rightOperand"
		self assert: self topEntity rightOperand isNotNil.

			stack push: self topEntity rightOperand.
			self assert: self topEntity sourceCode equals: 'x'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		stack pop.
	
	"Testing value of multivalued relation parameters"
	self assert: self topEntity parameters size equals: 3.
	
		stack push: (stack top parameters at: 1).
		self assert: self topEntity sourceCode equals: 'fp'.
		self assert: (self topEntity isOfType: FASTPyParameter).
		self assert: (self topEntity isOfType: FASTTVariableEntity).
		self assert: self topEntity name equals: 'fp'.
		stack pop.
	
		stack push: (stack top parameters at: 2).
		self assert: self topEntity sourceCode equals: '/'.
		self assert: (self topEntity isOfType: FASTPyPositionalSeparator).
		self assert: (self topEntity isOfType: FASTTVariableEntity).
		stack pop.
	
		stack push: (stack top parameters at: 3).
		self assert: self topEntity sourceCode equals: 'x'.
		self assert: (self topEntity isOfType: FASTPyParameter).
		self assert: (self topEntity isOfType: FASTTVariableEntity).
		self assert: self topEntity name equals: 'x'
]

{ #category : 'tests - collections' }
FASTPythonImporterTest >> testList [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testList 	"

	self parse: '[1, 2, 3]'.

	self assert: (fast allWithType: FASTPyInteger) size equals: 3.
	self assert: (fast allWithType: FASTPyList) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: '[1, 2, 3]'.
	self assert: (self topEntity isOfType: FASTPyList).
	
	"Testing value of multivalued relation initializers"
	self assert: self topEntity initializers size equals: 3.
	
		stack push: (stack top initializers at: 1).
		self assert: self topEntity sourceCode equals: '1'.
		self assert: (self topEntity isOfType: FASTPyInteger).
		self assert: (self topEntity isOfType: FASTTLiteral).
		self assert: (self topEntity isOfType: FASTTNumericalLiteral).
		stack pop.
	
		stack push: (stack top initializers at: 2).
		self assert: self topEntity sourceCode equals: '2'.
		self assert: (self topEntity isOfType: FASTPyInteger).
		self assert: (self topEntity isOfType: FASTTLiteral).
		self assert: (self topEntity isOfType: FASTTNumericalLiteral).
		stack pop.
	
		stack push: (stack top initializers at: 3).
		self assert: self topEntity sourceCode equals: '3'.
		self assert: (self topEntity isOfType: FASTPyInteger).
		self assert: (self topEntity isOfType: FASTTLiteral).
		self assert: (self topEntity isOfType: FASTTNumericalLiteral)
]

{ #category : 'tests - comprehension' }
FASTPythonImporterTest >> testListComprehension [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testListComprehension 	"

	self parse: '[x * x for x in range(5)]'.

	self assert: (fast allWithType: FASTPyBinaryOperator) size equals: 1.
	self assert: (fast allWithType: FASTPyCall) size equals: 1.
	self assert: (fast allWithType: FASTPyForInClause) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 4.
	self assert: (fast allWithType: FASTPyInteger) size equals: 1.
	self assert: (fast allWithType: FASTPyListComprehension) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: '[x * x for x in range(5)]'.
	self assert: (self topEntity isOfType: FASTPyListComprehension).
	self assert: (self topEntity isOfType: FASTPyTComprehension).
	self assert: (self topEntity isOfType: FASTTWithCondition).
	
	"Testing value of monovalue relation body"
	self assert: self topEntity body isNotNil.

		stack push: self topEntity body.
		self assert: self topEntity sourceCode equals: 'x * x'.
		self assert: (self topEntity isOfType: FASTPyBinaryOperator).
		self assert: (self topEntity isOfType: FASTTBinaryExpression).
		
		"Testing value of monovalue relation leftOperand"
		self assert: self topEntity leftOperand isNotNil.

			stack push: self topEntity leftOperand.
			self assert: self topEntity sourceCode equals: 'x'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		self assert: self topEntity operator equals: '*'.
		
		"Testing value of monovalue relation rightOperand"
		self assert: self topEntity rightOperand isNotNil.

			stack push: self topEntity rightOperand.
			self assert: self topEntity sourceCode equals: 'x'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		stack pop.
	
	"Testing value of multivalued relation clauses"
	self assert: self topEntity clauses size equals: 1.
	
		stack push: (stack top clauses at: 1).
		self assert: self topEntity sourceCode equals: 'for x in range(5)'.
		self assert: (self topEntity isOfType: FASTPyForInClause).
		
		"Testing value of monovalue relation left"
		self assert: self topEntity left isNotNil.

			stack push: self topEntity left.
			self assert: self topEntity sourceCode equals: 'x'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		
		"Testing value of monovalue relation right"
		self assert: self topEntity right isNotNil.

			stack push: self topEntity right.
			self assert: self topEntity sourceCode equals: 'range(5)'.
			self assert: (self topEntity isOfType: FASTPyCall).
			self assert: (self topEntity isOfType: FASTTInvocation).
			self assert: (self topEntity isOfType: FASTTWithArguments).
			
			"Testing value of multivalued relation arguments"
			self assert: self topEntity arguments size equals: 1.
			
				stack push: (stack top arguments at: 1).
				self assert: self topEntity sourceCode equals: '5'.
				self assert: (self topEntity isOfType: FASTPyInteger).
				self assert: (self topEntity isOfType: FASTTLiteral).
				self assert: (self topEntity isOfType: FASTTNumericalLiteral).
				stack pop.
			
			"Testing value of monovalue relation callee"
			self assert: self topEntity callee isNotNil.

				stack push: self topEntity callee.
				self assert: self topEntity sourceCode equals: 'range'.
				self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - comprehension' }
FASTPythonImporterTest >> testListComprehensionWithIfClause [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testListComprehensionWithIfClause 	"

	self parse: '[x for x in range(10) if x  == 0]'.

	self assert: (fast allWithType: FASTPyCall) size equals: 1.
	self assert: (fast allWithType: FASTPyComparisonOperator) size equals: 1.
	self assert: (fast allWithType: FASTPyForInClause) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 4.
	self assert: (fast allWithType: FASTPyInteger) size equals: 2.
	self assert: (fast allWithType: FASTPyListComprehension) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: '[x for x in range(10) if x  == 0]'.
	self assert: (self topEntity isOfType: FASTPyListComprehension).
	self assert: (self topEntity isOfType: FASTPyTComprehension).
	self assert: (self topEntity isOfType: FASTTWithCondition).
	
	"Testing value of monovalue relation body"
	self assert: self topEntity body isNotNil.

		stack push: self topEntity body.
		self assert: self topEntity sourceCode equals: 'x'.
		self assert: (self topEntity isOfType: FASTPyIdentifier).
		stack pop.
	
	"Testing value of multivalued relation clauses"
	self assert: self topEntity clauses size equals: 1.
	
		stack push: (stack top clauses at: 1).
		self assert: self topEntity sourceCode equals: 'for x in range(10)'.
		self assert: (self topEntity isOfType: FASTPyForInClause).
		
		"Testing value of monovalue relation left"
		self assert: self topEntity left isNotNil.

			stack push: self topEntity left.
			self assert: self topEntity sourceCode equals: 'x'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		
		"Testing value of monovalue relation right"
		self assert: self topEntity right isNotNil.

			stack push: self topEntity right.
			self assert: self topEntity sourceCode equals: 'range(10)'.
			self assert: (self topEntity isOfType: FASTPyCall).
			self assert: (self topEntity isOfType: FASTTInvocation).
			self assert: (self topEntity isOfType: FASTTWithArguments).
			
			"Testing value of multivalued relation arguments"
			self assert: self topEntity arguments size equals: 1.
			
				stack push: (stack top arguments at: 1).
				self assert: self topEntity sourceCode equals: '10'.
				self assert: (self topEntity isOfType: FASTPyInteger).
				self assert: (self topEntity isOfType: FASTTLiteral).
				self assert: (self topEntity isOfType: FASTTNumericalLiteral).
				stack pop.
			
			"Testing value of monovalue relation callee"
			self assert: self topEntity callee isNotNil.

				stack push: self topEntity callee.
				self assert: self topEntity sourceCode equals: 'range'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation condition"
	self assert: self topEntity condition isNotNil.

		stack push: self topEntity condition.
		self assert: self topEntity sourceCode equals: 'x  == 0'.
		self assert: (self topEntity isOfType: FASTPyComparisonOperator).
		
		"Testing value of multivalued relation operands"
		self assert: self topEntity operands size equals: 2.
		
			stack push: (stack top operands at: 1).
			self assert: self topEntity sourceCode equals: 'x'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		
			stack push: (stack top operands at: 2).
			self assert: self topEntity sourceCode equals: '0'.
			self assert: (self topEntity isOfType: FASTPyInteger).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTNumericalLiteral).
			stack pop.
		self assert: self topEntity operators equals: #('==')
]

{ #category : 'tests - comprehension' }
FASTPythonImporterTest >> testListComprehensionWithPatternList [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testListComprehensionWithPatternList 	"

	self parse: '[abs(x) + abs(y) for x, y in points]'.

	self assert: (fast allWithType: FASTPyBinaryOperator) size equals: 1.
	self assert: (fast allWithType: FASTPyCall) size equals: 2.
	self assert: (fast allWithType: FASTPyForInClause) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 7.
	self assert: (fast allWithType: FASTPyListComprehension) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyTuple) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: '[abs(x) + abs(y) for x, y in points]'.
	self assert: (self topEntity isOfType: FASTPyListComprehension).
	self assert: (self topEntity isOfType: FASTPyTComprehension).
	self assert: (self topEntity isOfType: FASTTWithCondition).
	
	"Testing value of monovalue relation body"
	self assert: self topEntity body isNotNil.

		stack push: self topEntity body.
		self assert: self topEntity sourceCode equals: 'abs(x) + abs(y)'.
		self assert: (self topEntity isOfType: FASTPyBinaryOperator).
		self assert: (self topEntity isOfType: FASTTBinaryExpression).
		
		"Testing value of monovalue relation leftOperand"
		self assert: self topEntity leftOperand isNotNil.

			stack push: self topEntity leftOperand.
			self assert: self topEntity sourceCode equals: 'abs(x)'.
			self assert: (self topEntity isOfType: FASTPyCall).
			self assert: (self topEntity isOfType: FASTTInvocation).
			self assert: (self topEntity isOfType: FASTTWithArguments).
			
			"Testing value of multivalued relation arguments"
			self assert: self topEntity arguments size equals: 1.
			
				stack push: (stack top arguments at: 1).
				self assert: self topEntity sourceCode equals: 'x'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			
			"Testing value of monovalue relation callee"
			self assert: self topEntity callee isNotNil.

				stack push: self topEntity callee.
				self assert: self topEntity sourceCode equals: 'abs'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			stack pop.
		self assert: self topEntity operator equals: '+'.
		
		"Testing value of monovalue relation rightOperand"
		self assert: self topEntity rightOperand isNotNil.

			stack push: self topEntity rightOperand.
			self assert: self topEntity sourceCode equals: 'abs(y)'.
			self assert: (self topEntity isOfType: FASTPyCall).
			self assert: (self topEntity isOfType: FASTTInvocation).
			self assert: (self topEntity isOfType: FASTTWithArguments).
			
			"Testing value of multivalued relation arguments"
			self assert: self topEntity arguments size equals: 1.
			
				stack push: (stack top arguments at: 1).
				self assert: self topEntity sourceCode equals: 'y'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			
			"Testing value of monovalue relation callee"
			self assert: self topEntity callee isNotNil.

				stack push: self topEntity callee.
				self assert: self topEntity sourceCode equals: 'abs'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			stack pop.
		stack pop.
	
	"Testing value of multivalued relation clauses"
	self assert: self topEntity clauses size equals: 1.
	
		stack push: (stack top clauses at: 1).
		self assert: self topEntity sourceCode equals: 'for x, y in points'.
		self assert: (self topEntity isOfType: FASTPyForInClause).
		
		"Testing value of monovalue relation left"
		self assert: self topEntity left isNotNil.

			stack push: self topEntity left.
			self assert: self topEntity sourceCode equals: 'x, y'.
			self assert: (self topEntity isOfType: FASTPyTuple).
			
			"Testing value of multivalued relation initializers"
			self assert: self topEntity initializers size equals: 2.
			
				stack push: (stack top initializers at: 1).
				self assert: self topEntity sourceCode equals: 'x'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			
				stack push: (stack top initializers at: 2).
				self assert: self topEntity sourceCode equals: 'y'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			stack pop.
		
		"Testing value of monovalue relation right"
		self assert: self topEntity right isNotNil.

			stack push: self topEntity right.
			self assert: self topEntity sourceCode equals: 'points'.
			self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - splats' }
FASTPythonImporterTest >> testListSplatInCall [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testListSplatInCall 	"

	self parse: 'func(*args)'.

	self assert: (fast allWithType: FASTPyCall) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 2.
	self assert: (fast allWithType: FASTPyListSplat) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'func(*args)'.
	self assert: (self topEntity isOfType: FASTPyCall).
	self assert: (self topEntity isOfType: FASTTInvocation).
	self assert: (self topEntity isOfType: FASTTWithArguments).
	
	"Testing value of multivalued relation arguments"
	self assert: self topEntity arguments size equals: 1.
	
		stack push: (stack top arguments at: 1).
		self assert: self topEntity sourceCode equals: '*args'.
		self assert: (self topEntity isOfType: FASTPyListSplat).
		
		"Testing value of monovalue relation expression"
		self assert: self topEntity expression isNotNil.

			stack push: self topEntity expression.
			self assert: self topEntity sourceCode equals: 'args'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation callee"
	self assert: self topEntity callee isNotNil.

		stack push: self topEntity callee.
		self assert: self topEntity sourceCode equals: 'func'.
		self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - splats' }
FASTPythonImporterTest >> testListSplatInList [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testListSplatInList 	"

	self parse: '[*args, 4]'.

	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyInteger) size equals: 1.
	self assert: (fast allWithType: FASTPyList) size equals: 1.
	self assert: (fast allWithType: FASTPyListSplat) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: '[*args, 4]'.
	self assert: (self topEntity isOfType: FASTPyList).
	
	"Testing value of multivalued relation initializers"
	self assert: self topEntity initializers size equals: 2.
	
		stack push: (stack top initializers at: 1).
		self assert: self topEntity sourceCode equals: '*args'.
		self assert: (self topEntity isOfType: FASTPyListSplat).
		
		"Testing value of monovalue relation expression"
		self assert: self topEntity expression isNotNil.

			stack push: self topEntity expression.
			self assert: self topEntity sourceCode equals: 'args'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		stack pop.
	
		stack push: (stack top initializers at: 2).
		self assert: self topEntity sourceCode equals: '4'.
		self assert: (self topEntity isOfType: FASTPyInteger).
		self assert: (self topEntity isOfType: FASTTLiteral).
		self assert: (self topEntity isOfType: FASTTNumericalLiteral)
]

{ #category : 'tests - splats' }
FASTPythonImporterTest >> testListSplatOnAttributeAccess [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testListSplatOnAttributeAccess 	"

	self parse: 'func(*obj.list)'.

	self assert: (fast allWithType: FASTPyAttributeAccess) size equals: 1.
	self assert: (fast allWithType: FASTPyCall) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 3.
	self assert: (fast allWithType: FASTPyListSplat) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'func(*obj.list)'.
	self assert: (self topEntity isOfType: FASTPyCall).
	self assert: (self topEntity isOfType: FASTTInvocation).
	self assert: (self topEntity isOfType: FASTTWithArguments).
	
	"Testing value of multivalued relation arguments"
	self assert: self topEntity arguments size equals: 1.
	
		stack push: (stack top arguments at: 1).
		self assert: self topEntity sourceCode equals: '*obj.list'.
		self assert: (self topEntity isOfType: FASTPyListSplat).
		
		"Testing value of monovalue relation expression"
		self assert: self topEntity expression isNotNil.

			stack push: self topEntity expression.
			self assert: self topEntity sourceCode equals: 'obj.list'.
			self assert: (self topEntity isOfType: FASTPyAttributeAccess).
			
			"Testing value of monovalue relation attribute"
			self assert: self topEntity attribute isNotNil.

				stack push: self topEntity attribute.
				self assert: self topEntity sourceCode equals: 'list'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			
			"Testing value of monovalue relation object"
			self assert: self topEntity object isNotNil.

				stack push: self topEntity object.
				self assert: self topEntity sourceCode equals: 'obj'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation callee"
	self assert: self topEntity callee isNotNil.

		stack push: self topEntity callee.
		self assert: self topEntity sourceCode equals: 'func'.
		self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - splats' }
FASTPythonImporterTest >> testListSplatOnBooleanOperator [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testListSplatOnBooleanOperator 	"

	self parse: 'func(*a or b)'.

	self assert: (fast allWithType: FASTPyBooleanOperator) size equals: 1.
	self assert: (fast allWithType: FASTPyCall) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 3.
	self assert: (fast allWithType: FASTPyListSplat) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'func(*a or b)'.
	self assert: (self topEntity isOfType: FASTPyCall).
	self assert: (self topEntity isOfType: FASTTInvocation).
	self assert: (self topEntity isOfType: FASTTWithArguments).
	
	"Testing value of multivalued relation arguments"
	self assert: self topEntity arguments size equals: 1.
	
		stack push: (stack top arguments at: 1).
		self assert: self topEntity sourceCode equals: '*a or b'.
		self assert: (self topEntity isOfType: FASTPyListSplat).
		
		"Testing value of monovalue relation expression"
		self assert: self topEntity expression isNotNil.

			stack push: self topEntity expression.
			self assert: self topEntity sourceCode equals: 'a or b'.
			self assert: (self topEntity isOfType: FASTPyBooleanOperator).
			self assert: (self topEntity isOfType: FASTTBinaryExpression).
			
			"Testing value of monovalue relation leftOperand"
			self assert: self topEntity leftOperand isNotNil.

				stack push: self topEntity leftOperand.
				self assert: self topEntity sourceCode equals: 'a'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			self assert: self topEntity operator equals: 'or'.
			
			"Testing value of monovalue relation rightOperand"
			self assert: self topEntity rightOperand isNotNil.

				stack push: self topEntity rightOperand.
				self assert: self topEntity sourceCode equals: 'b'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation callee"
	self assert: self topEntity callee isNotNil.

		stack push: self topEntity callee.
		self assert: self topEntity sourceCode equals: 'func'.
		self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - splats' }
FASTPythonImporterTest >> testListSplatOnCall [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testListSplatOnCall 	"

	self parse: 'func(*getList())'.

	self assert: (fast allWithType: FASTPyCall) size equals: 2.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 2.
	self assert: (fast allWithType: FASTPyListSplat) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'func(*getList())'.
	self assert: (self topEntity isOfType: FASTPyCall).
	self assert: (self topEntity isOfType: FASTTInvocation).
	self assert: (self topEntity isOfType: FASTTWithArguments).
	
	"Testing value of multivalued relation arguments"
	self assert: self topEntity arguments size equals: 1.
	
		stack push: (stack top arguments at: 1).
		self assert: self topEntity sourceCode equals: '*getList()'.
		self assert: (self topEntity isOfType: FASTPyListSplat).
		
		"Testing value of monovalue relation expression"
		self assert: self topEntity expression isNotNil.

			stack push: self topEntity expression.
			self assert: self topEntity sourceCode equals: 'getList()'.
			self assert: (self topEntity isOfType: FASTPyCall).
			self assert: (self topEntity isOfType: FASTTInvocation).
			self assert: (self topEntity isOfType: FASTTWithArguments).
			
			"Testing value of monovalue relation callee"
			self assert: self topEntity callee isNotNil.

				stack push: self topEntity callee.
				self assert: self topEntity sourceCode equals: 'getList'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation callee"
	self assert: self topEntity callee isNotNil.

		stack push: self topEntity callee.
		self assert: self topEntity sourceCode equals: 'func'.
		self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - splats' }
FASTPythonImporterTest >> testListSplatOnList [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testListSplatOnList 	"

	self parse: 'func(*[1, 2])'.

	self assert: (fast allWithType: FASTPyCall) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyInteger) size equals: 2.
	self assert: (fast allWithType: FASTPyList) size equals: 1.
	self assert: (fast allWithType: FASTPyListSplat) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'func(*[1, 2])'.
	self assert: (self topEntity isOfType: FASTPyCall).
	self assert: (self topEntity isOfType: FASTTInvocation).
	self assert: (self topEntity isOfType: FASTTWithArguments).
	
	"Testing value of multivalued relation arguments"
	self assert: self topEntity arguments size equals: 1.
	
		stack push: (stack top arguments at: 1).
		self assert: self topEntity sourceCode equals: '*[1, 2]'.
		self assert: (self topEntity isOfType: FASTPyListSplat).
		
		"Testing value of monovalue relation expression"
		self assert: self topEntity expression isNotNil.

			stack push: self topEntity expression.
			self assert: self topEntity sourceCode equals: '[1, 2]'.
			self assert: (self topEntity isOfType: FASTPyList).
			
			"Testing value of multivalued relation initializers"
			self assert: self topEntity initializers size equals: 2.
			
				stack push: (stack top initializers at: 1).
				self assert: self topEntity sourceCode equals: '1'.
				self assert: (self topEntity isOfType: FASTPyInteger).
				self assert: (self topEntity isOfType: FASTTLiteral).
				self assert: (self topEntity isOfType: FASTTNumericalLiteral).
				stack pop.
			
				stack push: (stack top initializers at: 2).
				self assert: self topEntity sourceCode equals: '2'.
				self assert: (self topEntity isOfType: FASTPyInteger).
				self assert: (self topEntity isOfType: FASTTLiteral).
				self assert: (self topEntity isOfType: FASTTNumericalLiteral).
				stack pop.
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation callee"
	self assert: self topEntity callee isNotNil.

		stack push: self topEntity callee.
		self assert: self topEntity sourceCode equals: 'func'.
		self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - splats' }
FASTPythonImporterTest >> testListSplatOnListComprehension [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testListSplatOnListComprehension 	"

	self parse: 'func(*[format(f) for f in FILES])'.

	self assert: (fast allWithType: FASTPyCall) size equals: 2.
	self assert: (fast allWithType: FASTPyForInClause) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 5.
	self assert: (fast allWithType: FASTPyListComprehension) size equals: 1.
	self assert: (fast allWithType: FASTPyListSplat) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'func(*[format(f) for f in FILES])'.
	self assert: (self topEntity isOfType: FASTPyCall).
	self assert: (self topEntity isOfType: FASTTInvocation).
	self assert: (self topEntity isOfType: FASTTWithArguments).
	
	"Testing value of multivalued relation arguments"
	self assert: self topEntity arguments size equals: 1.
	
		stack push: (stack top arguments at: 1).
		self assert: self topEntity sourceCode equals: '*[format(f) for f in FILES]'.
		self assert: (self topEntity isOfType: FASTPyListSplat).
		
		"Testing value of monovalue relation expression"
		self assert: self topEntity expression isNotNil.

			stack push: self topEntity expression.
			self assert: self topEntity sourceCode equals: '[format(f) for f in FILES]'.
			self assert: (self topEntity isOfType: FASTPyListComprehension).
			self assert: (self topEntity isOfType: FASTPyTComprehension).
			self assert: (self topEntity isOfType: FASTTWithCondition).
			
			"Testing value of monovalue relation body"
			self assert: self topEntity body isNotNil.

				stack push: self topEntity body.
				self assert: self topEntity sourceCode equals: 'format(f)'.
				self assert: (self topEntity isOfType: FASTPyCall).
				self assert: (self topEntity isOfType: FASTTInvocation).
				self assert: (self topEntity isOfType: FASTTWithArguments).
				
				"Testing value of multivalued relation arguments"
				self assert: self topEntity arguments size equals: 1.
				
					stack push: (stack top arguments at: 1).
					self assert: self topEntity sourceCode equals: 'f'.
					self assert: (self topEntity isOfType: FASTPyIdentifier).
					stack pop.
				
				"Testing value of monovalue relation callee"
				self assert: self topEntity callee isNotNil.

					stack push: self topEntity callee.
					self assert: self topEntity sourceCode equals: 'format'.
					self assert: (self topEntity isOfType: FASTPyIdentifier).
					stack pop.
				stack pop.
			
			"Testing value of multivalued relation clauses"
			self assert: self topEntity clauses size equals: 1.
			
				stack push: (stack top clauses at: 1).
				self assert: self topEntity sourceCode equals: 'for f in FILES'.
				self assert: (self topEntity isOfType: FASTPyForInClause).
				
				"Testing value of monovalue relation left"
				self assert: self topEntity left isNotNil.

					stack push: self topEntity left.
					self assert: self topEntity sourceCode equals: 'f'.
					self assert: (self topEntity isOfType: FASTPyIdentifier).
					stack pop.
				
				"Testing value of monovalue relation right"
				self assert: self topEntity right isNotNil.

					stack push: self topEntity right.
					self assert: self topEntity sourceCode equals: 'FILES'.
					self assert: (self topEntity isOfType: FASTPyIdentifier).
					stack pop.
				stack pop.
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation callee"
	self assert: self topEntity callee isNotNil.

		stack push: self topEntity callee.
		self assert: self topEntity sourceCode equals: 'func'.
		self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - splats' }
FASTPythonImporterTest >> testListSplatOnParenthesis [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testListSplatOnParenthesis 	"

	self parse: 'func(*(args))'.

	self assert: (fast allWithType: FASTPyCall) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 2.
	self assert: (fast allWithType: FASTPyListSplat) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'func(*(args))'.
	self assert: (self topEntity isOfType: FASTPyCall).
	self assert: (self topEntity isOfType: FASTTInvocation).
	self assert: (self topEntity isOfType: FASTTWithArguments).
	
	"Testing value of multivalued relation arguments"
	self assert: self topEntity arguments size equals: 1.
	
		stack push: (stack top arguments at: 1).
		self assert: self topEntity sourceCode equals: '*(args)'.
		self assert: (self topEntity isOfType: FASTPyListSplat).
		
		"Testing value of monovalue relation expression"
		self assert: self topEntity expression isNotNil.

			stack push: self topEntity expression.
			self assert: self topEntity sourceCode equals: 'args'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation callee"
	self assert: self topEntity callee isNotNil.

		stack push: self topEntity callee.
		self assert: self topEntity sourceCode equals: 'func'.
		self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - splats' }
FASTPythonImporterTest >> testListSplatOnString [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testListSplatOnString 	"

	self parse: 'func(*''absdef'')'.

	self assert: (fast allWithType: FASTPyCall) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyListSplat) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyString) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'func(*''absdef'')'.
	self assert: (self topEntity isOfType: FASTPyCall).
	self assert: (self topEntity isOfType: FASTTInvocation).
	self assert: (self topEntity isOfType: FASTTWithArguments).
	
	"Testing value of multivalued relation arguments"
	self assert: self topEntity arguments size equals: 1.
	
		stack push: (stack top arguments at: 1).
		self assert: self topEntity sourceCode equals: '*''absdef'''.
		self assert: (self topEntity isOfType: FASTPyListSplat).
		
		"Testing value of monovalue relation expression"
		self assert: self topEntity expression isNotNil.

			stack push: self topEntity expression.
			self assert: self topEntity sourceCode equals: '''absdef'''.
			self assert: (self topEntity isOfType: FASTPyString).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTStringLiteral).
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation callee"
	self assert: self topEntity callee isNotNil.

		stack push: self topEntity callee.
		self assert: self topEntity sourceCode equals: 'func'.
		self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - splats' }
FASTPythonImporterTest >> testListSplatOnSubscript [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testListSplatOnSubscript 	"

	self parse: 'func(*obj[1])'.

	self assert: (fast allWithType: FASTPyCall) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 2.
	self assert: (fast allWithType: FASTPyInteger) size equals: 1.
	self assert: (fast allWithType: FASTPyListSplat) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPySubscript) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'func(*obj[1])'.
	self assert: (self topEntity isOfType: FASTPyCall).
	self assert: (self topEntity isOfType: FASTTInvocation).
	self assert: (self topEntity isOfType: FASTTWithArguments).
	
	"Testing value of multivalued relation arguments"
	self assert: self topEntity arguments size equals: 1.
	
		stack push: (stack top arguments at: 1).
		self assert: self topEntity sourceCode equals: '*obj[1]'.
		self assert: (self topEntity isOfType: FASTPyListSplat).
		
		"Testing value of monovalue relation expression"
		self assert: self topEntity expression isNotNil.

			stack push: self topEntity expression.
			self assert: self topEntity sourceCode equals: 'obj[1]'.
			self assert: (self topEntity isOfType: FASTPySubscript).
			
			"Testing value of multivalued relation indices"
			self assert: self topEntity indices size equals: 1.
			
				stack push: (stack top indices at: 1).
				self assert: self topEntity sourceCode equals: '1'.
				self assert: (self topEntity isOfType: FASTPyInteger).
				self assert: (self topEntity isOfType: FASTTLiteral).
				self assert: (self topEntity isOfType: FASTTNumericalLiteral).
				stack pop.
			
			"Testing value of monovalue relation value"
			self assert: self topEntity value isNotNil.

				stack push: self topEntity value.
				self assert: self topEntity sourceCode equals: 'obj'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation callee"
	self assert: self topEntity callee isNotNil.

		stack push: self topEntity callee.
		self assert: self topEntity sourceCode equals: 'func'.
		self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - splats' }
FASTPythonImporterTest >> testListSplatOnTuple [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testListSplatOnTuple 	"

	self parse: 'funct(*(func, *args))'.

	self assert: (fast allWithType: FASTPyCall) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 3.
	self assert: (fast allWithType: FASTPyListSplat) size equals: 2.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyTuple) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'funct(*(func, *args))'.
	self assert: (self topEntity isOfType: FASTPyCall).
	self assert: (self topEntity isOfType: FASTTInvocation).
	self assert: (self topEntity isOfType: FASTTWithArguments).
	
	"Testing value of multivalued relation arguments"
	self assert: self topEntity arguments size equals: 1.
	
		stack push: (stack top arguments at: 1).
		self assert: self topEntity sourceCode equals: '*(func, *args)'.
		self assert: (self topEntity isOfType: FASTPyListSplat).
		
		"Testing value of monovalue relation expression"
		self assert: self topEntity expression isNotNil.

			stack push: self topEntity expression.
			self assert: self topEntity sourceCode equals: '(func, *args)'.
			self assert: (self topEntity isOfType: FASTPyTuple).
			
			"Testing value of multivalued relation initializers"
			self assert: self topEntity initializers size equals: 2.
			
				stack push: (stack top initializers at: 1).
				self assert: self topEntity sourceCode equals: 'func'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			
				stack push: (stack top initializers at: 2).
				self assert: self topEntity sourceCode equals: '*args'.
				self assert: (self topEntity isOfType: FASTPyListSplat).
				
				"Testing value of monovalue relation expression"
				self assert: self topEntity expression isNotNil.

					stack push: self topEntity expression.
					self assert: self topEntity sourceCode equals: 'args'.
					self assert: (self topEntity isOfType: FASTPyIdentifier).
					stack pop.
				stack pop.
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation callee"
	self assert: self topEntity callee isNotNil.

		stack push: self topEntity callee.
		self assert: self topEntity sourceCode equals: 'funct'.
		self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - function definitions' }
FASTPythonImporterTest >> testListSplatParameter [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testListSplatParameter 	"

	self parse: 'def function(*args): pass'.

	self assert: (fast allWithType: FASTPyBlock) size equals: 1.
	self assert: (fast allWithType: FASTPyFunctionDefinition) size equals: 1.
	self assert: (fast allWithType: FASTPyListSplatParameter) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPassStatement) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'def function(*args): pass'.
	self assert: (self topEntity isOfType: FASTPyFunctionDefinition).
	self assert: (self topEntity isOfType: FASTPyTDefinition).
	self assert: (self topEntity isOfType: FASTTWithParameters).
	self assert: (self topEntity isOfType: FASTTWithStatements).
	self assert: self topEntity name equals: 'function'.
	
	"Testing value of multivalued relation parameters"
	self assert: self topEntity parameters size equals: 1.
	
		stack push: (stack top parameters at: 1).
		self assert: self topEntity sourceCode equals: '*args'.
		self assert: (self topEntity isOfType: FASTPyListSplatParameter).
		self assert: (self topEntity isOfType: FASTTVariableEntity).
		self assert: self topEntity name equals: 'args'.
		stack pop.
	
	"Testing value of monovalue relation statementBlock"
	self assert: self topEntity statementBlock isNotNil.

		stack push: self topEntity statementBlock.
		self assert: self topEntity sourceCode equals: 'pass'.
		self assert: (self topEntity isOfType: FASTPyBlock).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'pass'.
			self assert: (self topEntity isOfType: FASTPyPassStatement)
]

{ #category : 'tests - collections' }
FASTPythonImporterTest >> testListWithAttribute [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testListWithAttribute 	"

	self parse: '[ obj.attr ]'.

	self assert: (fast allWithType: FASTPyAttributeAccess) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 2.
	self assert: (fast allWithType: FASTPyList) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: '[ obj.attr ]'.
	self assert: (self topEntity isOfType: FASTPyList).
	
	"Testing value of multivalued relation initializers"
	self assert: self topEntity initializers size equals: 1.
	
		stack push: (stack top initializers at: 1).
		self assert: self topEntity sourceCode equals: 'obj.attr'.
		self assert: (self topEntity isOfType: FASTPyAttributeAccess).
		
		"Testing value of monovalue relation attribute"
		self assert: self topEntity attribute isNotNil.

			stack push: self topEntity attribute.
			self assert: self topEntity sourceCode equals: 'attr'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		
		"Testing value of monovalue relation object"
		self assert: self topEntity object isNotNil.

			stack push: self topEntity object.
			self assert: self topEntity sourceCode equals: 'obj'.
			self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - collections' }
FASTPythonImporterTest >> testListWithBinaryOperator [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testListWithBinaryOperator 	"

	self parse: '[ 10 / 2 ]'.

	self assert: (fast allWithType: FASTPyBinaryOperator) size equals: 1.
	self assert: (fast allWithType: FASTPyInteger) size equals: 2.
	self assert: (fast allWithType: FASTPyList) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: '[ 10 / 2 ]'.
	self assert: (self topEntity isOfType: FASTPyList).
	
	"Testing value of multivalued relation initializers"
	self assert: self topEntity initializers size equals: 1.
	
		stack push: (stack top initializers at: 1).
		self assert: self topEntity sourceCode equals: '10 / 2'.
		self assert: (self topEntity isOfType: FASTPyBinaryOperator).
		self assert: (self topEntity isOfType: FASTTBinaryExpression).
		
		"Testing value of monovalue relation leftOperand"
		self assert: self topEntity leftOperand isNotNil.

			stack push: self topEntity leftOperand.
			self assert: self topEntity sourceCode equals: '10'.
			self assert: (self topEntity isOfType: FASTPyInteger).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTNumericalLiteral).
			stack pop.
		self assert: self topEntity operator equals: '/'.
		
		"Testing value of monovalue relation rightOperand"
		self assert: self topEntity rightOperand isNotNil.

			stack push: self topEntity rightOperand.
			self assert: self topEntity sourceCode equals: '2'.
			self assert: (self topEntity isOfType: FASTPyInteger).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTNumericalLiteral)
]

{ #category : 'tests - collections' }
FASTPythonImporterTest >> testListWithBooleanOperator [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testListWithBooleanOperator 	"

	self parse: '[ x and y ]'.

	self assert: (fast allWithType: FASTPyBooleanOperator) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 2.
	self assert: (fast allWithType: FASTPyList) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: '[ x and y ]'.
	self assert: (self topEntity isOfType: FASTPyList).
	
	"Testing value of multivalued relation initializers"
	self assert: self topEntity initializers size equals: 1.
	
		stack push: (stack top initializers at: 1).
		self assert: self topEntity sourceCode equals: 'x and y'.
		self assert: (self topEntity isOfType: FASTPyBooleanOperator).
		self assert: (self topEntity isOfType: FASTTBinaryExpression).
		
		"Testing value of monovalue relation leftOperand"
		self assert: self topEntity leftOperand isNotNil.

			stack push: self topEntity leftOperand.
			self assert: self topEntity sourceCode equals: 'x'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		self assert: self topEntity operator equals: 'and'.
		
		"Testing value of monovalue relation rightOperand"
		self assert: self topEntity rightOperand isNotNil.

			stack push: self topEntity rightOperand.
			self assert: self topEntity sourceCode equals: 'y'.
			self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - collections' }
FASTPythonImporterTest >> testListWithCall [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testListWithCall 	"

	self parse: '[ obj() ]'.

	self assert: (fast allWithType: FASTPyCall) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyList) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: '[ obj() ]'.
	self assert: (self topEntity isOfType: FASTPyList).
	
	"Testing value of multivalued relation initializers"
	self assert: self topEntity initializers size equals: 1.
	
		stack push: (stack top initializers at: 1).
		self assert: self topEntity sourceCode equals: 'obj()'.
		self assert: (self topEntity isOfType: FASTPyCall).
		self assert: (self topEntity isOfType: FASTTInvocation).
		self assert: (self topEntity isOfType: FASTTWithArguments).
		
		"Testing value of monovalue relation callee"
		self assert: self topEntity callee isNotNil.

			stack push: self topEntity callee.
			self assert: self topEntity sourceCode equals: 'obj'.
			self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - collections' }
FASTPythonImporterTest >> testListWithComparisonOperator [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testListWithComparisonOperator 	"

	self parse: '[ 1 > 3 ]'.

	self assert: (fast allWithType: FASTPyComparisonOperator) size equals: 1.
	self assert: (fast allWithType: FASTPyInteger) size equals: 2.
	self assert: (fast allWithType: FASTPyList) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: '[ 1 > 3 ]'.
	self assert: (self topEntity isOfType: FASTPyList).
	
	"Testing value of multivalued relation initializers"
	self assert: self topEntity initializers size equals: 1.
	
		stack push: (stack top initializers at: 1).
		self assert: self topEntity sourceCode equals: '1 > 3'.
		self assert: (self topEntity isOfType: FASTPyComparisonOperator).
		
		"Testing value of multivalued relation operands"
		self assert: self topEntity operands size equals: 2.
		
			stack push: (stack top operands at: 1).
			self assert: self topEntity sourceCode equals: '1'.
			self assert: (self topEntity isOfType: FASTPyInteger).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTNumericalLiteral).
			stack pop.
		
			stack push: (stack top operands at: 2).
			self assert: self topEntity sourceCode equals: '3'.
			self assert: (self topEntity isOfType: FASTPyInteger).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTNumericalLiteral).
			stack pop.
		self assert: self topEntity operators equals: #('>')
]

{ #category : 'tests - collections' }
FASTPythonImporterTest >> testListWithComplexe [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testListWithComplexe 	"

	self parse: '[ 1j ]'.

	self assert: (fast allWithType: FASTPyComplex) size equals: 1.
	self assert: (fast allWithType: FASTPyList) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: '[ 1j ]'.
	self assert: (self topEntity isOfType: FASTPyList).
	
	"Testing value of multivalued relation initializers"
	self assert: self topEntity initializers size equals: 1.
	
		stack push: (stack top initializers at: 1).
		self assert: self topEntity sourceCode equals: '1j'.
		self assert: (self topEntity isOfType: FASTPyComplex).
		self assert: (self topEntity isOfType: FASTTLiteral).
		self assert: (self topEntity isOfType: FASTTNumericalLiteral)
]

{ #category : 'tests - collections' }
FASTPythonImporterTest >> testListWithConditionalExpression [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testListWithConditionalExpression 	"

	self parse: '[ x if True else y ]'.

	self assert: (fast allWithType: FASTPyBoolean) size equals: 1.
	self assert: (fast allWithType: FASTPyConditionalExpression) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 2.
	self assert: (fast allWithType: FASTPyList) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: '[ x if True else y ]'.
	self assert: (self topEntity isOfType: FASTPyList).
	
	"Testing value of multivalued relation initializers"
	self assert: self topEntity initializers size equals: 1.
	
		stack push: (stack top initializers at: 1).
		self assert: self topEntity sourceCode equals: 'x if True else y'.
		self assert: (self topEntity isOfType: FASTPyConditionalExpression).
		self assert: (self topEntity isOfType: FASTTWithCondition).
		
		"Testing value of monovalue relation condition"
		self assert: self topEntity condition isNotNil.

			stack push: self topEntity condition.
			self assert: self topEntity sourceCode equals: 'True'.
			self assert: (self topEntity isOfType: FASTPyBoolean).
			self assert: (self topEntity isOfType: FASTTBooleanLiteral).
			self assert: (self topEntity isOfType: FASTTLiteral).
			stack pop.
		
		"Testing value of monovalue relation elseExpression"
		self assert: self topEntity elseExpression isNotNil.

			stack push: self topEntity elseExpression.
			self assert: self topEntity sourceCode equals: 'y'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		
		"Testing value of monovalue relation thenExpression"
		self assert: self topEntity thenExpression isNotNil.

			stack push: self topEntity thenExpression.
			self assert: self topEntity sourceCode equals: 'x'.
			self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - collections' }
FASTPythonImporterTest >> testListWithDictionary [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testListWithDictionary 	"

	self parse: '[ { 1:1 } ]'.

	self assert: (fast allWithType: FASTPyDictionary) size equals: 1.
	self assert: (fast allWithType: FASTPyInteger) size equals: 2.
	self assert: (fast allWithType: FASTPyList) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPair) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: '[ { 1:1 } ]'.
	self assert: (self topEntity isOfType: FASTPyList).
	
	"Testing value of multivalued relation initializers"
	self assert: self topEntity initializers size equals: 1.
	
		stack push: (stack top initializers at: 1).
		self assert: self topEntity sourceCode equals: '{ 1:1 }'.
		self assert: (self topEntity isOfType: FASTPyDictionary).
		
		"Testing value of multivalued relation initializers"
		self assert: self topEntity initializers size equals: 1.
		
			stack push: (stack top initializers at: 1).
			self assert: self topEntity sourceCode equals: '1:1'.
			self assert: (self topEntity isOfType: FASTPyPair).
			
			"Testing value of monovalue relation key"
			self assert: self topEntity key isNotNil.

				stack push: self topEntity key.
				self assert: self topEntity sourceCode equals: '1'.
				self assert: (self topEntity isOfType: FASTPyInteger).
				self assert: (self topEntity isOfType: FASTTLiteral).
				self assert: (self topEntity isOfType: FASTTNumericalLiteral).
				stack pop.
			
			"Testing value of monovalue relation value"
			self assert: self topEntity value isNotNil.

				stack push: self topEntity value.
				self assert: self topEntity sourceCode equals: '1'.
				self assert: (self topEntity isOfType: FASTPyInteger).
				self assert: (self topEntity isOfType: FASTTLiteral).
				self assert: (self topEntity isOfType: FASTTNumericalLiteral)
]

{ #category : 'tests - collections' }
FASTPythonImporterTest >> testListWithEllipsis [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testListWithEllipsis 	"

	self parse: '[ ... ]'.

	self assert: (fast allWithType: FASTPyEllipsis) size equals: 1.
	self assert: (fast allWithType: FASTPyList) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: '[ ... ]'.
	self assert: (self topEntity isOfType: FASTPyList).
	
	"Testing value of multivalued relation initializers"
	self assert: self topEntity initializers size equals: 1.
	
		stack push: (stack top initializers at: 1).
		self assert: self topEntity sourceCode equals: '...'.
		self assert: (self topEntity isOfType: FASTPyEllipsis).
		self assert: (self topEntity isOfType: FASTTLiteral)
]

{ #category : 'tests - collections' }
FASTPythonImporterTest >> testListWithFalse [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testListWithFalse 	"

	self parse: '[ False ]'.

	self assert: (fast allWithType: FASTPyBoolean) size equals: 1.
	self assert: (fast allWithType: FASTPyList) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: '[ False ]'.
	self assert: (self topEntity isOfType: FASTPyList).
	
	"Testing value of multivalued relation initializers"
	self assert: self topEntity initializers size equals: 1.
	
		stack push: (stack top initializers at: 1).
		self assert: self topEntity sourceCode equals: 'False'.
		self assert: (self topEntity isOfType: FASTPyBoolean).
		self assert: (self topEntity isOfType: FASTTBooleanLiteral).
		self assert: (self topEntity isOfType: FASTTLiteral)
]

{ #category : 'tests - collections' }
FASTPythonImporterTest >> testListWithFloat [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testListWithFloat 	"

	self parse: '[ 0.1 ]'.

	self assert: (fast allWithType: FASTPyFloat) size equals: 1.
	self assert: (fast allWithType: FASTPyList) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: '[ 0.1 ]'.
	self assert: (self topEntity isOfType: FASTPyList).
	
	"Testing value of multivalued relation initializers"
	self assert: self topEntity initializers size equals: 1.
	
		stack push: (stack top initializers at: 1).
		self assert: self topEntity sourceCode equals: '0.1'.
		self assert: (self topEntity isOfType: FASTPyFloat).
		self assert: (self topEntity isOfType: FASTTLiteral).
		self assert: (self topEntity isOfType: FASTTNumericalLiteral)
]

{ #category : 'tests - collections' }
FASTPythonImporterTest >> testListWithIdentifier [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testListWithIdentifier 	"

	self parse: '[ element ]'.

	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyList) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: '[ element ]'.
	self assert: (self topEntity isOfType: FASTPyList).
	
	"Testing value of multivalued relation initializers"
	self assert: self topEntity initializers size equals: 1.
	
		stack push: (stack top initializers at: 1).
		self assert: self topEntity sourceCode equals: 'element'.
		self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - collections' }
FASTPythonImporterTest >> testListWithInteger [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testListWithInteger 	"

	self parse: '[ 1 ]'.

	self assert: (fast allWithType: FASTPyInteger) size equals: 1.
	self assert: (fast allWithType: FASTPyList) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: '[ 1 ]'.
	self assert: (self topEntity isOfType: FASTPyList).
	
	"Testing value of multivalued relation initializers"
	self assert: self topEntity initializers size equals: 1.
	
		stack push: (stack top initializers at: 1).
		self assert: self topEntity sourceCode equals: '1'.
		self assert: (self topEntity isOfType: FASTPyInteger).
		self assert: (self topEntity isOfType: FASTTLiteral).
		self assert: (self topEntity isOfType: FASTTNumericalLiteral)
]

{ #category : 'tests - collections' }
FASTPythonImporterTest >> testListWithLambda [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testListWithLambda 	"

	self parse: '[ lambda x:x ]'.

	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyLambda) size equals: 1.
	self assert: (fast allWithType: FASTPyList) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyParameter) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: '[ lambda x:x ]'.
	self assert: (self topEntity isOfType: FASTPyList).
	
	"Testing value of multivalued relation initializers"
	self assert: self topEntity initializers size equals: 1.
	
		stack push: (stack top initializers at: 1).
		self assert: self topEntity sourceCode equals: 'lambda x:x'.
		self assert: (self topEntity isOfType: FASTPyLambda).
		self assert: (self topEntity isOfType: FASTTWithParameters).
		
		"Testing value of monovalue relation expression"
		self assert: self topEntity expression isNotNil.

			stack push: self topEntity expression.
			self assert: self topEntity sourceCode equals: 'x'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		
		"Testing value of multivalued relation parameters"
		self assert: self topEntity parameters size equals: 1.
		
			stack push: (stack top parameters at: 1).
			self assert: self topEntity sourceCode equals: 'x'.
			self assert: (self topEntity isOfType: FASTPyParameter).
			self assert: (self topEntity isOfType: FASTTVariableEntity).
			self assert: self topEntity name equals: 'x'
]

{ #category : 'tests - collections' }
FASTPythonImporterTest >> testListWithList [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testListWithList 	"

	self parse: '[ [ 1 ] ]'.

	self assert: (fast allWithType: FASTPyInteger) size equals: 1.
	self assert: (fast allWithType: FASTPyList) size equals: 2.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: '[ [ 1 ] ]'.
	self assert: (self topEntity isOfType: FASTPyList).
	
	"Testing value of multivalued relation initializers"
	self assert: self topEntity initializers size equals: 1.
	
		stack push: (stack top initializers at: 1).
		self assert: self topEntity sourceCode equals: '[ 1 ]'.
		self assert: (self topEntity isOfType: FASTPyList).
		
		"Testing value of multivalued relation initializers"
		self assert: self topEntity initializers size equals: 1.
		
			stack push: (stack top initializers at: 1).
			self assert: self topEntity sourceCode equals: '1'.
			self assert: (self topEntity isOfType: FASTPyInteger).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTNumericalLiteral)
]

{ #category : 'tests - collections' }
FASTPythonImporterTest >> testListWithListSplat [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testListWithListSplat 	"

	self parse: '[ *args ]'.

	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyList) size equals: 1.
	self assert: (fast allWithType: FASTPyListSplat) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: '[ *args ]'.
	self assert: (self topEntity isOfType: FASTPyList).
	
	"Testing value of multivalued relation initializers"
	self assert: self topEntity initializers size equals: 1.
	
		stack push: (stack top initializers at: 1).
		self assert: self topEntity sourceCode equals: '*args'.
		self assert: (self topEntity isOfType: FASTPyListSplat).
		
		"Testing value of monovalue relation expression"
		self assert: self topEntity expression isNotNil.

			stack push: self topEntity expression.
			self assert: self topEntity sourceCode equals: 'args'.
			self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - collections' }
FASTPythonImporterTest >> testListWithList_comprehension [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testListWithList_comprehension 	"

	self parse: '[ [random for i in range(3)] ]'.

	self assert: (fast allWithType: FASTPyCall) size equals: 1.
	self assert: (fast allWithType: FASTPyForInClause) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 3.
	self assert: (fast allWithType: FASTPyInteger) size equals: 1.
	self assert: (fast allWithType: FASTPyList) size equals: 1.
	self assert: (fast allWithType: FASTPyListComprehension) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: '[ [random for i in range(3)] ]'.
	self assert: (self topEntity isOfType: FASTPyList).
	
	"Testing value of multivalued relation initializers"
	self assert: self topEntity initializers size equals: 1.
	
		stack push: (stack top initializers at: 1).
		self assert: self topEntity sourceCode equals: '[random for i in range(3)]'.
		self assert: (self topEntity isOfType: FASTPyListComprehension).
		self assert: (self topEntity isOfType: FASTPyTComprehension).
		self assert: (self topEntity isOfType: FASTTWithCondition).
		
		"Testing value of monovalue relation body"
		self assert: self topEntity body isNotNil.

			stack push: self topEntity body.
			self assert: self topEntity sourceCode equals: 'random'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		
		"Testing value of multivalued relation clauses"
		self assert: self topEntity clauses size equals: 1.
		
			stack push: (stack top clauses at: 1).
			self assert: self topEntity sourceCode equals: 'for i in range(3)'.
			self assert: (self topEntity isOfType: FASTPyForInClause).
			
			"Testing value of monovalue relation left"
			self assert: self topEntity left isNotNil.

				stack push: self topEntity left.
				self assert: self topEntity sourceCode equals: 'i'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			
			"Testing value of monovalue relation right"
			self assert: self topEntity right isNotNil.

				stack push: self topEntity right.
				self assert: self topEntity sourceCode equals: 'range(3)'.
				self assert: (self topEntity isOfType: FASTPyCall).
				self assert: (self topEntity isOfType: FASTTInvocation).
				self assert: (self topEntity isOfType: FASTTWithArguments).
				
				"Testing value of multivalued relation arguments"
				self assert: self topEntity arguments size equals: 1.
				
					stack push: (stack top arguments at: 1).
					self assert: self topEntity sourceCode equals: '3'.
					self assert: (self topEntity isOfType: FASTPyInteger).
					self assert: (self topEntity isOfType: FASTTLiteral).
					self assert: (self topEntity isOfType: FASTTNumericalLiteral).
					stack pop.
				
				"Testing value of monovalue relation callee"
				self assert: self topEntity callee isNotNil.

					stack push: self topEntity callee.
					self assert: self topEntity sourceCode equals: 'range'.
					self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - collections' }
FASTPythonImporterTest >> testListWithNone [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testListWithNone 	"

	self parse: '[ None ]'.

	self assert: (fast allWithType: FASTPyList) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyNone) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: '[ None ]'.
	self assert: (self topEntity isOfType: FASTPyList).
	
	"Testing value of multivalued relation initializers"
	self assert: self topEntity initializers size equals: 1.
	
		stack push: (stack top initializers at: 1).
		self assert: self topEntity sourceCode equals: 'None'.
		self assert: (self topEntity isOfType: FASTPyNone).
		self assert: (self topEntity isOfType: FASTTLiteral)
]

{ #category : 'tests - collections' }
FASTPythonImporterTest >> testListWithNotOperator [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testListWithNotOperator 	"

	self parse: '[ not old ]'.

	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyList) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyNotOperator) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: '[ not old ]'.
	self assert: (self topEntity isOfType: FASTPyList).
	
	"Testing value of multivalued relation initializers"
	self assert: self topEntity initializers size equals: 1.
	
		stack push: (stack top initializers at: 1).
		self assert: self topEntity sourceCode equals: 'not old'.
		self assert: (self topEntity isOfType: FASTPyNotOperator).
		self assert: (self topEntity isOfType: FASTPyTUnaryOperator).
		
		"Testing value of monovalue relation argument"
		self assert: self topEntity argument isNotNil.

			stack push: self topEntity argument.
			self assert: self topEntity sourceCode equals: 'old'.
			self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - collections' }
FASTPythonImporterTest >> testListWithSet [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testListWithSet 	"

	self parse: '[ { 1 } ]'.

	self assert: (fast allWithType: FASTPyInteger) size equals: 1.
	self assert: (fast allWithType: FASTPyList) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPySet) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: '[ { 1 } ]'.
	self assert: (self topEntity isOfType: FASTPyList).
	
	"Testing value of multivalued relation initializers"
	self assert: self topEntity initializers size equals: 1.
	
		stack push: (stack top initializers at: 1).
		self assert: self topEntity sourceCode equals: '{ 1 }'.
		self assert: (self topEntity isOfType: FASTPySet).
		
		"Testing value of multivalued relation initializers"
		self assert: self topEntity initializers size equals: 1.
		
			stack push: (stack top initializers at: 1).
			self assert: self topEntity sourceCode equals: '1'.
			self assert: (self topEntity isOfType: FASTPyInteger).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTNumericalLiteral)
]

{ #category : 'tests - collections' }
FASTPythonImporterTest >> testListWithSetComprehension [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testListWithSetComprehension 	"

	self parse: '[ {random for i in range(3)} ]'.

	self assert: (fast allWithType: FASTPyCall) size equals: 1.
	self assert: (fast allWithType: FASTPyForInClause) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 3.
	self assert: (fast allWithType: FASTPyInteger) size equals: 1.
	self assert: (fast allWithType: FASTPyList) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPySetComprehension) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: '[ {random for i in range(3)} ]'.
	self assert: (self topEntity isOfType: FASTPyList).
	
	"Testing value of multivalued relation initializers"
	self assert: self topEntity initializers size equals: 1.
	
		stack push: (stack top initializers at: 1).
		self assert: self topEntity sourceCode equals: '{random for i in range(3)}'.
		self assert: (self topEntity isOfType: FASTPySetComprehension).
		self assert: (self topEntity isOfType: FASTPyTComprehension).
		self assert: (self topEntity isOfType: FASTTWithCondition).
		
		"Testing value of monovalue relation body"
		self assert: self topEntity body isNotNil.

			stack push: self topEntity body.
			self assert: self topEntity sourceCode equals: 'random'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		
		"Testing value of multivalued relation clauses"
		self assert: self topEntity clauses size equals: 1.
		
			stack push: (stack top clauses at: 1).
			self assert: self topEntity sourceCode equals: 'for i in range(3)'.
			self assert: (self topEntity isOfType: FASTPyForInClause).
			
			"Testing value of monovalue relation left"
			self assert: self topEntity left isNotNil.

				stack push: self topEntity left.
				self assert: self topEntity sourceCode equals: 'i'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			
			"Testing value of monovalue relation right"
			self assert: self topEntity right isNotNil.

				stack push: self topEntity right.
				self assert: self topEntity sourceCode equals: 'range(3)'.
				self assert: (self topEntity isOfType: FASTPyCall).
				self assert: (self topEntity isOfType: FASTTInvocation).
				self assert: (self topEntity isOfType: FASTTWithArguments).
				
				"Testing value of multivalued relation arguments"
				self assert: self topEntity arguments size equals: 1.
				
					stack push: (stack top arguments at: 1).
					self assert: self topEntity sourceCode equals: '3'.
					self assert: (self topEntity isOfType: FASTPyInteger).
					self assert: (self topEntity isOfType: FASTTLiteral).
					self assert: (self topEntity isOfType: FASTTNumericalLiteral).
					stack pop.
				
				"Testing value of monovalue relation callee"
				self assert: self topEntity callee isNotNil.

					stack push: self topEntity callee.
					self assert: self topEntity sourceCode equals: 'range'.
					self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - collections' }
FASTPythonImporterTest >> testListWithSplat [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testListWithSplat 	"

	self parse: '[*args, 1]'.

	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyInteger) size equals: 1.
	self assert: (fast allWithType: FASTPyList) size equals: 1.
	self assert: (fast allWithType: FASTPyListSplat) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: '[*args, 1]'.
	self assert: (self topEntity isOfType: FASTPyList).
	
	"Testing value of multivalued relation initializers"
	self assert: self topEntity initializers size equals: 2.
	
		stack push: (stack top initializers at: 1).
		self assert: self topEntity sourceCode equals: '*args'.
		self assert: (self topEntity isOfType: FASTPyListSplat).
		
		"Testing value of monovalue relation expression"
		self assert: self topEntity expression isNotNil.

			stack push: self topEntity expression.
			self assert: self topEntity sourceCode equals: 'args'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		stack pop.
	
		stack push: (stack top initializers at: 2).
		self assert: self topEntity sourceCode equals: '1'.
		self assert: (self topEntity isOfType: FASTPyInteger).
		self assert: (self topEntity isOfType: FASTTLiteral).
		self assert: (self topEntity isOfType: FASTTNumericalLiteral)
]

{ #category : 'tests - collections' }
FASTPythonImporterTest >> testListWithString [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testListWithString 	"

	self parse: '[ "Hello" ]'.

	self assert: (fast allWithType: FASTPyList) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyString) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: '[ "Hello" ]'.
	self assert: (self topEntity isOfType: FASTPyList).
	
	"Testing value of multivalued relation initializers"
	self assert: self topEntity initializers size equals: 1.
	
		stack push: (stack top initializers at: 1).
		self assert: self topEntity sourceCode equals: '"Hello"'.
		self assert: (self topEntity isOfType: FASTPyString).
		self assert: (self topEntity isOfType: FASTTLiteral).
		self assert: (self topEntity isOfType: FASTTStringLiteral)
]

{ #category : 'tests - collections' }
FASTPythonImporterTest >> testListWithSubscript [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testListWithSubscript 	"

	self parse: '[ attrs[2] ]'.

	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyInteger) size equals: 1.
	self assert: (fast allWithType: FASTPyList) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPySubscript) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: '[ attrs[2] ]'.
	self assert: (self topEntity isOfType: FASTPyList).
	
	"Testing value of multivalued relation initializers"
	self assert: self topEntity initializers size equals: 1.
	
		stack push: (stack top initializers at: 1).
		self assert: self topEntity sourceCode equals: 'attrs[2]'.
		self assert: (self topEntity isOfType: FASTPySubscript).
		
		"Testing value of multivalued relation indices"
		self assert: self topEntity indices size equals: 1.
		
			stack push: (stack top indices at: 1).
			self assert: self topEntity sourceCode equals: '2'.
			self assert: (self topEntity isOfType: FASTPyInteger).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTNumericalLiteral).
			stack pop.
		
		"Testing value of monovalue relation value"
		self assert: self topEntity value isNotNil.

			stack push: self topEntity value.
			self assert: self topEntity sourceCode equals: 'attrs'.
			self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - collections' }
FASTPythonImporterTest >> testListWithTrue [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testListWithTrue 	"

	self parse: '[ True ]'.

	self assert: (fast allWithType: FASTPyBoolean) size equals: 1.
	self assert: (fast allWithType: FASTPyList) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: '[ True ]'.
	self assert: (self topEntity isOfType: FASTPyList).
	
	"Testing value of multivalued relation initializers"
	self assert: self topEntity initializers size equals: 1.
	
		stack push: (stack top initializers at: 1).
		self assert: self topEntity sourceCode equals: 'True'.
		self assert: (self topEntity isOfType: FASTPyBoolean).
		self assert: (self topEntity isOfType: FASTTBooleanLiteral).
		self assert: (self topEntity isOfType: FASTTLiteral)
]

{ #category : 'tests - collections' }
FASTPythonImporterTest >> testListWithTuple [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testListWithTuple 	"

	self parse: '[ (1,) ]'.

	self assert: (fast allWithType: FASTPyInteger) size equals: 1.
	self assert: (fast allWithType: FASTPyList) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyTuple) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: '[ (1,) ]'.
	self assert: (self topEntity isOfType: FASTPyList).
	
	"Testing value of multivalued relation initializers"
	self assert: self topEntity initializers size equals: 1.
	
		stack push: (stack top initializers at: 1).
		self assert: self topEntity sourceCode equals: '(1,)'.
		self assert: (self topEntity isOfType: FASTPyTuple).
		
		"Testing value of multivalued relation initializers"
		self assert: self topEntity initializers size equals: 1.
		
			stack push: (stack top initializers at: 1).
			self assert: self topEntity sourceCode equals: '1'.
			self assert: (self topEntity isOfType: FASTPyInteger).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTNumericalLiteral)
]

{ #category : 'tests - collections' }
FASTPythonImporterTest >> testListWithUnaryOperator [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testListWithUnaryOperator 	"

	self parse: '[ -1 ]'.

	self assert: (fast allWithType: FASTPyInteger) size equals: 1.
	self assert: (fast allWithType: FASTPyList) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyUnaryOperator) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: '[ -1 ]'.
	self assert: (self topEntity isOfType: FASTPyList).
	
	"Testing value of multivalued relation initializers"
	self assert: self topEntity initializers size equals: 1.
	
		stack push: (stack top initializers at: 1).
		self assert: self topEntity sourceCode equals: '-1'.
		self assert: (self topEntity isOfType: FASTPyUnaryOperator).
		self assert: (self topEntity isOfType: FASTPyTUnaryOperator).
		
		"Testing value of monovalue relation argument"
		self assert: self topEntity argument isNotNil.

			stack push: self topEntity argument.
			self assert: self topEntity sourceCode equals: '1'.
			self assert: (self topEntity isOfType: FASTPyInteger).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTNumericalLiteral).
			stack pop.
		self assert: self topEntity operator equals: '-'
]

{ #category : 'tests - match' }
FASTPythonImporterTest >> testMatchAttribute [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testMatchAttribute 	"

	self parse: 'match x.y:
	case _: pass'.

	self assert: (fast allWithType: FASTPyAttributeAccess) size equals: 1.
	self assert: (fast allWithType: FASTPyCaseClause) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 2.
	self assert: (fast allWithType: FASTPyMatchStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPassStatement) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'match x.y:
	case _: pass' withPlatformLineEndings.
	self assert: (self topEntity isOfType: FASTPyMatchStatement).
	
	"Testing value of multivalued relation cases"
	self assert: self topEntity cases size equals: 1.
	
		stack push: (stack top cases at: 1).
		self assert: self topEntity sourceCode equals: 'case _: pass'.
		self assert: (self topEntity isOfType: FASTPyCaseClause).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		self assert: (self topEntity isOfType: FASTTWithCondition).
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'pass'.
			self assert: (self topEntity isOfType: FASTPyPassStatement).
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation subject"
	self assert: self topEntity subject isNotNil.

		stack push: self topEntity subject.
		self assert: self topEntity sourceCode equals: 'x.y'.
		self assert: (self topEntity isOfType: FASTPyAttributeAccess).
		
		"Testing value of monovalue relation attribute"
		self assert: self topEntity attribute isNotNil.

			stack push: self topEntity attribute.
			self assert: self topEntity sourceCode equals: 'y'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		
		"Testing value of monovalue relation object"
		self assert: self topEntity object isNotNil.

			stack push: self topEntity object.
			self assert: self topEntity sourceCode equals: 'x'.
			self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - match' }
FASTPythonImporterTest >> testMatchCall [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testMatchCall 	"

	self parse: 'match func():
	case _: pass'.

	self assert: (fast allWithType: FASTPyCall) size equals: 1.
	self assert: (fast allWithType: FASTPyCaseClause) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyMatchStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPassStatement) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'match func():
	case _: pass' withPlatformLineEndings.
	self assert: (self topEntity isOfType: FASTPyMatchStatement).
	
	"Testing value of multivalued relation cases"
	self assert: self topEntity cases size equals: 1.
	
		stack push: (stack top cases at: 1).
		self assert: self topEntity sourceCode equals: 'case _: pass'.
		self assert: (self topEntity isOfType: FASTPyCaseClause).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		self assert: (self topEntity isOfType: FASTTWithCondition).
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'pass'.
			self assert: (self topEntity isOfType: FASTPyPassStatement).
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation subject"
	self assert: self topEntity subject isNotNil.

		stack push: self topEntity subject.
		self assert: self topEntity sourceCode equals: 'func()'.
		self assert: (self topEntity isOfType: FASTPyCall).
		self assert: (self topEntity isOfType: FASTTInvocation).
		self assert: (self topEntity isOfType: FASTTWithArguments).
		
		"Testing value of monovalue relation callee"
		self assert: self topEntity callee isNotNil.

			stack push: self topEntity callee.
			self assert: self topEntity sourceCode equals: 'func'.
			self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - match' }
FASTPythonImporterTest >> testMatchDictionary [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testMatchDictionary 	"

	self parse: 'match { 0:0 }:
	case _: pass'.

	self assert: (fast allWithType: FASTPyCaseClause) size equals: 1.
	self assert: (fast allWithType: FASTPyDictionary) size equals: 1.
	self assert: (fast allWithType: FASTPyInteger) size equals: 2.
	self assert: (fast allWithType: FASTPyMatchStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPair) size equals: 1.
	self assert: (fast allWithType: FASTPyPassStatement) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'match { 0:0 }:
	case _: pass' withPlatformLineEndings.
	self assert: (self topEntity isOfType: FASTPyMatchStatement).
	
	"Testing value of multivalued relation cases"
	self assert: self topEntity cases size equals: 1.
	
		stack push: (stack top cases at: 1).
		self assert: self topEntity sourceCode equals: 'case _: pass'.
		self assert: (self topEntity isOfType: FASTPyCaseClause).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		self assert: (self topEntity isOfType: FASTTWithCondition).
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'pass'.
			self assert: (self topEntity isOfType: FASTPyPassStatement).
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation subject"
	self assert: self topEntity subject isNotNil.

		stack push: self topEntity subject.
		self assert: self topEntity sourceCode equals: '{ 0:0 }'.
		self assert: (self topEntity isOfType: FASTPyDictionary).
		
		"Testing value of multivalued relation initializers"
		self assert: self topEntity initializers size equals: 1.
		
			stack push: (stack top initializers at: 1).
			self assert: self topEntity sourceCode equals: '0:0'.
			self assert: (self topEntity isOfType: FASTPyPair).
			
			"Testing value of monovalue relation key"
			self assert: self topEntity key isNotNil.

				stack push: self topEntity key.
				self assert: self topEntity sourceCode equals: '0'.
				self assert: (self topEntity isOfType: FASTPyInteger).
				self assert: (self topEntity isOfType: FASTTLiteral).
				self assert: (self topEntity isOfType: FASTTNumericalLiteral).
				stack pop.
			
			"Testing value of monovalue relation value"
			self assert: self topEntity value isNotNil.

				stack push: self topEntity value.
				self assert: self topEntity sourceCode equals: '0'.
				self assert: (self topEntity isOfType: FASTPyInteger).
				self assert: (self topEntity isOfType: FASTTLiteral).
				self assert: (self topEntity isOfType: FASTTNumericalLiteral)
]

{ #category : 'tests - match' }
FASTPythonImporterTest >> testMatchEllipsis [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testMatchEllipsis 	"

	self parse: 'match ...:
	case _: pass'.

	self assert: (fast allWithType: FASTPyCaseClause) size equals: 1.
	self assert: (fast allWithType: FASTPyEllipsis) size equals: 1.
	self assert: (fast allWithType: FASTPyMatchStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPassStatement) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'match ...:
	case _: pass' withPlatformLineEndings.
	self assert: (self topEntity isOfType: FASTPyMatchStatement).
	
	"Testing value of multivalued relation cases"
	self assert: self topEntity cases size equals: 1.
	
		stack push: (stack top cases at: 1).
		self assert: self topEntity sourceCode equals: 'case _: pass'.
		self assert: (self topEntity isOfType: FASTPyCaseClause).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		self assert: (self topEntity isOfType: FASTTWithCondition).
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'pass'.
			self assert: (self topEntity isOfType: FASTPyPassStatement).
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation subject"
	self assert: self topEntity subject isNotNil.

		stack push: self topEntity subject.
		self assert: self topEntity sourceCode equals: '...'.
		self assert: (self topEntity isOfType: FASTPyEllipsis).
		self assert: (self topEntity isOfType: FASTTLiteral)
]

{ #category : 'tests - match' }
FASTPythonImporterTest >> testMatchIdentifier [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testMatchIdentifier 	"

	self parse: 'match x:
	case _: pass'.

	self assert: (fast allWithType: FASTPyCaseClause) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyMatchStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPassStatement) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'match x:
	case _: pass' withPlatformLineEndings.
	self assert: (self topEntity isOfType: FASTPyMatchStatement).
	
	"Testing value of multivalued relation cases"
	self assert: self topEntity cases size equals: 1.
	
		stack push: (stack top cases at: 1).
		self assert: self topEntity sourceCode equals: 'case _: pass'.
		self assert: (self topEntity isOfType: FASTPyCaseClause).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		self assert: (self topEntity isOfType: FASTTWithCondition).
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'pass'.
			self assert: (self topEntity isOfType: FASTPyPassStatement).
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation subject"
	self assert: self topEntity subject isNotNil.

		stack push: self topEntity subject.
		self assert: self topEntity sourceCode equals: 'x'.
		self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - match' }
FASTPythonImporterTest >> testMatchInteger [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testMatchInteger 	"

	self parse: 'match 1:
	case _: pass'.

	self assert: (fast allWithType: FASTPyCaseClause) size equals: 1.
	self assert: (fast allWithType: FASTPyInteger) size equals: 1.
	self assert: (fast allWithType: FASTPyMatchStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPassStatement) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'match 1:
	case _: pass' withPlatformLineEndings.
	self assert: (self topEntity isOfType: FASTPyMatchStatement).
	
	"Testing value of multivalued relation cases"
	self assert: self topEntity cases size equals: 1.
	
		stack push: (stack top cases at: 1).
		self assert: self topEntity sourceCode equals: 'case _: pass'.
		self assert: (self topEntity isOfType: FASTPyCaseClause).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		self assert: (self topEntity isOfType: FASTTWithCondition).
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'pass'.
			self assert: (self topEntity isOfType: FASTPyPassStatement).
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation subject"
	self assert: self topEntity subject isNotNil.

		stack push: self topEntity subject.
		self assert: self topEntity sourceCode equals: '1'.
		self assert: (self topEntity isOfType: FASTPyInteger).
		self assert: (self topEntity isOfType: FASTTLiteral).
		self assert: (self topEntity isOfType: FASTTNumericalLiteral)
]

{ #category : 'tests - match' }
FASTPythonImporterTest >> testMatchList [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testMatchList 	"

	self parse: 'match [1]:
	case _: pass'.

	self assert: (fast allWithType: FASTPyCaseClause) size equals: 1.
	self assert: (fast allWithType: FASTPyInteger) size equals: 1.
	self assert: (fast allWithType: FASTPyList) size equals: 1.
	self assert: (fast allWithType: FASTPyMatchStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPassStatement) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'match [1]:
	case _: pass' withPlatformLineEndings.
	self assert: (self topEntity isOfType: FASTPyMatchStatement).
	
	"Testing value of multivalued relation cases"
	self assert: self topEntity cases size equals: 1.
	
		stack push: (stack top cases at: 1).
		self assert: self topEntity sourceCode equals: 'case _: pass'.
		self assert: (self topEntity isOfType: FASTPyCaseClause).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		self assert: (self topEntity isOfType: FASTTWithCondition).
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'pass'.
			self assert: (self topEntity isOfType: FASTPyPassStatement).
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation subject"
	self assert: self topEntity subject isNotNil.

		stack push: self topEntity subject.
		self assert: self topEntity sourceCode equals: '[1]'.
		self assert: (self topEntity isOfType: FASTPyList).
		
		"Testing value of multivalued relation initializers"
		self assert: self topEntity initializers size equals: 1.
		
			stack push: (stack top initializers at: 1).
			self assert: self topEntity sourceCode equals: '1'.
			self assert: (self topEntity isOfType: FASTPyInteger).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTNumericalLiteral)
]

{ #category : 'tests - match' }
FASTPythonImporterTest >> testMatchPatternComplexOperation [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testMatchPatternComplexOperation 	"

	self parse: 'match value:
    case 0 + 1j: pass'.

	self assert: (fast allWithType: FASTPyBinaryOperator) size equals: 1.
	self assert: (fast allWithType: FASTPyCaseClause) size equals: 1.
	self assert: (fast allWithType: FASTPyComplex) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyInteger) size equals: 1.
	self assert: (fast allWithType: FASTPyMatchStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPassStatement) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'match value:
    case 0 + 1j: pass' withPlatformLineEndings.
	self assert: (self topEntity isOfType: FASTPyMatchStatement).
	
	"Testing value of multivalued relation cases"
	self assert: self topEntity cases size equals: 1.
	
		stack push: (stack top cases at: 1).
		self assert: self topEntity sourceCode equals: 'case 0 + 1j: pass'.
		self assert: (self topEntity isOfType: FASTPyCaseClause).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		self assert: (self topEntity isOfType: FASTTWithCondition).
		
		"Testing value of monovalue relation pattern"
		self assert: self topEntity pattern isNotNil.

			stack push: self topEntity pattern.
			self assert: self topEntity sourceCode equals: '0 + 1j'.
			self assert: (self topEntity isOfType: FASTPyBinaryOperator).
			self assert: (self topEntity isOfType: FASTTBinaryExpression).
			
			"Testing value of monovalue relation leftOperand"
			self assert: self topEntity leftOperand isNotNil.

				stack push: self topEntity leftOperand.
				self assert: self topEntity sourceCode equals: '0'.
				self assert: (self topEntity isOfType: FASTPyInteger).
				self assert: (self topEntity isOfType: FASTTLiteral).
				self assert: (self topEntity isOfType: FASTTNumericalLiteral).
				stack pop.
			self assert: self topEntity operator equals: '+'.
			
			"Testing value of monovalue relation rightOperand"
			self assert: self topEntity rightOperand isNotNil.

				stack push: self topEntity rightOperand.
				self assert: self topEntity sourceCode equals: '1j'.
				self assert: (self topEntity isOfType: FASTPyComplex).
				self assert: (self topEntity isOfType: FASTTLiteral).
				self assert: (self topEntity isOfType: FASTTNumericalLiteral).
				stack pop.
			stack pop.
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'pass'.
			self assert: (self topEntity isOfType: FASTPyPassStatement).
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation subject"
	self assert: self topEntity subject isNotNil.

		stack push: self topEntity subject.
		self assert: self topEntity sourceCode equals: 'value'.
		self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - match' }
FASTPythonImporterTest >> testMatchPatternComplexe [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testMatchPatternComplexe 	"

	self parse: 'match status:
	case 1j: pass'.

	self assert: (fast allWithType: FASTPyCaseClause) size equals: 1.
	self assert: (fast allWithType: FASTPyComplex) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyMatchStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPassStatement) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'match status:
	case 1j: pass' withPlatformLineEndings.
	self assert: (self topEntity isOfType: FASTPyMatchStatement).
	
	"Testing value of multivalued relation cases"
	self assert: self topEntity cases size equals: 1.
	
		stack push: (stack top cases at: 1).
		self assert: self topEntity sourceCode equals: 'case 1j: pass'.
		self assert: (self topEntity isOfType: FASTPyCaseClause).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		self assert: (self topEntity isOfType: FASTTWithCondition).
		
		"Testing value of monovalue relation pattern"
		self assert: self topEntity pattern isNotNil.

			stack push: self topEntity pattern.
			self assert: self topEntity sourceCode equals: '1j'.
			self assert: (self topEntity isOfType: FASTPyComplex).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTNumericalLiteral).
			stack pop.
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'pass'.
			self assert: (self topEntity isOfType: FASTPyPassStatement).
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation subject"
	self assert: self topEntity subject isNotNil.

		stack push: self topEntity subject.
		self assert: self topEntity sourceCode equals: 'status'.
		self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - match' }
FASTPythonImporterTest >> testMatchPatternDictionary [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testMatchPatternDictionary 	"

	self parse: 'match status:
	case { 0 : 0, 1 : 2 }: pass'.

	self assert: (fast allWithType: FASTPyCaseClause) size equals: 1.
	self assert: (fast allWithType: FASTPyDictionary) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyInteger) size equals: 4.
	self assert: (fast allWithType: FASTPyMatchStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPair) size equals: 2.
	self assert: (fast allWithType: FASTPyPassStatement) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'match status:
	case { 0 : 0, 1 : 2 }: pass' withPlatformLineEndings.
	self assert: (self topEntity isOfType: FASTPyMatchStatement).
	
	"Testing value of multivalued relation cases"
	self assert: self topEntity cases size equals: 1.
	
		stack push: (stack top cases at: 1).
		self assert: self topEntity sourceCode equals: 'case { 0 : 0, 1 : 2 }: pass'.
		self assert: (self topEntity isOfType: FASTPyCaseClause).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		self assert: (self topEntity isOfType: FASTTWithCondition).
		
		"Testing value of monovalue relation pattern"
		self assert: self topEntity pattern isNotNil.

			stack push: self topEntity pattern.
			self assert: self topEntity sourceCode equals: '{ 0 : 0, 1 : 2 }'.
			self assert: (self topEntity isOfType: FASTPyDictionary).
			
			"Testing value of multivalued relation initializers"
			self assert: self topEntity initializers size equals: 2.
			
				stack push: (stack top initializers at: 1).
				self assert: self topEntity sourceCode equals: '0 : 0'.
				self assert: (self topEntity isOfType: FASTPyPair).
				
				"Testing value of monovalue relation key"
				self assert: self topEntity key isNotNil.

					stack push: self topEntity key.
					self assert: self topEntity sourceCode equals: '0'.
					self assert: (self topEntity isOfType: FASTPyInteger).
					self assert: (self topEntity isOfType: FASTTLiteral).
					self assert: (self topEntity isOfType: FASTTNumericalLiteral).
					stack pop.
				
				"Testing value of monovalue relation value"
				self assert: self topEntity value isNotNil.

					stack push: self topEntity value.
					self assert: self topEntity sourceCode equals: '0'.
					self assert: (self topEntity isOfType: FASTPyInteger).
					self assert: (self topEntity isOfType: FASTTLiteral).
					self assert: (self topEntity isOfType: FASTTNumericalLiteral).
					stack pop.
				stack pop.
			
				stack push: (stack top initializers at: 2).
				self assert: self topEntity sourceCode equals: '1 : 2'.
				self assert: (self topEntity isOfType: FASTPyPair).
				
				"Testing value of monovalue relation key"
				self assert: self topEntity key isNotNil.

					stack push: self topEntity key.
					self assert: self topEntity sourceCode equals: '1'.
					self assert: (self topEntity isOfType: FASTPyInteger).
					self assert: (self topEntity isOfType: FASTTLiteral).
					self assert: (self topEntity isOfType: FASTTNumericalLiteral).
					stack pop.
				
				"Testing value of monovalue relation value"
				self assert: self topEntity value isNotNil.

					stack push: self topEntity value.
					self assert: self topEntity sourceCode equals: '2'.
					self assert: (self topEntity isOfType: FASTPyInteger).
					self assert: (self topEntity isOfType: FASTTLiteral).
					self assert: (self topEntity isOfType: FASTTNumericalLiteral).
					stack pop.
				stack pop.
			stack pop.
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'pass'.
			self assert: (self topEntity isOfType: FASTPyPassStatement).
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation subject"
	self assert: self topEntity subject isNotNil.

		stack push: self topEntity subject.
		self assert: self topEntity sourceCode equals: 'status'.
		self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - match' }
FASTPythonImporterTest >> testMatchPatternDictionarySplat [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testMatchPatternDictionarySplat 	"

	self parse: 'match status:
	case **kwargs: pass'.

	self assert: (fast allWithType: FASTPyCaseClause) size equals: 1.
	self assert: (fast allWithType: FASTPyDictionarySplat) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 2.
	self assert: (fast allWithType: FASTPyMatchStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPassStatement) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'match status:
	case **kwargs: pass' withPlatformLineEndings.
	self assert: (self topEntity isOfType: FASTPyMatchStatement).
	
	"Testing value of multivalued relation cases"
	self assert: self topEntity cases size equals: 1.
	
		stack push: (stack top cases at: 1).
		self assert: self topEntity sourceCode equals: 'case **kwargs: pass'.
		self assert: (self topEntity isOfType: FASTPyCaseClause).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		self assert: (self topEntity isOfType: FASTTWithCondition).
		
		"Testing value of monovalue relation pattern"
		self assert: self topEntity pattern isNotNil.

			stack push: self topEntity pattern.
			self assert: self topEntity sourceCode equals: '**kwargs'.
			self assert: (self topEntity isOfType: FASTPyDictionarySplat).
			
			"Testing value of monovalue relation expression"
			self assert: self topEntity expression isNotNil.

				stack push: self topEntity expression.
				self assert: self topEntity sourceCode equals: 'kwargs'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			stack pop.
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'pass'.
			self assert: (self topEntity isOfType: FASTPyPassStatement).
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation subject"
	self assert: self topEntity subject isNotNil.

		stack push: self topEntity subject.
		self assert: self topEntity sourceCode equals: 'status'.
		self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - match' }
FASTPythonImporterTest >> testMatchPatternDictionarySplatUnnamed [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testMatchPatternDictionarySplatUnnamed 	"

	self parse: 'match status:
	case **_: pass'.

	self assert: (fast allWithType: FASTPyCaseClause) size equals: 1.
	self assert: (fast allWithType: FASTPyDictionarySplat) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyMatchStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPassStatement) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'match status:
	case **_: pass' withPlatformLineEndings.
	self assert: (self topEntity isOfType: FASTPyMatchStatement).
	
	"Testing value of multivalued relation cases"
	self assert: self topEntity cases size equals: 1.
	
		stack push: (stack top cases at: 1).
		self assert: self topEntity sourceCode equals: 'case **_: pass'.
		self assert: (self topEntity isOfType: FASTPyCaseClause).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		self assert: (self topEntity isOfType: FASTTWithCondition).
		
		"Testing value of monovalue relation pattern"
		self assert: self topEntity pattern isNotNil.

			stack push: self topEntity pattern.
			self assert: self topEntity sourceCode equals: '**_'.
			self assert: (self topEntity isOfType: FASTPyDictionarySplat).
			stack pop.
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'pass'.
			self assert: (self topEntity isOfType: FASTPyPassStatement).
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation subject"
	self assert: self topEntity subject isNotNil.

		stack push: self topEntity subject.
		self assert: self topEntity sourceCode equals: 'status'.
		self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - match' }
FASTPythonImporterTest >> testMatchPatternDictionaryWithDictionary [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testMatchPatternDictionaryWithDictionary 	"

	self parse: 'match status:
	case {"data": {"id": 1, "name": name}}: pass'.

	self assert: (fast allWithType: FASTPyCaseClause) size equals: 1.
	self assert: (fast allWithType: FASTPyDictionary) size equals: 2.
	self assert: (fast allWithType: FASTPyDottedName) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyInteger) size equals: 1.
	self assert: (fast allWithType: FASTPyMatchStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPair) size equals: 3.
	self assert: (fast allWithType: FASTPyPassStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyString) size equals: 3.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'match status:
	case {"data": {"id": 1, "name": name}}: pass' withPlatformLineEndings.
	self assert: (self topEntity isOfType: FASTPyMatchStatement).
	
	"Testing value of multivalued relation cases"
	self assert: self topEntity cases size equals: 1.
	
		stack push: (stack top cases at: 1).
		self assert: self topEntity sourceCode equals: 'case {"data": {"id": 1, "name": name}}: pass'.
		self assert: (self topEntity isOfType: FASTPyCaseClause).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		self assert: (self topEntity isOfType: FASTTWithCondition).
		
		"Testing value of monovalue relation pattern"
		self assert: self topEntity pattern isNotNil.

			stack push: self topEntity pattern.
			self assert: self topEntity sourceCode equals: '{"data": {"id": 1, "name": name}}'.
			self assert: (self topEntity isOfType: FASTPyDictionary).
			
			"Testing value of multivalued relation initializers"
			self assert: self topEntity initializers size equals: 1.
			
				stack push: (stack top initializers at: 1).
				self assert: self topEntity sourceCode equals: '"data": {"id": 1, "name": name}'.
				self assert: (self topEntity isOfType: FASTPyPair).
				
				"Testing value of monovalue relation key"
				self assert: self topEntity key isNotNil.

					stack push: self topEntity key.
					self assert: self topEntity sourceCode equals: '"data"'.
					self assert: (self topEntity isOfType: FASTPyString).
					self assert: (self topEntity isOfType: FASTTLiteral).
					self assert: (self topEntity isOfType: FASTTStringLiteral).
					stack pop.
				
				"Testing value of monovalue relation value"
				self assert: self topEntity value isNotNil.

					stack push: self topEntity value.
					self assert: self topEntity sourceCode equals: '{"id": 1, "name": name}'.
					self assert: (self topEntity isOfType: FASTPyDictionary).
					
					"Testing value of multivalued relation initializers"
					self assert: self topEntity initializers size equals: 2.
					
						stack push: (stack top initializers at: 1).
						self assert: self topEntity sourceCode equals: '"id": 1'.
						self assert: (self topEntity isOfType: FASTPyPair).
						
						"Testing value of monovalue relation key"
						self assert: self topEntity key isNotNil.

							stack push: self topEntity key.
							self assert: self topEntity sourceCode equals: '"id"'.
							self assert: (self topEntity isOfType: FASTPyString).
							self assert: (self topEntity isOfType: FASTTLiteral).
							self assert: (self topEntity isOfType: FASTTStringLiteral).
							stack pop.
						
						"Testing value of monovalue relation value"
						self assert: self topEntity value isNotNil.

							stack push: self topEntity value.
							self assert: self topEntity sourceCode equals: '1'.
							self assert: (self topEntity isOfType: FASTPyInteger).
							self assert: (self topEntity isOfType: FASTTLiteral).
							self assert: (self topEntity isOfType: FASTTNumericalLiteral).
							stack pop.
						stack pop.
					
						stack push: (stack top initializers at: 2).
						self assert: self topEntity sourceCode equals: '"name": name'.
						self assert: (self topEntity isOfType: FASTPyPair).
						
						"Testing value of monovalue relation key"
						self assert: self topEntity key isNotNil.

							stack push: self topEntity key.
							self assert: self topEntity sourceCode equals: '"name"'.
							self assert: (self topEntity isOfType: FASTPyString).
							self assert: (self topEntity isOfType: FASTTLiteral).
							self assert: (self topEntity isOfType: FASTTStringLiteral).
							stack pop.
						
						"Testing value of monovalue relation value"
						self assert: self topEntity value isNotNil.

							stack push: self topEntity value.
							self assert: self topEntity sourceCode equals: 'name'.
							self assert: (self topEntity isOfType: FASTPyDottedName).
							self assert: self topEntity identifiers equals: #('name').
							stack pop.
						stack pop.
					stack pop.
				stack pop.
			stack pop.
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'pass'.
			self assert: (self topEntity isOfType: FASTPyPassStatement).
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation subject"
	self assert: self topEntity subject isNotNil.

		stack push: self topEntity subject.
		self assert: self topEntity sourceCode equals: 'status'.
		self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - match' }
FASTPythonImporterTest >> testMatchPatternDictionaryWithNoValues [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testMatchPatternDictionaryWithNoValues 	"

	self parse: 'match status:
	case {"x": _, "y": _}: pass'.

	self assert: (fast allWithType: FASTPyCaseClause) size equals: 1.
	self assert: (fast allWithType: FASTPyDictionary) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyMatchStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPair) size equals: 2.
	self assert: (fast allWithType: FASTPyPassStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyString) size equals: 2.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'match status:
	case {"x": _, "y": _}: pass' withPlatformLineEndings.
	self assert: (self topEntity isOfType: FASTPyMatchStatement).
	
	"Testing value of multivalued relation cases"
	self assert: self topEntity cases size equals: 1.
	
		stack push: (stack top cases at: 1).
		self assert: self topEntity sourceCode equals: 'case {"x": _, "y": _}: pass'.
		self assert: (self topEntity isOfType: FASTPyCaseClause).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		self assert: (self topEntity isOfType: FASTTWithCondition).
		
		"Testing value of monovalue relation pattern"
		self assert: self topEntity pattern isNotNil.

			stack push: self topEntity pattern.
			self assert: self topEntity sourceCode equals: '{"x": _, "y": _}'.
			self assert: (self topEntity isOfType: FASTPyDictionary).
			
			"Testing value of multivalued relation initializers"
			self assert: self topEntity initializers size equals: 2.
			
				stack push: (stack top initializers at: 1).
				self assert: self topEntity sourceCode equals: '"x": _'.
				self assert: (self topEntity isOfType: FASTPyPair).
				
				"Testing value of monovalue relation key"
				self assert: self topEntity key isNotNil.

					stack push: self topEntity key.
					self assert: self topEntity sourceCode equals: '"x"'.
					self assert: (self topEntity isOfType: FASTPyString).
					self assert: (self topEntity isOfType: FASTTLiteral).
					self assert: (self topEntity isOfType: FASTTStringLiteral).
					stack pop.
				stack pop.
			
				stack push: (stack top initializers at: 2).
				self assert: self topEntity sourceCode equals: '"y": _'.
				self assert: (self topEntity isOfType: FASTPyPair).
				
				"Testing value of monovalue relation key"
				self assert: self topEntity key isNotNil.

					stack push: self topEntity key.
					self assert: self topEntity sourceCode equals: '"y"'.
					self assert: (self topEntity isOfType: FASTPyString).
					self assert: (self topEntity isOfType: FASTTLiteral).
					self assert: (self topEntity isOfType: FASTTStringLiteral).
					stack pop.
				stack pop.
			stack pop.
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'pass'.
			self assert: (self topEntity isOfType: FASTPyPassStatement).
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation subject"
	self assert: self topEntity subject isNotNil.

		stack push: self topEntity subject.
		self assert: self topEntity sourceCode equals: 'status'.
		self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - match' }
FASTPythonImporterTest >> testMatchPatternDictionaryWithSplat [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testMatchPatternDictionaryWithSplat 	"

	self parse: 'match status:
	case { 0 : 0, **kwargs }: pass'.

	self assert: (fast allWithType: FASTPyCaseClause) size equals: 1.
	self assert: (fast allWithType: FASTPyDictionary) size equals: 1.
	self assert: (fast allWithType: FASTPyDictionarySplat) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 2.
	self assert: (fast allWithType: FASTPyInteger) size equals: 2.
	self assert: (fast allWithType: FASTPyMatchStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPair) size equals: 1.
	self assert: (fast allWithType: FASTPyPassStatement) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'match status:
	case { 0 : 0, **kwargs }: pass' withPlatformLineEndings.
	self assert: (self topEntity isOfType: FASTPyMatchStatement).
	
	"Testing value of multivalued relation cases"
	self assert: self topEntity cases size equals: 1.
	
		stack push: (stack top cases at: 1).
		self assert: self topEntity sourceCode equals: 'case { 0 : 0, **kwargs }: pass'.
		self assert: (self topEntity isOfType: FASTPyCaseClause).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		self assert: (self topEntity isOfType: FASTTWithCondition).
		
		"Testing value of monovalue relation pattern"
		self assert: self topEntity pattern isNotNil.

			stack push: self topEntity pattern.
			self assert: self topEntity sourceCode equals: '{ 0 : 0, **kwargs }'.
			self assert: (self topEntity isOfType: FASTPyDictionary).
			
			"Testing value of multivalued relation initializers"
			self assert: self topEntity initializers size equals: 2.
			
				stack push: (stack top initializers at: 1).
				self assert: self topEntity sourceCode equals: '0 : 0'.
				self assert: (self topEntity isOfType: FASTPyPair).
				
				"Testing value of monovalue relation key"
				self assert: self topEntity key isNotNil.

					stack push: self topEntity key.
					self assert: self topEntity sourceCode equals: '0'.
					self assert: (self topEntity isOfType: FASTPyInteger).
					self assert: (self topEntity isOfType: FASTTLiteral).
					self assert: (self topEntity isOfType: FASTTNumericalLiteral).
					stack pop.
				
				"Testing value of monovalue relation value"
				self assert: self topEntity value isNotNil.

					stack push: self topEntity value.
					self assert: self topEntity sourceCode equals: '0'.
					self assert: (self topEntity isOfType: FASTPyInteger).
					self assert: (self topEntity isOfType: FASTTLiteral).
					self assert: (self topEntity isOfType: FASTTNumericalLiteral).
					stack pop.
				stack pop.
			
				stack push: (stack top initializers at: 2).
				self assert: self topEntity sourceCode equals: '**kwargs'.
				self assert: (self topEntity isOfType: FASTPyDictionarySplat).
				
				"Testing value of monovalue relation expression"
				self assert: self topEntity expression isNotNil.

					stack push: self topEntity expression.
					self assert: self topEntity sourceCode equals: 'kwargs'.
					self assert: (self topEntity isOfType: FASTPyIdentifier).
					stack pop.
				stack pop.
			stack pop.
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'pass'.
			self assert: (self topEntity isOfType: FASTPyPassStatement).
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation subject"
	self assert: self topEntity subject isNotNil.

		stack push: self topEntity subject.
		self assert: self topEntity sourceCode equals: 'status'.
		self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - match' }
FASTPythonImporterTest >> testMatchPatternDictionaryWithVariables [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testMatchPatternDictionaryWithVariables 	"

	self parse: 'match status:
	case {"host": host, "port": port}: pass'.

	self assert: (fast allWithType: FASTPyCaseClause) size equals: 1.
	self assert: (fast allWithType: FASTPyDictionary) size equals: 1.
	self assert: (fast allWithType: FASTPyDottedName) size equals: 2.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyMatchStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPair) size equals: 2.
	self assert: (fast allWithType: FASTPyPassStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyString) size equals: 2.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'match status:
	case {"host": host, "port": port}: pass' withPlatformLineEndings.
	self assert: (self topEntity isOfType: FASTPyMatchStatement).
	
	"Testing value of multivalued relation cases"
	self assert: self topEntity cases size equals: 1.
	
		stack push: (stack top cases at: 1).
		self assert: self topEntity sourceCode equals: 'case {"host": host, "port": port}: pass'.
		self assert: (self topEntity isOfType: FASTPyCaseClause).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		self assert: (self topEntity isOfType: FASTTWithCondition).
		
		"Testing value of monovalue relation pattern"
		self assert: self topEntity pattern isNotNil.

			stack push: self topEntity pattern.
			self assert: self topEntity sourceCode equals: '{"host": host, "port": port}'.
			self assert: (self topEntity isOfType: FASTPyDictionary).
			
			"Testing value of multivalued relation initializers"
			self assert: self topEntity initializers size equals: 2.
			
				stack push: (stack top initializers at: 1).
				self assert: self topEntity sourceCode equals: '"host": host'.
				self assert: (self topEntity isOfType: FASTPyPair).
				
				"Testing value of monovalue relation key"
				self assert: self topEntity key isNotNil.

					stack push: self topEntity key.
					self assert: self topEntity sourceCode equals: '"host"'.
					self assert: (self topEntity isOfType: FASTPyString).
					self assert: (self topEntity isOfType: FASTTLiteral).
					self assert: (self topEntity isOfType: FASTTStringLiteral).
					stack pop.
				
				"Testing value of monovalue relation value"
				self assert: self topEntity value isNotNil.

					stack push: self topEntity value.
					self assert: self topEntity sourceCode equals: 'host'.
					self assert: (self topEntity isOfType: FASTPyDottedName).
					self assert: self topEntity identifiers equals: #('host').
					stack pop.
				stack pop.
			
				stack push: (stack top initializers at: 2).
				self assert: self topEntity sourceCode equals: '"port": port'.
				self assert: (self topEntity isOfType: FASTPyPair).
				
				"Testing value of monovalue relation key"
				self assert: self topEntity key isNotNil.

					stack push: self topEntity key.
					self assert: self topEntity sourceCode equals: '"port"'.
					self assert: (self topEntity isOfType: FASTPyString).
					self assert: (self topEntity isOfType: FASTTLiteral).
					self assert: (self topEntity isOfType: FASTTStringLiteral).
					stack pop.
				
				"Testing value of monovalue relation value"
				self assert: self topEntity value isNotNil.

					stack push: self topEntity value.
					self assert: self topEntity sourceCode equals: 'port'.
					self assert: (self topEntity isOfType: FASTPyDottedName).
					self assert: self topEntity identifiers equals: #('port').
					stack pop.
				stack pop.
			stack pop.
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'pass'.
			self assert: (self topEntity isOfType: FASTPyPassStatement).
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation subject"
	self assert: self topEntity subject isNotNil.

		stack push: self topEntity subject.
		self assert: self topEntity sourceCode equals: 'status'.
		self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - match' }
FASTPythonImporterTest >> testMatchPatternDottedName [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testMatchPatternDottedName 	"

	self parse: 'match value:
    case Status.OK: pass'.

	self assert: (fast allWithType: FASTPyCaseClause) size equals: 1.
	self assert: (fast allWithType: FASTPyDottedName) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyMatchStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPassStatement) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'match value:
    case Status.OK: pass' withPlatformLineEndings.
	self assert: (self topEntity isOfType: FASTPyMatchStatement).
	
	"Testing value of multivalued relation cases"
	self assert: self topEntity cases size equals: 1.
	
		stack push: (stack top cases at: 1).
		self assert: self topEntity sourceCode equals: 'case Status.OK: pass'.
		self assert: (self topEntity isOfType: FASTPyCaseClause).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		self assert: (self topEntity isOfType: FASTTWithCondition).
		
		"Testing value of monovalue relation pattern"
		self assert: self topEntity pattern isNotNil.

			stack push: self topEntity pattern.
			self assert: self topEntity sourceCode equals: 'Status.OK'.
			self assert: (self topEntity isOfType: FASTPyDottedName).
			self assert: self topEntity identifiers equals: #('Status' 'OK').
			stack pop.
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'pass'.
			self assert: (self topEntity isOfType: FASTPyPassStatement).
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation subject"
	self assert: self topEntity subject isNotNil.

		stack push: self topEntity subject.
		self assert: self topEntity sourceCode equals: 'value'.
		self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - match' }
FASTPythonImporterTest >> testMatchPatternFalse [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testMatchPatternFalse 	"

	self parse: 'match status:
	case False: pass'.

	self assert: (fast allWithType: FASTPyBoolean) size equals: 1.
	self assert: (fast allWithType: FASTPyCaseClause) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyMatchStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPassStatement) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'match status:
	case False: pass' withPlatformLineEndings.
	self assert: (self topEntity isOfType: FASTPyMatchStatement).
	
	"Testing value of multivalued relation cases"
	self assert: self topEntity cases size equals: 1.
	
		stack push: (stack top cases at: 1).
		self assert: self topEntity sourceCode equals: 'case False: pass'.
		self assert: (self topEntity isOfType: FASTPyCaseClause).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		self assert: (self topEntity isOfType: FASTTWithCondition).
		
		"Testing value of monovalue relation pattern"
		self assert: self topEntity pattern isNotNil.

			stack push: self topEntity pattern.
			self assert: self topEntity sourceCode equals: 'False'.
			self assert: (self topEntity isOfType: FASTPyBoolean).
			self assert: (self topEntity isOfType: FASTTBooleanLiteral).
			self assert: (self topEntity isOfType: FASTTLiteral).
			stack pop.
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'pass'.
			self assert: (self topEntity isOfType: FASTPyPassStatement).
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation subject"
	self assert: self topEntity subject isNotNil.

		stack push: self topEntity subject.
		self assert: self topEntity sourceCode equals: 'status'.
		self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - match' }
FASTPythonImporterTest >> testMatchPatternFloat [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testMatchPatternFloat 	"

	self parse: 'match status:
	case 1.0: pass'.

	self assert: (fast allWithType: FASTPyCaseClause) size equals: 1.
	self assert: (fast allWithType: FASTPyFloat) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyMatchStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPassStatement) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'match status:
	case 1.0: pass' withPlatformLineEndings.
	self assert: (self topEntity isOfType: FASTPyMatchStatement).
	
	"Testing value of multivalued relation cases"
	self assert: self topEntity cases size equals: 1.
	
		stack push: (stack top cases at: 1).
		self assert: self topEntity sourceCode equals: 'case 1.0: pass'.
		self assert: (self topEntity isOfType: FASTPyCaseClause).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		self assert: (self topEntity isOfType: FASTTWithCondition).
		
		"Testing value of monovalue relation pattern"
		self assert: self topEntity pattern isNotNil.

			stack push: self topEntity pattern.
			self assert: self topEntity sourceCode equals: '1.0'.
			self assert: (self topEntity isOfType: FASTPyFloat).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTNumericalLiteral).
			stack pop.
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'pass'.
			self assert: (self topEntity isOfType: FASTPyPassStatement).
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation subject"
	self assert: self topEntity subject isNotNil.

		stack push: self topEntity subject.
		self assert: self topEntity sourceCode equals: 'status'.
		self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - match' }
FASTPythonImporterTest >> testMatchPatternInteger [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testMatchPatternInteger 	"

	self parse: 'match status:
	case 1: pass'.

	self assert: (fast allWithType: FASTPyCaseClause) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyInteger) size equals: 1.
	self assert: (fast allWithType: FASTPyMatchStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPassStatement) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'match status:
	case 1: pass' withPlatformLineEndings.
	self assert: (self topEntity isOfType: FASTPyMatchStatement).
	
	"Testing value of multivalued relation cases"
	self assert: self topEntity cases size equals: 1.
	
		stack push: (stack top cases at: 1).
		self assert: self topEntity sourceCode equals: 'case 1: pass'.
		self assert: (self topEntity isOfType: FASTPyCaseClause).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		self assert: (self topEntity isOfType: FASTTWithCondition).
		
		"Testing value of monovalue relation pattern"
		self assert: self topEntity pattern isNotNil.

			stack push: self topEntity pattern.
			self assert: self topEntity sourceCode equals: '1'.
			self assert: (self topEntity isOfType: FASTPyInteger).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTNumericalLiteral).
			stack pop.
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'pass'.
			self assert: (self topEntity isOfType: FASTPyPassStatement).
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation subject"
	self assert: self topEntity subject isNotNil.

		stack push: self topEntity subject.
		self assert: self topEntity sourceCode equals: 'status'.
		self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - match' }
FASTPythonImporterTest >> testMatchPatternKeyword [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testMatchPatternKeyword 	"

	self parse: 'match status:
	case string=":": pass'.

	self assert: (fast allWithType: FASTPyCaseClause) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyKeywordPattern) size equals: 1.
	self assert: (fast allWithType: FASTPyMatchStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPassStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyString) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'match status:
	case string=":": pass' withPlatformLineEndings.
	self assert: (self topEntity isOfType: FASTPyMatchStatement).
	
	"Testing value of multivalued relation cases"
	self assert: self topEntity cases size equals: 1.
	
		stack push: (stack top cases at: 1).
		self assert: self topEntity sourceCode equals: 'case string=":": pass'.
		self assert: (self topEntity isOfType: FASTPyCaseClause).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		self assert: (self topEntity isOfType: FASTTWithCondition).
		
		"Testing value of monovalue relation pattern"
		self assert: self topEntity pattern isNotNil.

			stack push: self topEntity pattern.
			self assert: self topEntity sourceCode equals: 'string=":"'.
			self assert: (self topEntity isOfType: FASTPyKeywordPattern).
			self assert: self topEntity name equals: 'string'.
			
			"Testing value of monovalue relation value"
			self assert: self topEntity value isNotNil.

				stack push: self topEntity value.
				self assert: self topEntity sourceCode equals: '":"'.
				self assert: (self topEntity isOfType: FASTPyString).
				self assert: (self topEntity isOfType: FASTTLiteral).
				self assert: (self topEntity isOfType: FASTTStringLiteral).
				stack pop.
			stack pop.
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'pass'.
			self assert: (self topEntity isOfType: FASTPyPassStatement).
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation subject"
	self assert: self topEntity subject isNotNil.

		stack push: self topEntity subject.
		self assert: self topEntity sourceCode equals: 'status'.
		self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - match' }
FASTPythonImporterTest >> testMatchPatternKeywordWithClass [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testMatchPatternKeywordWithClass 	"

	self parse: 'match status:
	case val=AClass(): pass'.

	self assert: (fast allWithType: FASTPyCaseClause) size equals: 1.
	self assert: (fast allWithType: FASTPyClassPattern) size equals: 1.
	self assert: (fast allWithType: FASTPyDottedName) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyKeywordPattern) size equals: 1.
	self assert: (fast allWithType: FASTPyMatchStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPassStatement) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'match status:
	case val=AClass(): pass' withPlatformLineEndings.
	self assert: (self topEntity isOfType: FASTPyMatchStatement).
	
	"Testing value of multivalued relation cases"
	self assert: self topEntity cases size equals: 1.
	
		stack push: (stack top cases at: 1).
		self assert: self topEntity sourceCode equals: 'case val=AClass(): pass'.
		self assert: (self topEntity isOfType: FASTPyCaseClause).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		self assert: (self topEntity isOfType: FASTTWithCondition).
		
		"Testing value of monovalue relation pattern"
		self assert: self topEntity pattern isNotNil.

			stack push: self topEntity pattern.
			self assert: self topEntity sourceCode equals: 'val=AClass()'.
			self assert: (self topEntity isOfType: FASTPyKeywordPattern).
			self assert: self topEntity name equals: 'val'.
			
			"Testing value of monovalue relation value"
			self assert: self topEntity value isNotNil.

				stack push: self topEntity value.
				self assert: self topEntity sourceCode equals: 'AClass()'.
				self assert: (self topEntity isOfType: FASTPyClassPattern).
				
				"Testing value of multivalued relation elements"
				self assert: self topEntity elements size equals: 1.
				
					stack push: (stack top elements at: 1).
					self assert: self topEntity sourceCode equals: 'AClass'.
					self assert: (self topEntity isOfType: FASTPyDottedName).
					self assert: self topEntity identifiers equals: #('AClass').
					stack pop.
				stack pop.
			stack pop.
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'pass'.
			self assert: (self topEntity isOfType: FASTPyPassStatement).
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation subject"
	self assert: self topEntity subject isNotNil.

		stack push: self topEntity subject.
		self assert: self topEntity sourceCode equals: 'status'.
		self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - match' }
FASTPythonImporterTest >> testMatchPatternKeywordWithComplexPattern [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testMatchPatternKeywordWithComplexPattern 	"

	self parse: 'match status:
	case val=0+2j: pass'.

	self assert: (fast allWithType: FASTPyBinaryOperator) size equals: 1.
	self assert: (fast allWithType: FASTPyCaseClause) size equals: 1.
	self assert: (fast allWithType: FASTPyComplex) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyInteger) size equals: 1.
	self assert: (fast allWithType: FASTPyKeywordPattern) size equals: 1.
	self assert: (fast allWithType: FASTPyMatchStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPassStatement) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'match status:
	case val=0+2j: pass' withPlatformLineEndings.
	self assert: (self topEntity isOfType: FASTPyMatchStatement).
	
	"Testing value of multivalued relation cases"
	self assert: self topEntity cases size equals: 1.
	
		stack push: (stack top cases at: 1).
		self assert: self topEntity sourceCode equals: 'case val=0+2j: pass'.
		self assert: (self topEntity isOfType: FASTPyCaseClause).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		self assert: (self topEntity isOfType: FASTTWithCondition).
		
		"Testing value of monovalue relation pattern"
		self assert: self topEntity pattern isNotNil.

			stack push: self topEntity pattern.
			self assert: self topEntity sourceCode equals: 'val=0+2j'.
			self assert: (self topEntity isOfType: FASTPyKeywordPattern).
			self assert: self topEntity name equals: 'val'.
			
			"Testing value of monovalue relation value"
			self assert: self topEntity value isNotNil.

				stack push: self topEntity value.
				self assert: self topEntity sourceCode equals: '0+2j'.
				self assert: (self topEntity isOfType: FASTPyBinaryOperator).
				self assert: (self topEntity isOfType: FASTTBinaryExpression).
				
				"Testing value of monovalue relation leftOperand"
				self assert: self topEntity leftOperand isNotNil.

					stack push: self topEntity leftOperand.
					self assert: self topEntity sourceCode equals: '0'.
					self assert: (self topEntity isOfType: FASTPyInteger).
					self assert: (self topEntity isOfType: FASTTLiteral).
					self assert: (self topEntity isOfType: FASTTNumericalLiteral).
					stack pop.
				self assert: self topEntity operator equals: '+'.
				
				"Testing value of monovalue relation rightOperand"
				self assert: self topEntity rightOperand isNotNil.

					stack push: self topEntity rightOperand.
					self assert: self topEntity sourceCode equals: '2j'.
					self assert: (self topEntity isOfType: FASTPyComplex).
					self assert: (self topEntity isOfType: FASTTLiteral).
					self assert: (self topEntity isOfType: FASTTNumericalLiteral).
					stack pop.
				stack pop.
			stack pop.
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'pass'.
			self assert: (self topEntity isOfType: FASTPyPassStatement).
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation subject"
	self assert: self topEntity subject isNotNil.

		stack push: self topEntity subject.
		self assert: self topEntity sourceCode equals: 'status'.
		self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - match' }
FASTPythonImporterTest >> testMatchPatternKeywordWithDottedName [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testMatchPatternKeywordWithDottedName 	"

	self parse: 'match status:
	case val=x.y: pass'.

	self assert: (fast allWithType: FASTPyCaseClause) size equals: 1.
	self assert: (fast allWithType: FASTPyDottedName) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyKeywordPattern) size equals: 1.
	self assert: (fast allWithType: FASTPyMatchStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPassStatement) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'match status:
	case val=x.y: pass' withPlatformLineEndings.
	self assert: (self topEntity isOfType: FASTPyMatchStatement).
	
	"Testing value of multivalued relation cases"
	self assert: self topEntity cases size equals: 1.
	
		stack push: (stack top cases at: 1).
		self assert: self topEntity sourceCode equals: 'case val=x.y: pass'.
		self assert: (self topEntity isOfType: FASTPyCaseClause).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		self assert: (self topEntity isOfType: FASTTWithCondition).
		
		"Testing value of monovalue relation pattern"
		self assert: self topEntity pattern isNotNil.

			stack push: self topEntity pattern.
			self assert: self topEntity sourceCode equals: 'val=x.y'.
			self assert: (self topEntity isOfType: FASTPyKeywordPattern).
			self assert: self topEntity name equals: 'val'.
			
			"Testing value of monovalue relation value"
			self assert: self topEntity value isNotNil.

				stack push: self topEntity value.
				self assert: self topEntity sourceCode equals: 'x.y'.
				self assert: (self topEntity isOfType: FASTPyDottedName).
				self assert: self topEntity identifiers equals: #('x' 'y').
				stack pop.
			stack pop.
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'pass'.
			self assert: (self topEntity isOfType: FASTPyPassStatement).
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation subject"
	self assert: self topEntity subject isNotNil.

		stack push: self topEntity subject.
		self assert: self topEntity sourceCode equals: 'status'.
		self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - match' }
FASTPythonImporterTest >> testMatchPatternKeywordWithIdentifier [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testMatchPatternKeywordWithIdentifier 	"

	self parse: 'match status:
	case val=x: pass'.

	self assert: (fast allWithType: FASTPyCaseClause) size equals: 1.
	self assert: (fast allWithType: FASTPyDottedName) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyKeywordPattern) size equals: 1.
	self assert: (fast allWithType: FASTPyMatchStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPassStatement) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'match status:
	case val=x: pass' withPlatformLineEndings.
	self assert: (self topEntity isOfType: FASTPyMatchStatement).
	
	"Testing value of multivalued relation cases"
	self assert: self topEntity cases size equals: 1.
	
		stack push: (stack top cases at: 1).
		self assert: self topEntity sourceCode equals: 'case val=x: pass'.
		self assert: (self topEntity isOfType: FASTPyCaseClause).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		self assert: (self topEntity isOfType: FASTTWithCondition).
		
		"Testing value of monovalue relation pattern"
		self assert: self topEntity pattern isNotNil.

			stack push: self topEntity pattern.
			self assert: self topEntity sourceCode equals: 'val=x'.
			self assert: (self topEntity isOfType: FASTPyKeywordPattern).
			self assert: self topEntity name equals: 'val'.
			
			"Testing value of monovalue relation value"
			self assert: self topEntity value isNotNil.

				stack push: self topEntity value.
				self assert: self topEntity sourceCode equals: 'x'.
				self assert: (self topEntity isOfType: FASTPyDottedName).
				self assert: self topEntity identifiers equals: #('x').
				stack pop.
			stack pop.
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'pass'.
			self assert: (self topEntity isOfType: FASTPyPassStatement).
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation subject"
	self assert: self topEntity subject isNotNil.

		stack push: self topEntity subject.
		self assert: self topEntity sourceCode equals: 'status'.
		self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - match' }
FASTPythonImporterTest >> testMatchPatternKeywordWithInteger [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testMatchPatternKeywordWithInteger 	"

	self parse: 'match status:
	case val=1: pass'.

	self assert: (fast allWithType: FASTPyCaseClause) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyInteger) size equals: 1.
	self assert: (fast allWithType: FASTPyKeywordPattern) size equals: 1.
	self assert: (fast allWithType: FASTPyMatchStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPassStatement) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'match status:
	case val=1: pass' withPlatformLineEndings.
	self assert: (self topEntity isOfType: FASTPyMatchStatement).
	
	"Testing value of multivalued relation cases"
	self assert: self topEntity cases size equals: 1.
	
		stack push: (stack top cases at: 1).
		self assert: self topEntity sourceCode equals: 'case val=1: pass'.
		self assert: (self topEntity isOfType: FASTPyCaseClause).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		self assert: (self topEntity isOfType: FASTTWithCondition).
		
		"Testing value of monovalue relation pattern"
		self assert: self topEntity pattern isNotNil.

			stack push: self topEntity pattern.
			self assert: self topEntity sourceCode equals: 'val=1'.
			self assert: (self topEntity isOfType: FASTPyKeywordPattern).
			self assert: self topEntity name equals: 'val'.
			
			"Testing value of monovalue relation value"
			self assert: self topEntity value isNotNil.

				stack push: self topEntity value.
				self assert: self topEntity sourceCode equals: '1'.
				self assert: (self topEntity isOfType: FASTPyInteger).
				self assert: (self topEntity isOfType: FASTTLiteral).
				self assert: (self topEntity isOfType: FASTTNumericalLiteral).
				stack pop.
			stack pop.
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'pass'.
			self assert: (self topEntity isOfType: FASTPyPassStatement).
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation subject"
	self assert: self topEntity subject isNotNil.

		stack push: self topEntity subject.
		self assert: self topEntity sourceCode equals: 'status'.
		self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - match' }
FASTPythonImporterTest >> testMatchPatternKeywordWithList [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testMatchPatternKeywordWithList 	"

	self parse: 'match status:
	case val=[1]: pass'.

	self assert: (fast allWithType: FASTPyCaseClause) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyInteger) size equals: 1.
	self assert: (fast allWithType: FASTPyKeywordPattern) size equals: 1.
	self assert: (fast allWithType: FASTPyList) size equals: 1.
	self assert: (fast allWithType: FASTPyMatchStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPassStatement) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'match status:
	case val=[1]: pass' withPlatformLineEndings.
	self assert: (self topEntity isOfType: FASTPyMatchStatement).
	
	"Testing value of multivalued relation cases"
	self assert: self topEntity cases size equals: 1.
	
		stack push: (stack top cases at: 1).
		self assert: self topEntity sourceCode equals: 'case val=[1]: pass'.
		self assert: (self topEntity isOfType: FASTPyCaseClause).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		self assert: (self topEntity isOfType: FASTTWithCondition).
		
		"Testing value of monovalue relation pattern"
		self assert: self topEntity pattern isNotNil.

			stack push: self topEntity pattern.
			self assert: self topEntity sourceCode equals: 'val=[1]'.
			self assert: (self topEntity isOfType: FASTPyKeywordPattern).
			self assert: self topEntity name equals: 'val'.
			
			"Testing value of monovalue relation value"
			self assert: self topEntity value isNotNil.

				stack push: self topEntity value.
				self assert: self topEntity sourceCode equals: '[1]'.
				self assert: (self topEntity isOfType: FASTPyList).
				
				"Testing value of multivalued relation initializers"
				self assert: self topEntity initializers size equals: 1.
				
					stack push: (stack top initializers at: 1).
					self assert: self topEntity sourceCode equals: '1'.
					self assert: (self topEntity isOfType: FASTPyInteger).
					self assert: (self topEntity isOfType: FASTTLiteral).
					self assert: (self topEntity isOfType: FASTTNumericalLiteral).
					stack pop.
				stack pop.
			stack pop.
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'pass'.
			self assert: (self topEntity isOfType: FASTPyPassStatement).
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation subject"
	self assert: self topEntity subject isNotNil.

		stack push: self topEntity subject.
		self assert: self topEntity sourceCode equals: 'status'.
		self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - match' }
FASTPythonImporterTest >> testMatchPatternKeywordWithTuple [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testMatchPatternKeywordWithTuple 	"

	self parse: 'match status:
	case val=(1,): pass'.

	self assert: (fast allWithType: FASTPyCaseClause) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyInteger) size equals: 1.
	self assert: (fast allWithType: FASTPyKeywordPattern) size equals: 1.
	self assert: (fast allWithType: FASTPyMatchStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPassStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyTuple) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'match status:
	case val=(1,): pass' withPlatformLineEndings.
	self assert: (self topEntity isOfType: FASTPyMatchStatement).
	
	"Testing value of multivalued relation cases"
	self assert: self topEntity cases size equals: 1.
	
		stack push: (stack top cases at: 1).
		self assert: self topEntity sourceCode equals: 'case val=(1,): pass'.
		self assert: (self topEntity isOfType: FASTPyCaseClause).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		self assert: (self topEntity isOfType: FASTTWithCondition).
		
		"Testing value of monovalue relation pattern"
		self assert: self topEntity pattern isNotNil.

			stack push: self topEntity pattern.
			self assert: self topEntity sourceCode equals: 'val=(1,)'.
			self assert: (self topEntity isOfType: FASTPyKeywordPattern).
			self assert: self topEntity name equals: 'val'.
			
			"Testing value of monovalue relation value"
			self assert: self topEntity value isNotNil.

				stack push: self topEntity value.
				self assert: self topEntity sourceCode equals: '(1,)'.
				self assert: (self topEntity isOfType: FASTPyTuple).
				
				"Testing value of multivalued relation initializers"
				self assert: self topEntity initializers size equals: 1.
				
					stack push: (stack top initializers at: 1).
					self assert: self topEntity sourceCode equals: '1'.
					self assert: (self topEntity isOfType: FASTPyInteger).
					self assert: (self topEntity isOfType: FASTTLiteral).
					self assert: (self topEntity isOfType: FASTTNumericalLiteral).
					stack pop.
				stack pop.
			stack pop.
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'pass'.
			self assert: (self topEntity isOfType: FASTPyPassStatement).
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation subject"
	self assert: self topEntity subject isNotNil.

		stack push: self topEntity subject.
		self assert: self topEntity sourceCode equals: 'status'.
		self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - match' }
FASTPythonImporterTest >> testMatchPatternKeywordWithUnion [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testMatchPatternKeywordWithUnion 	"

	self parse: 'match status:
	case val= 1 | 2: pass'.

	self assert: (fast allWithType: FASTPyCaseClause) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyInteger) size equals: 2.
	self assert: (fast allWithType: FASTPyKeywordPattern) size equals: 1.
	self assert: (fast allWithType: FASTPyMatchStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPassStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyUnionPattern) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'match status:
	case val= 1 | 2: pass' withPlatformLineEndings.
	self assert: (self topEntity isOfType: FASTPyMatchStatement).
	
	"Testing value of multivalued relation cases"
	self assert: self topEntity cases size equals: 1.
	
		stack push: (stack top cases at: 1).
		self assert: self topEntity sourceCode equals: 'case val= 1 | 2: pass'.
		self assert: (self topEntity isOfType: FASTPyCaseClause).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		self assert: (self topEntity isOfType: FASTTWithCondition).
		
		"Testing value of monovalue relation pattern"
		self assert: self topEntity pattern isNotNil.

			stack push: self topEntity pattern.
			self assert: self topEntity sourceCode equals: 'val= 1 | 2'.
			self assert: (self topEntity isOfType: FASTPyKeywordPattern).
			self assert: self topEntity name equals: 'val'.
			
			"Testing value of monovalue relation value"
			self assert: self topEntity value isNotNil.

				stack push: self topEntity value.
				self assert: self topEntity sourceCode equals: '1 | 2'.
				self assert: (self topEntity isOfType: FASTPyUnionPattern).
				
				"Testing value of multivalued relation members"
				self assert: self topEntity members size equals: 2.
				
					stack push: (stack top members at: 1).
					self assert: self topEntity sourceCode equals: '1'.
					self assert: (self topEntity isOfType: FASTPyInteger).
					self assert: (self topEntity isOfType: FASTTLiteral).
					self assert: (self topEntity isOfType: FASTTNumericalLiteral).
					stack pop.
				
					stack push: (stack top members at: 2).
					self assert: self topEntity sourceCode equals: '2'.
					self assert: (self topEntity isOfType: FASTPyInteger).
					self assert: (self topEntity isOfType: FASTTLiteral).
					self assert: (self topEntity isOfType: FASTTNumericalLiteral).
					stack pop.
				stack pop.
			stack pop.
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'pass'.
			self assert: (self topEntity isOfType: FASTPyPassStatement).
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation subject"
	self assert: self topEntity subject isNotNil.

		stack push: self topEntity subject.
		self assert: self topEntity sourceCode equals: 'status'.
		self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - match' }
FASTPythonImporterTest >> testMatchPatternList [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testMatchPatternList 	"

	self parse: 'match value:
    case [1, 2]: pass'.

	self assert: (fast allWithType: FASTPyCaseClause) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyInteger) size equals: 2.
	self assert: (fast allWithType: FASTPyList) size equals: 1.
	self assert: (fast allWithType: FASTPyMatchStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPassStatement) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'match value:
    case [1, 2]: pass' withPlatformLineEndings.
	self assert: (self topEntity isOfType: FASTPyMatchStatement).
	
	"Testing value of multivalued relation cases"
	self assert: self topEntity cases size equals: 1.
	
		stack push: (stack top cases at: 1).
		self assert: self topEntity sourceCode equals: 'case [1, 2]: pass'.
		self assert: (self topEntity isOfType: FASTPyCaseClause).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		self assert: (self topEntity isOfType: FASTTWithCondition).
		
		"Testing value of monovalue relation pattern"
		self assert: self topEntity pattern isNotNil.

			stack push: self topEntity pattern.
			self assert: self topEntity sourceCode equals: '[1, 2]'.
			self assert: (self topEntity isOfType: FASTPyList).
			
			"Testing value of multivalued relation initializers"
			self assert: self topEntity initializers size equals: 2.
			
				stack push: (stack top initializers at: 1).
				self assert: self topEntity sourceCode equals: '1'.
				self assert: (self topEntity isOfType: FASTPyInteger).
				self assert: (self topEntity isOfType: FASTTLiteral).
				self assert: (self topEntity isOfType: FASTTNumericalLiteral).
				stack pop.
			
				stack push: (stack top initializers at: 2).
				self assert: self topEntity sourceCode equals: '2'.
				self assert: (self topEntity isOfType: FASTPyInteger).
				self assert: (self topEntity isOfType: FASTTLiteral).
				self assert: (self topEntity isOfType: FASTTNumericalLiteral).
				stack pop.
			stack pop.
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'pass'.
			self assert: (self topEntity isOfType: FASTPyPassStatement).
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation subject"
	self assert: self topEntity subject isNotNil.

		stack push: self topEntity subject.
		self assert: self topEntity sourceCode equals: 'value'.
		self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - match' }
FASTPythonImporterTest >> testMatchPatternListInList [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testMatchPatternListInList 	"

	self parse: 'match status:
	case [ 1, [ 2 ] ]: pass'.

	self assert: (fast allWithType: FASTPyCaseClause) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyInteger) size equals: 2.
	self assert: (fast allWithType: FASTPyList) size equals: 2.
	self assert: (fast allWithType: FASTPyMatchStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPassStatement) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'match status:
	case [ 1, [ 2 ] ]: pass' withPlatformLineEndings.
	self assert: (self topEntity isOfType: FASTPyMatchStatement).
	
	"Testing value of multivalued relation cases"
	self assert: self topEntity cases size equals: 1.
	
		stack push: (stack top cases at: 1).
		self assert: self topEntity sourceCode equals: 'case [ 1, [ 2 ] ]: pass'.
		self assert: (self topEntity isOfType: FASTPyCaseClause).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		self assert: (self topEntity isOfType: FASTTWithCondition).
		
		"Testing value of monovalue relation pattern"
		self assert: self topEntity pattern isNotNil.

			stack push: self topEntity pattern.
			self assert: self topEntity sourceCode equals: '[ 1, [ 2 ] ]'.
			self assert: (self topEntity isOfType: FASTPyList).
			
			"Testing value of multivalued relation initializers"
			self assert: self topEntity initializers size equals: 2.
			
				stack push: (stack top initializers at: 1).
				self assert: self topEntity sourceCode equals: '1'.
				self assert: (self topEntity isOfType: FASTPyInteger).
				self assert: (self topEntity isOfType: FASTTLiteral).
				self assert: (self topEntity isOfType: FASTTNumericalLiteral).
				stack pop.
			
				stack push: (stack top initializers at: 2).
				self assert: self topEntity sourceCode equals: '[ 2 ]'.
				self assert: (self topEntity isOfType: FASTPyList).
				
				"Testing value of multivalued relation initializers"
				self assert: self topEntity initializers size equals: 1.
				
					stack push: (stack top initializers at: 1).
					self assert: self topEntity sourceCode equals: '2'.
					self assert: (self topEntity isOfType: FASTPyInteger).
					self assert: (self topEntity isOfType: FASTTLiteral).
					self assert: (self topEntity isOfType: FASTTNumericalLiteral).
					stack pop.
				stack pop.
			stack pop.
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'pass'.
			self assert: (self topEntity isOfType: FASTPyPassStatement).
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation subject"
	self assert: self topEntity subject isNotNil.

		stack push: self topEntity subject.
		self assert: self topEntity sourceCode equals: 'status'.
		self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - match' }
FASTPythonImporterTest >> testMatchPatternListSplat [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testMatchPatternListSplat 	"

	self parse: 'match status:
	case *list: pass'.

	self assert: (fast allWithType: FASTPyCaseClause) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 2.
	self assert: (fast allWithType: FASTPyListSplat) size equals: 1.
	self assert: (fast allWithType: FASTPyMatchStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPassStatement) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'match status:
	case *list: pass' withPlatformLineEndings.
	self assert: (self topEntity isOfType: FASTPyMatchStatement).
	
	"Testing value of multivalued relation cases"
	self assert: self topEntity cases size equals: 1.
	
		stack push: (stack top cases at: 1).
		self assert: self topEntity sourceCode equals: 'case *list: pass'.
		self assert: (self topEntity isOfType: FASTPyCaseClause).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		self assert: (self topEntity isOfType: FASTTWithCondition).
		
		"Testing value of monovalue relation pattern"
		self assert: self topEntity pattern isNotNil.

			stack push: self topEntity pattern.
			self assert: self topEntity sourceCode equals: '*list'.
			self assert: (self topEntity isOfType: FASTPyListSplat).
			
			"Testing value of monovalue relation expression"
			self assert: self topEntity expression isNotNil.

				stack push: self topEntity expression.
				self assert: self topEntity sourceCode equals: 'list'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			stack pop.
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'pass'.
			self assert: (self topEntity isOfType: FASTPyPassStatement).
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation subject"
	self assert: self topEntity subject isNotNil.

		stack push: self topEntity subject.
		self assert: self topEntity sourceCode equals: 'status'.
		self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - match' }
FASTPythonImporterTest >> testMatchPatternListSplatUnnamed [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testMatchPatternListSplatUnnamed 	"

	self parse: 'match status:
	case *_: pass'.

	self assert: (fast allWithType: FASTPyCaseClause) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyListSplat) size equals: 1.
	self assert: (fast allWithType: FASTPyMatchStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPassStatement) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'match status:
	case *_: pass' withPlatformLineEndings.
	self assert: (self topEntity isOfType: FASTPyMatchStatement).
	
	"Testing value of multivalued relation cases"
	self assert: self topEntity cases size equals: 1.
	
		stack push: (stack top cases at: 1).
		self assert: self topEntity sourceCode equals: 'case *_: pass'.
		self assert: (self topEntity isOfType: FASTPyCaseClause).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		self assert: (self topEntity isOfType: FASTTWithCondition).
		
		"Testing value of monovalue relation pattern"
		self assert: self topEntity pattern isNotNil.

			stack push: self topEntity pattern.
			self assert: self topEntity sourceCode equals: '*_'.
			self assert: (self topEntity isOfType: FASTPyListSplat).
			stack pop.
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'pass'.
			self assert: (self topEntity isOfType: FASTPyPassStatement).
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation subject"
	self assert: self topEntity subject isNotNil.

		stack push: self topEntity subject.
		self assert: self topEntity sourceCode equals: 'status'.
		self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - match' }
FASTPythonImporterTest >> testMatchPatternListWithSplat [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testMatchPatternListWithSplat 	"

	self parse: 'match status:
	case [ 1, *args ]: pass'.

	self assert: (fast allWithType: FASTPyCaseClause) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 2.
	self assert: (fast allWithType: FASTPyInteger) size equals: 1.
	self assert: (fast allWithType: FASTPyList) size equals: 1.
	self assert: (fast allWithType: FASTPyListSplat) size equals: 1.
	self assert: (fast allWithType: FASTPyMatchStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPassStatement) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'match status:
	case [ 1, *args ]: pass' withPlatformLineEndings.
	self assert: (self topEntity isOfType: FASTPyMatchStatement).
	
	"Testing value of multivalued relation cases"
	self assert: self topEntity cases size equals: 1.
	
		stack push: (stack top cases at: 1).
		self assert: self topEntity sourceCode equals: 'case [ 1, *args ]: pass'.
		self assert: (self topEntity isOfType: FASTPyCaseClause).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		self assert: (self topEntity isOfType: FASTTWithCondition).
		
		"Testing value of monovalue relation pattern"
		self assert: self topEntity pattern isNotNil.

			stack push: self topEntity pattern.
			self assert: self topEntity sourceCode equals: '[ 1, *args ]'.
			self assert: (self topEntity isOfType: FASTPyList).
			
			"Testing value of multivalued relation initializers"
			self assert: self topEntity initializers size equals: 2.
			
				stack push: (stack top initializers at: 1).
				self assert: self topEntity sourceCode equals: '1'.
				self assert: (self topEntity isOfType: FASTPyInteger).
				self assert: (self topEntity isOfType: FASTTLiteral).
				self assert: (self topEntity isOfType: FASTTNumericalLiteral).
				stack pop.
			
				stack push: (stack top initializers at: 2).
				self assert: self topEntity sourceCode equals: '*args'.
				self assert: (self topEntity isOfType: FASTPyListSplat).
				
				"Testing value of monovalue relation expression"
				self assert: self topEntity expression isNotNil.

					stack push: self topEntity expression.
					self assert: self topEntity sourceCode equals: 'args'.
					self assert: (self topEntity isOfType: FASTPyIdentifier).
					stack pop.
				stack pop.
			stack pop.
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'pass'.
			self assert: (self topEntity isOfType: FASTPyPassStatement).
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation subject"
	self assert: self topEntity subject isNotNil.

		stack push: self topEntity subject.
		self assert: self topEntity sourceCode equals: 'status'.
		self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - match' }
FASTPythonImporterTest >> testMatchPatternListWithTuple [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testMatchPatternListWithTuple 	"

	self parse: 'match status:
	case [ 1, (1, 2) ]: pass'.

	self assert: (fast allWithType: FASTPyCaseClause) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyInteger) size equals: 3.
	self assert: (fast allWithType: FASTPyList) size equals: 1.
	self assert: (fast allWithType: FASTPyMatchStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPassStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyTuple) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'match status:
	case [ 1, (1, 2) ]: pass' withPlatformLineEndings.
	self assert: (self topEntity isOfType: FASTPyMatchStatement).
	
	"Testing value of multivalued relation cases"
	self assert: self topEntity cases size equals: 1.
	
		stack push: (stack top cases at: 1).
		self assert: self topEntity sourceCode equals: 'case [ 1, (1, 2) ]: pass'.
		self assert: (self topEntity isOfType: FASTPyCaseClause).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		self assert: (self topEntity isOfType: FASTTWithCondition).
		
		"Testing value of monovalue relation pattern"
		self assert: self topEntity pattern isNotNil.

			stack push: self topEntity pattern.
			self assert: self topEntity sourceCode equals: '[ 1, (1, 2) ]'.
			self assert: (self topEntity isOfType: FASTPyList).
			
			"Testing value of multivalued relation initializers"
			self assert: self topEntity initializers size equals: 2.
			
				stack push: (stack top initializers at: 1).
				self assert: self topEntity sourceCode equals: '1'.
				self assert: (self topEntity isOfType: FASTPyInteger).
				self assert: (self topEntity isOfType: FASTTLiteral).
				self assert: (self topEntity isOfType: FASTTNumericalLiteral).
				stack pop.
			
				stack push: (stack top initializers at: 2).
				self assert: self topEntity sourceCode equals: '(1, 2)'.
				self assert: (self topEntity isOfType: FASTPyTuple).
				
				"Testing value of multivalued relation initializers"
				self assert: self topEntity initializers size equals: 2.
				
					stack push: (stack top initializers at: 1).
					self assert: self topEntity sourceCode equals: '1'.
					self assert: (self topEntity isOfType: FASTPyInteger).
					self assert: (self topEntity isOfType: FASTTLiteral).
					self assert: (self topEntity isOfType: FASTTNumericalLiteral).
					stack pop.
				
					stack push: (stack top initializers at: 2).
					self assert: self topEntity sourceCode equals: '2'.
					self assert: (self topEntity isOfType: FASTPyInteger).
					self assert: (self topEntity isOfType: FASTTLiteral).
					self assert: (self topEntity isOfType: FASTTNumericalLiteral).
					stack pop.
				stack pop.
			stack pop.
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'pass'.
			self assert: (self topEntity isOfType: FASTPyPassStatement).
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation subject"
	self assert: self topEntity subject isNotNil.

		stack push: self topEntity subject.
		self assert: self topEntity sourceCode equals: 'status'.
		self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - match' }
FASTPythonImporterTest >> testMatchPatternNone [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testMatchPatternNone 	"

	self parse: 'match status:
	case None: pass'.

	self assert: (fast allWithType: FASTPyCaseClause) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyMatchStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyNone) size equals: 1.
	self assert: (fast allWithType: FASTPyPassStatement) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'match status:
	case None: pass' withPlatformLineEndings.
	self assert: (self topEntity isOfType: FASTPyMatchStatement).
	
	"Testing value of multivalued relation cases"
	self assert: self topEntity cases size equals: 1.
	
		stack push: (stack top cases at: 1).
		self assert: self topEntity sourceCode equals: 'case None: pass'.
		self assert: (self topEntity isOfType: FASTPyCaseClause).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		self assert: (self topEntity isOfType: FASTTWithCondition).
		
		"Testing value of monovalue relation pattern"
		self assert: self topEntity pattern isNotNil.

			stack push: self topEntity pattern.
			self assert: self topEntity sourceCode equals: 'None'.
			self assert: (self topEntity isOfType: FASTPyNone).
			self assert: (self topEntity isOfType: FASTTLiteral).
			stack pop.
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'pass'.
			self assert: (self topEntity isOfType: FASTPyPassStatement).
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation subject"
	self assert: self topEntity subject isNotNil.

		stack push: self topEntity subject.
		self assert: self topEntity sourceCode equals: 'status'.
		self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - match' }
FASTPythonImporterTest >> testMatchPatternString [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testMatchPatternString 	"

	self parse: 'match status:
	case "Hello": pass'.

	self assert: (fast allWithType: FASTPyCaseClause) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyMatchStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPassStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyString) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'match status:
	case "Hello": pass' withPlatformLineEndings.
	self assert: (self topEntity isOfType: FASTPyMatchStatement).
	
	"Testing value of multivalued relation cases"
	self assert: self topEntity cases size equals: 1.
	
		stack push: (stack top cases at: 1).
		self assert: self topEntity sourceCode equals: 'case "Hello": pass'.
		self assert: (self topEntity isOfType: FASTPyCaseClause).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		self assert: (self topEntity isOfType: FASTTWithCondition).
		
		"Testing value of monovalue relation pattern"
		self assert: self topEntity pattern isNotNil.

			stack push: self topEntity pattern.
			self assert: self topEntity sourceCode equals: '"Hello"'.
			self assert: (self topEntity isOfType: FASTPyString).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTStringLiteral).
			stack pop.
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'pass'.
			self assert: (self topEntity isOfType: FASTPyPassStatement).
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation subject"
	self assert: self topEntity subject isNotNil.

		stack push: self topEntity subject.
		self assert: self topEntity sourceCode equals: 'status'.
		self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - match' }
FASTPythonImporterTest >> testMatchPatternTrue [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testMatchPatternTrue 	"

	self parse: 'match status:
	case True: pass'.

	self assert: (fast allWithType: FASTPyBoolean) size equals: 1.
	self assert: (fast allWithType: FASTPyCaseClause) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyMatchStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPassStatement) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'match status:
	case True: pass' withPlatformLineEndings.
	self assert: (self topEntity isOfType: FASTPyMatchStatement).
	
	"Testing value of multivalued relation cases"
	self assert: self topEntity cases size equals: 1.
	
		stack push: (stack top cases at: 1).
		self assert: self topEntity sourceCode equals: 'case True: pass'.
		self assert: (self topEntity isOfType: FASTPyCaseClause).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		self assert: (self topEntity isOfType: FASTTWithCondition).
		
		"Testing value of monovalue relation pattern"
		self assert: self topEntity pattern isNotNil.

			stack push: self topEntity pattern.
			self assert: self topEntity sourceCode equals: 'True'.
			self assert: (self topEntity isOfType: FASTPyBoolean).
			self assert: (self topEntity isOfType: FASTTBooleanLiteral).
			self assert: (self topEntity isOfType: FASTTLiteral).
			stack pop.
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'pass'.
			self assert: (self topEntity isOfType: FASTPyPassStatement).
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation subject"
	self assert: self topEntity subject isNotNil.

		stack push: self topEntity subject.
		self assert: self topEntity sourceCode equals: 'status'.
		self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - match' }
FASTPythonImporterTest >> testMatchPatternTuple [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testMatchPatternTuple 	"

	self parse: 'match value:
    case (1, 2): pass'.

	self assert: (fast allWithType: FASTPyCaseClause) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyInteger) size equals: 2.
	self assert: (fast allWithType: FASTPyMatchStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPassStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyTuple) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'match value:
    case (1, 2): pass' withPlatformLineEndings.
	self assert: (self topEntity isOfType: FASTPyMatchStatement).
	
	"Testing value of multivalued relation cases"
	self assert: self topEntity cases size equals: 1.
	
		stack push: (stack top cases at: 1).
		self assert: self topEntity sourceCode equals: 'case (1, 2): pass'.
		self assert: (self topEntity isOfType: FASTPyCaseClause).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		self assert: (self topEntity isOfType: FASTTWithCondition).
		
		"Testing value of monovalue relation pattern"
		self assert: self topEntity pattern isNotNil.

			stack push: self topEntity pattern.
			self assert: self topEntity sourceCode equals: '(1, 2)'.
			self assert: (self topEntity isOfType: FASTPyTuple).
			
			"Testing value of multivalued relation initializers"
			self assert: self topEntity initializers size equals: 2.
			
				stack push: (stack top initializers at: 1).
				self assert: self topEntity sourceCode equals: '1'.
				self assert: (self topEntity isOfType: FASTPyInteger).
				self assert: (self topEntity isOfType: FASTTLiteral).
				self assert: (self topEntity isOfType: FASTTNumericalLiteral).
				stack pop.
			
				stack push: (stack top initializers at: 2).
				self assert: self topEntity sourceCode equals: '2'.
				self assert: (self topEntity isOfType: FASTPyInteger).
				self assert: (self topEntity isOfType: FASTTLiteral).
				self assert: (self topEntity isOfType: FASTTNumericalLiteral).
				stack pop.
			stack pop.
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'pass'.
			self assert: (self topEntity isOfType: FASTPyPassStatement).
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation subject"
	self assert: self topEntity subject isNotNil.

		stack push: self topEntity subject.
		self assert: self topEntity sourceCode equals: 'value'.
		self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - match' }
FASTPythonImporterTest >> testMatchPatternUnion [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testMatchPatternUnion 	"

	self parse: 'match status:
	case 1 | 2 | 3: pass'.

	self assert: (fast allWithType: FASTPyCaseClause) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyInteger) size equals: 3.
	self assert: (fast allWithType: FASTPyMatchStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPassStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyUnionPattern) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'match status:
	case 1 | 2 | 3: pass' withPlatformLineEndings.
	self assert: (self topEntity isOfType: FASTPyMatchStatement).
	
	"Testing value of multivalued relation cases"
	self assert: self topEntity cases size equals: 1.
	
		stack push: (stack top cases at: 1).
		self assert: self topEntity sourceCode equals: 'case 1 | 2 | 3: pass'.
		self assert: (self topEntity isOfType: FASTPyCaseClause).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		self assert: (self topEntity isOfType: FASTTWithCondition).
		
		"Testing value of monovalue relation pattern"
		self assert: self topEntity pattern isNotNil.

			stack push: self topEntity pattern.
			self assert: self topEntity sourceCode equals: '1 | 2 | 3'.
			self assert: (self topEntity isOfType: FASTPyUnionPattern).
			
			"Testing value of multivalued relation members"
			self assert: self topEntity members size equals: 3.
			
				stack push: (stack top members at: 1).
				self assert: self topEntity sourceCode equals: '1'.
				self assert: (self topEntity isOfType: FASTPyInteger).
				self assert: (self topEntity isOfType: FASTTLiteral).
				self assert: (self topEntity isOfType: FASTTNumericalLiteral).
				stack pop.
			
				stack push: (stack top members at: 2).
				self assert: self topEntity sourceCode equals: '2'.
				self assert: (self topEntity isOfType: FASTPyInteger).
				self assert: (self topEntity isOfType: FASTTLiteral).
				self assert: (self topEntity isOfType: FASTTNumericalLiteral).
				stack pop.
			
				stack push: (stack top members at: 3).
				self assert: self topEntity sourceCode equals: '3'.
				self assert: (self topEntity isOfType: FASTPyInteger).
				self assert: (self topEntity isOfType: FASTTLiteral).
				self assert: (self topEntity isOfType: FASTTNumericalLiteral).
				stack pop.
			stack pop.
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'pass'.
			self assert: (self topEntity isOfType: FASTPyPassStatement).
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation subject"
	self assert: self topEntity subject isNotNil.

		stack push: self topEntity subject.
		self assert: self topEntity sourceCode equals: 'status'.
		self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - match' }
FASTPythonImporterTest >> testMatchPatternUnionClassPattern [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testMatchPatternUnionClassPattern 	"

	self parse: 'match status:
	case AClass() | 2: pass'.

	self assert: (fast allWithType: FASTPyCaseClause) size equals: 1.
	self assert: (fast allWithType: FASTPyClassPattern) size equals: 1.
	self assert: (fast allWithType: FASTPyDottedName) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyInteger) size equals: 1.
	self assert: (fast allWithType: FASTPyMatchStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPassStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyUnionPattern) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'match status:
	case AClass() | 2: pass' withPlatformLineEndings.
	self assert: (self topEntity isOfType: FASTPyMatchStatement).
	
	"Testing value of multivalued relation cases"
	self assert: self topEntity cases size equals: 1.
	
		stack push: (stack top cases at: 1).
		self assert: self topEntity sourceCode equals: 'case AClass() | 2: pass'.
		self assert: (self topEntity isOfType: FASTPyCaseClause).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		self assert: (self topEntity isOfType: FASTTWithCondition).
		
		"Testing value of monovalue relation pattern"
		self assert: self topEntity pattern isNotNil.

			stack push: self topEntity pattern.
			self assert: self topEntity sourceCode equals: 'AClass() | 2'.
			self assert: (self topEntity isOfType: FASTPyUnionPattern).
			
			"Testing value of multivalued relation members"
			self assert: self topEntity members size equals: 2.
			
				stack push: (stack top members at: 1).
				self assert: self topEntity sourceCode equals: 'AClass()'.
				self assert: (self topEntity isOfType: FASTPyClassPattern).
				
				"Testing value of multivalued relation elements"
				self assert: self topEntity elements size equals: 1.
				
					stack push: (stack top elements at: 1).
					self assert: self topEntity sourceCode equals: 'AClass'.
					self assert: (self topEntity isOfType: FASTPyDottedName).
					self assert: self topEntity identifiers equals: #('AClass').
					stack pop.
				stack pop.
			
				stack push: (stack top members at: 2).
				self assert: self topEntity sourceCode equals: '2'.
				self assert: (self topEntity isOfType: FASTPyInteger).
				self assert: (self topEntity isOfType: FASTTLiteral).
				self assert: (self topEntity isOfType: FASTTNumericalLiteral).
				stack pop.
			stack pop.
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'pass'.
			self assert: (self topEntity isOfType: FASTPyPassStatement).
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation subject"
	self assert: self topEntity subject isNotNil.

		stack push: self topEntity subject.
		self assert: self topEntity sourceCode equals: 'status'.
		self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - match' }
FASTPythonImporterTest >> testMatchPatternUnionDictionary [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testMatchPatternUnionDictionary 	"

	self parse: 'match status:
	case { 0:0 } | 2: pass'.

	self assert: (fast allWithType: FASTPyCaseClause) size equals: 1.
	self assert: (fast allWithType: FASTPyDictionary) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyInteger) size equals: 3.
	self assert: (fast allWithType: FASTPyMatchStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPair) size equals: 1.
	self assert: (fast allWithType: FASTPyPassStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyUnionPattern) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'match status:
	case { 0:0 } | 2: pass' withPlatformLineEndings.
	self assert: (self topEntity isOfType: FASTPyMatchStatement).
	
	"Testing value of multivalued relation cases"
	self assert: self topEntity cases size equals: 1.
	
		stack push: (stack top cases at: 1).
		self assert: self topEntity sourceCode equals: 'case { 0:0 } | 2: pass'.
		self assert: (self topEntity isOfType: FASTPyCaseClause).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		self assert: (self topEntity isOfType: FASTTWithCondition).
		
		"Testing value of monovalue relation pattern"
		self assert: self topEntity pattern isNotNil.

			stack push: self topEntity pattern.
			self assert: self topEntity sourceCode equals: '{ 0:0 } | 2'.
			self assert: (self topEntity isOfType: FASTPyUnionPattern).
			
			"Testing value of multivalued relation members"
			self assert: self topEntity members size equals: 2.
			
				stack push: (stack top members at: 1).
				self assert: self topEntity sourceCode equals: '{ 0:0 }'.
				self assert: (self topEntity isOfType: FASTPyDictionary).
				
				"Testing value of multivalued relation initializers"
				self assert: self topEntity initializers size equals: 1.
				
					stack push: (stack top initializers at: 1).
					self assert: self topEntity sourceCode equals: '0:0'.
					self assert: (self topEntity isOfType: FASTPyPair).
					
					"Testing value of monovalue relation key"
					self assert: self topEntity key isNotNil.

						stack push: self topEntity key.
						self assert: self topEntity sourceCode equals: '0'.
						self assert: (self topEntity isOfType: FASTPyInteger).
						self assert: (self topEntity isOfType: FASTTLiteral).
						self assert: (self topEntity isOfType: FASTTNumericalLiteral).
						stack pop.
					
					"Testing value of monovalue relation value"
					self assert: self topEntity value isNotNil.

						stack push: self topEntity value.
						self assert: self topEntity sourceCode equals: '0'.
						self assert: (self topEntity isOfType: FASTPyInteger).
						self assert: (self topEntity isOfType: FASTTLiteral).
						self assert: (self topEntity isOfType: FASTTNumericalLiteral).
						stack pop.
					stack pop.
				stack pop.
			
				stack push: (stack top members at: 2).
				self assert: self topEntity sourceCode equals: '2'.
				self assert: (self topEntity isOfType: FASTPyInteger).
				self assert: (self topEntity isOfType: FASTTLiteral).
				self assert: (self topEntity isOfType: FASTTNumericalLiteral).
				stack pop.
			stack pop.
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'pass'.
			self assert: (self topEntity isOfType: FASTPyPassStatement).
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation subject"
	self assert: self topEntity subject isNotNil.

		stack push: self topEntity subject.
		self assert: self topEntity sourceCode equals: 'status'.
		self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - match' }
FASTPythonImporterTest >> testMatchPatternUnionDottedName [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testMatchPatternUnionDottedName 	"

	self parse: 'match status:
	case x.y | 2: pass'.

	self assert: (fast allWithType: FASTPyCaseClause) size equals: 1.
	self assert: (fast allWithType: FASTPyDottedName) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyInteger) size equals: 1.
	self assert: (fast allWithType: FASTPyMatchStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPassStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyUnionPattern) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'match status:
	case x.y | 2: pass' withPlatformLineEndings.
	self assert: (self topEntity isOfType: FASTPyMatchStatement).
	
	"Testing value of multivalued relation cases"
	self assert: self topEntity cases size equals: 1.
	
		stack push: (stack top cases at: 1).
		self assert: self topEntity sourceCode equals: 'case x.y | 2: pass'.
		self assert: (self topEntity isOfType: FASTPyCaseClause).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		self assert: (self topEntity isOfType: FASTTWithCondition).
		
		"Testing value of monovalue relation pattern"
		self assert: self topEntity pattern isNotNil.

			stack push: self topEntity pattern.
			self assert: self topEntity sourceCode equals: 'x.y | 2'.
			self assert: (self topEntity isOfType: FASTPyUnionPattern).
			
			"Testing value of multivalued relation members"
			self assert: self topEntity members size equals: 2.
			
				stack push: (stack top members at: 1).
				self assert: self topEntity sourceCode equals: 'x.y'.
				self assert: (self topEntity isOfType: FASTPyDottedName).
				self assert: self topEntity identifiers equals: #('x' 'y').
				stack pop.
			
				stack push: (stack top members at: 2).
				self assert: self topEntity sourceCode equals: '2'.
				self assert: (self topEntity isOfType: FASTPyInteger).
				self assert: (self topEntity isOfType: FASTTLiteral).
				self assert: (self topEntity isOfType: FASTTNumericalLiteral).
				stack pop.
			stack pop.
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'pass'.
			self assert: (self topEntity isOfType: FASTPyPassStatement).
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation subject"
	self assert: self topEntity subject isNotNil.

		stack push: self topEntity subject.
		self assert: self topEntity sourceCode equals: 'status'.
		self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - match' }
FASTPythonImporterTest >> testMatchPatternUnionList [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testMatchPatternUnionList 	"

	self parse: 'match status:
	case [ 1 ] | 2: pass'.

	self assert: (fast allWithType: FASTPyCaseClause) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyInteger) size equals: 2.
	self assert: (fast allWithType: FASTPyList) size equals: 1.
	self assert: (fast allWithType: FASTPyMatchStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPassStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyUnionPattern) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'match status:
	case [ 1 ] | 2: pass' withPlatformLineEndings.
	self assert: (self topEntity isOfType: FASTPyMatchStatement).
	
	"Testing value of multivalued relation cases"
	self assert: self topEntity cases size equals: 1.
	
		stack push: (stack top cases at: 1).
		self assert: self topEntity sourceCode equals: 'case [ 1 ] | 2: pass'.
		self assert: (self topEntity isOfType: FASTPyCaseClause).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		self assert: (self topEntity isOfType: FASTTWithCondition).
		
		"Testing value of monovalue relation pattern"
		self assert: self topEntity pattern isNotNil.

			stack push: self topEntity pattern.
			self assert: self topEntity sourceCode equals: '[ 1 ] | 2'.
			self assert: (self topEntity isOfType: FASTPyUnionPattern).
			
			"Testing value of multivalued relation members"
			self assert: self topEntity members size equals: 2.
			
				stack push: (stack top members at: 1).
				self assert: self topEntity sourceCode equals: '[ 1 ]'.
				self assert: (self topEntity isOfType: FASTPyList).
				
				"Testing value of multivalued relation initializers"
				self assert: self topEntity initializers size equals: 1.
				
					stack push: (stack top initializers at: 1).
					self assert: self topEntity sourceCode equals: '1'.
					self assert: (self topEntity isOfType: FASTPyInteger).
					self assert: (self topEntity isOfType: FASTTLiteral).
					self assert: (self topEntity isOfType: FASTTNumericalLiteral).
					stack pop.
				stack pop.
			
				stack push: (stack top members at: 2).
				self assert: self topEntity sourceCode equals: '2'.
				self assert: (self topEntity isOfType: FASTPyInteger).
				self assert: (self topEntity isOfType: FASTTLiteral).
				self assert: (self topEntity isOfType: FASTTNumericalLiteral).
				stack pop.
			stack pop.
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'pass'.
			self assert: (self topEntity isOfType: FASTPyPassStatement).
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation subject"
	self assert: self topEntity subject isNotNil.

		stack push: self topEntity subject.
		self assert: self topEntity sourceCode equals: 'status'.
		self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - match' }
FASTPythonImporterTest >> testMatchPatternUnionTuple [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testMatchPatternUnionTuple 	"

	self parse: 'match status:
	case ( 1, ) | 2: pass'.

	self assert: (fast allWithType: FASTPyCaseClause) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyInteger) size equals: 2.
	self assert: (fast allWithType: FASTPyMatchStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPassStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyTuple) size equals: 1.
	self assert: (fast allWithType: FASTPyUnionPattern) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'match status:
	case ( 1, ) | 2: pass' withPlatformLineEndings.
	self assert: (self topEntity isOfType: FASTPyMatchStatement).
	
	"Testing value of multivalued relation cases"
	self assert: self topEntity cases size equals: 1.
	
		stack push: (stack top cases at: 1).
		self assert: self topEntity sourceCode equals: 'case ( 1, ) | 2: pass'.
		self assert: (self topEntity isOfType: FASTPyCaseClause).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		self assert: (self topEntity isOfType: FASTTWithCondition).
		
		"Testing value of monovalue relation pattern"
		self assert: self topEntity pattern isNotNil.

			stack push: self topEntity pattern.
			self assert: self topEntity sourceCode equals: '( 1, ) | 2'.
			self assert: (self topEntity isOfType: FASTPyUnionPattern).
			
			"Testing value of multivalued relation members"
			self assert: self topEntity members size equals: 2.
			
				stack push: (stack top members at: 1).
				self assert: self topEntity sourceCode equals: '( 1, )'.
				self assert: (self topEntity isOfType: FASTPyTuple).
				
				"Testing value of multivalued relation initializers"
				self assert: self topEntity initializers size equals: 1.
				
					stack push: (stack top initializers at: 1).
					self assert: self topEntity sourceCode equals: '1'.
					self assert: (self topEntity isOfType: FASTPyInteger).
					self assert: (self topEntity isOfType: FASTTLiteral).
					self assert: (self topEntity isOfType: FASTTNumericalLiteral).
					stack pop.
				stack pop.
			
				stack push: (stack top members at: 2).
				self assert: self topEntity sourceCode equals: '2'.
				self assert: (self topEntity isOfType: FASTPyInteger).
				self assert: (self topEntity isOfType: FASTTLiteral).
				self assert: (self topEntity isOfType: FASTTNumericalLiteral).
				stack pop.
			stack pop.
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'pass'.
			self assert: (self topEntity isOfType: FASTPyPassStatement).
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation subject"
	self assert: self topEntity subject isNotNil.

		stack push: self topEntity subject.
		self assert: self topEntity sourceCode equals: 'status'.
		self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - match' }
FASTPythonImporterTest >> testMatchPatternWithAlias [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testMatchPatternWithAlias 	"

	self parse: 'match value:
    case x as kind: pass'.

	self assert: (fast allWithType: FASTPyCaseClause) size equals: 1.
	self assert: (fast allWithType: FASTPyDottedName) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyMatchStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPassStatement) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'match value:
    case x as kind: pass' withPlatformLineEndings.
	self assert: (self topEntity isOfType: FASTPyMatchStatement).
	
	"Testing value of multivalued relation cases"
	self assert: self topEntity cases size equals: 1.
	
		stack push: (stack top cases at: 1).
		self assert: self topEntity sourceCode equals: 'case x as kind: pass'.
		self assert: (self topEntity isOfType: FASTPyCaseClause).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		self assert: (self topEntity isOfType: FASTTWithCondition).
		self assert: self topEntity alias equals: 'kind'.
		
		"Testing value of monovalue relation pattern"
		self assert: self topEntity pattern isNotNil.

			stack push: self topEntity pattern.
			self assert: self topEntity sourceCode equals: 'x'.
			self assert: (self topEntity isOfType: FASTPyDottedName).
			self assert: self topEntity identifiers equals: #('x').
			stack pop.
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'pass'.
			self assert: (self topEntity isOfType: FASTPyPassStatement).
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation subject"
	self assert: self topEntity subject isNotNil.

		stack push: self topEntity subject.
		self assert: self topEntity sourceCode equals: 'value'.
		self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - match' }
FASTPythonImporterTest >> testMatchPatternWithGuard [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testMatchPatternWithGuard 	"

	self parse: 'match status:
	case n if n > 10: pass'.

	self assert: (fast allWithType: FASTPyCaseClause) size equals: 1.
	self assert: (fast allWithType: FASTPyComparisonOperator) size equals: 1.
	self assert: (fast allWithType: FASTPyDottedName) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 2.
	self assert: (fast allWithType: FASTPyInteger) size equals: 1.
	self assert: (fast allWithType: FASTPyMatchStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPassStatement) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'match status:
	case n if n > 10: pass' withPlatformLineEndings.
	self assert: (self topEntity isOfType: FASTPyMatchStatement).
	
	"Testing value of multivalued relation cases"
	self assert: self topEntity cases size equals: 1.
	
		stack push: (stack top cases at: 1).
		self assert: self topEntity sourceCode equals: 'case n if n > 10: pass'.
		self assert: (self topEntity isOfType: FASTPyCaseClause).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		self assert: (self topEntity isOfType: FASTTWithCondition).
		
		"Testing value of monovalue relation condition"
		self assert: self topEntity condition isNotNil.

			stack push: self topEntity condition.
			self assert: self topEntity sourceCode equals: 'n > 10'.
			self assert: (self topEntity isOfType: FASTPyComparisonOperator).
			
			"Testing value of multivalued relation operands"
			self assert: self topEntity operands size equals: 2.
			
				stack push: (stack top operands at: 1).
				self assert: self topEntity sourceCode equals: 'n'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			
				stack push: (stack top operands at: 2).
				self assert: self topEntity sourceCode equals: '10'.
				self assert: (self topEntity isOfType: FASTPyInteger).
				self assert: (self topEntity isOfType: FASTTLiteral).
				self assert: (self topEntity isOfType: FASTTNumericalLiteral).
				stack pop.
			self assert: self topEntity operators equals: #('>').
			stack pop.
		
		"Testing value of monovalue relation pattern"
		self assert: self topEntity pattern isNotNil.

			stack push: self topEntity pattern.
			self assert: self topEntity sourceCode equals: 'n'.
			self assert: (self topEntity isOfType: FASTPyDottedName).
			self assert: self topEntity identifiers equals: #('n').
			stack pop.
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'pass'.
			self assert: (self topEntity isOfType: FASTPyPassStatement).
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation subject"
	self assert: self topEntity subject isNotNil.

		stack push: self topEntity subject.
		self assert: self topEntity sourceCode equals: 'status'.
		self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - match' }
FASTPythonImporterTest >> testMatchString [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testMatchString 	"

	self parse: 'match "Hello":
	case _: pass'.

	self assert: (fast allWithType: FASTPyCaseClause) size equals: 1.
	self assert: (fast allWithType: FASTPyMatchStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPassStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyString) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'match "Hello":
	case _: pass' withPlatformLineEndings.
	self assert: (self topEntity isOfType: FASTPyMatchStatement).
	
	"Testing value of multivalued relation cases"
	self assert: self topEntity cases size equals: 1.
	
		stack push: (stack top cases at: 1).
		self assert: self topEntity sourceCode equals: 'case _: pass'.
		self assert: (self topEntity isOfType: FASTPyCaseClause).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		self assert: (self topEntity isOfType: FASTTWithCondition).
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'pass'.
			self assert: (self topEntity isOfType: FASTPyPassStatement).
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation subject"
	self assert: self topEntity subject isNotNil.

		stack push: self topEntity subject.
		self assert: self topEntity sourceCode equals: '"Hello"'.
		self assert: (self topEntity isOfType: FASTPyString).
		self assert: (self topEntity isOfType: FASTTLiteral).
		self assert: (self topEntity isOfType: FASTTStringLiteral)
]

{ #category : 'tests - match' }
FASTPythonImporterTest >> testMatchTuple [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testMatchTuple 	"

	self parse: 'match (1, ):
	case _: pass'.

	self assert: (fast allWithType: FASTPyCaseClause) size equals: 1.
	self assert: (fast allWithType: FASTPyInteger) size equals: 1.
	self assert: (fast allWithType: FASTPyMatchStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPassStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyTuple) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'match (1, ):
	case _: pass' withPlatformLineEndings.
	self assert: (self topEntity isOfType: FASTPyMatchStatement).
	
	"Testing value of multivalued relation cases"
	self assert: self topEntity cases size equals: 1.
	
		stack push: (stack top cases at: 1).
		self assert: self topEntity sourceCode equals: 'case _: pass'.
		self assert: (self topEntity isOfType: FASTPyCaseClause).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		self assert: (self topEntity isOfType: FASTTWithCondition).
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'pass'.
			self assert: (self topEntity isOfType: FASTPyPassStatement).
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation subject"
	self assert: self topEntity subject isNotNil.

		stack push: self topEntity subject.
		self assert: self topEntity sourceCode equals: '(1, )'.
		self assert: (self topEntity isOfType: FASTPyTuple).
		
		"Testing value of multivalued relation initializers"
		self assert: self topEntity initializers size equals: 1.
		
			stack push: (stack top initializers at: 1).
			self assert: self topEntity sourceCode equals: '1'.
			self assert: (self topEntity isOfType: FASTPyInteger).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTNumericalLiteral)
]

{ #category : 'tests' }
FASTPythonImporterTest >> testMethodDefinition [

	| method |
	self parse: 'class AClass():
	def method(self):
		pass'.

	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyClassDefinition) size equals: 1.
	self assert: (fast allWithType: FASTPyMethodDefinition) size equals: 1.

	method := (fast allWithType: FASTPyMethodDefinition) anyOne.
	self assert: method name equals: 'method'
]

{ #category : 'tests' }
FASTPythonImporterTest >> testModuleWithTwoStatements [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testModuleWithTwoStatements 	"

	self parse: 'True
False'.

	self assert: (fast allWithType: FASTPyBoolean) size equals: 2.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.

	stack push: fast rootEntities anyOne.
	self assert: self topEntity sourceCode equals: 'True
False' withPlatformLineEndings.
	self assert: (self topEntity isOfType: FASTPyModule).
	self assert: (self topEntity isOfType: FASTTStatementBlock).
	self assert: (self topEntity isOfType: FamixTHasImmediateSource).
	self assert: (self topEntity isOfType: FamixTSourceAnchor).
	self assert: self topEntity source equals: 'True
False' withPlatformLineEndings.
	
	"Testing value of multivalued relation statements"
	self assert: self topEntity statements size equals: 2.
	
		stack push: (stack top statements at: 1).
		self assert: self topEntity sourceCode equals: 'True'.
		self assert: (self topEntity isOfType: FASTPyBoolean).
		self assert: (self topEntity isOfType: FASTTBooleanLiteral).
		self assert: (self topEntity isOfType: FASTTLiteral).
		stack pop.
	
		stack push: (stack top statements at: 2).
		self assert: self topEntity sourceCode equals: 'False'.
		self assert: (self topEntity isOfType: FASTPyBoolean).
		self assert: (self topEntity isOfType: FASTTBooleanLiteral).
		self assert: (self topEntity isOfType: FASTTLiteral)
]

{ #category : 'tests - assignments' }
FASTPythonImporterTest >> testNestAssignment [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testNestAssignment 	"

	self parse: 'name_len = actual_len = 130'.

	self assert: (fast allWithType: FASTPyAssignment) size equals: 2.
	self assert: (fast allWithType: FASTPyInteger) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyVariable) size equals: 2.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'name_len = actual_len = 130'.
	self assert: (self topEntity isOfType: FASTPyAssignment).
	
	"Testing value of monovalue relation left"
	self assert: self topEntity left isNotNil.

		stack push: self topEntity left.
		self assert: self topEntity sourceCode equals: 'name_len'.
		self assert: (self topEntity isOfType: FASTPyVariable).
		self assert: (self topEntity isOfType: FASTTVariableEntity).
		stack pop.
	
	"Testing value of monovalue relation right"
	self assert: self topEntity right isNotNil.

		stack push: self topEntity right.
		self assert: self topEntity sourceCode equals: 'actual_len = 130'.
		self assert: (self topEntity isOfType: FASTPyAssignment).
		
		"Testing value of monovalue relation left"
		self assert: self topEntity left isNotNil.

			stack push: self topEntity left.
			self assert: self topEntity sourceCode equals: 'actual_len'.
			self assert: (self topEntity isOfType: FASTPyVariable).
			self assert: (self topEntity isOfType: FASTTVariableEntity).
			stack pop.
		
		"Testing value of monovalue relation right"
		self assert: self topEntity right isNotNil.

			stack push: self topEntity right.
			self assert: self topEntity sourceCode equals: '130'.
			self assert: (self topEntity isOfType: FASTPyInteger).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTNumericalLiteral)
]

{ #category : 'tests - advanced statements' }
FASTPythonImporterTest >> testNestedIf [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testNestedIf 	"

	self parse: 'if x > 0:
    if x < 10:
        1
    else:
        2
else:
    3'.

	self assert: (fast allWithType: FASTPyComparisonOperator) size equals: 2.
	self assert: (fast allWithType: FASTPyElseClause) size equals: 2.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 2.
	self assert: (fast allWithType: FASTPyIfStatement) size equals: 2.
	self assert: (fast allWithType: FASTPyInteger) size equals: 5.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyThenClause) size equals: 2.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'if x > 0:
    if x < 10:
        1
    else:
        2
else:
    3' withPlatformLineEndings.
	self assert: (self topEntity isOfType: FASTPyIfStatement).
	self assert: (self topEntity isOfType: FASTPyTWithElseClause).
	self assert: (self topEntity isOfType: FASTTConditionalStatement).
	self assert: (self topEntity isOfType: FASTTWithCondition).
	
	"Testing value of monovalue relation condition"
	self assert: self topEntity condition isNotNil.

		stack push: self topEntity condition.
		self assert: self topEntity sourceCode equals: 'x > 0'.
		self assert: (self topEntity isOfType: FASTPyComparisonOperator).
		
		"Testing value of multivalued relation operands"
		self assert: self topEntity operands size equals: 2.
		
			stack push: (stack top operands at: 1).
			self assert: self topEntity sourceCode equals: 'x'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		
			stack push: (stack top operands at: 2).
			self assert: self topEntity sourceCode equals: '0'.
			self assert: (self topEntity isOfType: FASTPyInteger).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTNumericalLiteral).
			stack pop.
		self assert: self topEntity operators equals: #('>').
		stack pop.
	
	"Testing value of monovalue relation elseClause"
	self assert: self topEntity elseClause isNotNil.

		stack push: self topEntity elseClause.
		self assert: self topEntity sourceCode equals: '3'.
		self assert: (self topEntity isOfType: FASTPyElseClause).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: '3'.
			self assert: (self topEntity isOfType: FASTPyInteger).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTNumericalLiteral).
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation thenClause"
	self assert: self topEntity thenClause isNotNil.

		stack push: self topEntity thenClause.
		self assert: self topEntity sourceCode equals: 'if x < 10:
        1
    else:
        2' withPlatformLineEndings.
		self assert: (self topEntity isOfType: FASTPyThenClause).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'if x < 10:
        1
    else:
        2' withPlatformLineEndings.
			self assert: (self topEntity isOfType: FASTPyIfStatement).
			self assert: (self topEntity isOfType: FASTPyTWithElseClause).
			self assert: (self topEntity isOfType: FASTTConditionalStatement).
			self assert: (self topEntity isOfType: FASTTWithCondition).
			
			"Testing value of monovalue relation condition"
			self assert: self topEntity condition isNotNil.

				stack push: self topEntity condition.
				self assert: self topEntity sourceCode equals: 'x < 10'.
				self assert: (self topEntity isOfType: FASTPyComparisonOperator).
				
				"Testing value of multivalued relation operands"
				self assert: self topEntity operands size equals: 2.
				
					stack push: (stack top operands at: 1).
					self assert: self topEntity sourceCode equals: 'x'.
					self assert: (self topEntity isOfType: FASTPyIdentifier).
					stack pop.
				
					stack push: (stack top operands at: 2).
					self assert: self topEntity sourceCode equals: '10'.
					self assert: (self topEntity isOfType: FASTPyInteger).
					self assert: (self topEntity isOfType: FASTTLiteral).
					self assert: (self topEntity isOfType: FASTTNumericalLiteral).
					stack pop.
				self assert: self topEntity operators equals: #('<').
				stack pop.
			
			"Testing value of monovalue relation elseClause"
			self assert: self topEntity elseClause isNotNil.

				stack push: self topEntity elseClause.
				self assert: self topEntity sourceCode equals: '2'.
				self assert: (self topEntity isOfType: FASTPyElseClause).
				self assert: (self topEntity isOfType: FASTTStatementBlock).
				
				"Testing value of multivalued relation statements"
				self assert: self topEntity statements size equals: 1.
				
					stack push: (stack top statements at: 1).
					self assert: self topEntity sourceCode equals: '2'.
					self assert: (self topEntity isOfType: FASTPyInteger).
					self assert: (self topEntity isOfType: FASTTLiteral).
					self assert: (self topEntity isOfType: FASTTNumericalLiteral).
					stack pop.
				stack pop.
			
			"Testing value of monovalue relation thenClause"
			self assert: self topEntity thenClause isNotNil.

				stack push: self topEntity thenClause.
				self assert: self topEntity sourceCode equals: '1'.
				self assert: (self topEntity isOfType: FASTPyThenClause).
				self assert: (self topEntity isOfType: FASTTStatementBlock).
				
				"Testing value of multivalued relation statements"
				self assert: self topEntity statements size equals: 1.
				
					stack push: (stack top statements at: 1).
					self assert: self topEntity sourceCode equals: '1'.
					self assert: (self topEntity isOfType: FASTPyInteger).
					self assert: (self topEntity isOfType: FASTTLiteral).
					self assert: (self topEntity isOfType: FASTTNumericalLiteral)
]

{ #category : 'tests - literals' }
FASTPythonImporterTest >> testNone [

	| boolean |
	self parse: 'None'.

	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyNone) size equals: 1.

	boolean := (fast allWithType: FASTPyNone) anyOne.
	self assert: boolean sourceCode equals: 'None'.
	self assert: (boolean isOfType: FASTTLiteral)
]

{ #category : 'tests - statements' }
FASTPythonImporterTest >> testNonlocalStatement [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testNonlocalStatement 	"

	self parse: 'def aFunction():
	nonlocal counter
	return counter'.

	self assert: (fast allWithType: FASTPyBlock) size equals: 1.
	self assert: (fast allWithType: FASTPyFunctionDefinition) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyNonlocalStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyReturnStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyVariable) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'def aFunction():
	nonlocal counter
	return counter' withPlatformLineEndings.
	self assert: (self topEntity isOfType: FASTPyFunctionDefinition).
	self assert: (self topEntity isOfType: FASTPyTDefinition).
	self assert: (self topEntity isOfType: FASTTWithParameters).
	self assert: (self topEntity isOfType: FASTTWithStatements).
	self assert: self topEntity name equals: 'aFunction'.
	
	"Testing value of monovalue relation statementBlock"
	self assert: self topEntity statementBlock isNotNil.

		stack push: self topEntity statementBlock.
		self assert: self topEntity sourceCode equals: 'nonlocal counter
	return counter' withPlatformLineEndings.
		self assert: (self topEntity isOfType: FASTPyBlock).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 2.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'nonlocal counter'.
			self assert: (self topEntity isOfType: FASTPyNonlocalStatement).
			
			"Testing value of multivalued relation variables"
			self assert: self topEntity variables size equals: 1.
			
				stack push: (stack top variables at: 1).
				self assert: self topEntity sourceCode equals: 'counter'.
				self assert: (self topEntity isOfType: FASTPyVariable).
				self assert: (self topEntity isOfType: FASTTVariableEntity).
				stack pop.
			stack pop.
		
			stack push: (stack top statements at: 2).
			self assert: self topEntity sourceCode equals: 'return counter'.
			self assert: (self topEntity isOfType: FASTPyReturnStatement).
			self assert: (self topEntity isOfType: FASTTReturnStatement).
			
			"Testing value of monovalue relation expression"
			self assert: self topEntity expression isNotNil.

				stack push: self topEntity expression.
				self assert: self topEntity sourceCode equals: 'counter'.
				self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - statements' }
FASTPythonImporterTest >> testNonlocalStatementMultiple [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testNonlocalStatementMultiple 	"

	self parse: 'def function ():
	nonlocal x, y, z
	return (x, y, z)'.

	self assert: (fast allWithType: FASTPyBlock) size equals: 1.
	self assert: (fast allWithType: FASTPyFunctionDefinition) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 3.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyNonlocalStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyReturnStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyTuple) size equals: 1.
	self assert: (fast allWithType: FASTPyVariable) size equals: 3.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'def function ():
	nonlocal x, y, z
	return (x, y, z)' withPlatformLineEndings.
	self assert: (self topEntity isOfType: FASTPyFunctionDefinition).
	self assert: (self topEntity isOfType: FASTPyTDefinition).
	self assert: (self topEntity isOfType: FASTTWithParameters).
	self assert: (self topEntity isOfType: FASTTWithStatements).
	self assert: self topEntity name equals: 'function'.
	
	"Testing value of monovalue relation statementBlock"
	self assert: self topEntity statementBlock isNotNil.

		stack push: self topEntity statementBlock.
		self assert: self topEntity sourceCode equals: 'nonlocal x, y, z
	return (x, y, z)' withPlatformLineEndings.
		self assert: (self topEntity isOfType: FASTPyBlock).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 2.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'nonlocal x, y, z'.
			self assert: (self topEntity isOfType: FASTPyNonlocalStatement).
			
			"Testing value of multivalued relation variables"
			self assert: self topEntity variables size equals: 3.
			
				stack push: (stack top variables at: 1).
				self assert: self topEntity sourceCode equals: 'x'.
				self assert: (self topEntity isOfType: FASTPyVariable).
				self assert: (self topEntity isOfType: FASTTVariableEntity).
				stack pop.
			
				stack push: (stack top variables at: 2).
				self assert: self topEntity sourceCode equals: 'y'.
				self assert: (self topEntity isOfType: FASTPyVariable).
				self assert: (self topEntity isOfType: FASTTVariableEntity).
				stack pop.
			
				stack push: (stack top variables at: 3).
				self assert: self topEntity sourceCode equals: 'z'.
				self assert: (self topEntity isOfType: FASTPyVariable).
				self assert: (self topEntity isOfType: FASTTVariableEntity).
				stack pop.
			stack pop.
		
			stack push: (stack top statements at: 2).
			self assert: self topEntity sourceCode equals: 'return (x, y, z)'.
			self assert: (self topEntity isOfType: FASTPyReturnStatement).
			self assert: (self topEntity isOfType: FASTTReturnStatement).
			
			"Testing value of monovalue relation expression"
			self assert: self topEntity expression isNotNil.

				stack push: self topEntity expression.
				self assert: self topEntity sourceCode equals: '(x, y, z)'.
				self assert: (self topEntity isOfType: FASTPyTuple).
				
				"Testing value of multivalued relation initializers"
				self assert: self topEntity initializers size equals: 3.
				
					stack push: (stack top initializers at: 1).
					self assert: self topEntity sourceCode equals: 'x'.
					self assert: (self topEntity isOfType: FASTPyIdentifier).
					stack pop.
				
					stack push: (stack top initializers at: 2).
					self assert: self topEntity sourceCode equals: 'y'.
					self assert: (self topEntity isOfType: FASTPyIdentifier).
					stack pop.
				
					stack push: (stack top initializers at: 3).
					self assert: self topEntity sourceCode equals: 'z'.
					self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - operators' }
FASTPythonImporterTest >> testNotOperator [

	| operator |
	self parse: 'not True'.

	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyNotOperator) size equals: 1.

	operator := (fast allWithType: FASTPyNotOperator) anyOne.
	self assert: operator sourceCode equals: 'not True'.
	self assert: (operator isOfType: FASTTExpression).
	self assert: operator argument sourceCode equals: 'True'.
	self assert: (operator argument isOfType: FASTPyBoolean)
]

{ #category : 'tests - function definitions' }
FASTPythonImporterTest >> testParameter [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testParameter 	"

	self parse: 'def function(a): pass'.

	self assert: (fast allWithType: FASTPyBlock) size equals: 1.
	self assert: (fast allWithType: FASTPyFunctionDefinition) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyParameter) size equals: 1.
	self assert: (fast allWithType: FASTPyPassStatement) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'def function(a): pass'.
	self assert: (self topEntity isOfType: FASTPyFunctionDefinition).
	self assert: (self topEntity isOfType: FASTPyTDefinition).
	self assert: (self topEntity isOfType: FASTTWithParameters).
	self assert: (self topEntity isOfType: FASTTWithStatements).
	self assert: self topEntity name equals: 'function'.
	
	"Testing value of multivalued relation parameters"
	self assert: self topEntity parameters size equals: 1.
	
		stack push: (stack top parameters at: 1).
		self assert: self topEntity sourceCode equals: 'a'.
		self assert: (self topEntity isOfType: FASTPyParameter).
		self assert: (self topEntity isOfType: FASTTVariableEntity).
		self assert: self topEntity name equals: 'a'.
		stack pop.
	
	"Testing value of monovalue relation statementBlock"
	self assert: self topEntity statementBlock isNotNil.

		stack push: self topEntity statementBlock.
		self assert: self topEntity sourceCode equals: 'pass'.
		self assert: (self topEntity isOfType: FASTPyBlock).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'pass'.
			self assert: (self topEntity isOfType: FASTPyPassStatement)
]

{ #category : 'tests - statements' }
FASTPythonImporterTest >> testPassStatement [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testPassStatement 	"

	self parse: 'def aFunction():
	pass'.

	self assert: (fast allWithType: FASTPyBlock) size equals: 1.
	self assert: (fast allWithType: FASTPyFunctionDefinition) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPassStatement) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'def aFunction():
	pass' withPlatformLineEndings.
	self assert: (self topEntity isOfType: FASTPyFunctionDefinition).
	self assert: (self topEntity isOfType: FASTPyTDefinition).
	self assert: (self topEntity isOfType: FASTTWithParameters).
	self assert: (self topEntity isOfType: FASTTWithStatements).
	self assert: self topEntity name equals: 'aFunction'.
	
	"Testing value of monovalue relation statementBlock"
	self assert: self topEntity statementBlock isNotNil.

		stack push: self topEntity statementBlock.
		self assert: self topEntity sourceCode equals: 'pass'.
		self assert: (self topEntity isOfType: FASTPyBlock).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'pass'.
			self assert: (self topEntity isOfType: FASTPyPassStatement)
]

{ #category : 'tests - function definitions' }
FASTPythonImporterTest >> testPositionalSeparator [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testPositionalSeparator 	"

	self parse: 'def function(fp, /, x): pass'.

	self assert: (fast allWithType: FASTPyBlock) size equals: 1.
	self assert: (fast allWithType: FASTPyFunctionDefinition) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyParameter) size equals: 2.
	self assert: (fast allWithType: FASTPyPassStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyPositionalSeparator) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'def function(fp, /, x): pass'.
	self assert: (self topEntity isOfType: FASTPyFunctionDefinition).
	self assert: (self topEntity isOfType: FASTPyTDefinition).
	self assert: (self topEntity isOfType: FASTTWithParameters).
	self assert: (self topEntity isOfType: FASTTWithStatements).
	self assert: self topEntity name equals: 'function'.
	
	"Testing value of multivalued relation parameters"
	self assert: self topEntity parameters size equals: 3.
	
		stack push: (stack top parameters at: 1).
		self assert: self topEntity sourceCode equals: 'fp'.
		self assert: (self topEntity isOfType: FASTPyParameter).
		self assert: (self topEntity isOfType: FASTTVariableEntity).
		self assert: self topEntity name equals: 'fp'.
		stack pop.
	
		stack push: (stack top parameters at: 2).
		self assert: self topEntity sourceCode equals: '/'.
		self assert: (self topEntity isOfType: FASTPyPositionalSeparator).
		self assert: (self topEntity isOfType: FASTTVariableEntity).
		stack pop.
	
		stack push: (stack top parameters at: 3).
		self assert: self topEntity sourceCode equals: 'x'.
		self assert: (self topEntity isOfType: FASTPyParameter).
		self assert: (self topEntity isOfType: FASTTVariableEntity).
		self assert: self topEntity name equals: 'x'.
		stack pop.
	
	"Testing value of monovalue relation statementBlock"
	self assert: self topEntity statementBlock isNotNil.

		stack push: self topEntity statementBlock.
		self assert: self topEntity sourceCode equals: 'pass'.
		self assert: (self topEntity isOfType: FASTPyBlock).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'pass'.
			self assert: (self topEntity isOfType: FASTPyPassStatement)
]

{ #category : 'tests - statements' }
FASTPythonImporterTest >> testPrintStatement [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testPrintStatement 	"

	self parse: 'print "Hello ", "World", 30'.

	self assert: (fast allWithType: FASTPyInteger) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPrintStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyString) size equals: 2.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'print "Hello ", "World", 30'.
	self assert: (self topEntity isOfType: FASTPyPrintStatement).
	
	"Testing value of multivalued relation expressions"
	self assert: self topEntity expressions size equals: 3.
	
		stack push: (stack top expressions at: 1).
		self assert: self topEntity sourceCode equals: '"Hello "'.
		self assert: (self topEntity isOfType: FASTPyString).
		self assert: (self topEntity isOfType: FASTTLiteral).
		self assert: (self topEntity isOfType: FASTTStringLiteral).
		stack pop.
	
		stack push: (stack top expressions at: 2).
		self assert: self topEntity sourceCode equals: '"World"'.
		self assert: (self topEntity isOfType: FASTPyString).
		self assert: (self topEntity isOfType: FASTTLiteral).
		self assert: (self topEntity isOfType: FASTTStringLiteral).
		stack pop.
	
		stack push: (stack top expressions at: 3).
		self assert: self topEntity sourceCode equals: '30'.
		self assert: (self topEntity isOfType: FASTPyInteger).
		self assert: (self topEntity isOfType: FASTTLiteral).
		self assert: (self topEntity isOfType: FASTTNumericalLiteral)
]

{ #category : 'tests - statements' }
FASTPythonImporterTest >> testPrintStatmentWithChevron [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testPrintStatmentWithChevron 	"

	self parse: 'print >> file_object, "message"'.

	self assert: (fast allWithType: FASTPyChevron) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPrintStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyString) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'print >> file_object, "message"'.
	self assert: (self topEntity isOfType: FASTPyPrintStatement).
	
	"Testing value of multivalued relation expressions"
	self assert: self topEntity expressions size equals: 1.
	
		stack push: (stack top expressions at: 1).
		self assert: self topEntity sourceCode equals: '"message"'.
		self assert: (self topEntity isOfType: FASTPyString).
		self assert: (self topEntity isOfType: FASTTLiteral).
		self assert: (self topEntity isOfType: FASTTStringLiteral).
		stack pop.
	
	"Testing value of monovalue relation redirection"
	self assert: self topEntity redirection isNotNil.

		stack push: self topEntity redirection.
		self assert: self topEntity sourceCode equals: '>> file_object'.
		self assert: (self topEntity isOfType: FASTPyChevron).
		
		"Testing value of monovalue relation expression"
		self assert: self topEntity expression isNotNil.

			stack push: self topEntity expression.
			self assert: self topEntity sourceCode equals: 'file_object'.
			self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - raise statement' }
FASTPythonImporterTest >> testRaise [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testRaise 	"

	self parse: 'raise'.

	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyRaiseStatement) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'raise'.
	self assert: (self topEntity isOfType: FASTPyRaiseStatement)
]

{ #category : 'tests - raise statement' }
FASTPythonImporterTest >> testRaiseAttribute [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testRaiseAttribute 	"

	self parse: 'raise module.Exception'.

	self assert: (fast allWithType: FASTPyAttributeAccess) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 2.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyRaiseStatement) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'raise module.Exception'.
	self assert: (self topEntity isOfType: FASTPyRaiseStatement).
	
	"Testing value of monovalue relation exception"
	self assert: self topEntity exception isNotNil.

		stack push: self topEntity exception.
		self assert: self topEntity sourceCode equals: 'module.Exception'.
		self assert: (self topEntity isOfType: FASTPyAttributeAccess).
		
		"Testing value of monovalue relation attribute"
		self assert: self topEntity attribute isNotNil.

			stack push: self topEntity attribute.
			self assert: self topEntity sourceCode equals: 'Exception'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		
		"Testing value of monovalue relation object"
		self assert: self topEntity object isNotNil.

			stack push: self topEntity object.
			self assert: self topEntity sourceCode equals: 'module'.
			self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - raise statement' }
FASTPythonImporterTest >> testRaiseBooleanOperator [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testRaiseBooleanOperator 	"

	self parse: 'raise (ValueError or TypeError)'.

	self assert: (fast allWithType: FASTPyBooleanOperator) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 2.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyRaiseStatement) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'raise (ValueError or TypeError)'.
	self assert: (self topEntity isOfType: FASTPyRaiseStatement).
	
	"Testing value of monovalue relation exception"
	self assert: self topEntity exception isNotNil.

		stack push: self topEntity exception.
		self assert: self topEntity sourceCode equals: 'ValueError or TypeError'.
		self assert: (self topEntity isOfType: FASTPyBooleanOperator).
		self assert: (self topEntity isOfType: FASTTBinaryExpression).
		
		"Testing value of monovalue relation leftOperand"
		self assert: self topEntity leftOperand isNotNil.

			stack push: self topEntity leftOperand.
			self assert: self topEntity sourceCode equals: 'ValueError'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		self assert: self topEntity operator equals: 'or'.
		
		"Testing value of monovalue relation rightOperand"
		self assert: self topEntity rightOperand isNotNil.

			stack push: self topEntity rightOperand.
			self assert: self topEntity sourceCode equals: 'TypeError'.
			self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - raise statement' }
FASTPythonImporterTest >> testRaiseCall [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testRaiseCall 	"

	self parse: 'raise Exception("New instance")'.

	self assert: (fast allWithType: FASTPyCall) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyRaiseStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyString) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'raise Exception("New instance")'.
	self assert: (self topEntity isOfType: FASTPyRaiseStatement).
	
	"Testing value of monovalue relation exception"
	self assert: self topEntity exception isNotNil.

		stack push: self topEntity exception.
		self assert: self topEntity sourceCode equals: 'Exception("New instance")'.
		self assert: (self topEntity isOfType: FASTPyCall).
		self assert: (self topEntity isOfType: FASTTInvocation).
		self assert: (self topEntity isOfType: FASTTWithArguments).
		
		"Testing value of multivalued relation arguments"
		self assert: self topEntity arguments size equals: 1.
		
			stack push: (stack top arguments at: 1).
			self assert: self topEntity sourceCode equals: '"New instance"'.
			self assert: (self topEntity isOfType: FASTPyString).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTStringLiteral).
			stack pop.
		
		"Testing value of monovalue relation callee"
		self assert: self topEntity callee isNotNil.

			stack push: self topEntity callee.
			self assert: self topEntity sourceCode equals: 'Exception'.
			self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - raise statement' }
FASTPythonImporterTest >> testRaiseIdentifier [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testRaiseIdentifier 	"

	self parse: 'raise Exception'.

	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyRaiseStatement) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'raise Exception'.
	self assert: (self topEntity isOfType: FASTPyRaiseStatement).
	
	"Testing value of monovalue relation exception"
	self assert: self topEntity exception isNotNil.

		stack push: self topEntity exception.
		self assert: self topEntity sourceCode equals: 'Exception'.
		self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - raise statement' }
FASTPythonImporterTest >> testRaiseSubscript [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testRaiseSubscript 	"

	self parse: 'raise exceptions[x]'.

	self assert: (fast allWithType: FASTPyIdentifier) size equals: 2.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyRaiseStatement) size equals: 1.
	self assert: (fast allWithType: FASTPySubscript) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'raise exceptions[x]'.
	self assert: (self topEntity isOfType: FASTPyRaiseStatement).
	
	"Testing value of monovalue relation exception"
	self assert: self topEntity exception isNotNil.

		stack push: self topEntity exception.
		self assert: self topEntity sourceCode equals: 'exceptions[x]'.
		self assert: (self topEntity isOfType: FASTPySubscript).
		
		"Testing value of multivalued relation indices"
		self assert: self topEntity indices size equals: 1.
		
			stack push: (stack top indices at: 1).
			self assert: self topEntity sourceCode equals: 'x'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		
		"Testing value of monovalue relation value"
		self assert: self topEntity value isNotNil.

			stack push: self topEntity value.
			self assert: self topEntity sourceCode equals: 'exceptions'.
			self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - literals' }
FASTPythonImporterTest >> testRawString [

	| string |
	self parse: 'r''This is a \string'''.

	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyString) size equals: 1.

	string := (fast allWithType: FASTPyString) anyOne.
	self assert: string sourceCode equals: 'r''This is a \string'''.
	self assert: (string isOfType: FASTTStringLiteral).
	self assertEmpty: string allContainedEntities
]

{ #category : 'tests - statements' }
FASTPythonImporterTest >> testReturnEmpty [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testReturnEmpty 	"

	self parse: 'def aFunction():
	return'.

	self assert: (fast allWithType: FASTPyBlock) size equals: 1.
	self assert: (fast allWithType: FASTPyFunctionDefinition) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyReturnStatement) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'def aFunction():
	return' withPlatformLineEndings.
	self assert: (self topEntity isOfType: FASTPyFunctionDefinition).
	self assert: (self topEntity isOfType: FASTPyTDefinition).
	self assert: (self topEntity isOfType: FASTTWithParameters).
	self assert: (self topEntity isOfType: FASTTWithStatements).
	self assert: self topEntity name equals: 'aFunction'.
	
	"Testing value of monovalue relation statementBlock"
	self assert: self topEntity statementBlock isNotNil.

		stack push: self topEntity statementBlock.
		self assert: self topEntity sourceCode equals: 'return'.
		self assert: (self topEntity isOfType: FASTPyBlock).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'return'.
			self assert: (self topEntity isOfType: FASTPyReturnStatement).
			self assert: (self topEntity isOfType: FASTTReturnStatement)
]

{ #category : 'tests - statements' }
FASTPythonImporterTest >> testReturnList [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testReturnList 	"

	self parse: 'def aFunction():
	return [1, 2, 3]'.

	self assert: (fast allWithType: FASTPyBlock) size equals: 1.
	self assert: (fast allWithType: FASTPyFunctionDefinition) size equals: 1.
	self assert: (fast allWithType: FASTPyInteger) size equals: 3.
	self assert: (fast allWithType: FASTPyList) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyReturnStatement) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'def aFunction():
	return [1, 2, 3]' withPlatformLineEndings.
	self assert: (self topEntity isOfType: FASTPyFunctionDefinition).
	self assert: (self topEntity isOfType: FASTPyTDefinition).
	self assert: (self topEntity isOfType: FASTTWithParameters).
	self assert: (self topEntity isOfType: FASTTWithStatements).
	self assert: self topEntity name equals: 'aFunction'.
	
	"Testing value of monovalue relation statementBlock"
	self assert: self topEntity statementBlock isNotNil.

		stack push: self topEntity statementBlock.
		self assert: self topEntity sourceCode equals: 'return [1, 2, 3]'.
		self assert: (self topEntity isOfType: FASTPyBlock).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'return [1, 2, 3]'.
			self assert: (self topEntity isOfType: FASTPyReturnStatement).
			self assert: (self topEntity isOfType: FASTTReturnStatement).
			
			"Testing value of monovalue relation expression"
			self assert: self topEntity expression isNotNil.

				stack push: self topEntity expression.
				self assert: self topEntity sourceCode equals: '[1, 2, 3]'.
				self assert: (self topEntity isOfType: FASTPyList).
				
				"Testing value of multivalued relation initializers"
				self assert: self topEntity initializers size equals: 3.
				
					stack push: (stack top initializers at: 1).
					self assert: self topEntity sourceCode equals: '1'.
					self assert: (self topEntity isOfType: FASTPyInteger).
					self assert: (self topEntity isOfType: FASTTLiteral).
					self assert: (self topEntity isOfType: FASTTNumericalLiteral).
					stack pop.
				
					stack push: (stack top initializers at: 2).
					self assert: self topEntity sourceCode equals: '2'.
					self assert: (self topEntity isOfType: FASTPyInteger).
					self assert: (self topEntity isOfType: FASTTLiteral).
					self assert: (self topEntity isOfType: FASTTNumericalLiteral).
					stack pop.
				
					stack push: (stack top initializers at: 3).
					self assert: self topEntity sourceCode equals: '3'.
					self assert: (self topEntity isOfType: FASTPyInteger).
					self assert: (self topEntity isOfType: FASTTLiteral).
					self assert: (self topEntity isOfType: FASTTNumericalLiteral)
]

{ #category : 'tests - statements' }
FASTPythonImporterTest >> testReturnLiteral [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testReturnLiteral 	"

	self parse: 'def aFunction():
	return 1'.

	self assert: (fast allWithType: FASTPyBlock) size equals: 1.
	self assert: (fast allWithType: FASTPyFunctionDefinition) size equals: 1.
	self assert: (fast allWithType: FASTPyInteger) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyReturnStatement) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'def aFunction():
	return 1' withPlatformLineEndings.
	self assert: (self topEntity isOfType: FASTPyFunctionDefinition).
	self assert: (self topEntity isOfType: FASTPyTDefinition).
	self assert: (self topEntity isOfType: FASTTWithParameters).
	self assert: (self topEntity isOfType: FASTTWithStatements).
	self assert: self topEntity name equals: 'aFunction'.
	
	"Testing value of monovalue relation statementBlock"
	self assert: self topEntity statementBlock isNotNil.

		stack push: self topEntity statementBlock.
		self assert: self topEntity sourceCode equals: 'return 1'.
		self assert: (self topEntity isOfType: FASTPyBlock).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'return 1'.
			self assert: (self topEntity isOfType: FASTPyReturnStatement).
			self assert: (self topEntity isOfType: FASTTReturnStatement).
			
			"Testing value of monovalue relation expression"
			self assert: self topEntity expression isNotNil.

				stack push: self topEntity expression.
				self assert: self topEntity sourceCode equals: '1'.
				self assert: (self topEntity isOfType: FASTPyInteger).
				self assert: (self topEntity isOfType: FASTTLiteral).
				self assert: (self topEntity isOfType: FASTTNumericalLiteral)
]

{ #category : 'tests - statements' }
FASTPythonImporterTest >> testReturnMultiple [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testReturnMultiple 	"

	self parse: 'def myFunct():
	return 1 + 2, len("abc"), 3 * 4'.

	self assert: (fast allWithType: FASTPyBinaryOperator) size equals: 2.
	self assert: (fast allWithType: FASTPyBlock) size equals: 1.
	self assert: (fast allWithType: FASTPyCall) size equals: 1.
	self assert: (fast allWithType: FASTPyFunctionDefinition) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyInteger) size equals: 4.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyReturnStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyString) size equals: 1.
	self assert: (fast allWithType: FASTPyTuple) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'def myFunct():
	return 1 + 2, len("abc"), 3 * 4' withPlatformLineEndings.
	self assert: (self topEntity isOfType: FASTPyFunctionDefinition).
	self assert: (self topEntity isOfType: FASTPyTDefinition).
	self assert: (self topEntity isOfType: FASTTWithParameters).
	self assert: (self topEntity isOfType: FASTTWithStatements).
	self assert: self topEntity name equals: 'myFunct'.
	
	"Testing value of monovalue relation statementBlock"
	self assert: self topEntity statementBlock isNotNil.

		stack push: self topEntity statementBlock.
		self assert: self topEntity sourceCode equals: 'return 1 + 2, len("abc"), 3 * 4'.
		self assert: (self topEntity isOfType: FASTPyBlock).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'return 1 + 2, len("abc"), 3 * 4'.
			self assert: (self topEntity isOfType: FASTPyReturnStatement).
			self assert: (self topEntity isOfType: FASTTReturnStatement).
			
			"Testing value of monovalue relation expression"
			self assert: self topEntity expression isNotNil.

				stack push: self topEntity expression.
				self assert: self topEntity sourceCode equals: '1 + 2, len("abc"), 3 * 4'.
				self assert: (self topEntity isOfType: FASTPyTuple).
				
				"Testing value of multivalued relation initializers"
				self assert: self topEntity initializers size equals: 3.
				
					stack push: (stack top initializers at: 1).
					self assert: self topEntity sourceCode equals: '1 + 2'.
					self assert: (self topEntity isOfType: FASTPyBinaryOperator).
					self assert: (self topEntity isOfType: FASTTBinaryExpression).
					
					"Testing value of monovalue relation leftOperand"
					self assert: self topEntity leftOperand isNotNil.

						stack push: self topEntity leftOperand.
						self assert: self topEntity sourceCode equals: '1'.
						self assert: (self topEntity isOfType: FASTPyInteger).
						self assert: (self topEntity isOfType: FASTTLiteral).
						self assert: (self topEntity isOfType: FASTTNumericalLiteral).
						stack pop.
					self assert: self topEntity operator equals: '+'.
					
					"Testing value of monovalue relation rightOperand"
					self assert: self topEntity rightOperand isNotNil.

						stack push: self topEntity rightOperand.
						self assert: self topEntity sourceCode equals: '2'.
						self assert: (self topEntity isOfType: FASTPyInteger).
						self assert: (self topEntity isOfType: FASTTLiteral).
						self assert: (self topEntity isOfType: FASTTNumericalLiteral).
						stack pop.
					stack pop.
				
					stack push: (stack top initializers at: 2).
					self assert: self topEntity sourceCode equals: 'len("abc")'.
					self assert: (self topEntity isOfType: FASTPyCall).
					self assert: (self topEntity isOfType: FASTTInvocation).
					self assert: (self topEntity isOfType: FASTTWithArguments).
					
					"Testing value of multivalued relation arguments"
					self assert: self topEntity arguments size equals: 1.
					
						stack push: (stack top arguments at: 1).
						self assert: self topEntity sourceCode equals: '"abc"'.
						self assert: (self topEntity isOfType: FASTPyString).
						self assert: (self topEntity isOfType: FASTTLiteral).
						self assert: (self topEntity isOfType: FASTTStringLiteral).
						stack pop.
					
					"Testing value of monovalue relation callee"
					self assert: self topEntity callee isNotNil.

						stack push: self topEntity callee.
						self assert: self topEntity sourceCode equals: 'len'.
						self assert: (self topEntity isOfType: FASTPyIdentifier).
						stack pop.
					stack pop.
				
					stack push: (stack top initializers at: 3).
					self assert: self topEntity sourceCode equals: '3 * 4'.
					self assert: (self topEntity isOfType: FASTPyBinaryOperator).
					self assert: (self topEntity isOfType: FASTTBinaryExpression).
					
					"Testing value of monovalue relation leftOperand"
					self assert: self topEntity leftOperand isNotNil.

						stack push: self topEntity leftOperand.
						self assert: self topEntity sourceCode equals: '3'.
						self assert: (self topEntity isOfType: FASTPyInteger).
						self assert: (self topEntity isOfType: FASTTLiteral).
						self assert: (self topEntity isOfType: FASTTNumericalLiteral).
						stack pop.
					self assert: self topEntity operator equals: '*'.
					
					"Testing value of monovalue relation rightOperand"
					self assert: self topEntity rightOperand isNotNil.

						stack push: self topEntity rightOperand.
						self assert: self topEntity sourceCode equals: '4'.
						self assert: (self topEntity isOfType: FASTPyInteger).
						self assert: (self topEntity isOfType: FASTTLiteral).
						self assert: (self topEntity isOfType: FASTTNumericalLiteral)
]

{ #category : 'tests - statements' }
FASTPythonImporterTest >> testReturnOperator [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testReturnOperator 	"

	self parse: 'def aFunction():
	return 10 > 13'.

	self assert: (fast allWithType: FASTPyBlock) size equals: 1.
	self assert: (fast allWithType: FASTPyComparisonOperator) size equals: 1.
	self assert: (fast allWithType: FASTPyFunctionDefinition) size equals: 1.
	self assert: (fast allWithType: FASTPyInteger) size equals: 2.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyReturnStatement) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'def aFunction():
	return 10 > 13' withPlatformLineEndings.
	self assert: (self topEntity isOfType: FASTPyFunctionDefinition).
	self assert: (self topEntity isOfType: FASTPyTDefinition).
	self assert: (self topEntity isOfType: FASTTWithParameters).
	self assert: (self topEntity isOfType: FASTTWithStatements).
	self assert: self topEntity name equals: 'aFunction'.
	
	"Testing value of monovalue relation statementBlock"
	self assert: self topEntity statementBlock isNotNil.

		stack push: self topEntity statementBlock.
		self assert: self topEntity sourceCode equals: 'return 10 > 13'.
		self assert: (self topEntity isOfType: FASTPyBlock).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'return 10 > 13'.
			self assert: (self topEntity isOfType: FASTPyReturnStatement).
			self assert: (self topEntity isOfType: FASTTReturnStatement).
			
			"Testing value of monovalue relation expression"
			self assert: self topEntity expression isNotNil.

				stack push: self topEntity expression.
				self assert: self topEntity sourceCode equals: '10 > 13'.
				self assert: (self topEntity isOfType: FASTPyComparisonOperator).
				
				"Testing value of multivalued relation operands"
				self assert: self topEntity operands size equals: 2.
				
					stack push: (stack top operands at: 1).
					self assert: self topEntity sourceCode equals: '10'.
					self assert: (self topEntity isOfType: FASTPyInteger).
					self assert: (self topEntity isOfType: FASTTLiteral).
					self assert: (self topEntity isOfType: FASTTNumericalLiteral).
					stack pop.
				
					stack push: (stack top operands at: 2).
					self assert: self topEntity sourceCode equals: '13'.
					self assert: (self topEntity isOfType: FASTPyInteger).
					self assert: (self topEntity isOfType: FASTTLiteral).
					self assert: (self topEntity isOfType: FASTTNumericalLiteral).
					stack pop.
				self assert: self topEntity operators equals: #('>')
]

{ #category : 'tests - collections' }
FASTPythonImporterTest >> testSet [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testSet 	"

	self parse: '{1, 2, 3}'.

	self assert: (fast allWithType: FASTPyInteger) size equals: 3.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPySet) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: '{1, 2, 3}'.
	self assert: (self topEntity isOfType: FASTPySet).
	
	"Testing value of multivalued relation initializers"
	self assert: self topEntity initializers size equals: 3.
	
		stack push: (stack top initializers at: 1).
		self assert: self topEntity sourceCode equals: '1'.
		self assert: (self topEntity isOfType: FASTPyInteger).
		self assert: (self topEntity isOfType: FASTTLiteral).
		self assert: (self topEntity isOfType: FASTTNumericalLiteral).
		stack pop.
	
		stack push: (stack top initializers at: 2).
		self assert: self topEntity sourceCode equals: '2'.
		self assert: (self topEntity isOfType: FASTPyInteger).
		self assert: (self topEntity isOfType: FASTTLiteral).
		self assert: (self topEntity isOfType: FASTTNumericalLiteral).
		stack pop.
	
		stack push: (stack top initializers at: 3).
		self assert: self topEntity sourceCode equals: '3'.
		self assert: (self topEntity isOfType: FASTPyInteger).
		self assert: (self topEntity isOfType: FASTTLiteral).
		self assert: (self topEntity isOfType: FASTTNumericalLiteral)
]

{ #category : 'tests - comprehension' }
FASTPythonImporterTest >> testSetComprehension [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testSetComprehension 	"

	self parse: '{k1 for k1 in v.items() }'.

	self assert: (fast allWithType: FASTPyAttributeAccess) size equals: 1.
	self assert: (fast allWithType: FASTPyCall) size equals: 1.
	self assert: (fast allWithType: FASTPyForInClause) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 4.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPySetComprehension) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: '{k1 for k1 in v.items() }'.
	self assert: (self topEntity isOfType: FASTPySetComprehension).
	self assert: (self topEntity isOfType: FASTPyTComprehension).
	self assert: (self topEntity isOfType: FASTTWithCondition).
	
	"Testing value of monovalue relation body"
	self assert: self topEntity body isNotNil.

		stack push: self topEntity body.
		self assert: self topEntity sourceCode equals: 'k1'.
		self assert: (self topEntity isOfType: FASTPyIdentifier).
		stack pop.
	
	"Testing value of multivalued relation clauses"
	self assert: self topEntity clauses size equals: 1.
	
		stack push: (stack top clauses at: 1).
		self assert: self topEntity sourceCode equals: 'for k1 in v.items()'.
		self assert: (self topEntity isOfType: FASTPyForInClause).
		
		"Testing value of monovalue relation left"
		self assert: self topEntity left isNotNil.

			stack push: self topEntity left.
			self assert: self topEntity sourceCode equals: 'k1'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		
		"Testing value of monovalue relation right"
		self assert: self topEntity right isNotNil.

			stack push: self topEntity right.
			self assert: self topEntity sourceCode equals: 'v.items()'.
			self assert: (self topEntity isOfType: FASTPyCall).
			self assert: (self topEntity isOfType: FASTTInvocation).
			self assert: (self topEntity isOfType: FASTTWithArguments).
			
			"Testing value of monovalue relation callee"
			self assert: self topEntity callee isNotNil.

				stack push: self topEntity callee.
				self assert: self topEntity sourceCode equals: 'v.items'.
				self assert: (self topEntity isOfType: FASTPyAttributeAccess).
				
				"Testing value of monovalue relation attribute"
				self assert: self topEntity attribute isNotNil.

					stack push: self topEntity attribute.
					self assert: self topEntity sourceCode equals: 'items'.
					self assert: (self topEntity isOfType: FASTPyIdentifier).
					stack pop.
				
				"Testing value of monovalue relation object"
				self assert: self topEntity object isNotNil.

					stack push: self topEntity object.
					self assert: self topEntity sourceCode equals: 'v'.
					self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - comprehension' }
FASTPythonImporterTest >> testSetComprehensionWithIfClause [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testSetComprehensionWithIfClause 	"

	self parse: '{k1 for k1 in v.items() if k1 > 4}'.

	self assert: (fast allWithType: FASTPyAttributeAccess) size equals: 1.
	self assert: (fast allWithType: FASTPyCall) size equals: 1.
	self assert: (fast allWithType: FASTPyComparisonOperator) size equals: 1.
	self assert: (fast allWithType: FASTPyForInClause) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 5.
	self assert: (fast allWithType: FASTPyInteger) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPySetComprehension) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: '{k1 for k1 in v.items() if k1 > 4}'.
	self assert: (self topEntity isOfType: FASTPySetComprehension).
	self assert: (self topEntity isOfType: FASTPyTComprehension).
	self assert: (self topEntity isOfType: FASTTWithCondition).
	
	"Testing value of monovalue relation body"
	self assert: self topEntity body isNotNil.

		stack push: self topEntity body.
		self assert: self topEntity sourceCode equals: 'k1'.
		self assert: (self topEntity isOfType: FASTPyIdentifier).
		stack pop.
	
	"Testing value of multivalued relation clauses"
	self assert: self topEntity clauses size equals: 1.
	
		stack push: (stack top clauses at: 1).
		self assert: self topEntity sourceCode equals: 'for k1 in v.items()'.
		self assert: (self topEntity isOfType: FASTPyForInClause).
		
		"Testing value of monovalue relation left"
		self assert: self topEntity left isNotNil.

			stack push: self topEntity left.
			self assert: self topEntity sourceCode equals: 'k1'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		
		"Testing value of monovalue relation right"
		self assert: self topEntity right isNotNil.

			stack push: self topEntity right.
			self assert: self topEntity sourceCode equals: 'v.items()'.
			self assert: (self topEntity isOfType: FASTPyCall).
			self assert: (self topEntity isOfType: FASTTInvocation).
			self assert: (self topEntity isOfType: FASTTWithArguments).
			
			"Testing value of monovalue relation callee"
			self assert: self topEntity callee isNotNil.

				stack push: self topEntity callee.
				self assert: self topEntity sourceCode equals: 'v.items'.
				self assert: (self topEntity isOfType: FASTPyAttributeAccess).
				
				"Testing value of monovalue relation attribute"
				self assert: self topEntity attribute isNotNil.

					stack push: self topEntity attribute.
					self assert: self topEntity sourceCode equals: 'items'.
					self assert: (self topEntity isOfType: FASTPyIdentifier).
					stack pop.
				
				"Testing value of monovalue relation object"
				self assert: self topEntity object isNotNil.

					stack push: self topEntity object.
					self assert: self topEntity sourceCode equals: 'v'.
					self assert: (self topEntity isOfType: FASTPyIdentifier).
					stack pop.
				stack pop.
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation condition"
	self assert: self topEntity condition isNotNil.

		stack push: self topEntity condition.
		self assert: self topEntity sourceCode equals: 'k1 > 4'.
		self assert: (self topEntity isOfType: FASTPyComparisonOperator).
		
		"Testing value of multivalued relation operands"
		self assert: self topEntity operands size equals: 2.
		
			stack push: (stack top operands at: 1).
			self assert: self topEntity sourceCode equals: 'k1'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		
			stack push: (stack top operands at: 2).
			self assert: self topEntity sourceCode equals: '4'.
			self assert: (self topEntity isOfType: FASTPyInteger).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTNumericalLiteral).
			stack pop.
		self assert: self topEntity operators equals: #('>')
]

{ #category : 'tests - collections' }
FASTPythonImporterTest >> testSetWithAttribute [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testSetWithAttribute 	"

	self parse: '{ obj.attr }'.

	self assert: (fast allWithType: FASTPyAttributeAccess) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 2.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPySet) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: '{ obj.attr }'.
	self assert: (self topEntity isOfType: FASTPySet).
	
	"Testing value of multivalued relation initializers"
	self assert: self topEntity initializers size equals: 1.
	
		stack push: (stack top initializers at: 1).
		self assert: self topEntity sourceCode equals: 'obj.attr'.
		self assert: (self topEntity isOfType: FASTPyAttributeAccess).
		
		"Testing value of monovalue relation attribute"
		self assert: self topEntity attribute isNotNil.

			stack push: self topEntity attribute.
			self assert: self topEntity sourceCode equals: 'attr'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		
		"Testing value of monovalue relation object"
		self assert: self topEntity object isNotNil.

			stack push: self topEntity object.
			self assert: self topEntity sourceCode equals: 'obj'.
			self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - collections' }
FASTPythonImporterTest >> testSetWithBinaryOperator [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testSetWithBinaryOperator 	"

	self parse: '{ 10 / 2 }'.

	self assert: (fast allWithType: FASTPyBinaryOperator) size equals: 1.
	self assert: (fast allWithType: FASTPyInteger) size equals: 2.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPySet) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: '{ 10 / 2 }'.
	self assert: (self topEntity isOfType: FASTPySet).
	
	"Testing value of multivalued relation initializers"
	self assert: self topEntity initializers size equals: 1.
	
		stack push: (stack top initializers at: 1).
		self assert: self topEntity sourceCode equals: '10 / 2'.
		self assert: (self topEntity isOfType: FASTPyBinaryOperator).
		self assert: (self topEntity isOfType: FASTTBinaryExpression).
		
		"Testing value of monovalue relation leftOperand"
		self assert: self topEntity leftOperand isNotNil.

			stack push: self topEntity leftOperand.
			self assert: self topEntity sourceCode equals: '10'.
			self assert: (self topEntity isOfType: FASTPyInteger).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTNumericalLiteral).
			stack pop.
		self assert: self topEntity operator equals: '/'.
		
		"Testing value of monovalue relation rightOperand"
		self assert: self topEntity rightOperand isNotNil.

			stack push: self topEntity rightOperand.
			self assert: self topEntity sourceCode equals: '2'.
			self assert: (self topEntity isOfType: FASTPyInteger).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTNumericalLiteral)
]

{ #category : 'tests - collections' }
FASTPythonImporterTest >> testSetWithBooleanOperator [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testSetWithBooleanOperator 	"

	self parse: '{ x and y }'.

	self assert: (fast allWithType: FASTPyBooleanOperator) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 2.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPySet) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: '{ x and y }'.
	self assert: (self topEntity isOfType: FASTPySet).
	
	"Testing value of multivalued relation initializers"
	self assert: self topEntity initializers size equals: 1.
	
		stack push: (stack top initializers at: 1).
		self assert: self topEntity sourceCode equals: 'x and y'.
		self assert: (self topEntity isOfType: FASTPyBooleanOperator).
		self assert: (self topEntity isOfType: FASTTBinaryExpression).
		
		"Testing value of monovalue relation leftOperand"
		self assert: self topEntity leftOperand isNotNil.

			stack push: self topEntity leftOperand.
			self assert: self topEntity sourceCode equals: 'x'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		self assert: self topEntity operator equals: 'and'.
		
		"Testing value of monovalue relation rightOperand"
		self assert: self topEntity rightOperand isNotNil.

			stack push: self topEntity rightOperand.
			self assert: self topEntity sourceCode equals: 'y'.
			self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - collections' }
FASTPythonImporterTest >> testSetWithCall [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testSetWithCall 	"

	self parse: '{ obj() }'.

	self assert: (fast allWithType: FASTPyCall) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPySet) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: '{ obj() }'.
	self assert: (self topEntity isOfType: FASTPySet).
	
	"Testing value of multivalued relation initializers"
	self assert: self topEntity initializers size equals: 1.
	
		stack push: (stack top initializers at: 1).
		self assert: self topEntity sourceCode equals: 'obj()'.
		self assert: (self topEntity isOfType: FASTPyCall).
		self assert: (self topEntity isOfType: FASTTInvocation).
		self assert: (self topEntity isOfType: FASTTWithArguments).
		
		"Testing value of monovalue relation callee"
		self assert: self topEntity callee isNotNil.

			stack push: self topEntity callee.
			self assert: self topEntity sourceCode equals: 'obj'.
			self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - collections' }
FASTPythonImporterTest >> testSetWithComparisonOperator [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testSetWithComparisonOperator 	"

	self parse: '{ 1 > 3 }'.

	self assert: (fast allWithType: FASTPyComparisonOperator) size equals: 1.
	self assert: (fast allWithType: FASTPyInteger) size equals: 2.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPySet) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: '{ 1 > 3 }'.
	self assert: (self topEntity isOfType: FASTPySet).
	
	"Testing value of multivalued relation initializers"
	self assert: self topEntity initializers size equals: 1.
	
		stack push: (stack top initializers at: 1).
		self assert: self topEntity sourceCode equals: '1 > 3'.
		self assert: (self topEntity isOfType: FASTPyComparisonOperator).
		
		"Testing value of multivalued relation operands"
		self assert: self topEntity operands size equals: 2.
		
			stack push: (stack top operands at: 1).
			self assert: self topEntity sourceCode equals: '1'.
			self assert: (self topEntity isOfType: FASTPyInteger).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTNumericalLiteral).
			stack pop.
		
			stack push: (stack top operands at: 2).
			self assert: self topEntity sourceCode equals: '3'.
			self assert: (self topEntity isOfType: FASTPyInteger).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTNumericalLiteral).
			stack pop.
		self assert: self topEntity operators equals: #('>')
]

{ #category : 'tests - collections' }
FASTPythonImporterTest >> testSetWithComplexe [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testSetWithComplexe 	"

	self parse: '{ 1j }'.

	self assert: (fast allWithType: FASTPyComplex) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPySet) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: '{ 1j }'.
	self assert: (self topEntity isOfType: FASTPySet).
	
	"Testing value of multivalued relation initializers"
	self assert: self topEntity initializers size equals: 1.
	
		stack push: (stack top initializers at: 1).
		self assert: self topEntity sourceCode equals: '1j'.
		self assert: (self topEntity isOfType: FASTPyComplex).
		self assert: (self topEntity isOfType: FASTTLiteral).
		self assert: (self topEntity isOfType: FASTTNumericalLiteral)
]

{ #category : 'tests - collections' }
FASTPythonImporterTest >> testSetWithConditionalExpression [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testSetWithConditionalExpression 	"

	self parse: '{ x if True else y }'.

	self assert: (fast allWithType: FASTPyBoolean) size equals: 1.
	self assert: (fast allWithType: FASTPyConditionalExpression) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 2.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPySet) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: '{ x if True else y }'.
	self assert: (self topEntity isOfType: FASTPySet).
	
	"Testing value of multivalued relation initializers"
	self assert: self topEntity initializers size equals: 1.
	
		stack push: (stack top initializers at: 1).
		self assert: self topEntity sourceCode equals: 'x if True else y'.
		self assert: (self topEntity isOfType: FASTPyConditionalExpression).
		self assert: (self topEntity isOfType: FASTTWithCondition).
		
		"Testing value of monovalue relation condition"
		self assert: self topEntity condition isNotNil.

			stack push: self topEntity condition.
			self assert: self topEntity sourceCode equals: 'True'.
			self assert: (self topEntity isOfType: FASTPyBoolean).
			self assert: (self topEntity isOfType: FASTTBooleanLiteral).
			self assert: (self topEntity isOfType: FASTTLiteral).
			stack pop.
		
		"Testing value of monovalue relation elseExpression"
		self assert: self topEntity elseExpression isNotNil.

			stack push: self topEntity elseExpression.
			self assert: self topEntity sourceCode equals: 'y'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		
		"Testing value of monovalue relation thenExpression"
		self assert: self topEntity thenExpression isNotNil.

			stack push: self topEntity thenExpression.
			self assert: self topEntity sourceCode equals: 'x'.
			self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - collections' }
FASTPythonImporterTest >> testSetWithDictionary [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testSetWithDictionary 	"

	self parse: '{ { 1:1 } }'.

	self assert: (fast allWithType: FASTPyDictionary) size equals: 1.
	self assert: (fast allWithType: FASTPyInteger) size equals: 2.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPair) size equals: 1.
	self assert: (fast allWithType: FASTPySet) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: '{ { 1:1 } }'.
	self assert: (self topEntity isOfType: FASTPySet).
	
	"Testing value of multivalued relation initializers"
	self assert: self topEntity initializers size equals: 1.
	
		stack push: (stack top initializers at: 1).
		self assert: self topEntity sourceCode equals: '{ 1:1 }'.
		self assert: (self topEntity isOfType: FASTPyDictionary).
		
		"Testing value of multivalued relation initializers"
		self assert: self topEntity initializers size equals: 1.
		
			stack push: (stack top initializers at: 1).
			self assert: self topEntity sourceCode equals: '1:1'.
			self assert: (self topEntity isOfType: FASTPyPair).
			
			"Testing value of monovalue relation key"
			self assert: self topEntity key isNotNil.

				stack push: self topEntity key.
				self assert: self topEntity sourceCode equals: '1'.
				self assert: (self topEntity isOfType: FASTPyInteger).
				self assert: (self topEntity isOfType: FASTTLiteral).
				self assert: (self topEntity isOfType: FASTTNumericalLiteral).
				stack pop.
			
			"Testing value of monovalue relation value"
			self assert: self topEntity value isNotNil.

				stack push: self topEntity value.
				self assert: self topEntity sourceCode equals: '1'.
				self assert: (self topEntity isOfType: FASTPyInteger).
				self assert: (self topEntity isOfType: FASTTLiteral).
				self assert: (self topEntity isOfType: FASTTNumericalLiteral)
]

{ #category : 'tests - collections' }
FASTPythonImporterTest >> testSetWithEllipsis [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testSetWithEllipsis 	"

	self parse: '{ ... }'.

	self assert: (fast allWithType: FASTPyEllipsis) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPySet) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: '{ ... }'.
	self assert: (self topEntity isOfType: FASTPySet).
	
	"Testing value of multivalued relation initializers"
	self assert: self topEntity initializers size equals: 1.
	
		stack push: (stack top initializers at: 1).
		self assert: self topEntity sourceCode equals: '...'.
		self assert: (self topEntity isOfType: FASTPyEllipsis).
		self assert: (self topEntity isOfType: FASTTLiteral)
]

{ #category : 'tests - collections' }
FASTPythonImporterTest >> testSetWithFalse [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testSetWithFalse 	"

	self parse: '{ False }'.

	self assert: (fast allWithType: FASTPyBoolean) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPySet) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: '{ False }'.
	self assert: (self topEntity isOfType: FASTPySet).
	
	"Testing value of multivalued relation initializers"
	self assert: self topEntity initializers size equals: 1.
	
		stack push: (stack top initializers at: 1).
		self assert: self topEntity sourceCode equals: 'False'.
		self assert: (self topEntity isOfType: FASTPyBoolean).
		self assert: (self topEntity isOfType: FASTTBooleanLiteral).
		self assert: (self topEntity isOfType: FASTTLiteral)
]

{ #category : 'tests - collections' }
FASTPythonImporterTest >> testSetWithFloat [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testSetWithFloat 	"

	self parse: '{ 0.1 }'.

	self assert: (fast allWithType: FASTPyFloat) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPySet) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: '{ 0.1 }'.
	self assert: (self topEntity isOfType: FASTPySet).
	
	"Testing value of multivalued relation initializers"
	self assert: self topEntity initializers size equals: 1.
	
		stack push: (stack top initializers at: 1).
		self assert: self topEntity sourceCode equals: '0.1'.
		self assert: (self topEntity isOfType: FASTPyFloat).
		self assert: (self topEntity isOfType: FASTTLiteral).
		self assert: (self topEntity isOfType: FASTTNumericalLiteral)
]

{ #category : 'tests - collections' }
FASTPythonImporterTest >> testSetWithIdentifier [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testSetWithIdentifier 	"

	self parse: '{ element }'.

	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPySet) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: '{ element }'.
	self assert: (self topEntity isOfType: FASTPySet).
	
	"Testing value of multivalued relation initializers"
	self assert: self topEntity initializers size equals: 1.
	
		stack push: (stack top initializers at: 1).
		self assert: self topEntity sourceCode equals: 'element'.
		self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - collections' }
FASTPythonImporterTest >> testSetWithInteger [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testSetWithInteger 	"

	self parse: '{ 1 }'.

	self assert: (fast allWithType: FASTPyInteger) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPySet) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: '{ 1 }'.
	self assert: (self topEntity isOfType: FASTPySet).
	
	"Testing value of multivalued relation initializers"
	self assert: self topEntity initializers size equals: 1.
	
		stack push: (stack top initializers at: 1).
		self assert: self topEntity sourceCode equals: '1'.
		self assert: (self topEntity isOfType: FASTPyInteger).
		self assert: (self topEntity isOfType: FASTTLiteral).
		self assert: (self topEntity isOfType: FASTTNumericalLiteral)
]

{ #category : 'tests - collections' }
FASTPythonImporterTest >> testSetWithLambda [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testSetWithLambda 	"

	self parse: '{ lambda x:x }'.

	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyLambda) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyParameter) size equals: 1.
	self assert: (fast allWithType: FASTPySet) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: '{ lambda x:x }'.
	self assert: (self topEntity isOfType: FASTPySet).
	
	"Testing value of multivalued relation initializers"
	self assert: self topEntity initializers size equals: 1.
	
		stack push: (stack top initializers at: 1).
		self assert: self topEntity sourceCode equals: 'lambda x:x'.
		self assert: (self topEntity isOfType: FASTPyLambda).
		self assert: (self topEntity isOfType: FASTTWithParameters).
		
		"Testing value of monovalue relation expression"
		self assert: self topEntity expression isNotNil.

			stack push: self topEntity expression.
			self assert: self topEntity sourceCode equals: 'x'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		
		"Testing value of multivalued relation parameters"
		self assert: self topEntity parameters size equals: 1.
		
			stack push: (stack top parameters at: 1).
			self assert: self topEntity sourceCode equals: 'x'.
			self assert: (self topEntity isOfType: FASTPyParameter).
			self assert: (self topEntity isOfType: FASTTVariableEntity).
			self assert: self topEntity name equals: 'x'
]

{ #category : 'tests - collections' }
FASTPythonImporterTest >> testSetWithList [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testSetWithList 	"

	self parse: '{ [ 1 ] }'.

	self assert: (fast allWithType: FASTPyInteger) size equals: 1.
	self assert: (fast allWithType: FASTPyList) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPySet) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: '{ [ 1 ] }'.
	self assert: (self topEntity isOfType: FASTPySet).
	
	"Testing value of multivalued relation initializers"
	self assert: self topEntity initializers size equals: 1.
	
		stack push: (stack top initializers at: 1).
		self assert: self topEntity sourceCode equals: '[ 1 ]'.
		self assert: (self topEntity isOfType: FASTPyList).
		
		"Testing value of multivalued relation initializers"
		self assert: self topEntity initializers size equals: 1.
		
			stack push: (stack top initializers at: 1).
			self assert: self topEntity sourceCode equals: '1'.
			self assert: (self topEntity isOfType: FASTPyInteger).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTNumericalLiteral)
]

{ #category : 'tests - collections' }
FASTPythonImporterTest >> testSetWithListSplat [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testSetWithListSplat 	"

	self parse: '{ *args }'.

	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyListSplat) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPySet) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: '{ *args }'.
	self assert: (self topEntity isOfType: FASTPySet).
	
	"Testing value of multivalued relation initializers"
	self assert: self topEntity initializers size equals: 1.
	
		stack push: (stack top initializers at: 1).
		self assert: self topEntity sourceCode equals: '*args'.
		self assert: (self topEntity isOfType: FASTPyListSplat).
		
		"Testing value of monovalue relation expression"
		self assert: self topEntity expression isNotNil.

			stack push: self topEntity expression.
			self assert: self topEntity sourceCode equals: 'args'.
			self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - collections' }
FASTPythonImporterTest >> testSetWithList_comprehension [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testSetWithList_comprehension 	"

	self parse: '{ [random for i in range(3)] }'.

	self assert: (fast allWithType: FASTPyCall) size equals: 1.
	self assert: (fast allWithType: FASTPyForInClause) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 3.
	self assert: (fast allWithType: FASTPyInteger) size equals: 1.
	self assert: (fast allWithType: FASTPyListComprehension) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPySet) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: '{ [random for i in range(3)] }'.
	self assert: (self topEntity isOfType: FASTPySet).
	
	"Testing value of multivalued relation initializers"
	self assert: self topEntity initializers size equals: 1.
	
		stack push: (stack top initializers at: 1).
		self assert: self topEntity sourceCode equals: '[random for i in range(3)]'.
		self assert: (self topEntity isOfType: FASTPyListComprehension).
		self assert: (self topEntity isOfType: FASTPyTComprehension).
		self assert: (self topEntity isOfType: FASTTWithCondition).
		
		"Testing value of monovalue relation body"
		self assert: self topEntity body isNotNil.

			stack push: self topEntity body.
			self assert: self topEntity sourceCode equals: 'random'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		
		"Testing value of multivalued relation clauses"
		self assert: self topEntity clauses size equals: 1.
		
			stack push: (stack top clauses at: 1).
			self assert: self topEntity sourceCode equals: 'for i in range(3)'.
			self assert: (self topEntity isOfType: FASTPyForInClause).
			
			"Testing value of monovalue relation left"
			self assert: self topEntity left isNotNil.

				stack push: self topEntity left.
				self assert: self topEntity sourceCode equals: 'i'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			
			"Testing value of monovalue relation right"
			self assert: self topEntity right isNotNil.

				stack push: self topEntity right.
				self assert: self topEntity sourceCode equals: 'range(3)'.
				self assert: (self topEntity isOfType: FASTPyCall).
				self assert: (self topEntity isOfType: FASTTInvocation).
				self assert: (self topEntity isOfType: FASTTWithArguments).
				
				"Testing value of multivalued relation arguments"
				self assert: self topEntity arguments size equals: 1.
				
					stack push: (stack top arguments at: 1).
					self assert: self topEntity sourceCode equals: '3'.
					self assert: (self topEntity isOfType: FASTPyInteger).
					self assert: (self topEntity isOfType: FASTTLiteral).
					self assert: (self topEntity isOfType: FASTTNumericalLiteral).
					stack pop.
				
				"Testing value of monovalue relation callee"
				self assert: self topEntity callee isNotNil.

					stack push: self topEntity callee.
					self assert: self topEntity sourceCode equals: 'range'.
					self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - collections' }
FASTPythonImporterTest >> testSetWithNone [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testSetWithNone 	"

	self parse: '{ None }'.

	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyNone) size equals: 1.
	self assert: (fast allWithType: FASTPySet) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: '{ None }'.
	self assert: (self topEntity isOfType: FASTPySet).
	
	"Testing value of multivalued relation initializers"
	self assert: self topEntity initializers size equals: 1.
	
		stack push: (stack top initializers at: 1).
		self assert: self topEntity sourceCode equals: 'None'.
		self assert: (self topEntity isOfType: FASTPyNone).
		self assert: (self topEntity isOfType: FASTTLiteral)
]

{ #category : 'tests - collections' }
FASTPythonImporterTest >> testSetWithNotOperator [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testSetWithNotOperator 	"

	self parse: '{ not old }'.

	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyNotOperator) size equals: 1.
	self assert: (fast allWithType: FASTPySet) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: '{ not old }'.
	self assert: (self topEntity isOfType: FASTPySet).
	
	"Testing value of multivalued relation initializers"
	self assert: self topEntity initializers size equals: 1.
	
		stack push: (stack top initializers at: 1).
		self assert: self topEntity sourceCode equals: 'not old'.
		self assert: (self topEntity isOfType: FASTPyNotOperator).
		self assert: (self topEntity isOfType: FASTPyTUnaryOperator).
		
		"Testing value of monovalue relation argument"
		self assert: self topEntity argument isNotNil.

			stack push: self topEntity argument.
			self assert: self topEntity sourceCode equals: 'old'.
			self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - collections' }
FASTPythonImporterTest >> testSetWithSet [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testSetWithSet 	"

	self parse: '{ { 1 } }'.

	self assert: (fast allWithType: FASTPyInteger) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPySet) size equals: 2.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: '{ { 1 } }'.
	self assert: (self topEntity isOfType: FASTPySet).
	
	"Testing value of multivalued relation initializers"
	self assert: self topEntity initializers size equals: 1.
	
		stack push: (stack top initializers at: 1).
		self assert: self topEntity sourceCode equals: '{ 1 }'.
		self assert: (self topEntity isOfType: FASTPySet).
		
		"Testing value of multivalued relation initializers"
		self assert: self topEntity initializers size equals: 1.
		
			stack push: (stack top initializers at: 1).
			self assert: self topEntity sourceCode equals: '1'.
			self assert: (self topEntity isOfType: FASTPyInteger).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTNumericalLiteral)
]

{ #category : 'tests - collections' }
FASTPythonImporterTest >> testSetWithSetComprehension [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testSetWithSetComprehension 	"

	self parse: '{ {random for i in range(3)} }'.

	self assert: (fast allWithType: FASTPyCall) size equals: 1.
	self assert: (fast allWithType: FASTPyForInClause) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 3.
	self assert: (fast allWithType: FASTPyInteger) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPySet) size equals: 1.
	self assert: (fast allWithType: FASTPySetComprehension) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: '{ {random for i in range(3)} }'.
	self assert: (self topEntity isOfType: FASTPySet).
	
	"Testing value of multivalued relation initializers"
	self assert: self topEntity initializers size equals: 1.
	
		stack push: (stack top initializers at: 1).
		self assert: self topEntity sourceCode equals: '{random for i in range(3)}'.
		self assert: (self topEntity isOfType: FASTPySetComprehension).
		self assert: (self topEntity isOfType: FASTPyTComprehension).
		self assert: (self topEntity isOfType: FASTTWithCondition).
		
		"Testing value of monovalue relation body"
		self assert: self topEntity body isNotNil.

			stack push: self topEntity body.
			self assert: self topEntity sourceCode equals: 'random'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		
		"Testing value of multivalued relation clauses"
		self assert: self topEntity clauses size equals: 1.
		
			stack push: (stack top clauses at: 1).
			self assert: self topEntity sourceCode equals: 'for i in range(3)'.
			self assert: (self topEntity isOfType: FASTPyForInClause).
			
			"Testing value of monovalue relation left"
			self assert: self topEntity left isNotNil.

				stack push: self topEntity left.
				self assert: self topEntity sourceCode equals: 'i'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			
			"Testing value of monovalue relation right"
			self assert: self topEntity right isNotNil.

				stack push: self topEntity right.
				self assert: self topEntity sourceCode equals: 'range(3)'.
				self assert: (self topEntity isOfType: FASTPyCall).
				self assert: (self topEntity isOfType: FASTTInvocation).
				self assert: (self topEntity isOfType: FASTTWithArguments).
				
				"Testing value of multivalued relation arguments"
				self assert: self topEntity arguments size equals: 1.
				
					stack push: (stack top arguments at: 1).
					self assert: self topEntity sourceCode equals: '3'.
					self assert: (self topEntity isOfType: FASTPyInteger).
					self assert: (self topEntity isOfType: FASTTLiteral).
					self assert: (self topEntity isOfType: FASTTNumericalLiteral).
					stack pop.
				
				"Testing value of monovalue relation callee"
				self assert: self topEntity callee isNotNil.

					stack push: self topEntity callee.
					self assert: self topEntity sourceCode equals: 'range'.
					self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - collections' }
FASTPythonImporterTest >> testSetWithSplat [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testSetWithSplat 	"

	self parse: '{*args, 1}'.

	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyInteger) size equals: 1.
	self assert: (fast allWithType: FASTPyListSplat) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPySet) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: '{*args, 1}'.
	self assert: (self topEntity isOfType: FASTPySet).
	
	"Testing value of multivalued relation initializers"
	self assert: self topEntity initializers size equals: 2.
	
		stack push: (stack top initializers at: 1).
		self assert: self topEntity sourceCode equals: '*args'.
		self assert: (self topEntity isOfType: FASTPyListSplat).
		
		"Testing value of monovalue relation expression"
		self assert: self topEntity expression isNotNil.

			stack push: self topEntity expression.
			self assert: self topEntity sourceCode equals: 'args'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		stack pop.
	
		stack push: (stack top initializers at: 2).
		self assert: self topEntity sourceCode equals: '1'.
		self assert: (self topEntity isOfType: FASTPyInteger).
		self assert: (self topEntity isOfType: FASTTLiteral).
		self assert: (self topEntity isOfType: FASTTNumericalLiteral)
]

{ #category : 'tests - collections' }
FASTPythonImporterTest >> testSetWithString [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testSetWithString 	"

	self parse: '{ "Hello" }'.

	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPySet) size equals: 1.
	self assert: (fast allWithType: FASTPyString) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: '{ "Hello" }'.
	self assert: (self topEntity isOfType: FASTPySet).
	
	"Testing value of multivalued relation initializers"
	self assert: self topEntity initializers size equals: 1.
	
		stack push: (stack top initializers at: 1).
		self assert: self topEntity sourceCode equals: '"Hello"'.
		self assert: (self topEntity isOfType: FASTPyString).
		self assert: (self topEntity isOfType: FASTTLiteral).
		self assert: (self topEntity isOfType: FASTTStringLiteral)
]

{ #category : 'tests - collections' }
FASTPythonImporterTest >> testSetWithSubscript [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testSetWithSubscript 	"

	self parse: '{ attrs[2] }'.

	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyInteger) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPySet) size equals: 1.
	self assert: (fast allWithType: FASTPySubscript) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: '{ attrs[2] }'.
	self assert: (self topEntity isOfType: FASTPySet).
	
	"Testing value of multivalued relation initializers"
	self assert: self topEntity initializers size equals: 1.
	
		stack push: (stack top initializers at: 1).
		self assert: self topEntity sourceCode equals: 'attrs[2]'.
		self assert: (self topEntity isOfType: FASTPySubscript).
		
		"Testing value of multivalued relation indices"
		self assert: self topEntity indices size equals: 1.
		
			stack push: (stack top indices at: 1).
			self assert: self topEntity sourceCode equals: '2'.
			self assert: (self topEntity isOfType: FASTPyInteger).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTNumericalLiteral).
			stack pop.
		
		"Testing value of monovalue relation value"
		self assert: self topEntity value isNotNil.

			stack push: self topEntity value.
			self assert: self topEntity sourceCode equals: 'attrs'.
			self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - collections' }
FASTPythonImporterTest >> testSetWithTrue [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testSetWithTrue 	"

	self parse: '{ True }'.

	self assert: (fast allWithType: FASTPyBoolean) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPySet) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: '{ True }'.
	self assert: (self topEntity isOfType: FASTPySet).
	
	"Testing value of multivalued relation initializers"
	self assert: self topEntity initializers size equals: 1.
	
		stack push: (stack top initializers at: 1).
		self assert: self topEntity sourceCode equals: 'True'.
		self assert: (self topEntity isOfType: FASTPyBoolean).
		self assert: (self topEntity isOfType: FASTTBooleanLiteral).
		self assert: (self topEntity isOfType: FASTTLiteral)
]

{ #category : 'tests - collections' }
FASTPythonImporterTest >> testSetWithTuple [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testSetWithTuple 	"

	self parse: '{ (1,) }'.

	self assert: (fast allWithType: FASTPyInteger) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPySet) size equals: 1.
	self assert: (fast allWithType: FASTPyTuple) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: '{ (1,) }'.
	self assert: (self topEntity isOfType: FASTPySet).
	
	"Testing value of multivalued relation initializers"
	self assert: self topEntity initializers size equals: 1.
	
		stack push: (stack top initializers at: 1).
		self assert: self topEntity sourceCode equals: '(1,)'.
		self assert: (self topEntity isOfType: FASTPyTuple).
		
		"Testing value of multivalued relation initializers"
		self assert: self topEntity initializers size equals: 1.
		
			stack push: (stack top initializers at: 1).
			self assert: self topEntity sourceCode equals: '1'.
			self assert: (self topEntity isOfType: FASTPyInteger).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTNumericalLiteral)
]

{ #category : 'tests - collections' }
FASTPythonImporterTest >> testSetWithUnaryOperator [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testSetWithUnaryOperator 	"

	self parse: '{ -1 }'.

	self assert: (fast allWithType: FASTPyInteger) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPySet) size equals: 1.
	self assert: (fast allWithType: FASTPyUnaryOperator) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: '{ -1 }'.
	self assert: (self topEntity isOfType: FASTPySet).
	
	"Testing value of multivalued relation initializers"
	self assert: self topEntity initializers size equals: 1.
	
		stack push: (stack top initializers at: 1).
		self assert: self topEntity sourceCode equals: '-1'.
		self assert: (self topEntity isOfType: FASTPyUnaryOperator).
		self assert: (self topEntity isOfType: FASTPyTUnaryOperator).
		
		"Testing value of monovalue relation argument"
		self assert: self topEntity argument isNotNil.

			stack push: self topEntity argument.
			self assert: self topEntity sourceCode equals: '1'.
			self assert: (self topEntity isOfType: FASTPyInteger).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTNumericalLiteral).
			stack pop.
		self assert: self topEntity operator equals: '-'
]

{ #category : 'tests - literals' }
FASTPythonImporterTest >> testSingleQuotedString [

	| string |
	self parse: '''This is a string'''.

	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyString) size equals: 1.

	string := (fast allWithType: FASTPyString) anyOne.
	self assert: string sourceCode equals: '''This is a string'''.
	self assert: (string isOfType: FASTTStringLiteral).
	self assertEmpty: string allContainedEntities
]

{ #category : 'tests - subscripts' }
FASTPythonImporterTest >> testSliceAttribute [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testSliceAttribute 	"

	self parse: 'values[:obj.attr]'.

	self assert: (fast allWithType: FASTPyAttributeAccess) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 3.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPySlice) size equals: 1.
	self assert: (fast allWithType: FASTPySubscript) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'values[:obj.attr]'.
	self assert: (self topEntity isOfType: FASTPySubscript).
	
	"Testing value of multivalued relation indices"
	self assert: self topEntity indices size equals: 1.
	
		stack push: (stack top indices at: 1).
		self assert: self topEntity sourceCode equals: ':obj.attr'.
		self assert: (self topEntity isOfType: FASTPySlice).
		
		"Testing value of multivalued relation components"
		self assert: self topEntity components size equals: 1.
		
			stack push: (stack top components at: 1).
			self assert: self topEntity sourceCode equals: 'obj.attr'.
			self assert: (self topEntity isOfType: FASTPyAttributeAccess).
			
			"Testing value of monovalue relation attribute"
			self assert: self topEntity attribute isNotNil.

				stack push: self topEntity attribute.
				self assert: self topEntity sourceCode equals: 'attr'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			
			"Testing value of monovalue relation object"
			self assert: self topEntity object isNotNil.

				stack push: self topEntity object.
				self assert: self topEntity sourceCode equals: 'obj'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation value"
	self assert: self topEntity value isNotNil.

		stack push: self topEntity value.
		self assert: self topEntity sourceCode equals: 'values'.
		self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - subscripts' }
FASTPythonImporterTest >> testSliceBinaryOperator [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testSliceBinaryOperator 	"

	self parse: 'lst[a+b:a+c:1]'.

	self assert: (fast allWithType: FASTPyBinaryOperator) size equals: 2.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 5.
	self assert: (fast allWithType: FASTPyInteger) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPySlice) size equals: 1.
	self assert: (fast allWithType: FASTPySubscript) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'lst[a+b:a+c:1]'.
	self assert: (self topEntity isOfType: FASTPySubscript).
	
	"Testing value of multivalued relation indices"
	self assert: self topEntity indices size equals: 1.
	
		stack push: (stack top indices at: 1).
		self assert: self topEntity sourceCode equals: 'a+b:a+c:1'.
		self assert: (self topEntity isOfType: FASTPySlice).
		
		"Testing value of multivalued relation components"
		self assert: self topEntity components size equals: 3.
		
			stack push: (stack top components at: 1).
			self assert: self topEntity sourceCode equals: 'a+b'.
			self assert: (self topEntity isOfType: FASTPyBinaryOperator).
			self assert: (self topEntity isOfType: FASTTBinaryExpression).
			
			"Testing value of monovalue relation leftOperand"
			self assert: self topEntity leftOperand isNotNil.

				stack push: self topEntity leftOperand.
				self assert: self topEntity sourceCode equals: 'a'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			self assert: self topEntity operator equals: '+'.
			
			"Testing value of monovalue relation rightOperand"
			self assert: self topEntity rightOperand isNotNil.

				stack push: self topEntity rightOperand.
				self assert: self topEntity sourceCode equals: 'b'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			stack pop.
		
			stack push: (stack top components at: 2).
			self assert: self topEntity sourceCode equals: 'a+c'.
			self assert: (self topEntity isOfType: FASTPyBinaryOperator).
			self assert: (self topEntity isOfType: FASTTBinaryExpression).
			
			"Testing value of monovalue relation leftOperand"
			self assert: self topEntity leftOperand isNotNil.

				stack push: self topEntity leftOperand.
				self assert: self topEntity sourceCode equals: 'a'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			self assert: self topEntity operator equals: '+'.
			
			"Testing value of monovalue relation rightOperand"
			self assert: self topEntity rightOperand isNotNil.

				stack push: self topEntity rightOperand.
				self assert: self topEntity sourceCode equals: 'c'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			stack pop.
		
			stack push: (stack top components at: 3).
			self assert: self topEntity sourceCode equals: '1'.
			self assert: (self topEntity isOfType: FASTPyInteger).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTNumericalLiteral).
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation value"
	self assert: self topEntity value isNotNil.

		stack push: self topEntity value.
		self assert: self topEntity sourceCode equals: 'lst'.
		self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - subscripts' }
FASTPythonImporterTest >> testSliceBoolean [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testSliceBoolean 	"

	self parse: 'lst[True:False]'.

	self assert: (fast allWithType: FASTPyBoolean) size equals: 2.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPySlice) size equals: 1.
	self assert: (fast allWithType: FASTPySubscript) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'lst[True:False]'.
	self assert: (self topEntity isOfType: FASTPySubscript).
	
	"Testing value of multivalued relation indices"
	self assert: self topEntity indices size equals: 1.
	
		stack push: (stack top indices at: 1).
		self assert: self topEntity sourceCode equals: 'True:False'.
		self assert: (self topEntity isOfType: FASTPySlice).
		
		"Testing value of multivalued relation components"
		self assert: self topEntity components size equals: 2.
		
			stack push: (stack top components at: 1).
			self assert: self topEntity sourceCode equals: 'True'.
			self assert: (self topEntity isOfType: FASTPyBoolean).
			self assert: (self topEntity isOfType: FASTTBooleanLiteral).
			self assert: (self topEntity isOfType: FASTTLiteral).
			stack pop.
		
			stack push: (stack top components at: 2).
			self assert: self topEntity sourceCode equals: 'False'.
			self assert: (self topEntity isOfType: FASTPyBoolean).
			self assert: (self topEntity isOfType: FASTTBooleanLiteral).
			self assert: (self topEntity isOfType: FASTTLiteral).
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation value"
	self assert: self topEntity value isNotNil.

		stack push: self topEntity value.
		self assert: self topEntity sourceCode equals: 'lst'.
		self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - subscripts' }
FASTPythonImporterTest >> testSliceCall [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testSliceCall 	"

	self parse: 'values[1:indexOf(c)]'.

	self assert: (fast allWithType: FASTPyCall) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 3.
	self assert: (fast allWithType: FASTPyInteger) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPySlice) size equals: 1.
	self assert: (fast allWithType: FASTPySubscript) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'values[1:indexOf(c)]'.
	self assert: (self topEntity isOfType: FASTPySubscript).
	
	"Testing value of multivalued relation indices"
	self assert: self topEntity indices size equals: 1.
	
		stack push: (stack top indices at: 1).
		self assert: self topEntity sourceCode equals: '1:indexOf(c)'.
		self assert: (self topEntity isOfType: FASTPySlice).
		
		"Testing value of multivalued relation components"
		self assert: self topEntity components size equals: 2.
		
			stack push: (stack top components at: 1).
			self assert: self topEntity sourceCode equals: '1'.
			self assert: (self topEntity isOfType: FASTPyInteger).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTNumericalLiteral).
			stack pop.
		
			stack push: (stack top components at: 2).
			self assert: self topEntity sourceCode equals: 'indexOf(c)'.
			self assert: (self topEntity isOfType: FASTPyCall).
			self assert: (self topEntity isOfType: FASTTInvocation).
			self assert: (self topEntity isOfType: FASTTWithArguments).
			
			"Testing value of multivalued relation arguments"
			self assert: self topEntity arguments size equals: 1.
			
				stack push: (stack top arguments at: 1).
				self assert: self topEntity sourceCode equals: 'c'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			
			"Testing value of monovalue relation callee"
			self assert: self topEntity callee isNotNil.

				stack push: self topEntity callee.
				self assert: self topEntity sourceCode equals: 'indexOf'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation value"
	self assert: self topEntity value isNotNil.

		stack push: self topEntity value.
		self assert: self topEntity sourceCode equals: 'values'.
		self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - subscripts' }
FASTPythonImporterTest >> testSliceConditionalExpression [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testSliceConditionalExpression 	"

	self parse: 'lst[a if cond else b:c]'.

	self assert: (fast allWithType: FASTPyConditionalExpression) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 5.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPySlice) size equals: 1.
	self assert: (fast allWithType: FASTPySubscript) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'lst[a if cond else b:c]'.
	self assert: (self topEntity isOfType: FASTPySubscript).
	
	"Testing value of multivalued relation indices"
	self assert: self topEntity indices size equals: 1.
	
		stack push: (stack top indices at: 1).
		self assert: self topEntity sourceCode equals: 'a if cond else b:c'.
		self assert: (self topEntity isOfType: FASTPySlice).
		
		"Testing value of multivalued relation components"
		self assert: self topEntity components size equals: 2.
		
			stack push: (stack top components at: 1).
			self assert: self topEntity sourceCode equals: 'a if cond else b'.
			self assert: (self topEntity isOfType: FASTPyConditionalExpression).
			self assert: (self topEntity isOfType: FASTTWithCondition).
			
			"Testing value of monovalue relation condition"
			self assert: self topEntity condition isNotNil.

				stack push: self topEntity condition.
				self assert: self topEntity sourceCode equals: 'cond'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			
			"Testing value of monovalue relation elseExpression"
			self assert: self topEntity elseExpression isNotNil.

				stack push: self topEntity elseExpression.
				self assert: self topEntity sourceCode equals: 'b'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			
			"Testing value of monovalue relation thenExpression"
			self assert: self topEntity thenExpression isNotNil.

				stack push: self topEntity thenExpression.
				self assert: self topEntity sourceCode equals: 'a'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			stack pop.
		
			stack push: (stack top components at: 2).
			self assert: self topEntity sourceCode equals: 'c'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation value"
	self assert: self topEntity value isNotNil.

		stack push: self topEntity value.
		self assert: self topEntity sourceCode equals: 'lst'.
		self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - subscripts' }
FASTPythonImporterTest >> testSliceEmpty [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testSliceEmpty 	"

	self parse: 'values[:]'.

	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPySlice) size equals: 1.
	self assert: (fast allWithType: FASTPySubscript) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'values[:]'.
	self assert: (self topEntity isOfType: FASTPySubscript).
	
	"Testing value of multivalued relation indices"
	self assert: self topEntity indices size equals: 1.
	
		stack push: (stack top indices at: 1).
		self assert: self topEntity sourceCode equals: ':'.
		self assert: (self topEntity isOfType: FASTPySlice).
		stack pop.
	
	"Testing value of monovalue relation value"
	self assert: self topEntity value isNotNil.

		stack push: self topEntity value.
		self assert: self topEntity sourceCode equals: 'values'.
		self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - subscripts' }
FASTPythonImporterTest >> testSliceNone [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testSliceNone 	"

	self parse: 'values[1:None]'.

	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyInteger) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyNone) size equals: 1.
	self assert: (fast allWithType: FASTPySlice) size equals: 1.
	self assert: (fast allWithType: FASTPySubscript) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'values[1:None]'.
	self assert: (self topEntity isOfType: FASTPySubscript).
	
	"Testing value of multivalued relation indices"
	self assert: self topEntity indices size equals: 1.
	
		stack push: (stack top indices at: 1).
		self assert: self topEntity sourceCode equals: '1:None'.
		self assert: (self topEntity isOfType: FASTPySlice).
		
		"Testing value of multivalued relation components"
		self assert: self topEntity components size equals: 2.
		
			stack push: (stack top components at: 1).
			self assert: self topEntity sourceCode equals: '1'.
			self assert: (self topEntity isOfType: FASTPyInteger).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTNumericalLiteral).
			stack pop.
		
			stack push: (stack top components at: 2).
			self assert: self topEntity sourceCode equals: 'None'.
			self assert: (self topEntity isOfType: FASTPyNone).
			self assert: (self topEntity isOfType: FASTTLiteral).
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation value"
	self assert: self topEntity value isNotNil.

		stack push: self topEntity value.
		self assert: self topEntity sourceCode equals: 'values'.
		self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - subscripts' }
FASTPythonImporterTest >> testSliceParenthesis [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testSliceParenthesis 	"

	self parse: 'values[i:(i + h)]'.

	self assert: (fast allWithType: FASTPyBinaryOperator) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 4.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPySlice) size equals: 1.
	self assert: (fast allWithType: FASTPySubscript) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'values[i:(i + h)]'.
	self assert: (self topEntity isOfType: FASTPySubscript).
	
	"Testing value of multivalued relation indices"
	self assert: self topEntity indices size equals: 1.
	
		stack push: (stack top indices at: 1).
		self assert: self topEntity sourceCode equals: 'i:(i + h)'.
		self assert: (self topEntity isOfType: FASTPySlice).
		
		"Testing value of multivalued relation components"
		self assert: self topEntity components size equals: 2.
		
			stack push: (stack top components at: 1).
			self assert: self topEntity sourceCode equals: 'i'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		
			stack push: (stack top components at: 2).
			self assert: self topEntity sourceCode equals: 'i + h'.
			self assert: (self topEntity isOfType: FASTPyBinaryOperator).
			self assert: (self topEntity isOfType: FASTTBinaryExpression).
			
			"Testing value of monovalue relation leftOperand"
			self assert: self topEntity leftOperand isNotNil.

				stack push: self topEntity leftOperand.
				self assert: self topEntity sourceCode equals: 'i'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			self assert: self topEntity operator equals: '+'.
			
			"Testing value of monovalue relation rightOperand"
			self assert: self topEntity rightOperand isNotNil.

				stack push: self topEntity rightOperand.
				self assert: self topEntity sourceCode equals: 'h'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation value"
	self assert: self topEntity value isNotNil.

		stack push: self topEntity value.
		self assert: self topEntity sourceCode equals: 'values'.
		self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - subscripts' }
FASTPythonImporterTest >> testSliceReverse [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testSliceReverse 	"

	self parse: 'values[::-1]'.

	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyInteger) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPySlice) size equals: 1.
	self assert: (fast allWithType: FASTPySubscript) size equals: 1.
	self assert: (fast allWithType: FASTPyUnaryOperator) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'values[::-1]'.
	self assert: (self topEntity isOfType: FASTPySubscript).
	
	"Testing value of multivalued relation indices"
	self assert: self topEntity indices size equals: 1.
	
		stack push: (stack top indices at: 1).
		self assert: self topEntity sourceCode equals: '::-1'.
		self assert: (self topEntity isOfType: FASTPySlice).
		
		"Testing value of multivalued relation components"
		self assert: self topEntity components size equals: 1.
		
			stack push: (stack top components at: 1).
			self assert: self topEntity sourceCode equals: '-1'.
			self assert: (self topEntity isOfType: FASTPyUnaryOperator).
			self assert: (self topEntity isOfType: FASTPyTUnaryOperator).
			
			"Testing value of monovalue relation argument"
			self assert: self topEntity argument isNotNil.

				stack push: self topEntity argument.
				self assert: self topEntity sourceCode equals: '1'.
				self assert: (self topEntity isOfType: FASTPyInteger).
				self assert: (self topEntity isOfType: FASTTLiteral).
				self assert: (self topEntity isOfType: FASTTNumericalLiteral).
				stack pop.
			self assert: self topEntity operator equals: '-'.
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation value"
	self assert: self topEntity value isNotNil.

		stack push: self topEntity value.
		self assert: self topEntity sourceCode equals: 'values'.
		self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - subscripts' }
FASTPythonImporterTest >> testSliceSubscript [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testSliceSubscript 	"

	self parse: 'values[pady[0]:pady[1]]'.

	self assert: (fast allWithType: FASTPyIdentifier) size equals: 3.
	self assert: (fast allWithType: FASTPyInteger) size equals: 2.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPySlice) size equals: 1.
	self assert: (fast allWithType: FASTPySubscript) size equals: 3.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'values[pady[0]:pady[1]]'.
	self assert: (self topEntity isOfType: FASTPySubscript).
	
	"Testing value of multivalued relation indices"
	self assert: self topEntity indices size equals: 1.
	
		stack push: (stack top indices at: 1).
		self assert: self topEntity sourceCode equals: 'pady[0]:pady[1]'.
		self assert: (self topEntity isOfType: FASTPySlice).
		
		"Testing value of multivalued relation components"
		self assert: self topEntity components size equals: 2.
		
			stack push: (stack top components at: 1).
			self assert: self topEntity sourceCode equals: 'pady[0]'.
			self assert: (self topEntity isOfType: FASTPySubscript).
			
			"Testing value of multivalued relation indices"
			self assert: self topEntity indices size equals: 1.
			
				stack push: (stack top indices at: 1).
				self assert: self topEntity sourceCode equals: '0'.
				self assert: (self topEntity isOfType: FASTPyInteger).
				self assert: (self topEntity isOfType: FASTTLiteral).
				self assert: (self topEntity isOfType: FASTTNumericalLiteral).
				stack pop.
			
			"Testing value of monovalue relation value"
			self assert: self topEntity value isNotNil.

				stack push: self topEntity value.
				self assert: self topEntity sourceCode equals: 'pady'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			stack pop.
		
			stack push: (stack top components at: 2).
			self assert: self topEntity sourceCode equals: 'pady[1]'.
			self assert: (self topEntity isOfType: FASTPySubscript).
			
			"Testing value of multivalued relation indices"
			self assert: self topEntity indices size equals: 1.
			
				stack push: (stack top indices at: 1).
				self assert: self topEntity sourceCode equals: '1'.
				self assert: (self topEntity isOfType: FASTPyInteger).
				self assert: (self topEntity isOfType: FASTTLiteral).
				self assert: (self topEntity isOfType: FASTTNumericalLiteral).
				stack pop.
			
			"Testing value of monovalue relation value"
			self assert: self topEntity value isNotNil.

				stack push: self topEntity value.
				self assert: self topEntity sourceCode equals: 'pady'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation value"
	self assert: self topEntity value isNotNil.

		stack push: self topEntity value.
		self assert: self topEntity sourceCode equals: 'values'.
		self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - subscripts' }
FASTPythonImporterTest >> testSliceUnaryOperator [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testSliceUnaryOperator 	"

	self parse: 'values[-1:-5:-1]'.

	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyInteger) size equals: 3.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPySlice) size equals: 1.
	self assert: (fast allWithType: FASTPySubscript) size equals: 1.
	self assert: (fast allWithType: FASTPyUnaryOperator) size equals: 3.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'values[-1:-5:-1]'.
	self assert: (self topEntity isOfType: FASTPySubscript).
	
	"Testing value of multivalued relation indices"
	self assert: self topEntity indices size equals: 1.
	
		stack push: (stack top indices at: 1).
		self assert: self topEntity sourceCode equals: '-1:-5:-1'.
		self assert: (self topEntity isOfType: FASTPySlice).
		
		"Testing value of multivalued relation components"
		self assert: self topEntity components size equals: 3.
		
			stack push: (stack top components at: 1).
			self assert: self topEntity sourceCode equals: '-1'.
			self assert: (self topEntity isOfType: FASTPyUnaryOperator).
			self assert: (self topEntity isOfType: FASTPyTUnaryOperator).
			
			"Testing value of monovalue relation argument"
			self assert: self topEntity argument isNotNil.

				stack push: self topEntity argument.
				self assert: self topEntity sourceCode equals: '1'.
				self assert: (self topEntity isOfType: FASTPyInteger).
				self assert: (self topEntity isOfType: FASTTLiteral).
				self assert: (self topEntity isOfType: FASTTNumericalLiteral).
				stack pop.
			self assert: self topEntity operator equals: '-'.
			stack pop.
		
			stack push: (stack top components at: 2).
			self assert: self topEntity sourceCode equals: '-5'.
			self assert: (self topEntity isOfType: FASTPyUnaryOperator).
			self assert: (self topEntity isOfType: FASTPyTUnaryOperator).
			
			"Testing value of monovalue relation argument"
			self assert: self topEntity argument isNotNil.

				stack push: self topEntity argument.
				self assert: self topEntity sourceCode equals: '5'.
				self assert: (self topEntity isOfType: FASTPyInteger).
				self assert: (self topEntity isOfType: FASTTLiteral).
				self assert: (self topEntity isOfType: FASTTNumericalLiteral).
				stack pop.
			self assert: self topEntity operator equals: '-'.
			stack pop.
		
			stack push: (stack top components at: 3).
			self assert: self topEntity sourceCode equals: '-1'.
			self assert: (self topEntity isOfType: FASTPyUnaryOperator).
			self assert: (self topEntity isOfType: FASTPyTUnaryOperator).
			
			"Testing value of monovalue relation argument"
			self assert: self topEntity argument isNotNil.

				stack push: self topEntity argument.
				self assert: self topEntity sourceCode equals: '1'.
				self assert: (self topEntity isOfType: FASTPyInteger).
				self assert: (self topEntity isOfType: FASTTLiteral).
				self assert: (self topEntity isOfType: FASTTNumericalLiteral).
				stack pop.
			self assert: self topEntity operator equals: '-'.
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation value"
	self assert: self topEntity value isNotNil.

		stack push: self topEntity value.
		self assert: self topEntity sourceCode equals: 'values'.
		self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - subscripts' }
FASTPythonImporterTest >> testSliceVariables [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testSliceVariables 	"

	self parse: 'values[x:y]'.

	self assert: (fast allWithType: FASTPyIdentifier) size equals: 3.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPySlice) size equals: 1.
	self assert: (fast allWithType: FASTPySubscript) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'values[x:y]'.
	self assert: (self topEntity isOfType: FASTPySubscript).
	
	"Testing value of multivalued relation indices"
	self assert: self topEntity indices size equals: 1.
	
		stack push: (stack top indices at: 1).
		self assert: self topEntity sourceCode equals: 'x:y'.
		self assert: (self topEntity isOfType: FASTPySlice).
		
		"Testing value of multivalued relation components"
		self assert: self topEntity components size equals: 2.
		
			stack push: (stack top components at: 1).
			self assert: self topEntity sourceCode equals: 'x'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		
			stack push: (stack top components at: 2).
			self assert: self topEntity sourceCode equals: 'y'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation value"
	self assert: self topEntity value isNotNil.

		stack push: self topEntity value.
		self assert: self topEntity sourceCode equals: 'values'.
		self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - subscripts' }
FASTPythonImporterTest >> testSliceWithEnd [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testSliceWithEnd 	"

	self parse: 'values[:7]'.

	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyInteger) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPySlice) size equals: 1.
	self assert: (fast allWithType: FASTPySubscript) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'values[:7]'.
	self assert: (self topEntity isOfType: FASTPySubscript).
	
	"Testing value of multivalued relation indices"
	self assert: self topEntity indices size equals: 1.
	
		stack push: (stack top indices at: 1).
		self assert: self topEntity sourceCode equals: ':7'.
		self assert: (self topEntity isOfType: FASTPySlice).
		
		"Testing value of multivalued relation components"
		self assert: self topEntity components size equals: 1.
		
			stack push: (stack top components at: 1).
			self assert: self topEntity sourceCode equals: '7'.
			self assert: (self topEntity isOfType: FASTPyInteger).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTNumericalLiteral).
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation value"
	self assert: self topEntity value isNotNil.

		stack push: self topEntity value.
		self assert: self topEntity sourceCode equals: 'values'.
		self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - subscripts' }
FASTPythonImporterTest >> testSliceWithStart [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testSliceWithStart 	"

	self parse: 'values[2:]'.

	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyInteger) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPySlice) size equals: 1.
	self assert: (fast allWithType: FASTPySubscript) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'values[2:]'.
	self assert: (self topEntity isOfType: FASTPySubscript).
	
	"Testing value of multivalued relation indices"
	self assert: self topEntity indices size equals: 1.
	
		stack push: (stack top indices at: 1).
		self assert: self topEntity sourceCode equals: '2:'.
		self assert: (self topEntity isOfType: FASTPySlice).
		
		"Testing value of multivalued relation components"
		self assert: self topEntity components size equals: 1.
		
			stack push: (stack top components at: 1).
			self assert: self topEntity sourceCode equals: '2'.
			self assert: (self topEntity isOfType: FASTPyInteger).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTNumericalLiteral).
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation value"
	self assert: self topEntity value isNotNil.

		stack push: self topEntity value.
		self assert: self topEntity sourceCode equals: 'values'.
		self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - subscripts' }
FASTPythonImporterTest >> testSliceWithStartStopStep [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testSliceWithStartStopStep 	"

	self parse: 'values[2:10:2]'.

	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyInteger) size equals: 3.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPySlice) size equals: 1.
	self assert: (fast allWithType: FASTPySubscript) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'values[2:10:2]'.
	self assert: (self topEntity isOfType: FASTPySubscript).
	
	"Testing value of multivalued relation indices"
	self assert: self topEntity indices size equals: 1.
	
		stack push: (stack top indices at: 1).
		self assert: self topEntity sourceCode equals: '2:10:2'.
		self assert: (self topEntity isOfType: FASTPySlice).
		
		"Testing value of multivalued relation components"
		self assert: self topEntity components size equals: 3.
		
			stack push: (stack top components at: 1).
			self assert: self topEntity sourceCode equals: '2'.
			self assert: (self topEntity isOfType: FASTPyInteger).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTNumericalLiteral).
			stack pop.
		
			stack push: (stack top components at: 2).
			self assert: self topEntity sourceCode equals: '10'.
			self assert: (self topEntity isOfType: FASTPyInteger).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTNumericalLiteral).
			stack pop.
		
			stack push: (stack top components at: 3).
			self assert: self topEntity sourceCode equals: '2'.
			self assert: (self topEntity isOfType: FASTPyInteger).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTNumericalLiteral).
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation value"
	self assert: self topEntity value isNotNil.

		stack push: self topEntity value.
		self assert: self topEntity sourceCode equals: 'values'.
		self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - subscripts' }
FASTPythonImporterTest >> testSliceWithStep [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testSliceWithStep 	"

	self parse: 'values[::4]'.

	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyInteger) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPySlice) size equals: 1.
	self assert: (fast allWithType: FASTPySubscript) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'values[::4]'.
	self assert: (self topEntity isOfType: FASTPySubscript).
	
	"Testing value of multivalued relation indices"
	self assert: self topEntity indices size equals: 1.
	
		stack push: (stack top indices at: 1).
		self assert: self topEntity sourceCode equals: '::4'.
		self assert: (self topEntity isOfType: FASTPySlice).
		
		"Testing value of multivalued relation components"
		self assert: self topEntity components size equals: 1.
		
			stack push: (stack top components at: 1).
			self assert: self topEntity sourceCode equals: '4'.
			self assert: (self topEntity isOfType: FASTPyInteger).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTNumericalLiteral).
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation value"
	self assert: self topEntity value isNotNil.

		stack push: self topEntity value.
		self assert: self topEntity sourceCode equals: 'values'.
		self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - literals' }
FASTPythonImporterTest >> testStringWithEscapedInterpolation [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testStringWithEscapedInterpolation 	"

	self parse: 'f"The result is {{value}}"'.

	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyString) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'f"The result is {{value}}"'.
	self assert: (self topEntity isOfType: FASTPyString).
	self assert: (self topEntity isOfType: FASTTLiteral).
	self assert: (self topEntity isOfType: FASTTStringLiteral)
]

{ #category : 'tests - literals' }
FASTPythonImporterTest >> testStringWithEscapedSequence [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testStringWithEscapedSequence 	"

	self parse: '"Line 1\nLine 2"'.

	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyString) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: '"Line 1\nLine 2"'.
	self assert: (self topEntity isOfType: FASTPyString).
	self assert: (self topEntity isOfType: FASTTLiteral).
	self assert: (self topEntity isOfType: FASTTStringLiteral)
]

{ #category : 'tests - subscripts' }
FASTPythonImporterTest >> testSubscript [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testSubscript 	"

	self parse: 'row[''id'']'.

	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyString) size equals: 1.
	self assert: (fast allWithType: FASTPySubscript) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'row[''id'']'.
	self assert: (self topEntity isOfType: FASTPySubscript).
	
	"Testing value of multivalued relation indices"
	self assert: self topEntity indices size equals: 1.
	
		stack push: (stack top indices at: 1).
		self assert: self topEntity sourceCode equals: '''id'''.
		self assert: (self topEntity isOfType: FASTPyString).
		self assert: (self topEntity isOfType: FASTTLiteral).
		self assert: (self topEntity isOfType: FASTTStringLiteral).
		stack pop.
	
	"Testing value of monovalue relation value"
	self assert: self topEntity value isNotNil.

		stack push: self topEntity value.
		self assert: self topEntity sourceCode equals: 'row'.
		self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - subscripts' }
FASTPythonImporterTest >> testSubscriptOnAttribute [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testSubscriptOnAttribute 	"

	self parse: 'os.environ[''CUDA_LAUNCH_BLOCKING'']'.

	self assert: (fast allWithType: FASTPyAttributeAccess) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 2.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyString) size equals: 1.
	self assert: (fast allWithType: FASTPySubscript) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'os.environ[''CUDA_LAUNCH_BLOCKING'']'.
	self assert: (self topEntity isOfType: FASTPySubscript).
	
	"Testing value of multivalued relation indices"
	self assert: self topEntity indices size equals: 1.
	
		stack push: (stack top indices at: 1).
		self assert: self topEntity sourceCode equals: '''CUDA_LAUNCH_BLOCKING'''.
		self assert: (self topEntity isOfType: FASTPyString).
		self assert: (self topEntity isOfType: FASTTLiteral).
		self assert: (self topEntity isOfType: FASTTStringLiteral).
		stack pop.
	
	"Testing value of monovalue relation value"
	self assert: self topEntity value isNotNil.

		stack push: self topEntity value.
		self assert: self topEntity sourceCode equals: 'os.environ'.
		self assert: (self topEntity isOfType: FASTPyAttributeAccess).
		
		"Testing value of monovalue relation attribute"
		self assert: self topEntity attribute isNotNil.

			stack push: self topEntity attribute.
			self assert: self topEntity sourceCode equals: 'environ'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		
		"Testing value of monovalue relation object"
		self assert: self topEntity object isNotNil.

			stack push: self topEntity object.
			self assert: self topEntity sourceCode equals: 'os'.
			self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - subscripts' }
FASTPythonImporterTest >> testSubscriptOnAttributeWithParenthesis [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testSubscriptOnAttributeWithParenthesis 	"

	self parse: '(os.environ)[''ATTR'']'.

	self assert: (fast allWithType: FASTPyAttributeAccess) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 2.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyString) size equals: 1.
	self assert: (fast allWithType: FASTPySubscript) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: '(os.environ)[''ATTR'']'.
	self assert: (self topEntity isOfType: FASTPySubscript).
	
	"Testing value of multivalued relation indices"
	self assert: self topEntity indices size equals: 1.
	
		stack push: (stack top indices at: 1).
		self assert: self topEntity sourceCode equals: '''ATTR'''.
		self assert: (self topEntity isOfType: FASTPyString).
		self assert: (self topEntity isOfType: FASTTLiteral).
		self assert: (self topEntity isOfType: FASTTStringLiteral).
		stack pop.
	
	"Testing value of monovalue relation value"
	self assert: self topEntity value isNotNil.

		stack push: self topEntity value.
		self assert: self topEntity sourceCode equals: 'os.environ'.
		self assert: (self topEntity isOfType: FASTPyAttributeAccess).
		
		"Testing value of monovalue relation attribute"
		self assert: self topEntity attribute isNotNil.

			stack push: self topEntity attribute.
			self assert: self topEntity sourceCode equals: 'environ'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		
		"Testing value of monovalue relation object"
		self assert: self topEntity object isNotNil.

			stack push: self topEntity object.
			self assert: self topEntity sourceCode equals: 'os'.
			self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - subscripts' }
FASTPythonImporterTest >> testSubscriptOnSubscript [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testSubscriptOnSubscript 	"

	self parse: 'coll[1][2]'.

	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyInteger) size equals: 2.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPySubscript) size equals: 2.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'coll[1][2]'.
	self assert: (self topEntity isOfType: FASTPySubscript).
	
	"Testing value of multivalued relation indices"
	self assert: self topEntity indices size equals: 1.
	
		stack push: (stack top indices at: 1).
		self assert: self topEntity sourceCode equals: '2'.
		self assert: (self topEntity isOfType: FASTPyInteger).
		self assert: (self topEntity isOfType: FASTTLiteral).
		self assert: (self topEntity isOfType: FASTTNumericalLiteral).
		stack pop.
	
	"Testing value of monovalue relation value"
	self assert: self topEntity value isNotNil.

		stack push: self topEntity value.
		self assert: self topEntity sourceCode equals: 'coll[1]'.
		self assert: (self topEntity isOfType: FASTPySubscript).
		
		"Testing value of multivalued relation indices"
		self assert: self topEntity indices size equals: 1.
		
			stack push: (stack top indices at: 1).
			self assert: self topEntity sourceCode equals: '1'.
			self assert: (self topEntity isOfType: FASTPyInteger).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTNumericalLiteral).
			stack pop.
		
		"Testing value of monovalue relation value"
		self assert: self topEntity value isNotNil.

			stack push: self topEntity value.
			self assert: self topEntity sourceCode equals: 'coll'.
			self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - subscripts' }
FASTPythonImporterTest >> testSubscriptWithAttributeInSubscript [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testSubscriptWithAttributeInSubscript 	"

	self parse: 'coll[obj.attr]'.

	self assert: (fast allWithType: FASTPyAttributeAccess) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 3.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPySubscript) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'coll[obj.attr]'.
	self assert: (self topEntity isOfType: FASTPySubscript).
	
	"Testing value of multivalued relation indices"
	self assert: self topEntity indices size equals: 1.
	
		stack push: (stack top indices at: 1).
		self assert: self topEntity sourceCode equals: 'obj.attr'.
		self assert: (self topEntity isOfType: FASTPyAttributeAccess).
		
		"Testing value of monovalue relation attribute"
		self assert: self topEntity attribute isNotNil.

			stack push: self topEntity attribute.
			self assert: self topEntity sourceCode equals: 'attr'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		
		"Testing value of monovalue relation object"
		self assert: self topEntity object isNotNil.

			stack push: self topEntity object.
			self assert: self topEntity sourceCode equals: 'obj'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation value"
	self assert: self topEntity value isNotNil.

		stack push: self topEntity value.
		self assert: self topEntity sourceCode equals: 'coll'.
		self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - subscripts' }
FASTPythonImporterTest >> testSubscriptWithBinaryOperator [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testSubscriptWithBinaryOperator 	"

	self parse: 'values[1 + 2]'.

	self assert: (fast allWithType: FASTPyBinaryOperator) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyInteger) size equals: 2.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPySubscript) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'values[1 + 2]'.
	self assert: (self topEntity isOfType: FASTPySubscript).
	
	"Testing value of multivalued relation indices"
	self assert: self topEntity indices size equals: 1.
	
		stack push: (stack top indices at: 1).
		self assert: self topEntity sourceCode equals: '1 + 2'.
		self assert: (self topEntity isOfType: FASTPyBinaryOperator).
		self assert: (self topEntity isOfType: FASTTBinaryExpression).
		
		"Testing value of monovalue relation leftOperand"
		self assert: self topEntity leftOperand isNotNil.

			stack push: self topEntity leftOperand.
			self assert: self topEntity sourceCode equals: '1'.
			self assert: (self topEntity isOfType: FASTPyInteger).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTNumericalLiteral).
			stack pop.
		self assert: self topEntity operator equals: '+'.
		
		"Testing value of monovalue relation rightOperand"
		self assert: self topEntity rightOperand isNotNil.

			stack push: self topEntity rightOperand.
			self assert: self topEntity sourceCode equals: '2'.
			self assert: (self topEntity isOfType: FASTPyInteger).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTNumericalLiteral).
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation value"
	self assert: self topEntity value isNotNil.

		stack push: self topEntity value.
		self assert: self topEntity sourceCode equals: 'values'.
		self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - subscripts' }
FASTPythonImporterTest >> testSubscriptWithEllipsis [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testSubscriptWithEllipsis 	"

	self parse: 'imgs[..., i]'.

	self assert: (fast allWithType: FASTPyEllipsis) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 2.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPySubscript) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'imgs[..., i]'.
	self assert: (self topEntity isOfType: FASTPySubscript).
	
	"Testing value of multivalued relation indices"
	self assert: self topEntity indices size equals: 2.
	
		stack push: (stack top indices at: 1).
		self assert: self topEntity sourceCode equals: '...'.
		self assert: (self topEntity isOfType: FASTPyEllipsis).
		self assert: (self topEntity isOfType: FASTTLiteral).
		stack pop.
	
		stack push: (stack top indices at: 2).
		self assert: self topEntity sourceCode equals: 'i'.
		self assert: (self topEntity isOfType: FASTPyIdentifier).
		stack pop.
	
	"Testing value of monovalue relation value"
	self assert: self topEntity value isNotNil.

		stack push: self topEntity value.
		self assert: self topEntity sourceCode equals: 'imgs'.
		self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - subscripts' }
FASTPythonImporterTest >> testSubscriptWithTwoIndicies [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testSubscriptWithTwoIndicies 	"

	self parse: 'loc[1, 2]'.

	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyInteger) size equals: 2.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPySubscript) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'loc[1, 2]'.
	self assert: (self topEntity isOfType: FASTPySubscript).
	
	"Testing value of multivalued relation indices"
	self assert: self topEntity indices size equals: 2.
	
		stack push: (stack top indices at: 1).
		self assert: self topEntity sourceCode equals: '1'.
		self assert: (self topEntity isOfType: FASTPyInteger).
		self assert: (self topEntity isOfType: FASTTLiteral).
		self assert: (self topEntity isOfType: FASTTNumericalLiteral).
		stack pop.
	
		stack push: (stack top indices at: 2).
		self assert: self topEntity sourceCode equals: '2'.
		self assert: (self topEntity isOfType: FASTPyInteger).
		self assert: (self topEntity isOfType: FASTTLiteral).
		self assert: (self topEntity isOfType: FASTTNumericalLiteral).
		stack pop.
	
	"Testing value of monovalue relation value"
	self assert: self topEntity value isNotNil.

		stack push: self topEntity value.
		self assert: self topEntity sourceCode equals: 'loc'.
		self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - subscripts' }
FASTPythonImporterTest >> testSubscriptWithTwoSlices [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testSubscriptWithTwoSlices 	"

	self parse: 'arr[1:3, 2:4] '.

	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyInteger) size equals: 4.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPySlice) size equals: 2.
	self assert: (fast allWithType: FASTPySubscript) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'arr[1:3, 2:4]'.
	self assert: (self topEntity isOfType: FASTPySubscript).
	
	"Testing value of multivalued relation indices"
	self assert: self topEntity indices size equals: 2.
	
		stack push: (stack top indices at: 1).
		self assert: self topEntity sourceCode equals: '1:3'.
		self assert: (self topEntity isOfType: FASTPySlice).
		
		"Testing value of multivalued relation components"
		self assert: self topEntity components size equals: 2.
		
			stack push: (stack top components at: 1).
			self assert: self topEntity sourceCode equals: '1'.
			self assert: (self topEntity isOfType: FASTPyInteger).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTNumericalLiteral).
			stack pop.
		
			stack push: (stack top components at: 2).
			self assert: self topEntity sourceCode equals: '3'.
			self assert: (self topEntity isOfType: FASTPyInteger).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTNumericalLiteral).
			stack pop.
		stack pop.
	
		stack push: (stack top indices at: 2).
		self assert: self topEntity sourceCode equals: '2:4'.
		self assert: (self topEntity isOfType: FASTPySlice).
		
		"Testing value of multivalued relation components"
		self assert: self topEntity components size equals: 2.
		
			stack push: (stack top components at: 1).
			self assert: self topEntity sourceCode equals: '2'.
			self assert: (self topEntity isOfType: FASTPyInteger).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTNumericalLiteral).
			stack pop.
		
			stack push: (stack top components at: 2).
			self assert: self topEntity sourceCode equals: '4'.
			self assert: (self topEntity isOfType: FASTPyInteger).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTNumericalLiteral).
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation value"
	self assert: self topEntity value isNotNil.

		stack push: self topEntity value.
		self assert: self topEntity sourceCode equals: 'arr'.
		self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - literals' }
FASTPythonImporterTest >> testTrue [

	| boolean |
	self parse: 'True'.

	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyBoolean) size equals: 1.

	boolean := (fast allWithType: FASTPyBoolean) anyOne.
	self assert: boolean sourceCode equals: 'True'.
	self assert: (boolean isOfType: FASTTBooleanLiteral)
]

{ #category : 'tests - try statement' }
FASTPythonImporterTest >> testTryStatement [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testTryStatement 	"

	self parse: 'try: pass
except: pass '.

	self assert: (fast allWithType: FASTPyExceptClause) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPassStatement) size equals: 2.
	self assert: (fast allWithType: FASTPyTryStatement) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'try: pass
except: pass' withPlatformLineEndings.
	self assert: (self topEntity isOfType: FASTPyTryStatement).
	self assert: (self topEntity isOfType: FASTPyTWithElseClause).
	self assert: (self topEntity isOfType: FASTTStatementBlock).
	
	"Testing value of multivalued relation excepts"
	self assert: self topEntity excepts size equals: 1.
	
		stack push: (stack top excepts at: 1).
		self assert: self topEntity sourceCode equals: 'except: pass'.
		self assert: (self topEntity isOfType: FASTPyExceptClause).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'pass'.
			self assert: (self topEntity isOfType: FASTPyPassStatement).
			stack pop.
		stack pop.
	
	"Testing value of multivalued relation statements"
	self assert: self topEntity statements size equals: 1.
	
		stack push: (stack top statements at: 1).
		self assert: self topEntity sourceCode equals: 'pass'.
		self assert: (self topEntity isOfType: FASTPyPassStatement)
]

{ #category : 'tests - try statement' }
FASTPythonImporterTest >> testTryStatementWithElseClause [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testTryStatementWithElseClause 	"

	self parse: 'try: pass
except Exception: pass
else: print(1) '.

	self assert: (fast allWithType: FASTPyCall) size equals: 1.
	self assert: (fast allWithType: FASTPyElseClause) size equals: 1.
	self assert: (fast allWithType: FASTPyExceptClause) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 2.
	self assert: (fast allWithType: FASTPyInteger) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPassStatement) size equals: 2.
	self assert: (fast allWithType: FASTPyTryStatement) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'try: pass
except Exception: pass
else: print(1)' withPlatformLineEndings.
	self assert: (self topEntity isOfType: FASTPyTryStatement).
	self assert: (self topEntity isOfType: FASTPyTWithElseClause).
	self assert: (self topEntity isOfType: FASTTStatementBlock).
	
	"Testing value of monovalue relation elseClause"
	self assert: self topEntity elseClause isNotNil.

		stack push: self topEntity elseClause.
		self assert: self topEntity sourceCode equals: 'print(1)'.
		self assert: (self topEntity isOfType: FASTPyElseClause).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'print(1)'.
			self assert: (self topEntity isOfType: FASTPyCall).
			self assert: (self topEntity isOfType: FASTTInvocation).
			self assert: (self topEntity isOfType: FASTTWithArguments).
			
			"Testing value of multivalued relation arguments"
			self assert: self topEntity arguments size equals: 1.
			
				stack push: (stack top arguments at: 1).
				self assert: self topEntity sourceCode equals: '1'.
				self assert: (self topEntity isOfType: FASTPyInteger).
				self assert: (self topEntity isOfType: FASTTLiteral).
				self assert: (self topEntity isOfType: FASTTNumericalLiteral).
				stack pop.
			
			"Testing value of monovalue relation callee"
			self assert: self topEntity callee isNotNil.

				stack push: self topEntity callee.
				self assert: self topEntity sourceCode equals: 'print'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			stack pop.
		stack pop.
	
	"Testing value of multivalued relation excepts"
	self assert: self topEntity excepts size equals: 1.
	
		stack push: (stack top excepts at: 1).
		self assert: self topEntity sourceCode equals: 'except Exception: pass'.
		self assert: (self topEntity isOfType: FASTPyExceptClause).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		
		"Testing value of multivalued relation expressions"
		self assert: self topEntity expressions size equals: 1.
		
			stack push: (stack top expressions at: 1).
			self assert: self topEntity sourceCode equals: 'Exception'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'pass'.
			self assert: (self topEntity isOfType: FASTPyPassStatement).
			stack pop.
		stack pop.
	
	"Testing value of multivalued relation statements"
	self assert: self topEntity statements size equals: 1.
	
		stack push: (stack top statements at: 1).
		self assert: self topEntity sourceCode equals: 'pass'.
		self assert: (self topEntity isOfType: FASTPyPassStatement)
]

{ #category : 'tests - try statement' }
FASTPythonImporterTest >> testTryStatementWithFinallyClause [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testTryStatementWithFinallyClause 	"

	self parse: 'try: pass
except Exception: pass
finally: print(1) '.

	self assert: (fast allWithType: FASTPyCall) size equals: 1.
	self assert: (fast allWithType: FASTPyExceptClause) size equals: 1.
	self assert: (fast allWithType: FASTPyFinallyClause) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 2.
	self assert: (fast allWithType: FASTPyInteger) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPassStatement) size equals: 2.
	self assert: (fast allWithType: FASTPyTryStatement) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'try: pass
except Exception: pass
finally: print(1)' withPlatformLineEndings.
	self assert: (self topEntity isOfType: FASTPyTryStatement).
	self assert: (self topEntity isOfType: FASTPyTWithElseClause).
	self assert: (self topEntity isOfType: FASTTStatementBlock).
	
	"Testing value of multivalued relation excepts"
	self assert: self topEntity excepts size equals: 1.
	
		stack push: (stack top excepts at: 1).
		self assert: self topEntity sourceCode equals: 'except Exception: pass'.
		self assert: (self topEntity isOfType: FASTPyExceptClause).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		
		"Testing value of multivalued relation expressions"
		self assert: self topEntity expressions size equals: 1.
		
			stack push: (stack top expressions at: 1).
			self assert: self topEntity sourceCode equals: 'Exception'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'pass'.
			self assert: (self topEntity isOfType: FASTPyPassStatement).
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation finally"
	self assert: self topEntity finally isNotNil.

		stack push: self topEntity finally.
		self assert: self topEntity sourceCode equals: 'finally: print(1)'.
		self assert: (self topEntity isOfType: FASTPyFinallyClause).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'print(1)'.
			self assert: (self topEntity isOfType: FASTPyCall).
			self assert: (self topEntity isOfType: FASTTInvocation).
			self assert: (self topEntity isOfType: FASTTWithArguments).
			
			"Testing value of multivalued relation arguments"
			self assert: self topEntity arguments size equals: 1.
			
				stack push: (stack top arguments at: 1).
				self assert: self topEntity sourceCode equals: '1'.
				self assert: (self topEntity isOfType: FASTPyInteger).
				self assert: (self topEntity isOfType: FASTTLiteral).
				self assert: (self topEntity isOfType: FASTTNumericalLiteral).
				stack pop.
			
			"Testing value of monovalue relation callee"
			self assert: self topEntity callee isNotNil.

				stack push: self topEntity callee.
				self assert: self topEntity sourceCode equals: 'print'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			stack pop.
		stack pop.
	
	"Testing value of multivalued relation statements"
	self assert: self topEntity statements size equals: 1.
	
		stack push: (stack top statements at: 1).
		self assert: self topEntity sourceCode equals: 'pass'.
		self assert: (self topEntity isOfType: FASTPyPassStatement)
]

{ #category : 'tests - collections' }
FASTPythonImporterTest >> testTuple [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testTuple 	"

	self parse: '(1, 2, 3)'.

	self assert: (fast allWithType: FASTPyInteger) size equals: 3.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyTuple) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: '(1, 2, 3)'.
	self assert: (self topEntity isOfType: FASTPyTuple).
	
	"Testing value of multivalued relation initializers"
	self assert: self topEntity initializers size equals: 3.
	
		stack push: (stack top initializers at: 1).
		self assert: self topEntity sourceCode equals: '1'.
		self assert: (self topEntity isOfType: FASTPyInteger).
		self assert: (self topEntity isOfType: FASTTLiteral).
		self assert: (self topEntity isOfType: FASTTNumericalLiteral).
		stack pop.
	
		stack push: (stack top initializers at: 2).
		self assert: self topEntity sourceCode equals: '2'.
		self assert: (self topEntity isOfType: FASTPyInteger).
		self assert: (self topEntity isOfType: FASTTLiteral).
		self assert: (self topEntity isOfType: FASTTNumericalLiteral).
		stack pop.
	
		stack push: (stack top initializers at: 3).
		self assert: self topEntity sourceCode equals: '3'.
		self assert: (self topEntity isOfType: FASTPyInteger).
		self assert: (self topEntity isOfType: FASTTLiteral).
		self assert: (self topEntity isOfType: FASTTNumericalLiteral)
]

{ #category : 'tests - collections' }
FASTPythonImporterTest >> testTupleExpression [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testTupleExpression 	"

	self parse: '3, 5,'.

	self assert: (fast allWithType: FASTPyInteger) size equals: 2.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyTuple) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: '3, 5,'.
	self assert: (self topEntity isOfType: FASTPyTuple).
	
	"Testing value of multivalued relation initializers"
	self assert: self topEntity initializers size equals: 2.
	
		stack push: (stack top initializers at: 1).
		self assert: self topEntity sourceCode equals: '3'.
		self assert: (self topEntity isOfType: FASTPyInteger).
		self assert: (self topEntity isOfType: FASTTLiteral).
		self assert: (self topEntity isOfType: FASTTNumericalLiteral).
		stack pop.
	
		stack push: (stack top initializers at: 2).
		self assert: self topEntity sourceCode equals: '5'.
		self assert: (self topEntity isOfType: FASTPyInteger).
		self assert: (self topEntity isOfType: FASTTLiteral).
		self assert: (self topEntity isOfType: FASTTNumericalLiteral)
]

{ #category : 'tests - collections' }
FASTPythonImporterTest >> testTupleExpressionWithCall [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testTupleExpressionWithCall 	"

	self parse: 'toto(), 5,'.

	self assert: (fast allWithType: FASTPyCall) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyInteger) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyTuple) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'toto(), 5,'.
	self assert: (self topEntity isOfType: FASTPyTuple).
	
	"Testing value of multivalued relation initializers"
	self assert: self topEntity initializers size equals: 2.
	
		stack push: (stack top initializers at: 1).
		self assert: self topEntity sourceCode equals: 'toto()'.
		self assert: (self topEntity isOfType: FASTPyCall).
		self assert: (self topEntity isOfType: FASTTInvocation).
		self assert: (self topEntity isOfType: FASTTWithArguments).
		
		"Testing value of monovalue relation callee"
		self assert: self topEntity callee isNotNil.

			stack push: self topEntity callee.
			self assert: self topEntity sourceCode equals: 'toto'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		stack pop.
	
		stack push: (stack top initializers at: 2).
		self assert: self topEntity sourceCode equals: '5'.
		self assert: (self topEntity isOfType: FASTPyInteger).
		self assert: (self topEntity isOfType: FASTTLiteral).
		self assert: (self topEntity isOfType: FASTTNumericalLiteral)
]

{ #category : 'tests - collections' }
FASTPythonImporterTest >> testTupleOverview [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testTupleOverview 	"

	self parse: '(2, 2.0, 2j, "abc", [3], (2,2), {3: 3}, b"def", bytearray(b"ghi"), True, False, None, ..., self.a)'.

	self assert: (fast allWithType: FASTPyAttributeAccess) size equals: 1.
	self assert: (fast allWithType: FASTPyBoolean) size equals: 2.
	self assert: (fast allWithType: FASTPyCall) size equals: 1.
	self assert: (fast allWithType: FASTPyComplex) size equals: 1.
	self assert: (fast allWithType: FASTPyDictionary) size equals: 1.
	self assert: (fast allWithType: FASTPyEllipsis) size equals: 1.
	self assert: (fast allWithType: FASTPyFloat) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 3.
	self assert: (fast allWithType: FASTPyInteger) size equals: 6.
	self assert: (fast allWithType: FASTPyList) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyNone) size equals: 1.
	self assert: (fast allWithType: FASTPyPair) size equals: 1.
	self assert: (fast allWithType: FASTPyString) size equals: 3.
	self assert: (fast allWithType: FASTPyTuple) size equals: 2.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: '(2, 2.0, 2j, "abc", [3], (2,2), {3: 3}, b"def", bytearray(b"ghi"), True, False, None, ..., self.a)'.
	self assert: (self topEntity isOfType: FASTPyTuple).
	
	"Testing value of multivalued relation initializers"
	self assert: self topEntity initializers size equals: 14.
	
		stack push: (stack top initializers at: 1).
		self assert: self topEntity sourceCode equals: '2'.
		self assert: (self topEntity isOfType: FASTPyInteger).
		self assert: (self topEntity isOfType: FASTTLiteral).
		self assert: (self topEntity isOfType: FASTTNumericalLiteral).
		stack pop.
	
		stack push: (stack top initializers at: 2).
		self assert: self topEntity sourceCode equals: '2.0'.
		self assert: (self topEntity isOfType: FASTPyFloat).
		self assert: (self topEntity isOfType: FASTTLiteral).
		self assert: (self topEntity isOfType: FASTTNumericalLiteral).
		stack pop.
	
		stack push: (stack top initializers at: 3).
		self assert: self topEntity sourceCode equals: '2j'.
		self assert: (self topEntity isOfType: FASTPyComplex).
		self assert: (self topEntity isOfType: FASTTLiteral).
		self assert: (self topEntity isOfType: FASTTNumericalLiteral).
		stack pop.
	
		stack push: (stack top initializers at: 4).
		self assert: self topEntity sourceCode equals: '"abc"'.
		self assert: (self topEntity isOfType: FASTPyString).
		self assert: (self topEntity isOfType: FASTTLiteral).
		self assert: (self topEntity isOfType: FASTTStringLiteral).
		stack pop.
	
		stack push: (stack top initializers at: 5).
		self assert: self topEntity sourceCode equals: '[3]'.
		self assert: (self topEntity isOfType: FASTPyList).
		
		"Testing value of multivalued relation initializers"
		self assert: self topEntity initializers size equals: 1.
		
			stack push: (stack top initializers at: 1).
			self assert: self topEntity sourceCode equals: '3'.
			self assert: (self topEntity isOfType: FASTPyInteger).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTNumericalLiteral).
			stack pop.
		stack pop.
	
		stack push: (stack top initializers at: 6).
		self assert: self topEntity sourceCode equals: '(2,2)'.
		self assert: (self topEntity isOfType: FASTPyTuple).
		
		"Testing value of multivalued relation initializers"
		self assert: self topEntity initializers size equals: 2.
		
			stack push: (stack top initializers at: 1).
			self assert: self topEntity sourceCode equals: '2'.
			self assert: (self topEntity isOfType: FASTPyInteger).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTNumericalLiteral).
			stack pop.
		
			stack push: (stack top initializers at: 2).
			self assert: self topEntity sourceCode equals: '2'.
			self assert: (self topEntity isOfType: FASTPyInteger).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTNumericalLiteral).
			stack pop.
		stack pop.
	
		stack push: (stack top initializers at: 7).
		self assert: self topEntity sourceCode equals: '{3: 3}'.
		self assert: (self topEntity isOfType: FASTPyDictionary).
		
		"Testing value of multivalued relation initializers"
		self assert: self topEntity initializers size equals: 1.
		
			stack push: (stack top initializers at: 1).
			self assert: self topEntity sourceCode equals: '3: 3'.
			self assert: (self topEntity isOfType: FASTPyPair).
			
			"Testing value of monovalue relation key"
			self assert: self topEntity key isNotNil.

				stack push: self topEntity key.
				self assert: self topEntity sourceCode equals: '3'.
				self assert: (self topEntity isOfType: FASTPyInteger).
				self assert: (self topEntity isOfType: FASTTLiteral).
				self assert: (self topEntity isOfType: FASTTNumericalLiteral).
				stack pop.
			
			"Testing value of monovalue relation value"
			self assert: self topEntity value isNotNil.

				stack push: self topEntity value.
				self assert: self topEntity sourceCode equals: '3'.
				self assert: (self topEntity isOfType: FASTPyInteger).
				self assert: (self topEntity isOfType: FASTTLiteral).
				self assert: (self topEntity isOfType: FASTTNumericalLiteral).
				stack pop.
			stack pop.
		stack pop.
	
		stack push: (stack top initializers at: 8).
		self assert: self topEntity sourceCode equals: 'b"def"'.
		self assert: (self topEntity isOfType: FASTPyString).
		self assert: (self topEntity isOfType: FASTTLiteral).
		self assert: (self topEntity isOfType: FASTTStringLiteral).
		stack pop.
	
		stack push: (stack top initializers at: 9).
		self assert: self topEntity sourceCode equals: 'bytearray(b"ghi")'.
		self assert: (self topEntity isOfType: FASTPyCall).
		self assert: (self topEntity isOfType: FASTTInvocation).
		self assert: (self topEntity isOfType: FASTTWithArguments).
		
		"Testing value of multivalued relation arguments"
		self assert: self topEntity arguments size equals: 1.
		
			stack push: (stack top arguments at: 1).
			self assert: self topEntity sourceCode equals: 'b"ghi"'.
			self assert: (self topEntity isOfType: FASTPyString).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTStringLiteral).
			stack pop.
		
		"Testing value of monovalue relation callee"
		self assert: self topEntity callee isNotNil.

			stack push: self topEntity callee.
			self assert: self topEntity sourceCode equals: 'bytearray'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		stack pop.
	
		stack push: (stack top initializers at: 10).
		self assert: self topEntity sourceCode equals: 'True'.
		self assert: (self topEntity isOfType: FASTPyBoolean).
		self assert: (self topEntity isOfType: FASTTBooleanLiteral).
		self assert: (self topEntity isOfType: FASTTLiteral).
		stack pop.
	
		stack push: (stack top initializers at: 11).
		self assert: self topEntity sourceCode equals: 'False'.
		self assert: (self topEntity isOfType: FASTPyBoolean).
		self assert: (self topEntity isOfType: FASTTBooleanLiteral).
		self assert: (self topEntity isOfType: FASTTLiteral).
		stack pop.
	
		stack push: (stack top initializers at: 12).
		self assert: self topEntity sourceCode equals: 'None'.
		self assert: (self topEntity isOfType: FASTPyNone).
		self assert: (self topEntity isOfType: FASTTLiteral).
		stack pop.
	
		stack push: (stack top initializers at: 13).
		self assert: self topEntity sourceCode equals: '...'.
		self assert: (self topEntity isOfType: FASTPyEllipsis).
		self assert: (self topEntity isOfType: FASTTLiteral).
		stack pop.
	
		stack push: (stack top initializers at: 14).
		self assert: self topEntity sourceCode equals: 'self.a'.
		self assert: (self topEntity isOfType: FASTPyAttributeAccess).
		
		"Testing value of monovalue relation attribute"
		self assert: self topEntity attribute isNotNil.

			stack push: self topEntity attribute.
			self assert: self topEntity sourceCode equals: 'a'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		
		"Testing value of monovalue relation object"
		self assert: self topEntity object isNotNil.

			stack push: self topEntity object.
			self assert: self topEntity sourceCode equals: 'self'.
			self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - collections' }
FASTPythonImporterTest >> testTupleWithEllipsis [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testTupleWithEllipsis 	"

	self parse: '(...,)'.

	self assert: (fast allWithType: FASTPyEllipsis) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyTuple) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: '(...,)'.
	self assert: (self topEntity isOfType: FASTPyTuple).
	
	"Testing value of multivalued relation initializers"
	self assert: self topEntity initializers size equals: 1.
	
		stack push: (stack top initializers at: 1).
		self assert: self topEntity sourceCode equals: '...'.
		self assert: (self topEntity isOfType: FASTPyEllipsis).
		self assert: (self topEntity isOfType: FASTTLiteral)
]

{ #category : 'tests - collections' }
FASTPythonImporterTest >> testTupleWithListSplat [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testTupleWithListSplat 	"

	self parse: '(*tuple, 3)'.

	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyInteger) size equals: 1.
	self assert: (fast allWithType: FASTPyListSplat) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyTuple) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: '(*tuple, 3)'.
	self assert: (self topEntity isOfType: FASTPyTuple).
	
	"Testing value of multivalued relation initializers"
	self assert: self topEntity initializers size equals: 2.
	
		stack push: (stack top initializers at: 1).
		self assert: self topEntity sourceCode equals: '*tuple'.
		self assert: (self topEntity isOfType: FASTPyListSplat).
		
		"Testing value of monovalue relation expression"
		self assert: self topEntity expression isNotNil.

			stack push: self topEntity expression.
			self assert: self topEntity sourceCode equals: 'tuple'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		stack pop.
	
		stack push: (stack top initializers at: 2).
		self assert: self topEntity sourceCode equals: '3'.
		self assert: (self topEntity isOfType: FASTPyInteger).
		self assert: (self topEntity isOfType: FASTTLiteral).
		self assert: (self topEntity isOfType: FASTTNumericalLiteral)
]

{ #category : 'tests - comments' }
FASTPythonImporterTest >> testTwoComments [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testTwoComments 	"

	self parse: '# Comment 1
	
# Comment 2'.

	self assert: (fast allWithType: FASTPyComment) size equals: 2.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.

	stack push: fast rootEntities anyOne.
	self assert: self topEntity sourceCode equals: '# Comment 1
	
# Comment 2' withPlatformLineEndings.
	self assert: (self topEntity isOfType: FASTPyModule).
	self assert: (self topEntity isOfType: FASTTStatementBlock).
	self assert: (self topEntity isOfType: FamixTHasImmediateSource).
	self assert: (self topEntity isOfType: FamixTSourceAnchor).
	
	"Testing value of multivalued relation comments"
	self assert: self topEntity comments size equals: 2.
	
		stack push: (stack top comments at: 1).
		self assert: self topEntity sourceCode equals: '# Comment 1'.
		self assert: (self topEntity isOfType: FASTPyComment).
		self assert: (self topEntity isOfType: FASTTComment).
		stack pop.
	
		stack push: (stack top comments at: 2).
		self assert: self topEntity sourceCode equals: '# Comment 2'.
		self assert: (self topEntity isOfType: FASTPyComment).
		self assert: (self topEntity isOfType: FASTTComment).
		stack pop.
	self assert: self topEntity source equals: '# Comment 1
	
# Comment 2' withPlatformLineEndings
]

{ #category : 'tests - imports' }
FASTPythonImporterTest >> testTwoImports [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testTwoImports 	"

	self parse: 'import numpy as np
import mathplotlib as mpl'.

	self assert: (fast allWithType: FASTPyImportStatement) size equals: 2.
	self assert: (fast allWithType: FASTPyImportedEntity) size equals: 2.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.

	stack push: fast rootEntities anyOne.
	self assert: self topEntity sourceCode equals: 'import numpy as np
import mathplotlib as mpl' withPlatformLineEndings.
	self assert: (self topEntity isOfType: FASTPyModule).
	self assert: (self topEntity isOfType: FASTTStatementBlock).
	self assert: (self topEntity isOfType: FamixTHasImmediateSource).
	self assert: (self topEntity isOfType: FamixTSourceAnchor).
	self assert: self topEntity source equals: 'import numpy as np
import mathplotlib as mpl' withPlatformLineEndings.
	
	"Testing value of multivalued relation statements"
	self assert: self topEntity statements size equals: 2.
	
		stack push: (stack top statements at: 1).
		self assert: self topEntity sourceCode equals: 'import numpy as np'.
		self assert: (self topEntity isOfType: FASTPyImportStatement).
		
		"Testing value of multivalued relation importedEntities"
		self assert: self topEntity importedEntities size equals: 1.
		
			stack push: (stack top importedEntities at: 1).
			self assert: self topEntity sourceCode equals: 'numpy as np'.
			self assert: (self topEntity isOfType: FASTPyImportedEntity).
			self assert: self topEntity alias equals: 'np'.
			self assert: self topEntity segments equals: #('numpy').
			stack pop.
		stack pop.
	
		stack push: (stack top statements at: 2).
		self assert: self topEntity sourceCode equals: 'import mathplotlib as mpl'.
		self assert: (self topEntity isOfType: FASTPyImportStatement).
		
		"Testing value of multivalued relation importedEntities"
		self assert: self topEntity importedEntities size equals: 1.
		
			stack push: (stack top importedEntities at: 1).
			self assert: self topEntity sourceCode equals: 'mathplotlib as mpl'.
			self assert: (self topEntity isOfType: FASTPyImportedEntity).
			self assert: self topEntity alias equals: 'mpl'.
			self assert: self topEntity segments equals: #('mathplotlib')
]

{ #category : 'tests - statements' }
FASTPythonImporterTest >> testTypeAliasStatement [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testTypeAliasStatement 	"

	self parse: 'type MyInt = int'.

	self assert: (fast allWithType: FASTPyIdentifier) size equals: 2.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyType) size equals: 2.
	self assert: (fast allWithType: FASTPyTypeAliasStatement) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'type MyInt = int'.
	self assert: (self topEntity isOfType: FASTPyTypeAliasStatement).
	
	"Testing value of monovalue relation left"
	self assert: self topEntity left isNotNil.

		stack push: self topEntity left.
		self assert: self topEntity sourceCode equals: 'MyInt'.
		self assert: (self topEntity isOfType: FASTPyType).
		
		"Testing value of monovalue relation content"
		self assert: self topEntity content isNotNil.

			stack push: self topEntity content.
			self assert: self topEntity sourceCode equals: 'MyInt'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation right"
	self assert: self topEntity right isNotNil.

		stack push: self topEntity right.
		self assert: self topEntity sourceCode equals: 'int'.
		self assert: (self topEntity isOfType: FASTPyType).
		
		"Testing value of monovalue relation content"
		self assert: self topEntity content isNotNil.

			stack push: self topEntity content.
			self assert: self topEntity sourceCode equals: 'int'.
			self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - statements' }
FASTPythonImporterTest >> testTypeAliasStatementInBlock [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testTypeAliasStatementInBlock 	"

	self parse: 'def f(): type MyInt = int'.

	self assert: (fast allWithType: FASTPyBlock) size equals: 1.
	self assert: (fast allWithType: FASTPyFunctionDefinition) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 2.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyType) size equals: 2.
	self assert: (fast allWithType: FASTPyTypeAliasStatement) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'def f(): type MyInt = int'.
	self assert: (self topEntity isOfType: FASTPyFunctionDefinition).
	self assert: (self topEntity isOfType: FASTPyTDefinition).
	self assert: (self topEntity isOfType: FASTTWithParameters).
	self assert: (self topEntity isOfType: FASTTWithStatements).
	self assert: self topEntity name equals: 'f'.
	
	"Testing value of monovalue relation statementBlock"
	self assert: self topEntity statementBlock isNotNil.

		stack push: self topEntity statementBlock.
		self assert: self topEntity sourceCode equals: 'type MyInt = int'.
		self assert: (self topEntity isOfType: FASTPyBlock).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'type MyInt = int'.
			self assert: (self topEntity isOfType: FASTPyTypeAliasStatement).
			
			"Testing value of monovalue relation left"
			self assert: self topEntity left isNotNil.

				stack push: self topEntity left.
				self assert: self topEntity sourceCode equals: 'MyInt'.
				self assert: (self topEntity isOfType: FASTPyType).
				
				"Testing value of monovalue relation content"
				self assert: self topEntity content isNotNil.

					stack push: self topEntity content.
					self assert: self topEntity sourceCode equals: 'MyInt'.
					self assert: (self topEntity isOfType: FASTPyIdentifier).
					stack pop.
				stack pop.
			
			"Testing value of monovalue relation right"
			self assert: self topEntity right isNotNil.

				stack push: self topEntity right.
				self assert: self topEntity sourceCode equals: 'int'.
				self assert: (self topEntity isOfType: FASTPyType).
				
				"Testing value of monovalue relation content"
				self assert: self topEntity content isNotNil.

					stack push: self topEntity content.
					self assert: self topEntity sourceCode equals: 'int'.
					self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - types' }
FASTPythonImporterTest >> testTypeAttribute [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testTypeAttribute 	"

	self parse: 'def func() -> subprocess.CompletedProcess: pass'.

	self assert: (fast allWithType: FASTPyAttributeAccess) size equals: 1.
	self assert: (fast allWithType: FASTPyBlock) size equals: 1.
	self assert: (fast allWithType: FASTPyFunctionDefinition) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 2.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPassStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyType) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'def func() -> subprocess.CompletedProcess: pass'.
	self assert: (self topEntity isOfType: FASTPyFunctionDefinition).
	self assert: (self topEntity isOfType: FASTPyTDefinition).
	self assert: (self topEntity isOfType: FASTTWithParameters).
	self assert: (self topEntity isOfType: FASTTWithStatements).
	self assert: self topEntity name equals: 'func'.
	
	"Testing value of monovalue relation returnType"
	self assert: self topEntity returnType isNotNil.

		stack push: self topEntity returnType.
		self assert: self topEntity sourceCode equals: 'subprocess.CompletedProcess'.
		self assert: (self topEntity isOfType: FASTPyType).
		
		"Testing value of monovalue relation content"
		self assert: self topEntity content isNotNil.

			stack push: self topEntity content.
			self assert: self topEntity sourceCode equals: 'subprocess.CompletedProcess'.
			self assert: (self topEntity isOfType: FASTPyAttributeAccess).
			
			"Testing value of monovalue relation attribute"
			self assert: self topEntity attribute isNotNil.

				stack push: self topEntity attribute.
				self assert: self topEntity sourceCode equals: 'CompletedProcess'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			
			"Testing value of monovalue relation object"
			self assert: self topEntity object isNotNil.

				stack push: self topEntity object.
				self assert: self topEntity sourceCode equals: 'subprocess'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation statementBlock"
	self assert: self topEntity statementBlock isNotNil.

		stack push: self topEntity statementBlock.
		self assert: self topEntity sourceCode equals: 'pass'.
		self assert: (self topEntity isOfType: FASTPyBlock).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'pass'.
			self assert: (self topEntity isOfType: FASTPyPassStatement)
]

{ #category : 'tests - types' }
FASTPythonImporterTest >> testTypeBinaryOperator [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testTypeBinaryOperator 	"

	self parse: 'def func() -> int | str: pass'.

	self assert: (fast allWithType: FASTPyBinaryOperator) size equals: 1.
	self assert: (fast allWithType: FASTPyBlock) size equals: 1.
	self assert: (fast allWithType: FASTPyFunctionDefinition) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 2.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPassStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyType) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'def func() -> int | str: pass'.
	self assert: (self topEntity isOfType: FASTPyFunctionDefinition).
	self assert: (self topEntity isOfType: FASTPyTDefinition).
	self assert: (self topEntity isOfType: FASTTWithParameters).
	self assert: (self topEntity isOfType: FASTTWithStatements).
	self assert: self topEntity name equals: 'func'.
	
	"Testing value of monovalue relation returnType"
	self assert: self topEntity returnType isNotNil.

		stack push: self topEntity returnType.
		self assert: self topEntity sourceCode equals: 'int | str'.
		self assert: (self topEntity isOfType: FASTPyType).
		
		"Testing value of monovalue relation content"
		self assert: self topEntity content isNotNil.

			stack push: self topEntity content.
			self assert: self topEntity sourceCode equals: 'int | str'.
			self assert: (self topEntity isOfType: FASTPyBinaryOperator).
			self assert: (self topEntity isOfType: FASTTBinaryExpression).
			
			"Testing value of monovalue relation leftOperand"
			self assert: self topEntity leftOperand isNotNil.

				stack push: self topEntity leftOperand.
				self assert: self topEntity sourceCode equals: 'int'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			self assert: self topEntity operator equals: '|'.
			
			"Testing value of monovalue relation rightOperand"
			self assert: self topEntity rightOperand isNotNil.

				stack push: self topEntity rightOperand.
				self assert: self topEntity sourceCode equals: 'str'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation statementBlock"
	self assert: self topEntity statementBlock isNotNil.

		stack push: self topEntity statementBlock.
		self assert: self topEntity sourceCode equals: 'pass'.
		self assert: (self topEntity isOfType: FASTPyBlock).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'pass'.
			self assert: (self topEntity isOfType: FASTPyPassStatement)
]

{ #category : 'tests - types' }
FASTPythonImporterTest >> testTypeCall [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testTypeCall 	"

	self parse: 'def func() -> f(): pass'.

	self assert: (fast allWithType: FASTPyBlock) size equals: 1.
	self assert: (fast allWithType: FASTPyCall) size equals: 1.
	self assert: (fast allWithType: FASTPyFunctionDefinition) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPassStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyType) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'def func() -> f(): pass'.
	self assert: (self topEntity isOfType: FASTPyFunctionDefinition).
	self assert: (self topEntity isOfType: FASTPyTDefinition).
	self assert: (self topEntity isOfType: FASTTWithParameters).
	self assert: (self topEntity isOfType: FASTTWithStatements).
	self assert: self topEntity name equals: 'func'.
	
	"Testing value of monovalue relation returnType"
	self assert: self topEntity returnType isNotNil.

		stack push: self topEntity returnType.
		self assert: self topEntity sourceCode equals: 'f()'.
		self assert: (self topEntity isOfType: FASTPyType).
		
		"Testing value of monovalue relation content"
		self assert: self topEntity content isNotNil.

			stack push: self topEntity content.
			self assert: self topEntity sourceCode equals: 'f()'.
			self assert: (self topEntity isOfType: FASTPyCall).
			self assert: (self topEntity isOfType: FASTTInvocation).
			self assert: (self topEntity isOfType: FASTTWithArguments).
			
			"Testing value of monovalue relation callee"
			self assert: self topEntity callee isNotNil.

				stack push: self topEntity callee.
				self assert: self topEntity sourceCode equals: 'f'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation statementBlock"
	self assert: self topEntity statementBlock isNotNil.

		stack push: self topEntity statementBlock.
		self assert: self topEntity sourceCode equals: 'pass'.
		self assert: (self topEntity isOfType: FASTPyBlock).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'pass'.
			self assert: (self topEntity isOfType: FASTPyPassStatement)
]

{ #category : 'tests - types' }
FASTPythonImporterTest >> testTypeComparisonOperator [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testTypeComparisonOperator 	"

	self parse: 'def func() -> 1 > 3: pass'.

	self assert: (fast allWithType: FASTPyBlock) size equals: 1.
	self assert: (fast allWithType: FASTPyComparisonOperator) size equals: 1.
	self assert: (fast allWithType: FASTPyFunctionDefinition) size equals: 1.
	self assert: (fast allWithType: FASTPyInteger) size equals: 2.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPassStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyType) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'def func() -> 1 > 3: pass'.
	self assert: (self topEntity isOfType: FASTPyFunctionDefinition).
	self assert: (self topEntity isOfType: FASTPyTDefinition).
	self assert: (self topEntity isOfType: FASTTWithParameters).
	self assert: (self topEntity isOfType: FASTTWithStatements).
	self assert: self topEntity name equals: 'func'.
	
	"Testing value of monovalue relation returnType"
	self assert: self topEntity returnType isNotNil.

		stack push: self topEntity returnType.
		self assert: self topEntity sourceCode equals: '1 > 3'.
		self assert: (self topEntity isOfType: FASTPyType).
		
		"Testing value of monovalue relation content"
		self assert: self topEntity content isNotNil.

			stack push: self topEntity content.
			self assert: self topEntity sourceCode equals: '1 > 3'.
			self assert: (self topEntity isOfType: FASTPyComparisonOperator).
			
			"Testing value of multivalued relation operands"
			self assert: self topEntity operands size equals: 2.
			
				stack push: (stack top operands at: 1).
				self assert: self topEntity sourceCode equals: '1'.
				self assert: (self topEntity isOfType: FASTPyInteger).
				self assert: (self topEntity isOfType: FASTTLiteral).
				self assert: (self topEntity isOfType: FASTTNumericalLiteral).
				stack pop.
			
				stack push: (stack top operands at: 2).
				self assert: self topEntity sourceCode equals: '3'.
				self assert: (self topEntity isOfType: FASTPyInteger).
				self assert: (self topEntity isOfType: FASTTLiteral).
				self assert: (self topEntity isOfType: FASTTNumericalLiteral).
				stack pop.
			self assert: self topEntity operators equals: #('>').
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation statementBlock"
	self assert: self topEntity statementBlock isNotNil.

		stack push: self topEntity statementBlock.
		self assert: self topEntity sourceCode equals: 'pass'.
		self assert: (self topEntity isOfType: FASTPyBlock).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'pass'.
			self assert: (self topEntity isOfType: FASTPyPassStatement)
]

{ #category : 'tests - types' }
FASTPythonImporterTest >> testTypeDictionary [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testTypeDictionary 	"

	self parse: 'def func() -> {42: ''ham''}: pass #Valid in Python3...'.

	self assert: (fast allWithType: FASTPyBlock) size equals: 1.
	self assert: (fast allWithType: FASTPyComment) size equals: 1.
	self assert: (fast allWithType: FASTPyDictionary) size equals: 1.
	self assert: (fast allWithType: FASTPyFunctionDefinition) size equals: 1.
	self assert: (fast allWithType: FASTPyInteger) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPair) size equals: 1.
	self assert: (fast allWithType: FASTPyPassStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyString) size equals: 1.
	self assert: (fast allWithType: FASTPyType) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'def func() -> {42: ''ham''}: pass #Valid in Python3...'.
	self assert: (self topEntity isOfType: FASTPyFunctionDefinition).
	self assert: (self topEntity isOfType: FASTPyTDefinition).
	self assert: (self topEntity isOfType: FASTTWithParameters).
	self assert: (self topEntity isOfType: FASTTWithStatements).
	self assert: self topEntity name equals: 'func'.
	
	"Testing value of monovalue relation returnType"
	self assert: self topEntity returnType isNotNil.

		stack push: self topEntity returnType.
		self assert: self topEntity sourceCode equals: '{42: ''ham''}'.
		self assert: (self topEntity isOfType: FASTPyType).
		
		"Testing value of monovalue relation content"
		self assert: self topEntity content isNotNil.

			stack push: self topEntity content.
			self assert: self topEntity sourceCode equals: '{42: ''ham''}'.
			self assert: (self topEntity isOfType: FASTPyDictionary).
			
			"Testing value of multivalued relation initializers"
			self assert: self topEntity initializers size equals: 1.
			
				stack push: (stack top initializers at: 1).
				self assert: self topEntity sourceCode equals: '42: ''ham'''.
				self assert: (self topEntity isOfType: FASTPyPair).
				
				"Testing value of monovalue relation key"
				self assert: self topEntity key isNotNil.

					stack push: self topEntity key.
					self assert: self topEntity sourceCode equals: '42'.
					self assert: (self topEntity isOfType: FASTPyInteger).
					self assert: (self topEntity isOfType: FASTTLiteral).
					self assert: (self topEntity isOfType: FASTTNumericalLiteral).
					stack pop.
				
				"Testing value of monovalue relation value"
				self assert: self topEntity value isNotNil.

					stack push: self topEntity value.
					self assert: self topEntity sourceCode equals: '''ham'''.
					self assert: (self topEntity isOfType: FASTPyString).
					self assert: (self topEntity isOfType: FASTTLiteral).
					self assert: (self topEntity isOfType: FASTTStringLiteral).
					stack pop.
				stack pop.
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation statementBlock"
	self assert: self topEntity statementBlock isNotNil.

		stack push: self topEntity statementBlock.
		self assert: self topEntity sourceCode equals: 'pass #Valid in Python3...'.
		self assert: (self topEntity isOfType: FASTPyBlock).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		
		"Testing value of multivalued relation comments"
		self assert: self topEntity comments size equals: 1.
		
			stack push: (stack top comments at: 1).
			self assert: self topEntity sourceCode equals: '#Valid in Python3...'.
			self assert: (self topEntity isOfType: FASTPyComment).
			self assert: (self topEntity isOfType: FASTTComment).
			stack pop.
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'pass'.
			self assert: (self topEntity isOfType: FASTPyPassStatement)
]

{ #category : 'tests - types' }
FASTPythonImporterTest >> testTypeEllipsis [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testTypeEllipsis 	"

	self parse: 'def func() -> ...: pass'.

	self assert: (fast allWithType: FASTPyBlock) size equals: 1.
	self assert: (fast allWithType: FASTPyEllipsis) size equals: 1.
	self assert: (fast allWithType: FASTPyFunctionDefinition) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPassStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyType) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'def func() -> ...: pass'.
	self assert: (self topEntity isOfType: FASTPyFunctionDefinition).
	self assert: (self topEntity isOfType: FASTPyTDefinition).
	self assert: (self topEntity isOfType: FASTTWithParameters).
	self assert: (self topEntity isOfType: FASTTWithStatements).
	self assert: self topEntity name equals: 'func'.
	
	"Testing value of monovalue relation returnType"
	self assert: self topEntity returnType isNotNil.

		stack push: self topEntity returnType.
		self assert: self topEntity sourceCode equals: '...'.
		self assert: (self topEntity isOfType: FASTPyType).
		
		"Testing value of monovalue relation content"
		self assert: self topEntity content isNotNil.

			stack push: self topEntity content.
			self assert: self topEntity sourceCode equals: '...'.
			self assert: (self topEntity isOfType: FASTPyEllipsis).
			self assert: (self topEntity isOfType: FASTTLiteral).
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation statementBlock"
	self assert: self topEntity statementBlock isNotNil.

		stack push: self topEntity statementBlock.
		self assert: self topEntity sourceCode equals: 'pass'.
		self assert: (self topEntity isOfType: FASTPyBlock).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'pass'.
			self assert: (self topEntity isOfType: FASTPyPassStatement)
]

{ #category : 'tests - types' }
FASTPythonImporterTest >> testTypeFalse [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testTypeFalse 	"

	self parse: 'def func() -> False: pass'.

	self assert: (fast allWithType: FASTPyBlock) size equals: 1.
	self assert: (fast allWithType: FASTPyBoolean) size equals: 1.
	self assert: (fast allWithType: FASTPyFunctionDefinition) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPassStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyType) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'def func() -> False: pass'.
	self assert: (self topEntity isOfType: FASTPyFunctionDefinition).
	self assert: (self topEntity isOfType: FASTPyTDefinition).
	self assert: (self topEntity isOfType: FASTTWithParameters).
	self assert: (self topEntity isOfType: FASTTWithStatements).
	self assert: self topEntity name equals: 'func'.
	
	"Testing value of monovalue relation returnType"
	self assert: self topEntity returnType isNotNil.

		stack push: self topEntity returnType.
		self assert: self topEntity sourceCode equals: 'False'.
		self assert: (self topEntity isOfType: FASTPyType).
		
		"Testing value of monovalue relation content"
		self assert: self topEntity content isNotNil.

			stack push: self topEntity content.
			self assert: self topEntity sourceCode equals: 'False'.
			self assert: (self topEntity isOfType: FASTPyBoolean).
			self assert: (self topEntity isOfType: FASTTBooleanLiteral).
			self assert: (self topEntity isOfType: FASTTLiteral).
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation statementBlock"
	self assert: self topEntity statementBlock isNotNil.

		stack push: self topEntity statementBlock.
		self assert: self topEntity sourceCode equals: 'pass'.
		self assert: (self topEntity isOfType: FASTPyBlock).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'pass'.
			self assert: (self topEntity isOfType: FASTPyPassStatement)
]

{ #category : 'tests - types' }
FASTPythonImporterTest >> testTypeFloat [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testTypeFloat 	"

	self parse: 'def func() -> 1.5: pass'.

	self assert: (fast allWithType: FASTPyBlock) size equals: 1.
	self assert: (fast allWithType: FASTPyFloat) size equals: 1.
	self assert: (fast allWithType: FASTPyFunctionDefinition) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPassStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyType) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'def func() -> 1.5: pass'.
	self assert: (self topEntity isOfType: FASTPyFunctionDefinition).
	self assert: (self topEntity isOfType: FASTPyTDefinition).
	self assert: (self topEntity isOfType: FASTTWithParameters).
	self assert: (self topEntity isOfType: FASTTWithStatements).
	self assert: self topEntity name equals: 'func'.
	
	"Testing value of monovalue relation returnType"
	self assert: self topEntity returnType isNotNil.

		stack push: self topEntity returnType.
		self assert: self topEntity sourceCode equals: '1.5'.
		self assert: (self topEntity isOfType: FASTPyType).
		
		"Testing value of monovalue relation content"
		self assert: self topEntity content isNotNil.

			stack push: self topEntity content.
			self assert: self topEntity sourceCode equals: '1.5'.
			self assert: (self topEntity isOfType: FASTPyFloat).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTNumericalLiteral).
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation statementBlock"
	self assert: self topEntity statementBlock isNotNil.

		stack push: self topEntity statementBlock.
		self assert: self topEntity sourceCode equals: 'pass'.
		self assert: (self topEntity isOfType: FASTPyBlock).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'pass'.
			self assert: (self topEntity isOfType: FASTPyPassStatement)
]

{ #category : 'tests - types' }
FASTPythonImporterTest >> testTypeIdentifier [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testTypeIdentifier 	"

	self parse: 'def func() -> Path: pass'.

	self assert: (fast allWithType: FASTPyBlock) size equals: 1.
	self assert: (fast allWithType: FASTPyFunctionDefinition) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPassStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyType) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'def func() -> Path: pass'.
	self assert: (self topEntity isOfType: FASTPyFunctionDefinition).
	self assert: (self topEntity isOfType: FASTPyTDefinition).
	self assert: (self topEntity isOfType: FASTTWithParameters).
	self assert: (self topEntity isOfType: FASTTWithStatements).
	self assert: self topEntity name equals: 'func'.
	
	"Testing value of monovalue relation returnType"
	self assert: self topEntity returnType isNotNil.

		stack push: self topEntity returnType.
		self assert: self topEntity sourceCode equals: 'Path'.
		self assert: (self topEntity isOfType: FASTPyType).
		
		"Testing value of monovalue relation content"
		self assert: self topEntity content isNotNil.

			stack push: self topEntity content.
			self assert: self topEntity sourceCode equals: 'Path'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation statementBlock"
	self assert: self topEntity statementBlock isNotNil.

		stack push: self topEntity statementBlock.
		self assert: self topEntity sourceCode equals: 'pass'.
		self assert: (self topEntity isOfType: FASTPyBlock).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'pass'.
			self assert: (self topEntity isOfType: FASTPyPassStatement)
]

{ #category : 'tests - types' }
FASTPythonImporterTest >> testTypeInteger [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testTypeInteger 	"

	self parse: 'def func() -> 1: pass'.

	self assert: (fast allWithType: FASTPyBlock) size equals: 1.
	self assert: (fast allWithType: FASTPyFunctionDefinition) size equals: 1.
	self assert: (fast allWithType: FASTPyInteger) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPassStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyType) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'def func() -> 1: pass'.
	self assert: (self topEntity isOfType: FASTPyFunctionDefinition).
	self assert: (self topEntity isOfType: FASTPyTDefinition).
	self assert: (self topEntity isOfType: FASTTWithParameters).
	self assert: (self topEntity isOfType: FASTTWithStatements).
	self assert: self topEntity name equals: 'func'.
	
	"Testing value of monovalue relation returnType"
	self assert: self topEntity returnType isNotNil.

		stack push: self topEntity returnType.
		self assert: self topEntity sourceCode equals: '1'.
		self assert: (self topEntity isOfType: FASTPyType).
		
		"Testing value of monovalue relation content"
		self assert: self topEntity content isNotNil.

			stack push: self topEntity content.
			self assert: self topEntity sourceCode equals: '1'.
			self assert: (self topEntity isOfType: FASTPyInteger).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTNumericalLiteral).
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation statementBlock"
	self assert: self topEntity statementBlock isNotNil.

		stack push: self topEntity statementBlock.
		self assert: self topEntity sourceCode equals: 'pass'.
		self assert: (self topEntity isOfType: FASTPyBlock).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'pass'.
			self assert: (self topEntity isOfType: FASTPyPassStatement)
]

{ #category : 'tests - types' }
FASTPythonImporterTest >> testTypeLambda [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testTypeLambda 	"

	self parse: 'def func() -> lambda : None: pass'.

	self assert: (fast allWithType: FASTPyBlock) size equals: 1.
	self assert: (fast allWithType: FASTPyFunctionDefinition) size equals: 1.
	self assert: (fast allWithType: FASTPyLambda) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyNone) size equals: 1.
	self assert: (fast allWithType: FASTPyPassStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyType) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'def func() -> lambda : None: pass'.
	self assert: (self topEntity isOfType: FASTPyFunctionDefinition).
	self assert: (self topEntity isOfType: FASTPyTDefinition).
	self assert: (self topEntity isOfType: FASTTWithParameters).
	self assert: (self topEntity isOfType: FASTTWithStatements).
	self assert: self topEntity name equals: 'func'.
	
	"Testing value of monovalue relation returnType"
	self assert: self topEntity returnType isNotNil.

		stack push: self topEntity returnType.
		self assert: self topEntity sourceCode equals: 'lambda : None'.
		self assert: (self topEntity isOfType: FASTPyType).
		
		"Testing value of monovalue relation content"
		self assert: self topEntity content isNotNil.

			stack push: self topEntity content.
			self assert: self topEntity sourceCode equals: 'lambda : None'.
			self assert: (self topEntity isOfType: FASTPyLambda).
			self assert: (self topEntity isOfType: FASTTWithParameters).
			
			"Testing value of monovalue relation expression"
			self assert: self topEntity expression isNotNil.

				stack push: self topEntity expression.
				self assert: self topEntity sourceCode equals: 'None'.
				self assert: (self topEntity isOfType: FASTPyNone).
				self assert: (self topEntity isOfType: FASTTLiteral).
				stack pop.
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation statementBlock"
	self assert: self topEntity statementBlock isNotNil.

		stack push: self topEntity statementBlock.
		self assert: self topEntity sourceCode equals: 'pass'.
		self assert: (self topEntity isOfType: FASTPyBlock).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'pass'.
			self assert: (self topEntity isOfType: FASTPyPassStatement)
]

{ #category : 'tests - types' }
FASTPythonImporterTest >> testTypeList [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testTypeList 	"

	self parse: 'def func() -> [object]: pass'.

	self assert: (fast allWithType: FASTPyBlock) size equals: 1.
	self assert: (fast allWithType: FASTPyFunctionDefinition) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyList) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPassStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyType) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'def func() -> [object]: pass'.
	self assert: (self topEntity isOfType: FASTPyFunctionDefinition).
	self assert: (self topEntity isOfType: FASTPyTDefinition).
	self assert: (self topEntity isOfType: FASTTWithParameters).
	self assert: (self topEntity isOfType: FASTTWithStatements).
	self assert: self topEntity name equals: 'func'.
	
	"Testing value of monovalue relation returnType"
	self assert: self topEntity returnType isNotNil.

		stack push: self topEntity returnType.
		self assert: self topEntity sourceCode equals: '[object]'.
		self assert: (self topEntity isOfType: FASTPyType).
		
		"Testing value of monovalue relation content"
		self assert: self topEntity content isNotNil.

			stack push: self topEntity content.
			self assert: self topEntity sourceCode equals: '[object]'.
			self assert: (self topEntity isOfType: FASTPyList).
			
			"Testing value of multivalued relation initializers"
			self assert: self topEntity initializers size equals: 1.
			
				stack push: (stack top initializers at: 1).
				self assert: self topEntity sourceCode equals: 'object'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation statementBlock"
	self assert: self topEntity statementBlock isNotNil.

		stack push: self topEntity statementBlock.
		self assert: self topEntity sourceCode equals: 'pass'.
		self assert: (self topEntity isOfType: FASTPyBlock).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'pass'.
			self assert: (self topEntity isOfType: FASTPyPassStatement)
]

{ #category : 'tests - types' }
FASTPythonImporterTest >> testTypeListComprehension [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testTypeListComprehension 	"

	self parse: 'def func() -> [T for T in T.name]: pass'.

	self assert: (fast allWithType: FASTPyAttributeAccess) size equals: 1.
	self assert: (fast allWithType: FASTPyBlock) size equals: 1.
	self assert: (fast allWithType: FASTPyForInClause) size equals: 1.
	self assert: (fast allWithType: FASTPyFunctionDefinition) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 4.
	self assert: (fast allWithType: FASTPyListComprehension) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPassStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyType) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'def func() -> [T for T in T.name]: pass'.
	self assert: (self topEntity isOfType: FASTPyFunctionDefinition).
	self assert: (self topEntity isOfType: FASTPyTDefinition).
	self assert: (self topEntity isOfType: FASTTWithParameters).
	self assert: (self topEntity isOfType: FASTTWithStatements).
	self assert: self topEntity name equals: 'func'.
	
	"Testing value of monovalue relation returnType"
	self assert: self topEntity returnType isNotNil.

		stack push: self topEntity returnType.
		self assert: self topEntity sourceCode equals: '[T for T in T.name]'.
		self assert: (self topEntity isOfType: FASTPyType).
		
		"Testing value of monovalue relation content"
		self assert: self topEntity content isNotNil.

			stack push: self topEntity content.
			self assert: self topEntity sourceCode equals: '[T for T in T.name]'.
			self assert: (self topEntity isOfType: FASTPyListComprehension).
			self assert: (self topEntity isOfType: FASTPyTComprehension).
			self assert: (self topEntity isOfType: FASTTWithCondition).
			
			"Testing value of monovalue relation body"
			self assert: self topEntity body isNotNil.

				stack push: self topEntity body.
				self assert: self topEntity sourceCode equals: 'T'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			
			"Testing value of multivalued relation clauses"
			self assert: self topEntity clauses size equals: 1.
			
				stack push: (stack top clauses at: 1).
				self assert: self topEntity sourceCode equals: 'for T in T.name'.
				self assert: (self topEntity isOfType: FASTPyForInClause).
				
				"Testing value of monovalue relation left"
				self assert: self topEntity left isNotNil.

					stack push: self topEntity left.
					self assert: self topEntity sourceCode equals: 'T'.
					self assert: (self topEntity isOfType: FASTPyIdentifier).
					stack pop.
				
				"Testing value of monovalue relation right"
				self assert: self topEntity right isNotNil.

					stack push: self topEntity right.
					self assert: self topEntity sourceCode equals: 'T.name'.
					self assert: (self topEntity isOfType: FASTPyAttributeAccess).
					
					"Testing value of monovalue relation attribute"
					self assert: self topEntity attribute isNotNil.

						stack push: self topEntity attribute.
						self assert: self topEntity sourceCode equals: 'name'.
						self assert: (self topEntity isOfType: FASTPyIdentifier).
						stack pop.
					
					"Testing value of monovalue relation object"
					self assert: self topEntity object isNotNil.

						stack push: self topEntity object.
						self assert: self topEntity sourceCode equals: 'T'.
						self assert: (self topEntity isOfType: FASTPyIdentifier).
						stack pop.
					stack pop.
				stack pop.
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation statementBlock"
	self assert: self topEntity statementBlock isNotNil.

		stack push: self topEntity statementBlock.
		self assert: self topEntity sourceCode equals: 'pass'.
		self assert: (self topEntity isOfType: FASTPyBlock).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'pass'.
			self assert: (self topEntity isOfType: FASTPyPassStatement)
]

{ #category : 'tests - types' }
FASTPythonImporterTest >> testTypeNone [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testTypeNone 	"

	self parse: 'def func() -> None: pass'.

	self assert: (fast allWithType: FASTPyBlock) size equals: 1.
	self assert: (fast allWithType: FASTPyFunctionDefinition) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyNone) size equals: 1.
	self assert: (fast allWithType: FASTPyPassStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyType) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'def func() -> None: pass'.
	self assert: (self topEntity isOfType: FASTPyFunctionDefinition).
	self assert: (self topEntity isOfType: FASTPyTDefinition).
	self assert: (self topEntity isOfType: FASTTWithParameters).
	self assert: (self topEntity isOfType: FASTTWithStatements).
	self assert: self topEntity name equals: 'func'.
	
	"Testing value of monovalue relation returnType"
	self assert: self topEntity returnType isNotNil.

		stack push: self topEntity returnType.
		self assert: self topEntity sourceCode equals: 'None'.
		self assert: (self topEntity isOfType: FASTPyType).
		
		"Testing value of monovalue relation content"
		self assert: self topEntity content isNotNil.

			stack push: self topEntity content.
			self assert: self topEntity sourceCode equals: 'None'.
			self assert: (self topEntity isOfType: FASTPyNone).
			self assert: (self topEntity isOfType: FASTTLiteral).
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation statementBlock"
	self assert: self topEntity statementBlock isNotNil.

		stack push: self topEntity statementBlock.
		self assert: self topEntity sourceCode equals: 'pass'.
		self assert: (self topEntity isOfType: FASTPyBlock).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'pass'.
			self assert: (self topEntity isOfType: FASTPyPassStatement)
]

{ #category : 'tests - types' }
FASTPythonImporterTest >> testTypeNotOperator [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testTypeNotOperator 	"

	self parse: 'def func() -> not True: pass'.

	self assert: (fast allWithType: FASTPyBlock) size equals: 1.
	self assert: (fast allWithType: FASTPyBoolean) size equals: 1.
	self assert: (fast allWithType: FASTPyFunctionDefinition) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyNotOperator) size equals: 1.
	self assert: (fast allWithType: FASTPyPassStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyType) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'def func() -> not True: pass'.
	self assert: (self topEntity isOfType: FASTPyFunctionDefinition).
	self assert: (self topEntity isOfType: FASTPyTDefinition).
	self assert: (self topEntity isOfType: FASTTWithParameters).
	self assert: (self topEntity isOfType: FASTTWithStatements).
	self assert: self topEntity name equals: 'func'.
	
	"Testing value of monovalue relation returnType"
	self assert: self topEntity returnType isNotNil.

		stack push: self topEntity returnType.
		self assert: self topEntity sourceCode equals: 'not True'.
		self assert: (self topEntity isOfType: FASTPyType).
		
		"Testing value of monovalue relation content"
		self assert: self topEntity content isNotNil.

			stack push: self topEntity content.
			self assert: self topEntity sourceCode equals: 'not True'.
			self assert: (self topEntity isOfType: FASTPyNotOperator).
			self assert: (self topEntity isOfType: FASTPyTUnaryOperator).
			
			"Testing value of monovalue relation argument"
			self assert: self topEntity argument isNotNil.

				stack push: self topEntity argument.
				self assert: self topEntity sourceCode equals: 'True'.
				self assert: (self topEntity isOfType: FASTPyBoolean).
				self assert: (self topEntity isOfType: FASTTBooleanLiteral).
				self assert: (self topEntity isOfType: FASTTLiteral).
				stack pop.
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation statementBlock"
	self assert: self topEntity statementBlock isNotNil.

		stack push: self topEntity statementBlock.
		self assert: self topEntity sourceCode equals: 'pass'.
		self assert: (self topEntity isOfType: FASTPyBlock).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'pass'.
			self assert: (self topEntity isOfType: FASTPyPassStatement)
]

{ #category : 'tests - types' }
FASTPythonImporterTest >> testTypeParametersInFunctionDefinition [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testTypeParametersInFunctionDefinition 	"

	self parse: 'def f[T](): pass'.

	self assert: (fast allWithType: FASTPyBlock) size equals: 1.
	self assert: (fast allWithType: FASTPyFunctionDefinition) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPassStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyType) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'def f[T](): pass'.
	self assert: (self topEntity isOfType: FASTPyFunctionDefinition).
	self assert: (self topEntity isOfType: FASTPyTDefinition).
	self assert: (self topEntity isOfType: FASTTWithParameters).
	self assert: (self topEntity isOfType: FASTTWithStatements).
	self assert: self topEntity name equals: 'f'.
	
	"Testing value of monovalue relation statementBlock"
	self assert: self topEntity statementBlock isNotNil.

		stack push: self topEntity statementBlock.
		self assert: self topEntity sourceCode equals: 'pass'.
		self assert: (self topEntity isOfType: FASTPyBlock).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'pass'.
			self assert: (self topEntity isOfType: FASTPyPassStatement).
			stack pop.
		stack pop.
	
	"Testing value of multivalued relation typeParameters"
	self assert: self topEntity typeParameters size equals: 1.
	
		stack push: (stack top typeParameters at: 1).
		self assert: self topEntity sourceCode equals: 'T'.
		self assert: (self topEntity isOfType: FASTPyType).
		
		"Testing value of monovalue relation content"
		self assert: self topEntity content isNotNil.

			stack push: self topEntity content.
			self assert: self topEntity sourceCode equals: 'T'.
			self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - types' }
FASTPythonImporterTest >> testTypeParametersMultiple [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testTypeParametersMultiple 	"

	self parse: 'def f[T, K, L](): pass'.

	self assert: (fast allWithType: FASTPyBlock) size equals: 1.
	self assert: (fast allWithType: FASTPyFunctionDefinition) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 3.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPassStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyType) size equals: 3.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'def f[T, K, L](): pass'.
	self assert: (self topEntity isOfType: FASTPyFunctionDefinition).
	self assert: (self topEntity isOfType: FASTPyTDefinition).
	self assert: (self topEntity isOfType: FASTTWithParameters).
	self assert: (self topEntity isOfType: FASTTWithStatements).
	self assert: self topEntity name equals: 'f'.
	
	"Testing value of monovalue relation statementBlock"
	self assert: self topEntity statementBlock isNotNil.

		stack push: self topEntity statementBlock.
		self assert: self topEntity sourceCode equals: 'pass'.
		self assert: (self topEntity isOfType: FASTPyBlock).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'pass'.
			self assert: (self topEntity isOfType: FASTPyPassStatement).
			stack pop.
		stack pop.
	
	"Testing value of multivalued relation typeParameters"
	self assert: self topEntity typeParameters size equals: 3.
	
		stack push: (stack top typeParameters at: 1).
		self assert: self topEntity sourceCode equals: 'T'.
		self assert: (self topEntity isOfType: FASTPyType).
		
		"Testing value of monovalue relation content"
		self assert: self topEntity content isNotNil.

			stack push: self topEntity content.
			self assert: self topEntity sourceCode equals: 'T'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		stack pop.
	
		stack push: (stack top typeParameters at: 2).
		self assert: self topEntity sourceCode equals: 'K'.
		self assert: (self topEntity isOfType: FASTPyType).
		
		"Testing value of monovalue relation content"
		self assert: self topEntity content isNotNil.

			stack push: self topEntity content.
			self assert: self topEntity sourceCode equals: 'K'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		stack pop.
	
		stack push: (stack top typeParameters at: 3).
		self assert: self topEntity sourceCode equals: 'L'.
		self assert: (self topEntity isOfType: FASTPyType).
		
		"Testing value of monovalue relation content"
		self assert: self topEntity content isNotNil.

			stack push: self topEntity content.
			self assert: self topEntity sourceCode equals: 'L'.
			self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - types' }
FASTPythonImporterTest >> testTypeSet [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testTypeSet 	"

	self parse: 'def func() -> { 1 }: pass'.

	self assert: (fast allWithType: FASTPyBlock) size equals: 1.
	self assert: (fast allWithType: FASTPyFunctionDefinition) size equals: 1.
	self assert: (fast allWithType: FASTPyInteger) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPassStatement) size equals: 1.
	self assert: (fast allWithType: FASTPySet) size equals: 1.
	self assert: (fast allWithType: FASTPyType) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'def func() -> { 1 }: pass'.
	self assert: (self topEntity isOfType: FASTPyFunctionDefinition).
	self assert: (self topEntity isOfType: FASTPyTDefinition).
	self assert: (self topEntity isOfType: FASTTWithParameters).
	self assert: (self topEntity isOfType: FASTTWithStatements).
	self assert: self topEntity name equals: 'func'.
	
	"Testing value of monovalue relation returnType"
	self assert: self topEntity returnType isNotNil.

		stack push: self topEntity returnType.
		self assert: self topEntity sourceCode equals: '{ 1 }'.
		self assert: (self topEntity isOfType: FASTPyType).
		
		"Testing value of monovalue relation content"
		self assert: self topEntity content isNotNil.

			stack push: self topEntity content.
			self assert: self topEntity sourceCode equals: '{ 1 }'.
			self assert: (self topEntity isOfType: FASTPySet).
			
			"Testing value of multivalued relation initializers"
			self assert: self topEntity initializers size equals: 1.
			
				stack push: (stack top initializers at: 1).
				self assert: self topEntity sourceCode equals: '1'.
				self assert: (self topEntity isOfType: FASTPyInteger).
				self assert: (self topEntity isOfType: FASTTLiteral).
				self assert: (self topEntity isOfType: FASTTNumericalLiteral).
				stack pop.
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation statementBlock"
	self assert: self topEntity statementBlock isNotNil.

		stack push: self topEntity statementBlock.
		self assert: self topEntity sourceCode equals: 'pass'.
		self assert: (self topEntity isOfType: FASTPyBlock).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'pass'.
			self assert: (self topEntity isOfType: FASTPyPassStatement)
]

{ #category : 'tests - types' }
FASTPythonImporterTest >> testTypeString [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testTypeString 	"

	self parse: 'def func() -> "test": pass'.

	self assert: (fast allWithType: FASTPyBlock) size equals: 1.
	self assert: (fast allWithType: FASTPyFunctionDefinition) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPassStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyString) size equals: 1.
	self assert: (fast allWithType: FASTPyType) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'def func() -> "test": pass'.
	self assert: (self topEntity isOfType: FASTPyFunctionDefinition).
	self assert: (self topEntity isOfType: FASTPyTDefinition).
	self assert: (self topEntity isOfType: FASTTWithParameters).
	self assert: (self topEntity isOfType: FASTTWithStatements).
	self assert: self topEntity name equals: 'func'.
	
	"Testing value of monovalue relation returnType"
	self assert: self topEntity returnType isNotNil.

		stack push: self topEntity returnType.
		self assert: self topEntity sourceCode equals: '"test"'.
		self assert: (self topEntity isOfType: FASTPyType).
		
		"Testing value of monovalue relation content"
		self assert: self topEntity content isNotNil.

			stack push: self topEntity content.
			self assert: self topEntity sourceCode equals: '"test"'.
			self assert: (self topEntity isOfType: FASTPyString).
			self assert: (self topEntity isOfType: FASTTLiteral).
			self assert: (self topEntity isOfType: FASTTStringLiteral).
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation statementBlock"
	self assert: self topEntity statementBlock isNotNil.

		stack push: self topEntity statementBlock.
		self assert: self topEntity sourceCode equals: 'pass'.
		self assert: (self topEntity isOfType: FASTPyBlock).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'pass'.
			self assert: (self topEntity isOfType: FASTPyPassStatement)
]

{ #category : 'tests - types' }
FASTPythonImporterTest >> testTypeTrue [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testTypeTrue 	"

	self parse: 'def func() -> True: pass'.

	self assert: (fast allWithType: FASTPyBlock) size equals: 1.
	self assert: (fast allWithType: FASTPyBoolean) size equals: 1.
	self assert: (fast allWithType: FASTPyFunctionDefinition) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPassStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyType) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'def func() -> True: pass'.
	self assert: (self topEntity isOfType: FASTPyFunctionDefinition).
	self assert: (self topEntity isOfType: FASTPyTDefinition).
	self assert: (self topEntity isOfType: FASTTWithParameters).
	self assert: (self topEntity isOfType: FASTTWithStatements).
	self assert: self topEntity name equals: 'func'.
	
	"Testing value of monovalue relation returnType"
	self assert: self topEntity returnType isNotNil.

		stack push: self topEntity returnType.
		self assert: self topEntity sourceCode equals: 'True'.
		self assert: (self topEntity isOfType: FASTPyType).
		
		"Testing value of monovalue relation content"
		self assert: self topEntity content isNotNil.

			stack push: self topEntity content.
			self assert: self topEntity sourceCode equals: 'True'.
			self assert: (self topEntity isOfType: FASTPyBoolean).
			self assert: (self topEntity isOfType: FASTTBooleanLiteral).
			self assert: (self topEntity isOfType: FASTTLiteral).
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation statementBlock"
	self assert: self topEntity statementBlock isNotNil.

		stack push: self topEntity statementBlock.
		self assert: self topEntity sourceCode equals: 'pass'.
		self assert: (self topEntity isOfType: FASTPyBlock).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'pass'.
			self assert: (self topEntity isOfType: FASTPyPassStatement)
]

{ #category : 'tests - types' }
FASTPythonImporterTest >> testTypeTuple [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testTypeTuple 	"

	self parse: 'def func() -> (1, ): pass'.

	self assert: (fast allWithType: FASTPyBlock) size equals: 1.
	self assert: (fast allWithType: FASTPyFunctionDefinition) size equals: 1.
	self assert: (fast allWithType: FASTPyInteger) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPassStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyTuple) size equals: 1.
	self assert: (fast allWithType: FASTPyType) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'def func() -> (1, ): pass'.
	self assert: (self topEntity isOfType: FASTPyFunctionDefinition).
	self assert: (self topEntity isOfType: FASTPyTDefinition).
	self assert: (self topEntity isOfType: FASTTWithParameters).
	self assert: (self topEntity isOfType: FASTTWithStatements).
	self assert: self topEntity name equals: 'func'.
	
	"Testing value of monovalue relation returnType"
	self assert: self topEntity returnType isNotNil.

		stack push: self topEntity returnType.
		self assert: self topEntity sourceCode equals: '(1, )'.
		self assert: (self topEntity isOfType: FASTPyType).
		
		"Testing value of monovalue relation content"
		self assert: self topEntity content isNotNil.

			stack push: self topEntity content.
			self assert: self topEntity sourceCode equals: '(1, )'.
			self assert: (self topEntity isOfType: FASTPyTuple).
			
			"Testing value of multivalued relation initializers"
			self assert: self topEntity initializers size equals: 1.
			
				stack push: (stack top initializers at: 1).
				self assert: self topEntity sourceCode equals: '1'.
				self assert: (self topEntity isOfType: FASTPyInteger).
				self assert: (self topEntity isOfType: FASTTLiteral).
				self assert: (self topEntity isOfType: FASTTNumericalLiteral).
				stack pop.
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation statementBlock"
	self assert: self topEntity statementBlock isNotNil.

		stack push: self topEntity statementBlock.
		self assert: self topEntity sourceCode equals: 'pass'.
		self assert: (self topEntity isOfType: FASTPyBlock).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'pass'.
			self assert: (self topEntity isOfType: FASTPyPassStatement)
]

{ #category : 'tests - types' }
FASTPythonImporterTest >> testTypeUnaryOperator [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testTypeUnaryOperator 	"

	self parse: 'def func() -> -1: pass'.

	self assert: (fast allWithType: FASTPyBlock) size equals: 1.
	self assert: (fast allWithType: FASTPyFunctionDefinition) size equals: 1.
	self assert: (fast allWithType: FASTPyInteger) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPassStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyType) size equals: 1.
	self assert: (fast allWithType: FASTPyUnaryOperator) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'def func() -> -1: pass'.
	self assert: (self topEntity isOfType: FASTPyFunctionDefinition).
	self assert: (self topEntity isOfType: FASTPyTDefinition).
	self assert: (self topEntity isOfType: FASTTWithParameters).
	self assert: (self topEntity isOfType: FASTTWithStatements).
	self assert: self topEntity name equals: 'func'.
	
	"Testing value of monovalue relation returnType"
	self assert: self topEntity returnType isNotNil.

		stack push: self topEntity returnType.
		self assert: self topEntity sourceCode equals: '-1'.
		self assert: (self topEntity isOfType: FASTPyType).
		
		"Testing value of monovalue relation content"
		self assert: self topEntity content isNotNil.

			stack push: self topEntity content.
			self assert: self topEntity sourceCode equals: '-1'.
			self assert: (self topEntity isOfType: FASTPyUnaryOperator).
			self assert: (self topEntity isOfType: FASTPyTUnaryOperator).
			
			"Testing value of monovalue relation argument"
			self assert: self topEntity argument isNotNil.

				stack push: self topEntity argument.
				self assert: self topEntity sourceCode equals: '1'.
				self assert: (self topEntity isOfType: FASTPyInteger).
				self assert: (self topEntity isOfType: FASTTLiteral).
				self assert: (self topEntity isOfType: FASTTNumericalLiteral).
				stack pop.
			self assert: self topEntity operator equals: '-'.
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation statementBlock"
	self assert: self topEntity statementBlock isNotNil.

		stack push: self topEntity statementBlock.
		self assert: self topEntity sourceCode equals: 'pass'.
		self assert: (self topEntity isOfType: FASTPyBlock).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'pass'.
			self assert: (self topEntity isOfType: FASTPyPassStatement)
]

{ #category : 'tests - function definitions' }
FASTPythonImporterTest >> testTypedListSplatParameter [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testTypedListSplatParameter 	"

	self parse: 'def f(*arg: Any): pass'.

	self assert: (fast allWithType: FASTPyBlock) size equals: 1.
	self assert: (fast allWithType: FASTPyFunctionDefinition) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyListSplatParameter) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPassStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyType) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'def f(*arg: Any): pass'.
	self assert: (self topEntity isOfType: FASTPyFunctionDefinition).
	self assert: (self topEntity isOfType: FASTPyTDefinition).
	self assert: (self topEntity isOfType: FASTTWithParameters).
	self assert: (self topEntity isOfType: FASTTWithStatements).
	self assert: self topEntity name equals: 'f'.
	
	"Testing value of multivalued relation parameters"
	self assert: self topEntity parameters size equals: 1.
	
		stack push: (stack top parameters at: 1).
		self assert: self topEntity sourceCode equals: '*arg: Any'.
		self assert: (self topEntity isOfType: FASTPyListSplatParameter).
		self assert: (self topEntity isOfType: FASTTVariableEntity).
		self assert: self topEntity name equals: 'arg'.
		
		"Testing value of monovalue relation type"
		self assert: self topEntity type isNotNil.

			stack push: self topEntity type.
			self assert: self topEntity sourceCode equals: 'Any'.
			self assert: (self topEntity isOfType: FASTPyType).
			
			"Testing value of monovalue relation content"
			self assert: self topEntity content isNotNil.

				stack push: self topEntity content.
				self assert: self topEntity sourceCode equals: 'Any'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation statementBlock"
	self assert: self topEntity statementBlock isNotNil.

		stack push: self topEntity statementBlock.
		self assert: self topEntity sourceCode equals: 'pass'.
		self assert: (self topEntity isOfType: FASTPyBlock).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'pass'.
			self assert: (self topEntity isOfType: FASTPyPassStatement)
]

{ #category : 'tests - function definitions' }
FASTPythonImporterTest >> testTypedParameter [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testTypedParameter 	"

	self parse: 'def f(location: Path): pass'.

	self assert: (fast allWithType: FASTPyBlock) size equals: 1.
	self assert: (fast allWithType: FASTPyFunctionDefinition) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyParameter) size equals: 1.
	self assert: (fast allWithType: FASTPyPassStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyType) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'def f(location: Path): pass'.
	self assert: (self topEntity isOfType: FASTPyFunctionDefinition).
	self assert: (self topEntity isOfType: FASTPyTDefinition).
	self assert: (self topEntity isOfType: FASTTWithParameters).
	self assert: (self topEntity isOfType: FASTTWithStatements).
	self assert: self topEntity name equals: 'f'.
	
	"Testing value of multivalued relation parameters"
	self assert: self topEntity parameters size equals: 1.
	
		stack push: (stack top parameters at: 1).
		self assert: self topEntity sourceCode equals: 'location: Path'.
		self assert: (self topEntity isOfType: FASTPyParameter).
		self assert: (self topEntity isOfType: FASTTVariableEntity).
		self assert: self topEntity name equals: 'location'.
		
		"Testing value of monovalue relation type"
		self assert: self topEntity type isNotNil.

			stack push: self topEntity type.
			self assert: self topEntity sourceCode equals: 'Path'.
			self assert: (self topEntity isOfType: FASTPyType).
			
			"Testing value of monovalue relation content"
			self assert: self topEntity content isNotNil.

				stack push: self topEntity content.
				self assert: self topEntity sourceCode equals: 'Path'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation statementBlock"
	self assert: self topEntity statementBlock isNotNil.

		stack push: self topEntity statementBlock.
		self assert: self topEntity sourceCode equals: 'pass'.
		self assert: (self topEntity isOfType: FASTPyBlock).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'pass'.
			self assert: (self topEntity isOfType: FASTPyPassStatement)
]

{ #category : 'tests - operators' }
FASTPythonImporterTest >> testUnaryOperator [

	| operator |
	self parse: '-13'.

	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyUnaryOperator) size equals: 1.

	operator := (fast allWithType: FASTPyUnaryOperator) anyOne.
	self assert: operator sourceCode equals: '-13'.
	self assert: (operator isOfType: FASTTExpression).
	self assert: operator argument sourceCode equals: '13'.
	self assert: (operator argument isOfType: FASTPyInteger).
	self assert: operator operator equals: '-'
]

{ #category : 'tests - advanced statements' }
FASTPythonImporterTest >> testWhile [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testWhile 	"

	self parse: 'while True:
	break'.

	self assert: (fast allWithType: FASTPyBlock) size equals: 1.
	self assert: (fast allWithType: FASTPyBoolean) size equals: 1.
	self assert: (fast allWithType: FASTPyBreakStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyWhileStatement) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'while True:
	break' withPlatformLineEndings.
	self assert: (self topEntity isOfType: FASTPyWhileStatement).
	self assert: (self topEntity isOfType: FASTPyTWithElseClause).
	self assert: (self topEntity isOfType: FASTTConditionalStatement).
	self assert: (self topEntity isOfType: FASTTLoopStatement).
	self assert: (self topEntity isOfType: FASTTWithCondition).
	
	"Testing value of monovalue relation body"
	self assert: self topEntity body isNotNil.

		stack push: self topEntity body.
		self assert: self topEntity sourceCode equals: 'break'.
		self assert: (self topEntity isOfType: FASTPyBlock).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'break'.
			self assert: (self topEntity isOfType: FASTPyBreakStatement).
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation condition"
	self assert: self topEntity condition isNotNil.

		stack push: self topEntity condition.
		self assert: self topEntity sourceCode equals: 'True'.
		self assert: (self topEntity isOfType: FASTPyBoolean).
		self assert: (self topEntity isOfType: FASTTBooleanLiteral).
		self assert: (self topEntity isOfType: FASTTLiteral)
]

{ #category : 'tests - advanced statements' }
FASTPythonImporterTest >> testWhileStatementWithElseClause [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testWhileStatementWithElseClause 	"

	self parse: 'while True:
    fun(c)
else: print("Hello")'.

	self assert: (fast allWithType: FASTPyBlock) size equals: 1.
	self assert: (fast allWithType: FASTPyBoolean) size equals: 1.
	self assert: (fast allWithType: FASTPyCall) size equals: 2.
	self assert: (fast allWithType: FASTPyElseClause) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 3.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyString) size equals: 1.
	self assert: (fast allWithType: FASTPyWhileStatement) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'while True:
    fun(c)
else: print("Hello")' withPlatformLineEndings.
	self assert: (self topEntity isOfType: FASTPyWhileStatement).
	self assert: (self topEntity isOfType: FASTPyTWithElseClause).
	self assert: (self topEntity isOfType: FASTTConditionalStatement).
	self assert: (self topEntity isOfType: FASTTLoopStatement).
	self assert: (self topEntity isOfType: FASTTWithCondition).
	
	"Testing value of monovalue relation body"
	self assert: self topEntity body isNotNil.

		stack push: self topEntity body.
		self assert: self topEntity sourceCode equals: 'fun(c)'.
		self assert: (self topEntity isOfType: FASTPyBlock).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'fun(c)'.
			self assert: (self topEntity isOfType: FASTPyCall).
			self assert: (self topEntity isOfType: FASTTInvocation).
			self assert: (self topEntity isOfType: FASTTWithArguments).
			
			"Testing value of multivalued relation arguments"
			self assert: self topEntity arguments size equals: 1.
			
				stack push: (stack top arguments at: 1).
				self assert: self topEntity sourceCode equals: 'c'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			
			"Testing value of monovalue relation callee"
			self assert: self topEntity callee isNotNil.

				stack push: self topEntity callee.
				self assert: self topEntity sourceCode equals: 'fun'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			stack pop.
		stack pop.
	
	"Testing value of monovalue relation condition"
	self assert: self topEntity condition isNotNil.

		stack push: self topEntity condition.
		self assert: self topEntity sourceCode equals: 'True'.
		self assert: (self topEntity isOfType: FASTPyBoolean).
		self assert: (self topEntity isOfType: FASTTBooleanLiteral).
		self assert: (self topEntity isOfType: FASTTLiteral).
		stack pop.
	
	"Testing value of monovalue relation elseClause"
	self assert: self topEntity elseClause isNotNil.

		stack push: self topEntity elseClause.
		self assert: self topEntity sourceCode equals: 'print("Hello")'.
		self assert: (self topEntity isOfType: FASTPyElseClause).
		self assert: (self topEntity isOfType: FASTTStatementBlock).
		
		"Testing value of multivalued relation statements"
		self assert: self topEntity statements size equals: 1.
		
			stack push: (stack top statements at: 1).
			self assert: self topEntity sourceCode equals: 'print("Hello")'.
			self assert: (self topEntity isOfType: FASTPyCall).
			self assert: (self topEntity isOfType: FASTTInvocation).
			self assert: (self topEntity isOfType: FASTTWithArguments).
			
			"Testing value of multivalued relation arguments"
			self assert: self topEntity arguments size equals: 1.
			
				stack push: (stack top arguments at: 1).
				self assert: self topEntity sourceCode equals: '"Hello"'.
				self assert: (self topEntity isOfType: FASTPyString).
				self assert: (self topEntity isOfType: FASTTLiteral).
				self assert: (self topEntity isOfType: FASTTStringLiteral).
				stack pop.
			
			"Testing value of monovalue relation callee"
			self assert: self topEntity callee isNotNil.

				stack push: self topEntity callee.
				self assert: self topEntity sourceCode equals: 'print'.
				self assert: (self topEntity isOfType: FASTPyIdentifier)
]

{ #category : 'tests - with statement' }
FASTPythonImporterTest >> testWithStatement [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testWithStatement 	"

	self parse: 'with open(): pass'.

	self assert: (fast allWithType: FASTPyCall) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 1.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPassStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyWithStatement) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'with open(): pass'.
	self assert: (self topEntity isOfType: FASTPyWithStatement).
	self assert: (self topEntity isOfType: FASTTStatementBlock).
	
	"Testing value of multivalued relation items"
	self assert: self topEntity items size equals: 1.
	
		stack push: (stack top items at: 1).
		self assert: self topEntity sourceCode equals: 'open()'.
		self assert: (self topEntity isOfType: FASTPyCall).
		self assert: (self topEntity isOfType: FASTTInvocation).
		self assert: (self topEntity isOfType: FASTTWithArguments).
		
		"Testing value of monovalue relation callee"
		self assert: self topEntity callee isNotNil.

			stack push: self topEntity callee.
			self assert: self topEntity sourceCode equals: 'open'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		stack pop.
	
	"Testing value of multivalued relation statements"
	self assert: self topEntity statements size equals: 1.
	
		stack push: (stack top statements at: 1).
		self assert: self topEntity sourceCode equals: 'pass'.
		self assert: (self topEntity isOfType: FASTPyPassStatement)
]

{ #category : 'tests - with statement' }
FASTPythonImporterTest >> testWithStatementWithAsPattern [
	<generated>
	"Generated as regression test by FASTPyImporterTestGenerator>>#generateTestNamed:fromCode:protocol:
	Regenerate executing: 	self regenerateTest: #testWithStatementWithAsPattern 	"

	self parse: 'with open() as f: pass'.

	self assert: (fast allWithType: FASTPyAsPattern) size equals: 1.
	self assert: (fast allWithType: FASTPyCall) size equals: 1.
	self assert: (fast allWithType: FASTPyIdentifier) size equals: 2.
	self assert: (fast allWithType: FASTPyModule) size equals: 1.
	self assert: (fast allWithType: FASTPyPassStatement) size equals: 1.
	self assert: (fast allWithType: FASTPyWithStatement) size equals: 1.

	stack push: fast rootEntities anyOne containedEntities first.
	self assert: self topEntity sourceCode equals: 'with open() as f: pass'.
	self assert: (self topEntity isOfType: FASTPyWithStatement).
	self assert: (self topEntity isOfType: FASTTStatementBlock).
	
	"Testing value of multivalued relation items"
	self assert: self topEntity items size equals: 1.
	
		stack push: (stack top items at: 1).
		self assert: self topEntity sourceCode equals: 'open() as f'.
		self assert: (self topEntity isOfType: FASTPyAsPattern).
		
		"Testing value of monovalue relation source"
		self assert: self topEntity source isNotNil.

			stack push: self topEntity source.
			self assert: self topEntity sourceCode equals: 'open()'.
			self assert: (self topEntity isOfType: FASTPyCall).
			self assert: (self topEntity isOfType: FASTTInvocation).
			self assert: (self topEntity isOfType: FASTTWithArguments).
			
			"Testing value of monovalue relation callee"
			self assert: self topEntity callee isNotNil.

				stack push: self topEntity callee.
				self assert: self topEntity sourceCode equals: 'open'.
				self assert: (self topEntity isOfType: FASTPyIdentifier).
				stack pop.
			stack pop.
		
		"Testing value of monovalue relation target"
		self assert: self topEntity target isNotNil.

			stack push: self topEntity target.
			self assert: self topEntity sourceCode equals: 'f'.
			self assert: (self topEntity isOfType: FASTPyIdentifier).
			stack pop.
		stack pop.
	
	"Testing value of multivalued relation statements"
	self assert: self topEntity statements size equals: 1.
	
		stack push: (stack top statements at: 1).
		self assert: self topEntity sourceCode equals: 'pass'.
		self assert: (self topEntity isOfType: FASTPyPassStatement)
]

{ #category : 'running' }
FASTPythonImporterTest >> topEntity [

	^ stack top
]
